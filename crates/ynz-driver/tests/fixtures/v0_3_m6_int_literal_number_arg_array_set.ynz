// WHY: int literal into `array<number>.set(i, 5)` elem slot (arg 1) — same
// unchecked-builtin-arg class as `.add`. Pre-fix: silent type corruption.
function entrypoint() -> nothing {
  let prices: array<number> = [1.5, 2.5]
  prices.set(0, 5)
  let got = prices.get(0)
  print(got.or(0.0))
}
