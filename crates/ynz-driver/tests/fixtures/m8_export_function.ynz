// M8 fixture: export function syntax — library file (no entrypoint required).

export function greet(name: string) -> string {
  return `hello ${name}`
}

export function add(a: int, b: int) -> int {
  return a + b
}
