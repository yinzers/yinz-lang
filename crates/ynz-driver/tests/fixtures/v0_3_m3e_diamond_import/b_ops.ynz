// M3e adversarial axis 5a.iii: B imports and calls the shared leaf D. Exports b_work().
import { suspend } from `leaf_ops`

export function b_work() -> int {
  let val: int = 20
  suspend()
  return val
}
