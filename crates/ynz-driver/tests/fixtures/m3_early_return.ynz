function sign(x: int) -> int {
  if (x > 0) {
    return 1
  }
  if (x < 0) {
    return -1
  }
  return 0
}

function entrypoint() -> nothing {
  print(sign(42))
  print(sign(-7))
  print(sign(0))
}
