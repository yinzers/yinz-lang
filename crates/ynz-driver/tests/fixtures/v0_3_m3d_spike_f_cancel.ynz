function fib(n: int) -> int {
  if (n < 2) {
    return n
  }
  return fib(n - 1) + fib(n - 2)
}

function will_panic(n: int) -> int {
  let x = 100 / n
  return x
}

function entrypoint() -> nothing {
  let a = fib(35)
  let b = will_panic(0)
  print(a)
  print(b)
}
