// M3b loud-reject case (c): errors-capable export that suspends TRANSITIVELY.
// compute() is EC and calls innerWork() (non-exported, suspends via sleep).
// This fixture tests the M3b-era loud-reject path; frame_layouts_query now computes
// the EC staging slot + child sub-frame interaction correctly at the import site.

function innerWork() -> nothing {
  sleep(10)
}

export function compute() -> int errors {
  innerWork()
  return 42
}
