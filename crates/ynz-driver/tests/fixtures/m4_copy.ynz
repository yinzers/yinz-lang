shape Coord {
  x: int
  y: int
}

function show(share self: Coord) -> nothing {
  print(self.x)
  print(self.y)
}

function entrypoint() -> nothing {
  let a: Coord = { x: 10, y: 20 }
  let b = a.copy()
  b.show()
  a.show()
}
