// External module — pure CPU function (does not suspend).
// v0.3-M3b P1: the compiler propagates suspends=false across the module boundary,
// so the entrypoint's analysis correctly sees remoteOp as non-suspending.
export function remoteOp() -> nothing {
  print(`remote op`)
}
