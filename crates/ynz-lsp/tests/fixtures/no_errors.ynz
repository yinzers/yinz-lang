// Minimal clean fixture with zero diagnostics.
// Used by regression tests to verify the LSP reports no errors on valid code.
function add(a: int, b: int) -> int {
    return a + b
}

function entrypoint() -> nothing {
    const result = add(1, 2)
    print(result)
}
