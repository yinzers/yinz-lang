---
slug: v0-3-m3d-cpu-parallelization
type: execution
owner: Patrick Rizzardi
status: active
roadmap: v0-3-concurrency-perf
milestone: v0-3-m3d-cpu-parallelization
depends_on: [v0-3-m3b-auto-parallelization]
plan_base: 8b99ac9c626468ff7ce7c123f2e061e3a8df22cd
files:
  - crates/ynz-runtime/src/**
  - crates/ynz-typeck/src/**
  - crates/ynz-codegen/src/**
  - crates/ynz-lsp/src/**
  - crates/ynz-driver/**
  - registry/features.toml
  - design/concurrency.md
  - spec/concurrency.md
  - examples/pirates-roster/**
  - examples/primantis-orders/**
  - tooling/vscode-ynz/**
created: 2026-06-11
last_updated: 2026-06-14
---

# Plan: v0.3-M3d — Pure-CPU Statement Parallelization

Created: 2026-06-11
Status: approved — in execution (Phase 0, ROUND-8 re-gate in flight 2026-06-12 — Patrick authorized round 8 at arbitration #3 ("Round 8: one-clause fix (Recommended)"). Round-7 closed 3 PASS / 1 BLOCK (judge #15 silent-narrowing — see Findings Log 16:30). Round-8 executor applied judge #15's named fix exactly: stripped `stmt_contains_wait` from the POST-PAIR check at all three gate sites (emit.rs:6574/6752/6940), keeping only `stmt_contains_suspending_call`; pre-pair gates unchanged (ISSUE-A); stale prune comment at emit.rs:2613 rewritten; fixture headers verified accurate. FIRES table IR-verified: (g)/(h)/(i)/(j)/(n) went 0→2 `ynz_rt_spawn_blocking_joinable` call instructions each — the cross-suspension reload machinery is LIVE again; (m)/(p)/(q) still decline at 0. All 17 fixtures byte-identical both modes, full workspace green, clippy/fmt clean, zero new deviations. Scratch deviation #15 amended (round 8) with recomputed identity hash fdc8dc2. Post-R8 tree pre-verified (delegation intact, 9/9 m3d tests green, no probe artifacts). ROUND-8 re-gate CLOSED ALL GREEN 2026-06-12T17:03: judge #15 PASS (fresh adversarial inputs on the narrowed predicate all held) + acceptance-verifier PASS (all 4 ACs MET on own live runs; FIRES independently IR-verified; 1924 workspace tests green). Phase 0 gate fully green after 8 rounds. Plan writeback complete: 4 ACs ticked with R8 evidence, Phase Review Gates ticked (5 reviewers + 17 judges at latest verdicts; #1/#11 deferred to cumulative 4.a per authorization), FIRES/DECLINES envelope table added to AC1 evidence, fixture (m) note corrected. Phase 0 COMMITTED dcc1432 (2026-06-12, Patrick-confirmed; 38 files, +4067/-45, explicit-path staging). Post-phase /learn completed (2026-06-12): new graveyard corpse "Silent Envelope Narrowing" in .claude/graveyard.md (UNCOMMITTED — rides with next commit; Bouncer checks: decline-widening needs fire-evidence, fixture FIRE claims verified vs IR, gated-path plans need FIRE/DECLINE envelope table) + generic principle "Assert the Mechanism Fired" in engineering-principles.md + project memory gated-path-fire-assertions + mutation-probe serialization folded into reviewer-judge-read-only-git memory. Next: Phase 1 via /execute-plan in a future session (phase-by-phase mode); P1 plans MUST declare the spike admission envelope as a FIRE/DECLINE table up front per the new corpse. Cumulative 4.a at end-of-plan re-judges all 17 deviations incl. the #1/#11 deferred re-judges. P1 carry-forwards logged in plan: end-to-end C-unwind resume-fn ABI; emit_suspending_call_heap_boxed stale frame_layouts; null-from-spawn silently skips; spike-on oracle is manual-only (needs CI gate when productionized).)

## Context & Why

**Goal**: Two (or more) independent heavy pure-CPU operations in a function body run on separate cores — real multicore speedup — with zero user syntax and byte-identical results to `--no-auto-parallel`. This is the "your code just got faster" win for compute-bound code, complementing M3b's I/O-overlap win.

**Why**: M3b shipped I/O-overlap auto-parallelization by INTERLEAVING suspensions on one thread (zero new runtime — both child sub-frames embed in the parent frame and inline-poll). CPU work doesn't yield, so overlapping it needs ACTUAL extra threads with a joinable handle. The existing spawn ABI cannot provide that: `ynz_rt_spawn` (`runtime.rs:591`) and `ynz_rt_spawn_blocking` (`runtime.rs:140`) both return void / fire-and-forget. A pure-CPU function isn't a state machine today, so it can't await a join either. This was SPLIT out of M3b on 2026-06-05 precisely because it is genuinely-new runtime + codegen work sitting one wrong implementation choice (a synchronous join) from the `block_on` M2-HALT corpse.

**Background — what exists today**:
- M3b's independence analysis (`crates/ynz-codegen/src/independence.rs`) partitions straight-line statement lists into `Singleton` / `Parallel` groups with class-agnostic data-dep + type-based write-effect criteria — but candidacy criterion 3 currently requires all group members to be SUSPENDING calls to DISTINCT callee names.
- The interleaved inline-poll join (`emit.rs:6061` `emit_independent_group_poll`) polls embedded child frames; it has no concept of a thread-spawned child.
- The SM-lowering trigger is `suspend_set.contains(&f.name)` (`emit.rs:1847` → `lower_function_with_waits` at `emit.rs:2063`); the suspend set is computed by typeck's may-block fixpoint (`may_block.rs:96 analyze`) and carried cross-module via `FunctionSig.suspends` (`signatures.rs:38`).
- The sleep-handle poll protocol (`ynz_rt_async_sleep_create` → handle stored in frame slot → `ynz_rt_async_sleep_poll(handle, waker_ctx)` until Ready, freed-on-Ready, freed-by-drop-shim on cancellation) is the proven precedent the joinable CPU spawn mirrors.

**Constraints** (from the roadmap + design docs, all locked):
- No new user-facing syntax. Existing programs produce IDENTICAL output (`--no-auto-parallel` is the oracle).
- The join MUST be a poll-based suspension through the existing waker protocol — a synchronous join (`block_on`, thread-park) is the M2-HALT corpse and is banned at every call path.
- No function coloring (design/no-function-coloring.md "Invariant"); promotion must be invisible at signature level.
- Auto-promotion must never turn previously-valid code into a compile error (`.claude/rules/auto-promotion.md` — promotion only when the compiler can PROVE the fit).
- Adversarial gate mandatory per the roadmap milestone entry.

**Success criteria**: a demo with two independent heavy CPU calls (`let a = work(40)` / `let b = work(41)`) measurably overlaps on two cores (wall-clock < sequential), byte-identical output vs `--no-auto-parallel`, zero regressions across the full fixture corpus, full teaching surface shipped (registry + LSP hint + VSCode + demo), released as `v0.3.0-m7`.

## Research Findings

1. **Tokio 1.49 (verified via context7, 2026-06-11)**: `tokio::task::spawn_blocking(closure) -> JoinHandle<R>`. `JoinHandle<T>` implements `Future<Output = Result<T, JoinError>>`; polling registers the real waker (no fabricated wakers needed — same forwarding discipline as the sleep poll). **`spawn_blocking` tasks cannot be aborted once started** — `JoinHandle::abort()` only prevents not-yet-started tasks; dropping the handle DETACHES (task runs to completion). Pool saturation (default `max_blocking_threads` = 512) QUEUES additional `spawn_blocking` calls — they run as threads free up. Panic inside the closure is caught and surfaces as `JoinError::is_panic()` on the handle.

2. **Deadlock-safety consequence**: because the join is an inline poll that returns `Pending` up to the scheduler (never holds a thread), pool saturation cannot deadlock the joiner — the joining state machine is suspended, not parked on a pool thread; queued spawns always eventually run. The ONLY way to deadlock is a synchronous join — which is banned (corpse). The stress fixture still proves this empirically.

3. **Cancellation + borrowed heap args = UAF hazard**: if a parent frame is dropped mid-join (task cancellation), the detached `spawn_blocking` child keeps running. A string argument passed by pointer into the child would dangle the moment the parent frame's drop frees it. **Fix: reuse M1's existing arg-copy + `BgArgDropEntry` machinery** (`runtime.rs:89` ctx-copy region; `ynz_rt_spawn`'s `arg_drop_ptr` discipline) — heap args are COPIED into the spawned task's ctx; the child owns its copies; nothing dangles regardless of cancellation timing.

4. **SM-promotion can transitively break compiles — must decline, never error**: promoting non-suspending `F` to a state machine makes every caller of `F` a state machine too (normal may-block propagation). Any newly-SM function can then trip the existing suspension guards on previously-compiling code: `ShadowsCrossingLocal`, `UnsupportedCrossingLocalType` (union/maybe/dynamic/fixed/range/MapEntry crossing the join), `WideValueSuspendingReturn` (`-> Shape` / `-> Shape errors`), `MutualSuspensionCycle`, `FixedArrayIterWithWait`, `StoredRangeWithWait`, `ExpressionIterWithWait`, `ArrayShapeRuntimeFieldWithWait`. The promotion pass MUST probe all of these across the full transitive closure of newly-SM functions and ROLL BACK any promotion whose propagation would fire a guard. Decline = sequential lowering (silent perf opt-out), NEVER a new compile error.

5. **Suspend-set single source of truth**: registry entries for `wait_points` (`features.toml:2078`) and `background_routing` (`features.toml:2086`) explicitly promise "the same set that drives codegen routing, so the hint and the binary always agree." Promotion therefore must be computed in TYPECK (extending the suspend set `may_block.rs` produces) so guards, inlay hints, cross-module `FunctionSig.suspends`, and codegen all agree. Consequence: the independence partition (currently `ynz-codegen/src/independence.rs`, deps only on `ynz_ast` + `ynz_typeck` types) moves into `ynz-typeck`; codegen consumes it from there. Not a parallel implementation — a relocation.

6. **Same-callee CPU calls CAN parallelize** (unlike M3b's I/O members): the I/O same-callee restriction exists because composed sub-frames are keyed by callee NAME in `build_frame_layouts_with_resolver` (`emit.rs:254`). CPU children don't embed a child frame — each spawn gets its own heap ctx, and the parent frame needs only a handle slot + result slot PER GROUP-MEMBER INDEX. `let a = fib(40); let b = fib(41)` — the single most common CPU-parallel pattern — works. This is also the headline demo.

7. **Result ABI**: 16 bytes covers every supported return class — i64 scalars (int/bool), f64 (float), heap pointer (string/array/map), i128 (number/decimal128), `{i64,i64}` EC pair (`-> T errors`). Closure returns a 16-byte POD; `ynz_rt_join_poll(handle, waker_ctx, result_out: *mut u8) -> i32` writes it on Ready. `-> Shape` / `-> Shape errors` are DECLINED (consistent with `WideValueSuspendingReturn` — the non-suspending shape-return base bug is a named pre-existing deferral, not this milestone's to fix).

8. **Panic semantics locked by the identical-output gate**: sequential execution of a panicking CPU callee aborts the program. Parallel execution must match → on `ynz_rt_join_poll` Ready(panic), the parent RE-RAISES (aborts with the same diagnostic path sequential execution takes). Best-effort-discard applies to `background` (fire-and-forget) only — a JOINED result is load-bearing, discard would be a silent wrong answer.

9. **Worth-it heuristic**: spawn ONLY when the callee transitively contains a loop or recursion (proxy for "does real work"); trivial/leaf/arithmetic calls run inline. Implemented as a `does_real_work` fixpoint beside `may_block.rs`'s call-graph walker, carried cross-module via a new `FunctionSig` field (rides the existing `module_signatures_query` propagation exactly like `suspends` — building intra-module-only and retrofitting later would be build-twice). The benchmark-calibrated cost threshold that refines this proxy is this milestone's own perf-spike (Phase 5), per the roadmap ("ship a safe constant, calibrate with evidence" — same protocol as auto-SoA's SIZE_THRESHOLD).

10. **stdout interleaving inside parallel members is locked Model A behavior** (design/concurrency.md "Suspension vs. Ordering", LOCKED 2026-06-05): independent side effects may race; ordering is the user's job via `wait`. Fixtures must assert via aggregates or post-join prints — never interleaving-dependent output. Same policy M3b shipped under.

11. **`wait_on_non_may_block` diagnostic becomes stale** (`features.toml:1553`): its why-text says `wait` on a CPU-bound callee has "identical runtime semantics" — after M3d, an explicit `wait` before a CPU call in a promoted function is a real ORDERING BARRIER (resets the parallel group, per the partition's existing barrier rule). Text must be reworded in Phase 4.

## Design-Doc Alignment

**Governing docs read in full before this plan was drafted**:
- `design/concurrency.md` `[locked]` — auto-parallelization model, Model A ordering semantics, loop-iterations-sequential, M3b design divergences (type-based write-effect floor, same-callee), permanent positional constraints.
- `design/no-function-coloring.md` `[locked]` — no-coloring invariant, CPU-bound task routing ("tasks whose call graph contains zero may-block calls route to a separate blocking thread pool"), Task Cancellation ("CPU-bound tasks have no `wait` points, so cancellation cannot be injected mid-computation… the same behavior as Rust's `spawn_blocking`" — documented constraint, not a gap), Scheduler Preemption Model, work-stealing scheduler lock.
- `design/ide-hints.md` `[locked]` — Informational placement category for the new hint domain.

**Conformance statement**: this plan implements exactly the documented model — poll-based suspension at the join (no coloring, no bridge), blocking-pool routing for zero-may-block work, detach-on-cancel for in-flight CPU tasks, IDE visibility of compiler scheduling decisions ("IDE Execution Plan — Non-Negotiable" in `design/concurrency.md`).

**Divergences carried forward from M3b (already recorded in `design/concurrency.md` "Design Divergences", not new)**: (a) type-based write-effect floor — transfers unchanged to CPU candidacy per the doc's own note ("M3d CPU-parallel reuses this same type-based write-effect source — the floor transfers unchanged"); (b) same-callee sequential — REMAINS for suspending (I/O) members; this plan LIFTS it for CPU members only (per-invocation ctx, no name-keyed sub-frame — see Research Finding 6). The doc's divergence entry gets a scope amendment in Phase 4.

**Milestone-boundary assumptions**: `background` handle-form, channels, auto-Arc, auto-SoA are M4 (roadmap-documented). The borrow-checker narrowing of the write-effect floor is M4 (doc-documented reversal path). Nothing this plan defers is load-bearing for M3d's correctness.

**One gap being closed beyond the milestone's nominal scope, surfaced loudly**: `design/concurrency.md` "IDE Execution Plan — Non-Negotiable" requires the IDE to show which operations run concurrently. M3b shipped `wait_points` + `background_routing` but NO hint at parallel-GROUP sites (verified: no such pass in `inlay_hint_passes.rs`, no registry domain). M3d's new `parallel_groups` Informational hint domain covers BOTH classes (M3b's I/O-overlap groups AND M3d's CPU groups), closing the gap rather than widening it. This is a deliberate scope addition (~1 inlay pass + registry entry + LSP wiring) — flagged in Questions for explicit approval.

## Risks

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| A synchronous join sneaks into ANY path (the `block_on` M2-HALT corpse) | Medium | Critical | P0 spike validates poll-based join end-to-end through the REAL compiler before production code; P5 adversarial gate stress-runs every join path; grep-able invariant: no new `block_on`/`Handle::block_on`/`thread::park` call sites in the diff outside the existing main→entrypoint driver |
| SM-promotion fires an existing suspension guard on previously-compiling code (silent compile break) | High (without the decline design) | Critical | Decline-to-promote with transitive rollback (Research Finding 4) is a named P2 deliverable with dedicated tests: every guard-triggering shape compiled BOTH before and after M3d with identical results |
| Cancellation mid-join dangles a borrowed heap arg in the detached child (UAF) | Medium | Critical | Heap args copied into ctx via existing `BgArgDropEntry` machinery (Research Finding 3); P5 cancellation fixtures run under the alloc=free accounting harness |
| Promotion fixpoint diverges or is order-dependent (nondeterministic builds) | Medium | High | P0 locks the algorithm: intrinsic-rooted may-block fixpoint FIRST, then bottom-up promotion pass in deterministic call-graph order, then guard-probe rollback loop (monotone downward — converges); salsa-tracked so it's also incremental-safe |
| Blocking-pool exhaustion deadlock (N spawns ≫ pool, or recursion spawning groups) | Low (poll-join makes it structurally impossible) | High | Poll-based join never holds a thread (Research Finding 2); P5 stress fixture: recursion fan-out ≫ 512 spawns must complete |
| Same-callee per-invocation keying corrupts frame slots (the M3b name-keying assumption violated) | Medium | High | P0 spike includes the same-callee case explicitly; P3 keys handle/result slots by (group, member-index), never by callee name; danger matrix covers same-callee × every return type |
| Panic in CPU child silently discarded → wrong value bound | Medium | High | Re-raise semantics locked (Research Finding 8); P5 panic fixture asserts identical behavior vs sequential |
| stdout interleaving makes the cross-impl consistency gate flaky | Medium | Medium | Fixture discipline: aggregate/post-join assertions only (Research Finding 10); any fixture printing inside a parallel member must assert order-independent output |
| Spawn overhead exceeds the win for small workloads (perf regression) | Medium | Medium | Worth-it proxy (loop/recursion required) ships from day one; P5 perf spike measures spawn overhead on the CI machine and calibrates; `--no-auto-parallel` is the user escape hatch; explicit `wait` is the per-site escape hatch |
| Moving `independence.rs` to ynz-typeck breaks codegen consumers | Low | Medium | Pure relocation with re-export, all existing unit tests move with it, zero behavior change asserted by the full suite before any M3d feature code lands on top |
| Compile-time cost of new analyses (does_real_work + promotion probe) | Low | Medium | Both ride the existing call-graph walk; roadmap target <10% `ynz build` wall-clock increase on pirates-roster, measured in P5 |

## Questions

1. **Mixed groups (CPU member + I/O member in ONE parallel group)** — e.g. `let data = fetchThing()` (suspending) overlapping `let score = crunch(n)` (CPU). The poll loop handles per-child class naturally (inline-poll vs join-poll); declining them would need MORE code (a class-homogeneity check). **Recommendation: in scope.** Adds adversarial surface — the danger matrix covers it.
2. **`parallel_groups` hint domain retroactively covering M3b's I/O groups** (scope addition per Design-Doc Alignment). **Recommendation: yes** — it's the design doc's non-negotiable IDE surface, and the marginal cost over a CPU-only hint is near zero since both read the same partition.
3. **Same-callee CPU parallelization** (lifting M3b's restriction for CPU members only). **Recommendation: in scope** — it's the headline demo pattern and structurally unblocked (Research Finding 6).

## Risk Assessment & Rollout Strategy

**Risk level: MEDIUM** (compiler project — no payments/auth/PII/SQL; the "data" at risk is miscompiled user programs).

| Criteria | Applies? | Notes |
|---|---|---|
| Touches payments/billing | No | — |
| Touches auth/permissions | No | — |
| Raw SQL / literals | No | — |
| Modifies existing data | No | Codegen-only behavior change, gated by identical-output oracle |
| Third-party integration | Yes (Tokio) | Internal implementation detail; pinned `tokio = "1"`, verified against 1.49 docs |
| Changes existing behavior | Yes | Existing programs get FASTER but must stay byte-identical (`--no-auto-parallel` oracle on every fixture) |

**Mitigations applied:**
- Cross-impl consistency oracle (every fixture, default vs `--no-auto-parallel`, byte-identical stdout/stderr/exit) → the equivalent of a kill switch + full regression harness → MEDIUM stays MEDIUM with high confidence
- Decline-to-promote design → no new compile errors possible from this milestone
- Adversarial gate mandatory (P5) per roadmap

**Rollout plan**: compiler milestone — ships as tagged pre-release `v0.3.0-m7` (local-install VSCode artifact, no marketplace). No staged percentage rollout applies; the staging IS the `-mN` pre-release tag sequence before the `v0.3.0` final (M4).

## Design Divergences

| Doc | What it says | What we do instead | Approved rationale (named cost + reversal path) |
|-----|-------------|-------------------|------------------------------------------------|
| `design/concurrency.md` "Design Divergences → Write-effect type-based floor" | (already-recorded M3b divergence) traces actual writes | CPU candidacy reuses the same type-based floor: mutable-heap-argument calls never parallelize | Pre-approved in the doc itself ("M3d CPU-parallel reuses this same type-based write-effect source — the floor transfers unchanged"). Cost: read-only mutable-heap-arg CPU calls don't overlap. Reversal: M4 borrow-checker narrowing (already documented there). |
| `design/concurrency.md` "Same-callee concurrent calls run sequentially" | (already-recorded M3b divergence) | Lifted for CPU members ONLY (per-invocation ctx + member-index slot keying); REMAINS for suspending members | Cost of keeping it for I/O: unchanged from M3b. Phase 4 amends the doc entry's scope line. No new divergence — a partial reversal of a recorded one. |

_(No NEW divergences from any `[locked]` doc. The plan implements the documented model.)_

## Documentation Deliverables

| Deliverable | Phase | Notes |
|---|---|---|
| `design/concurrency.md` — new "CPU Statement Parallelization (M3d)" section: promotion + decline rules, panic re-raise semantics, cancellation/detach constraint, worth-it proxy, same-callee scope amendment to the M3b divergence entry | Phase 4 | Cross-cutting: documents decisions locked in P0 and implemented in P1–P3 |
| `spec/concurrency.md` — short user-facing note: independent heavy computations run on separate cores automatically; the IDE shows the overlap; `wait` forces order | Phase 4 | HS-grad tone per `.claude/rules/spec-writing.md` |
| `CHANGELOG.md` `[0.3.0-m7]` section | Release (post-P5) | Generated via `/release` |

## Planned RED Repros

| What's intentionally broken (file + function/symbol + line-range) | Locking RED test (path::test name) | Asserted contract | Fixing phase | Prod-exposure note |
|---|---|---|---|---|
| — | — | — | — | _(empty — no planned RED repros; this is feature work, not a bug-fix plan, and no phase ships intentionally-failing tests)_ |

## Invariants This Milestone Must Preserve

### Safety
- A synchronous join (`block_on`, `Handle::block_on`, thread-park, busy-wait) appears in ZERO new code paths; the only `block_on` in the binary remains the pre-existing main→entrypoint driver. (Checkable: grep the diff.)
- Every program that compiles before M3d compiles after M3d with the same diagnostics. Promotion NEVER introduces a compile error — guard-conflicting candidates decline to sequential lowering. (Checkable: guard-shape test corpus compiled pre/post.)
- Every fixture produces byte-identical stdout/stderr/exit-code under default mode vs `--no-auto-parallel`. (Checkable: cross-impl consistency harness.)
- Heap arguments to CPU-spawned children are copied into the child's ctx (`BgArgDropEntry` discipline); no pointer owned by the parent frame is reachable from a detached child. (Checkable: cancellation fixtures under alloc=free accounting.)
- A panicking CPU child re-raises in the parent — identical observable behavior to sequential execution. (Checkable: panic fixture diff vs `--no-auto-parallel`.)
- alloc=free on every new fixture (frames, ctx blocks, handle boxes, arg copies).

### Performance
- Two independent heavy CPU calls overlap on separate cores: demo wall-clock measurably under sequential baseline on the CI machine (P5 records numbers).
- Non-promoted functions pay ZERO new cost: no frame, no spawn, no preemption change — straight-line codegen unchanged (checkable: IR diff on a non-promoted fixture is empty).
- Trivial/leaf callees never spawn (worth-it proxy: transitive loop/recursion required).
- Compile-time: <10% `ynz build` wall-clock increase on `examples/pirates-roster/` (roadmap target; measured P5).
- **Auto-promotion analysis** (mandatory per `.claude/rules/auto-promotion.md`): this milestone IS an auto-promotion. Stricter/faster form = thread-parallel execution of independent CPU statements; compiler proves fit via independence + worth-it + guard-probe. Surfaces: codegen auto-promotion YES; muted hint — NO TYPEABLE explicit form exists (there is no "run in parallel" syntax), so per the inference rule the hint is the **Informational** `parallel_groups` domain (comment-style), not click-to-make-explicit; Tier 3 lint — NO (nothing to rewrite toward; consistent with auto-SoA precedent). Override directions: force-the-OTHER-pick (sequential) = existing syntax — explicit `wait` between statements (per-site) or `--no-auto-parallel` (program-wide); force-the-auto-pick (parallel) = existing syntax — explicit `background` spawns. Both directions handled by existing syntax → no new API (documented omission per the rule).

### Teaching
- New `parallel_groups` Informational muted-hint domain fires on every member of every Parallel group (CPU and I/O classes), with WHAT/WHAT-INSTEAD/WHY hover text; wired through registry → LSP inlayHint → VSCode.
- `wait_on_non_may_block` why-text reworded: explicit `wait` before a CPU call is an ordering barrier (group reset), not a no-op (in promoted contexts).
- No new compile-error classes ship (decline is silent by design) — explicitly documented in the error-gallery note so reviewers know it was considered, not forgotten.
- No banned jargon in any new user-facing text (`jargon_audit` green; no "thread", "spawn", "join" jargon leaks into hints — naming per `.claude/rules/vocabulary.md`, e.g. "runs at the same time as line N").

### Runtime Dependencies
- `ynz_rt_spawn_blocking_joinable` / `ynz_rt_join_poll` / handle-free shim: require the Tokio runtime (`ynz_rt_init`) and malloc — same dependency class as existing spawn primitives.
- Promotion analysis, independence partition, worth-it fixpoint: compile-time only, zero runtime dependency.
- Non-promoted code: zero new runtime dependencies.

### Kernel-Mode Behavior
- `--kernel` mode: the CPU-parallel pass is DISABLED entirely (no scheduler exists to spawn onto). Functions lower sequentially — identical output, no new diagnostics. This is the "decline" path, not an error: auto-promotion never errors on valid code, and kernel mode simply fails the runtime-dependency fit. (Decision locked here per the roadmap's "clean reject vs. inline-sequential fallback — decide at plan time": **inline-sequential fallback**. A clean reject would turn valid sequential-looking code into an error because the OPTIMIZER wanted to fire — that violates the auto-promotion rule and the "no new user-facing syntax / identical semantics" constraint.)
- `wait`/`background` remain compile errors in kernel mode (unchanged, `check.rs:142-186`).

### Demo & Error Gallery
- `examples/pirates-roster/entrypoint.ynz` gains a CPU multicore section (two independent heavy computations — realistic roster-stats crunch, not `print(featureName())`) with inline comments pointing at the `parallel_groups` IDE hint. insta stdout snapshot.
- `examples/primantis-orders/`: M3d introduces NO new compile-error classes (decline is silent). The milestone gallery file gets a header note recording that fact explicitly (the considered-not-forgotten requirement). Existing gallery snapshots re-verified against the reworded `wait_on_non_may_block` text.
- Both files snapshot-verified in P5.

### Feature Registry Entries
- New `[[muted_hint_domain]]`: `parallel_groups` (placement_category = "Informational", fires on members of auto-parallel groups, both classes).
- Modify `[[diagnostic_template]]` `wait_on_non_may_block` (`features.toml:1553` region): why-text reworded for ordering-barrier semantics.
- Modify `[[muted_hint_domain]]` `background_routing` description if wording drifts from the new shared analysis home (verify in P4; likely unchanged).
- No new keywords, banned_jargon, primitive_intrinsics, type_attached_constants. No new `[[deferred_language_feature]]`/`[[deferred_tooling_feature]]` expected; if the P5 perf spike defers the calibrated cost threshold, it lands as a documented constant + design-doc note, not a registry deferral (it refines an internal heuristic, not a user-facing feature).

## Anti-Pattern Callouts

- **Splitting into commits instead of PRs**: each phase is an independently-reviewable PR on `feat/m3d-cpu-parallelization` via `/pr`; phase boundaries are commit+review gates per the Exit Sequence.
- **Shadow main branches**: one feature branch off `main`, merged per phase or at milestone end via PR — no long-lived parallel main.
- **Building the engine before shipping value**: P0–P1 are deliberately thin (spike + ABI); the first user-visible value (working CPU overlap on real fixtures) lands in P3, teaching in P4 — no phase builds speculative infrastructure beyond what the next phase consumes.
- **Hotfix that isn't**: no hotfixes planned; any pre-existing bug discovered (e.g., the non-suspending `-> Shape` return base bug) is DECLINED-AROUND (decline-to-promote) and stays on its existing roadmap/todos track — not quietly half-fixed here.
- **Abandoned branches**: the branch lives exactly as long as the milestone; `/release` for `v0.3.0-m7` closes it.
- **Flag graveyards**: no feature flags — the oracle flag `--no-auto-parallel` is a permanent test utility (M1-mandated), not a rollout flag needing cleanup.

## Phase Execution Protocol

Each phase ends with the Exit Sequence: persist plan bookkeeping → resolve `$BASE` (Phase N≥2: prior phase's `Committed:` SHA; Phase 0: `plan_base:` from front-matter) → fan out the full reviewer set + one deviation-judge per documented deviation in parallel → coordinator writes Evidence + Phase Review Gates → handle verdicts → prompt commit. Canonical fan-out spec: `~/.claude/commands/execute-plan.md` Step 3.d–3.h (referenced, not duplicated, per `no-duct-tape.md` #7). Final phase additionally runs the cumulative Opus sweep per Step 4.a.

## Phases

### Phase 0: Spike — joinable-join vertical slice on the REAL compiler + decision lockdown
**PR scope**: Throwaway-tolerant vertical slice proving the poll-based CPU join end-to-end through `./target/debug/ynz run` on real `.ynz`, plus the locked decision record. Accept/reject gate.
**Branch**: `feat/m3d-cpu-parallelization`
**Flag**: N/A
**Est. lines**: ~400 (spike code, may be reshaped in P1–P3)
**Ships via**: `/pr` (draft; the PR carries the spike verdict + locked decisions)
**Objective**: Validate, against the real compiler and runtime, that (a) a prototype `ynz_rt_spawn_blocking_joinable` + `ynz_rt_join_poll` pair drives a 2-member CPU group through spawn → suspend-at-join → resume → result-bind with correct values; (b) the same-callee case works with per-invocation keying; (c) pool saturation queues without deadlock; (d) panic re-raise and cancellation-detach behave as locked. Lock the promotion-fixpoint algorithm.
**Why this phase exists**: M2's HALT happened because its spike validated a hand-written model that diverged from emitted codegen. This spike compiles real `.ynz` through `./target/debug/ynz` (spike-artifact-validity rule). The CPU join is M2-corpse-adjacent — one synchronous-join mistake reintroduces the crash — so the riskiest mechanism is proven before any production phase builds on it.
**Current-state anchors**:
- `crates/ynz-runtime/src/runtime.rs:591` — `ynz_rt_spawn` (existing fire-and-forget SM spawn; the future-wrapper pattern to mirror)
- `crates/ynz-runtime/src/runtime.rs:140` — `ynz_rt_spawn_blocking` (ctx-copy discipline at `runtime.rs:89`)
- `crates/ynz-runtime/src/runtime.rs:663` region — `ynz_rt_async_sleep_create`/`_poll` (the handle-in-frame-slot poll protocol the join mirrors)
- `crates/ynz-codegen/src/emit.rs:6061` — `emit_independent_group_poll` (the join loop the CPU child class extends)
- `crates/ynz-codegen/src/emit.rs:1847` — SM-lowering dispatch (`suspend_set.contains`)
**Files (expected scope)**: `crates/ynz-runtime/src/runtime.rs`, `crates/ynz-codegen/src/emit.rs` (spike-gated path), one spike fixture dir under `crates/ynz-driver/tests/fixtures/`, this plan file (decision record).
**Deviation rule**: Executor MAY touch files not listed if the change serves the planned work (lint fix in adjacent code, blocking bug, missing dependency). Document each deviation in the PR description with a one-line reason. If a deviation is its own concern, STOP — split into a separate PR or revise this plan.
**Steps**:
1. Prototype runtime pair: `ynz_rt_spawn_blocking_joinable(fn_ptr: extern "C" fn(*mut u8) -> YnzCpuResult, ctx_ptr, ctx_size) -> *mut u8` (heap handle boxing `JoinHandle<YnzCpuResult>` where `YnzCpuResult = [i64; 2]`), `ynz_rt_join_poll(handle, waker_ctx, result_out: *mut u8) -> i32` (1=Pending with waker registered, 0=Ready with 16 bytes written + handle box dropped; Ready(panic) → re-raise), and a handle-free shim for the frame drop path.
2. Hardwire a minimal promotion + lowering path for ONE fixture shape (two int-returning calls to a recursive `fib`-style callee), gated behind env `YNZ_M3D_SPIKE=1` so nothing leaks into default builds.
3. Spike fixtures, all run via `./target/debug/ynz run`: (a) distinct-callee 2-group, correct values bound; (b) SAME-callee 2-group (`fib(35)` / `fib(36)`), correct distinct values; (c) timing proof: wall-clock < sequential sum (coarse — e.g. two ~1s workloads complete in <1.6s); (d) saturation: ≥600 spawned joins complete (queueing proven, no deadlock); (e) panic-in-child re-raises; (f) cancellation: parent dropped mid-join → no UAF/leak under the alloc accounting; (g) MIXED group (one CPU child + one `sleep`-backed I/O child sharing the continuation state) — including the CPU-child-finishes-FIRST ordering, since a Ready join-poll must not corrupt the still-Pending inline-poll state (reviewer-flagged highest-risk interaction; proving it here, not first in P3).
4. Lock and record in this plan + P0 PR description: the promotion fixpoint algorithm (intrinsic-rooted may-block fixpoint first → bottom-up promotion pass in deterministic call-graph order → guard-probe transitive rollback loop, monotone downward), the decline-rule enumeration (all guards from Research Finding 4 + Shape returns + kernel mode + `--no-auto-parallel` + call-cycle membership), result ABI, panic re-raise, heap-arg copy discipline, mixed-group support.
5. **Accept/reject gate**: if the simplest distinct-callee case cannot work without a synchronous join anywhere, or fixture (c)'s overlap doesn't materialize, HALT the milestone and re-plan — do NOT proceed to P1 with a workaround.
**Acceptance criteria**:
- [x] Spike fixtures (a)–(f) all pass through `./target/debug/ynz run` with documented output in the PR
  - Evidence: All six fixtures live-verified by R8 acceptance-verifier 2026-06-12 in devcontainer (LLVM 18) — outputs below re-confirmed; (c) timing re-measured 1218ms (spike) vs 1780ms (default), < 1600ms threshold ✓; saturation_600_joins re-run green (640 joins, no deadlock); drop-witness tests re-run green. Original documented outputs (2026-06-11):
    - (a) distinct callee (`v0_3_m3d_spike_a_distinct.ynz`): `6765\n10946\n`, exit 0 — fib(20)=6765, fib2(21)=10946 ✓
    - (b) same callee (`v0_3_m3d_spike_b_same_callee.ynz`): `6765\n10946\n`, exit 0 — fib(20), fib(21) distinct values ✓
    - (c) timing proof (`v0_3_m3d_spike_c_timing.ynz`): `102334155\n165580141\n`, exit 0 — parallel wall-clock 1317ms vs sequential 1913ms (<1600ms threshold) ✓
    - (d) saturation (`v0_3_m3d_spike_d_saturation.ynz`): fib(30)/fib(31) pair; output `832040\n1346269\n`, exit 0 — 2 joins, 0 failures ✓. ≥600-join saturation proof: `saturation_600_joins` unit test in `crates/ynz-runtime/src/lib.rs` spawns 640 individual joinable tasks via `ynz_rt_spawn_blocking_joinable` (JOIN_COUNT=640, above the 512-thread default pool), polls all 640 to Ready, asserts all results correct — no SpawnStateFnFuture involved.
    - (e) panic re-raise (`v0_3_m3d_spike_e_panic.ynz`): `RUNTIME ERROR: division by zero (int)`, exit 1 — identical sequential vs spike ✓
    - (f) cancellation (`v0_3_m3d_spike_f_cancel.ynz`): `RUNTIME ERROR: division by zero (int)`, exit 1 — will_panic(0) panics, in-flight fib(35) handle freed by frame drop ✓
  - Bonus fixture (g) (`v0_3_m3d_spike_g_mixed.ynz`): `55\n89\ndone\n`, exit 0 — CPU group (fib(10)+fib(11)) joins, then `wait sleep(50)` runs sequentially; prior "continuation state 3 out of range" error resolved by R3-2 (sm_scope_depth guard) + R3-3 (reload exclusion). Fixture (g) is NOT in ACs.
  - Bonus fixture (h) (`v0_3_m3d_spike_h_two_waits.ynz`): `55\n89\n`, exit 0 — CPU group (fib(10)+fib(11)) then two sequential `wait sleep(0)` calls; proves spike_reload_cpu_results_from_frame correctly reloads frame bytes into pre-allocated sm_entry allocas across each suspension (LLVM SSA dominance satisfied: allocas are in the function entry block, dominate all state blocks). Fixture (h) is NOT in ACs; added in R3, dominance fix landed in R5 post-gate.
  - Regression fixture (i) (`v0_3_m3d_spike_i_mixed_locals.ynz`): `99\n55\n89`, exit 0 — `let c = 99` pre-pair, CPU group (fib(10)+fib(11)), then `wait sleep(0)` then `print(c)`, `print(a)`, `print(b)`. Pre-pair local `c` is lowered before spawn (F6-2); crossing-local slot base shifted past 6-slot spike reserve (SPIKE_SLOT_RESERVE=6, locals start at byte 80+) so flush of `c` no longer collides with spike result slots. Added R4 (frame-slot collision regression); filename corrected R6. Fixture (i) is NOT in ACs.
  - Regression fixture (j) (`v0_3_m3d_spike_j_locals_after_join.ynz`): `55\n89\n144`, exit 0 — CPU group (fib(10)+fib(11)) then `let c = a + b` then `wait sleep(0)` then `print(a)`, `print(b)`, `print(c)`; confirms crossing-local after join is correctly flushed and reloaded across the suspension. Added R4; filename corrected R6. Fixture (j) is NOT in ACs.
  - Admission fixture (k) (`v0_3_m3d_spike_k_param_host.ynz`): host function has ≥1 params; CPU pair is present but spike is declined (zero-param admission invariant). Both modes byte-identical (sequential output). Added R5. Fixture (k) is NOT in ACs.
  - Admission fixture (l) (`v0_3_m3d_spike_l_arity_decline.ynz`): CPU calls have arity ≠ 1 (`add(10,20)` or zero-arg `getval()`); spike is declined by `args_lowerable` guard. Both modes byte-identical. Added R5. Fixture (l) is NOT in ACs.
  - Mutation fixture (m) (`v0_3_m3d_spike_m_mutated_result.ynz`): CPU group produces `a=fib(10)`, `b=fib(11)`, then `a=999` in rest_stmts; both modes print `999` then `89`. DECLINE fixture: result-name reassignment in rest_stmts refuses spike admission (R6 replaced the R5 prune mechanism with admission decline), so both modes lower sequentially — byte-identical trivially. IR-verified 0 spawn-call instructions (R7/R8). Added R5; mechanism description corrected R8. Fixture (m) is NOT in ACs.
  - Nested-wait fixture (n) (`v0_3_m3d_spike_n_nested_wait.ynz`): nested wait inside an if-block in rest_stmts; CPU results remain correct across the nested suspension (reload_params_from_frame calls spike_reload at every sm_scope_depth via reload_crossing:true; result allocas pre-allocated in sm_entry dominate all state blocks). Both modes byte-identical. Added R5. Fixture (n) is NOT in ACs.
  - FIRES/DECLINES envelope (IR-verified R8 by executor AND independently by acceptance-verifier): (a)/(b)/(c)/(d)/(e)/(f)/(g)/(h)/(i)/(j)/(n) each emit 2 `call @ynz_rt_spawn_blocking_joinable` instructions under `YNZ_M3D_SPIKE=1` — the spike FIRES, including through post-pair intrinsic waits, so the cross-suspension reload machinery is live-exercised. (k)/(l)/(m)/(o)/(p)/(q) emit 0 — admission correctly declined (params, arity, mutation, non-entrypoint, pre-pair wait, post-pair suspending callee respectively); byte-identical via sequential lowering.
- [x] No `block_on`/thread-park in any spike path (grep of diff cited)
  - Evidence: R8 acceptance-verifier: `git diff 8b99ac9 -- crates/ynz-codegen/src/emit.rs crates/ynz-runtime/src/runtime.rs | grep -E "^\+.*(block_on|thread::park|spin_wait|busy.wait)"` → two comment lines only (the invariant declaration comment at runtime.rs:176-177). Zero actual call sites in production code. The sole `block_on` in the full diff is inside the `#[tokio::test]` `panic_reraises_in_parent` in lib.rs (test harness needing a synchronous poll to catch the panic payload) — not a spike path.
- [x] Decision record (fixpoint algorithm + decline rules + ABI + panic + cancellation + mixed groups) written into this plan's Phase 0 notes and the PR description
  - Evidence: Phase 0 Decision Record section below (items 1–10), locked 2026-06-11; R8 acceptance-verifier confirmed all six AC-named categories present: fixpoint algorithm (item 8), decline rules / admission envelope (item 9), ABI (items 1–2), panic (item 6), cancellation (items 4–5), mixed groups (item 10).
- [x] Accept/reject verdict stated explicitly with the timing numbers
  - Evidence: Phase 0 Step 5 Verdict below — explicit "ACCEPT. Proceed to Phase 1." with timing numbers (parallel ~1256ms vs sequential ~1795ms, < 1600ms threshold), all 6 gate conditions enumerated. R8 live corroboration: 1218ms (spike) vs 1780ms (default) — still under threshold.
**Quality gate**:
- [x] Spike code clearly gated (`YNZ_M3D_SPIKE`) — zero default-build behavior change (full existing suite green)
  - Evidence: `cargo test --workspace` (spike env var absent) → all test-result lines `ok`, 0 failed across all crates (verified 2026-06-11).
- [x] Spike exercised the REAL compiler artifact, not a hand-written model
  - Evidence: All fixtures run via `./target/debug/ynz run <fixture.ynz>` — real `.ynz` source through the full pipeline (parser → typeck → codegen → LLVM → binary → execute).
- [x] Cancellation fixture ran under alloc=free accounting
  - Evidence: `SpawnStateFnFuture::drop` step 1.5 calls `cleanup_spike_cpu_handles` which reads the discriminator at frame offset 4; if `SPIKE_FRAME_MAGIC` is present it frees and nulls each non-null handle slot. Test evidence: `discriminator_drop_frees_spike_handles` (direct helper test — slot-0 live, slot-1 null, normal-frame no-op) and `spawn_state_fn_future_drop_before_first_poll_frees_handles` (end-to-end delegation: SpawnStateFnFuture created with spike-magic frame + live handle, dropped without polling — blocking task completes after detach).
**Verification**: `cargo test --workspace` green (spike gated off); `YNZ_M3D_SPIKE=1 ./target/debug/ynz run <spike fixtures>` outputs recorded; timing numbers in PR.

**Phase Review Gates** (filled at phase completion by coordinator; 8 gate rounds total — each line shows the agent's LATEST verdict; full per-round history in Findings Log):
- [x] code-reviewer: PASS 2026-06-12T16:30 (R7 — ISSUE-A/B confirmed resolved via 6 adversarial inputs; non-blocking concerns logged for P1)
- [x] rules-compliance-reviewer: PASS 2026-06-12T05:37 (R6 — zero violations; R4/R5 not-confirmed findings adjudicated per Rule 11, recorded in Findings Log for Patrick's overrule)
- [x] plan-adherence-verifier: PASS 2026-06-12T05:37 (R6 — all R5 items resolved)
- [x] acceptance-verifier: PASS 2026-06-12T17:03 (R8 — all 4 ACs MET on own live runs; FIRES independently IR-verified; 1924 workspace tests green)
- [x] design-compliance-reviewer: PASS 2026-06-12T05:37 (R6 — no [locked]-doc contradictions; M2-HALT corpse clean: no block_on/park in production paths)
- [x] deviation-judge #1 (scope: lib.rs m3d_join_shims test module): last direct verdict R5 BLOCK (ISSUE-1 drop-test blindness) — fix verified R6 by judges #13/#17 + drop-witness mutation test; direct re-judge deferred to cumulative 4.a per Patrick's focused-re-gate authorization 2026-06-12
- [x] deviation-judge #2 (scope: runtime_decls.rs + 13 golden snapshots): PASS 2026-06-11T23:30 (R5)
- [x] deviation-judge #3 (scope: state.md build-env note): PASS 2026-06-11T23:30 (R5)
- [x] deviation-judge #4 (approach: extern "C-unwind" ABI honesty): PASS 2026-06-11T23:30 (R5 — honest C-unwind docs verified at every constructible call site)
- [x] deviation-judge #5 (approach: CpuJoinHandle private field + constructor): PASS 2026-06-11T23:30 (R5)
- [x] deviation-judge #6 (approach: detection-scope gate agreement): PASS 2026-06-12T05:37 (R6)
- [x] deviation-judge #7 (approach: spawn→poll extraction + adjacency/data-dep/arity guards): PASS 2026-06-12T05:37 (R6)
- [x] deviation-judge #8 (approach: null-guard on spawn return): PASS 2026-06-11T23:30 (R5)
- [x] deviation-judge #9 (approach: F7 reload / spike-owned crossing exclusion): PASS 2026-06-12T05:37 (R6 — 7 pure-read adversarial shapes clean)
- [x] deviation-judge #10 (approach: Cg-field spike_crossing carry): PASS 2026-06-12T05:37 (R6)
- [x] deviation-judge #11 (approach: spike frame discriminator): last direct verdict R5 BLOCK (ISSUE-1, same root as #1) — fix verified R6 by judges #13/#17 + drop-witness mutation test; direct re-judge deferred to cumulative 4.a per Patrick's authorization 2026-06-12
- [x] deviation-judge #12 (approach: locals-base shift past spike reserve): PASS 2026-06-12T05:37 (R6 — heap corruption gone)
- [x] deviation-judge #13 (approach: cleanup_spike_cpu_handles helper extraction): PASS 2026-06-12T05:37 (R6 — delegation deletion → both tests fail 0≠1)
- [x] deviation-judge #14 (approach: sm_entry result-alloca pre-allocation): PASS 2026-06-11T23:30 (R5)
- [x] deviation-judge #15 (approach: prune→admission-decline + pre-pair lowering + pre/post-pair suspending gates, R8-narrowed): PASS 2026-06-12T17:03 (R8 — fresh adversarial inputs on the narrowed predicate all held; identity hash fdc8dc2)
- [x] deviation-judge #16 (approach: entrypoint-only admission gate): PASS 2026-06-12T16:30 (R7 — background post-pair admitted+harmless; ISSUE-B repro declines byte-identically)
- [x] deviation-judge #17 (approach: #[cfg(test)] drop-witness probe in CpuJoinHandle): PASS 2026-06-12T05:37 (R6 — layout-safe, no spurious increments)
- [x] Committed: dcc1432

**Findings Log**:
- 2026-06-11T18:52 — code-reviewer round 1: BLOCK. Panic fixture (e) and cancellation fixture (f) validate by process-abort coincidence — Yinz div-by-zero calls `process::abort()` from the child thread, so `ynz_rt_join_poll`'s `resume_unwind` branch never fires end-to-end, and `ynz_rt_join_handle_free` is declared but NEVER called by spike codegen (no drop-shim wiring); Step 5 ACCEPT gate condition 5 overstated. `crates/ynz-runtime/src/runtime.rs` fn_ptr path, `crates/ynz-codegen/src/emit.rs` spike path, fixtures e/f. Also: wrong "LLVM normalizes allocas" comment ~emit.rs:6640; 13 snapshots gained 3 unconditional declares ("IR diff empty" invariant literally false).
- 2026-06-11T18:52 — plan-adherence-verifier round 1: BLOCK. (1) `crates/ynz-codegen/src/runtime_decls.rs` is Phase 1 declared scope, touched in Phase 0 silently (3 unconditional declarations); (2) 13 golden snapshot updates cascade, undocumented; (3) Step 3(g) "proving it here, not first in P3" — spike FAILS fixture (g) ("continuation state 3 out of range") and the failure was reclassified as out-of-scope observation without deviation entry or user sign-off (silent scope narrowing per no-duct-tape.md).
- 2026-06-11T18:52 — acceptance-verifier round 1: BLOCK (AC1 WEAK, AC-quality-gate-cancellation WEAK). Saturation fixture (d) runs exactly 2 joins (one fib(30)/fib(31) pair, 2 output lines), NOT the ≥600 spawned joins Step 3(d) requires — plan Evidence claim "300 instances × 2 = 600 joins" factually wrong. Cancellation "alloc=free accounting" claim has no allocation ledger behind it (Arc-witness unit test only). Other ACs MET by live runs: (a)(b)(c)(e)(f) confirmed, timing 1199ms parallel vs 1906ms sequential.
- 2026-06-11T18:52 — deviation-judge #2 (approach: extern "C-unwind" ABI) round 1: BLOCK. `fn_ptr` is `extern "C" fn` at runtime.rs:924 — a panicking worker aborts the process at the spawn-closure boundary (RFC 2945) BEFORE Tokio's catch_unwind can form a JoinError; the C-unwind fix on `ynz_rt_join_poll` fixes the exit but the entry is welded shut. Sibling pattern: `ynz_rt_spawn_blocking` wraps fn_ptr in catch_unwind at runtime.rs:198-200.
- 2026-06-11T18:52 — deviation-judge #3 (approach: CpuJoinHandle pub(crate)) round 1: BLOCK. Inner field `pub(crate)` lets any crate code call `.abort()`/`.poll()` on a handle the doc declares opaque with mutually-exclusive consumption paths. Narrow fix: private field + `pub(crate) fn new()` constructor (~4 lines), test uses `CpuJoinHandle::new(...)`. runtime.rs:884-898.
- 2026-06-11T18:52 — deviation-judge #4 (approach: detection scope) round 1: BLOCK. `spike_extract_cpu_group` (emit.rs:6258+) lacks the `int_returning_callees` filter `spike_cpu_candidates` has — a suspending parent calling one int + one non-int callee gets NO extra frame bytes (gate 1 None) but the spawn path fires anyway (gate 2 Some) → out-of-bounds frame writes at offsets 32-79 + LLVM type mismatch on the trampoline. Fix: same membership check in both gates.
- 2026-06-11T18:52 — deviation-judge #5 (approach: spawn→poll_state) round 1: BLOCK. Extraction takes any two non-suspending calls BY INDEX with no data-dep check: `let a = fib(20); let b = a + 1; let c = fib(b)` extracts calls 0+2, intervening `let b` lands in rest (lowered after spawn) → fib(b) spawns with missing/stale b. Fix: adjacency guard `second_idx == first_idx + 1` at emit.rs:~6290.
- 2026-06-11T19:50 — ROUND 2 (post F1–F9 fix round; all round-1 findings verified fixed by respective reviewers/judges). 6 PASS: rules-compliance, design-compliance, judges #1 (lib.rs scope), #2 (runtime_decls scope), #3 (C-unwind + catch_unwind), #4 (CpuJoinHandle constructor). 7 BLOCK, all NEW findings:
- 2026-06-11T19:50 — code-reviewer round 2: BLOCK. `spike_reload_cpu_results_from_frame` emits invalid LLVM IR when >1 suspension follows the CPU group (live repro: "Instruction does not dominate all uses" on reload alloca); Deviation #8 rationale + doc comment claim generality ("after each suspension") the code lacks. emit.rs:6367-6411. Concerns: Decision Record item 6 has self-contradicting "enables true end-to-end resume_unwind propagation" clause; detach_ctx_alloc_balanced doc oversells "alloc=1, free=1".
- 2026-06-11T19:50 — plan-adherence-verifier round 2: BLOCK. `.claude/state.md` executor-written build-env note is an undocumented scope deviation (not in any of the 8 scratch entries). Cross-flags: fixture (g) Evidence text at plan ~line 244 is stale (still describes the old failure); no-block_on grep evidence cites `git diff HEAD` not `git diff 8b99ac9`. Round-1 BLOCKs #1-#3 all verified RESOLVED.
- 2026-06-11T19:50 — acceptance-verifier round 2: BLOCK (AC1 WEAK only). All fixtures (a)-(g) pass live (verifier's own runs; (c) 1265ms parallel vs 1742ms sequential); saturation_600_joins + detach_ctx_alloc_balanced + drop_detach_no_uaf pass live; quality gates MET incl. alloc-ledger. Sole fix: fixture (d) Evidence sub-bullet (plan ~line 241) still claims "300 concurrent instances × 2 spawns = 600 joins" — false; fixture runs 2 joins; ≥600-join proof lives in saturation_600_joins (lib.rs:~3150). Correct the text.
- 2026-06-11T19:50 — deviation-judge #5 identity 7a6f20e (detection scope) round 2: BLOCK (fresh input). Filter unified but SCOPE not: gate 1 (spike_cpu_candidates) scans top-level stmts non-recursively; gate 2 fires in lower_sm_block's RECURSION into if/loop bodies. Nested adjacent CPU pair in an if-body of an already-suspending fn → gate 1 allocates 0 extra frame/state, gate 2 spawns anyway → state-index collision + OOB frame GEPs. Fix: guard spike path with `cg.sm_scope_depth == 0`.
- 2026-06-11T19:50 — deviation-judge #6 identity bba0e83 (spawn→poll) round 2: BLOCK (fresh input; round-1 input confirmed excluded). ADJACENT data-dependent pair `let a = fib(20); let b = fib(a)` passes adjacency guard → child 1 arg eval at spawn time hits unbound `a` → codegen Err on valid program. Fix: in spike_extract_cpu_group, return None when second call's args reference first call's bind name (~5-8 lines).
- 2026-06-11T19:50 — deviation-judge #7 identity 73c553f (null-guard) round 2: BLOCK (fresh input; round-1 orderings still hold). Cancellation leak: SpawnStateFnFuture::drop reads only FRAME_SLEEP_HANDLE_OFFSET=8 (runtime.rs:448-454); spike CPU handle slots 32/40 never cleaned → cancel parent mid-join with a live handle leaks the CpuJoinHandle box. Rationale's "matches sleep-handle protocol" is half-true (poll leg yes, drop leg missing). Fix: cleanup leg for spike handle slots — MUST NOT misread non-spike frames (use per-frame drop-shim mechanism or spike-frame discrimination).
- 2026-06-11T19:50 — deviation-judge #8 identity 8f8ffa4 (F7 reload) round 2: BLOCK. Same multi-suspension dominance failure as code-reviewer, deeper diagnosis: standard crossing slots for CPU results never written (reload_params_from_frame loads garbage into them at first wait), and second wait's reload_params_from_frame stores to the stale post_wait_bb_1 reload alloca from non-dominated cont_state_bb_2 → LLVM verifier abort. Surgical fix: exclude spike-owned CPU-result names from reload_params_from_frame's crossing loop (emit.rs:3182 region); spike_reload owns them exclusively.
- 2026-06-11T20:56 — ROUND 3 (post R3-1–R3-8; all round-2 findings verified fixed by respective reviewers/judges; D_count=11 incl. coordinator-identified #11 frame discriminator). 12 PASS: rules, design, plan-adherence (all R2 items resolved), acceptance-verifier (all ACs + quality gates MET by live runs), judges #2 (runtime_decls ABI), #3 (state.md), #4 (C-unwind; noted "Box leaks on panic path" comment now stale — discriminator rescues spike frames), #5 (constructor; #[cfg(test)] would be tighter — P1 note), #6 (gate divergence → harmless over-allocation), #8 (null-guard; TOCTOU closed by Tokio cooperative scheduling; P1 note: null-from-spawn silently skips instead of aborting), #10 (Cg-field exclusion tight), #11 (discriminator sound — offset 4 unreachable by non-spike writes, allocator verified zeroing). 4 BLOCK:
- 2026-06-11T20:56 — code-reviewer round 3: BLOCK. SILENT ORACLE DIVERGENCE (live repro): spike result/handle slots hardcoded at frame offsets 32/40/48/64 COLLIDE with crossing-local slots (FRAME_OFFSET_LOCALS_START=32 + idx*8). Mixed body (CPU pair + `let c = a + b` between two waits) → flush of `c` physically overwrites spike result region → prints 144 where 55 belongs, exit 0. emit.rs:2189 + 6238-6241, state_machine.rs:496. The R3 name-exclusion patched reloads; nothing stops the FLUSH from clobbering. Fix: spike slots must not overlap the crossing-local region (shift one region); committed mixed-shape regression fixture required. Concern: discriminator drop branch (runtime.rs:490-500) has zero test coverage.
- 2026-06-11T20:56 — deviation-judge #9 identity 8f8ffa4 (F7 reload) round 3: BLOCK. Same root cause as code-reviewer, independent live repro: 3rd crossing local `c` lands at byte 48 = SPIKE_RESULT_0_OFFSET; flush clobbers fib(10) result; output `99\n99\n89` vs expected `99\n55\n89`. Fixes named: dynamic spike-slot base after all crossing-local slots, OR decline-to-spike when non-spike crossing locals exist.
- 2026-06-11T20:56 — deviation-judge #7 identity bba0e83 (spawn→poll) round 3: BLOCK (fresh input; R2 input confirmed excluded). Data-dep guard is a FLAT Expr::Ident match — `fib(a + 1)` / `fib(max(a, 2))` pass the guard, then crash at the unsupported-arg arm (emit.rs:6710) instead of declining to sequential. Fix: recursive expr_contains_ident walk, or (narrower) extraction admits ONLY args the spike arg-evaluator can lower (IntLit / Ident-not-matching-bind-name) and declines everything else.
- 2026-06-11T20:56 — deviation-judge #1 identity 1b473f7 (lib.rs scope) round 3: BLOCK (fresh angle after 2 PASSes). Test `detach_ctx_alloc_balanced` name asserts alloc balance; its body proves only "child ran once, no UAF" — and the R3 doc trim explicitly admits "Does NOT directly measure malloc/free." False contract. Fix: rename to match assertion (e.g. detach_child_runs_after_handle_free) OR add real alloc/free instrumentation.
- 2026-06-11T20:56 — acceptance-verifier round 3: PASS with one coordinator-actionable text correction: fixture (d) Evidence still says "300 concurrent SpawnStateFnFuture instances × 2 = 600" — actual test is 640 individual joinable spawns via ynz_rt_spawn_blocking_joinable (JOIN_COUNT=640), no SpawnStateFnFuture involved. Replacement language provided in verifier report.
- 2026-06-11T21:48 — ROUND 4 (post F4-1–F4-5; D_count=13). 9 PASS: design, acceptance-verifier (all ACs + gates MET live; fixtures (i)/(j) byte-identical), judges #1 (rename clean), #2, #3, #5 (field privacy E0616-verified), #6 (declined-but-shifted frames consistent, 4 live inputs), #8 (poll/null/drop ordering intact), #11 (discriminator holds; slot floor=byte 32). 9 BLOCK:
- 2026-06-11T21:48 — code-reviewer round 4: BLOCK. PARAM-SLOT COLLISION (live repros): param slots use raw indices 0..n_params → bytes 32+ collide with fixed spike region. 1-param host: silent wrong output (seed 7→0, b 89→88). 2-param host: param value 13 forged as Box pointer → `free(): invalid pointer`. emit.rs:2704-2711, 3198-3199. All 10 fixtures use 0-param entrypoint — dimension never exercised. Fix: shift params past reserve OR decline-to-spike on n_params>0 + param'd-host fixtures. Concern: frame-layout comment documents the overlap without flagging it.
- 2026-06-11T21:48 — deviation-judge #12 identity 8fee9ca (locals-base shift) round 4: BLOCK. Same param collision, independent live repro (compute(42) prints a pointer value). Narrow fix named: `if !f.params.is_empty() { return None; }` in spike_cpu_candidates (~3 lines); all fixtures stay green (all 0-param).
- 2026-06-11T21:48 — deviation-judge #7 identity bba0e83 round 4: BLOCK (fresh input). args_lowerable checks arg FORM not COUNT: `add(10, 20)` passes (both IntLit) → one-arg trampoline calls two-param callee → LLVM verifier abort (live). Zero-arg passes vacuously → Err arm. Fix: `call.args.len() == 1 &&` at emit.rs:~6428 (doc comment already says "exactly one").
- 2026-06-11T21:48 — deviation-judge #9 identity 8f8ffa4 round 4: BLOCK (fresh input; R3 collision input now passes). MUTATION STALENESS (live repro): `let a = fib(10); ... wait; a = 999; wait; print(a)` → prints 55 not 999. Mutation flushes to crossing slot (byte 80) but spike_reload re-reads frozen SPIKE_RESULT slot (byte 48) on next wake. Fix: prune name from spike_crossing + un-exclude on Stmt::Assign in rest_stmts; crossing machinery takes over.
- 2026-06-11T21:48 — deviation-judge #10 identity dc62ffd round 4: BLOCK (fresh input). DEPTH ASYMMETRY: exclusion fires at ALL sm_scope_depths in reload_params_from_frame, but spike_reload only runs in the depth-0 rest_stmts loop → nested wait in an if-block leaves CPU results as stale allocas (neither reload path fires). Fix: carry (name, frame_offset) pairs in the Cg field; call spike_reload from reload_params_from_frame's reload_crossing:true path at any depth. MUST be implemented coherently with judge #9's pruning fix (one source of truth for which names are spike-reloaded vs crossing-reloaded).
- 2026-06-11T21:48 — deviation-judge #4 identity 6a66433 round 4: BLOCK (fresh trace after 2 PASSes). Deviation rationale claims "SM poll functions are themselves extern C-unwind (existing M2 discipline)" — FALSE: declare_resume_fn emits plain extern "C" resume fns (state_machine.rs:9, 101-112), so a real unwinding panic through emitted codegen aborts at the SM frame boundary BEFORE Tokio's catch_unwind; runtime.rs:1120-1122 comment is factually wrong. Unobserved because Yinz panics abort (not unwind) and the unit test bypasses SM frames. Fix: correct the rationale + Decision Record item 2 + runtime.rs comment honestly (through-codegen unwind path aborts by construction; C-unwind end-to-end is P1's, named), OR flip resume-fn ABI to C-unwind now.
- 2026-06-11T21:48 — deviation-judge #13 identity 01e0de3 round 4: BLOCK. Drop delegation at runtime.rs:513 is UNTESTED — delete the line, all tests stay green ("transitive coverage" claim false). Plus: helper lacks slot-nulling after free (double-free on hypothetical re-call) and size guard (UB on <48-byte buffer with colliding magic). Fixes: drop-path test via ynz_rt_spawn + drop-before-first-poll (no resume-fn needed — judge-verified constructable); null slots after free; debug-assert/document min frame size.
- 2026-06-11T21:48 — plan-adherence-verifier round 4: BLOCK (3 plan-text items): Decision Record item 8 stale ("no drop-shim wiring exists" — false post-discriminator); quality-gate evidence should cite discriminator_drop_frees_spike_handles; fixtures (i)/(j) undocumented in plan evidence.
- 2026-06-11T21:48 — rules-compliance-reviewer round 4: BLOCK (7 findings). COORDINATOR ADJUDICATION per CLAUDE.md Rule 11 (verify-real-then-fix): findings 1/2 (SPIKE_SLOT_RESERVE provenance doc placement), 4 (spike_cpu_candidates called 3× — cache it), 5 (Tier-2 doc comments on the 4 large spike helpers) = CONFIRMED REAL → routed to fix round 5. Findings 3/6/7 = NOT CONFIRMED: (3)+(6) demand test-execution-evidence comments in code/state.md ("Verified at <timestamp>") — that is changelog-style commentary banned by comments.md, and the execution evidence lives where the persistence model puts it (plan-file Evidence + acceptance-verifier live-run reports, rounds 2-4); (7) claims the spike codegen is "untested in the actual compiler context" — factually contradicted by fixtures (a)-(j) run through ./target/debug/ynz (full pipeline) and live-verified by the acceptance-verifier in rounds 2, 3, AND 4 (same round as this finding). These three findings are not routed; adjudication recorded here for Patrick's review — overrule available at the commit prompt.
- 2026-06-11T23:30 — ROUND 5 (FINAL gate; D_count=14; round cap reached). 11 PASS: design, plan-adherence (all R4 items resolved; cosmetic fixture (i)/(j) names+output stale in plan evidence — coordinator fixes at commit), acceptance-verifier (all 7 AC/gate entries MET by live runs: 14 fixtures byte-identical both modes, timing 1185ms vs 1771ms, 71 tests green), judges #2, #3, #4 (honest C-unwind docs verified at every constructible call site), #5 (production caller at runtime.rs:1109 justifies ungated pub(crate); prior cfg(test) note invalid), #6 (3-gate agreement, 4 live adversarial runs), #8 (dual null paths disjoint + correct), #10 (depth asymmetry fixed; sibling/while/partial-prune all live-verified), #14 (sm_entry pre-allocation tight; ok_or_else guards; fresh HashMap per Cg). 8 BLOCK → 4 distinct issues + adjudication:
- 2026-06-11T23:30 — [R5 ISSUE-1] code-reviewer + judges #1, #11, #13 (4 agents, each independently mutation-tested): drop-delegation test `spawn_state_fn_future_drop_before_first_poll_frees_handles` asserts task completion, which spawn_blocking's detach satisfies whether the handle box was freed or LEAKED — delete the delegation at runtime.rs:551, test stays green. Sibling `discriminator_drop_frees_spike_handles` case-1 free path equally blind. Fix (all four agents converge): Drop-witness — wrapper whose Drop flips an Arc<AtomicBool>, assert THE FLAG, in both tests.
- 2026-06-11T23:30 — [R5 ISSUE-2] deviation-judge #9 identity 8f8ffa4: BLOCK (live repro `0\n999\n89` vs oracle `55\n999\n89`). Prune-on-Assign fires UPFRONT for any assign anywhere in rest_stmts — a READ between the first wait and a later assignment reads the never-populated (zeroed) crossing slot. Fix: prune only when the assignment is the name's first use, or prune inline at the assignment site after lowering+flush; simplest spike-scope alternative: DECLINE-to-spike when rest_stmts assigns any CPU-result name.
- 2026-06-11T23:30 — [R5 ISSUE-3] deviation-judge #7 identity bba0e83: BLOCK (live repro `16994814376953` vs oracle `45`). spike_extract_cpu_group routes PRE-pair statements into rest_stmts (lowered AFTER the spawn) — an Ident arg naming a pre-pair crossing local reads an uninitialized sm_entry alloca at spawn time. Fix: lower pre-pair statements before emit_cpu_group_spawn_join (semantically right), or decline when first_idx != 0.
- 2026-06-11T23:30 — [R5 ISSUE-4] deviation-judge #12 identity 8fee9ca: BLOCK (live repro `55 88` off-by-one / `inconsistent park state` crash). NON-ENTRYPOINT spike host: frame_layouts_query runs BEFORE spike promotion, so a caller heap-allocating the promoted callee's frame via emit_suspending_call_heap_boxed (emit.rs:8156-8160) falls back to FRAME_HEADER_SIZE=32 bytes while the spike resume writes at bytes 48/64/80+ → heap corruption past the allocation. Fix: spike-aware fallback size at the call site, or gate spike admission to entrypoint-only (decline path, simplest).
- 2026-06-11T23:30 — rules-compliance-reviewer round 5: BLOCK (6 findings). COORDINATOR ADJUDICATION vs artifacts: finding 3 (offset constants lack names) NOT CONFIRMED — `SPIKE_HANDLE_0_OFFSET` etc. exist as documented consts in BOTH crates (emit.rs:6410-6412, runtime.rs:70); finding 4 (runtime fns lack Tier-3 docs) NOT CONFIRMED — docstrings verified by R1/R3/R4 reviewers and multiple judges; finding 6 (state.md note = duct-tape) NOT CONFIRMED — environment facts in state.md are exactly CLAUDE.md Rule 6's prescription, judge #3 PASSed it 3× incl. this round. Finding 2 CONFIRMED-TRIVIAL (no comment directly above the m3d_spike env read at emit.rs:727; the Cg field at 1564-1565 documents it — add a one-liner). Findings 1 (env-var parse helper at 2 occurrences — below the rule's own 3+ threshold, reviewer self-describes as "borderline") and 5 (spike_cpu_candidates at 2 cross-pass call sites; 2163 could consult the extended suspend set) = real-but-cheap improvements, routed with ISSUE fixes if a round 6 is authorized.
- 2026-06-11T23:30 — Working-tree integrity verified post-gate: delegation call intact at runtime.rs:551; modified-file set matches known phase scope (4 agents' temporary mutations all restored).
- 2026-06-12T05:37 — ROUND 6 (focused re-gate, user-authorized; D_count=17; stable judges #1-#5, #8, #11, #14 deferred to cumulative 4.a). 11 PASS: design, rules (zero violations), plan-adherence (all R5 items resolved; cosmetic: consolidate admission envelope into Decision Record item 9; fixture (m) header still describes removed prune), acceptance-verifier (all 7 AC/gate entries MET live: 15 fixtures byte-identical, 1048ms parallel, 71 tests green, drop-witness verified real), judges #6 (3-gate agreement, type-system-bounded asymmetry), #7 (pre-pair mutation propagates correctly), #9 (7 pure-read adversarial shapes clean), #10 (Cg field single-write verified; stale prune comments at emit.rs:3234/3566), #12 (heap corruption gone; P3 note: emit_suspending_call_heap_boxed still reads stale layouts — gated unreachable), #13 (3-round saga closed: delegation deletion → both tests fail 0≠1), #17 (probe layout-safe, no spurious increments). 3 BLOCK → 2 root causes:
- 2026-06-12T05:37 — [R6 ISSUE-A] code-reviewer + deviation-judge #15 (identity 3df244e, convergent live repros): SUSPENDING PRE-PAIR STATEMENT (`wait sleep(0)` before the CPU pair) passes all gates; pre-stmts loop calls lower_sm_stmt_with_wait, then emit_cpu_group_spawn_join repositions past the unterminated post_wait_bb → LLVM verifier crash ("Basic Block does not have terminator") on valid code; 3 of 4 wait-bearing variants broke. Fix (both agents converge): decline admission when pre_stmts contains any wait/suspending call (one line, helpers exist in the same loop, applied in all 3 gates) + committed leading-wait regression fixture.
- 2026-06-12T05:37 — [R6 ISSUE-B] deviation-judge #16 (identity 3a091cf, live repro `55\n1\n89` vs oracle `55\n55\n89`): SUSPENDING CALLEE under spike — `wait worker()` in post_stmts embeds worker's child sub-frame at byte 48 per the PRE-SPIKE frame_layouts (own_base computed without the 48-byte reserve) = SPIKE_RESULT_0; worker's resume_point write clobbers the joined result `a`. Same stale-frame_layouts root class as its R5 finding, different door (embedded children vs caller allocation). Fix options (judge-named): (A, immediate) force heap-boxed path for suspending callees when cg.m3d_spike (emit_suspending_call_inline_poll skip-embedded); (B, P3) include the reserve in build_frame_layouts. Plus committed suspending-callee regression fixture.
- 2026-06-12T05:37 — Post-gate tree verification (probe-window hazard from this round): delegation intact at runtime.rs:551, zero MUTATION-PROBE markers in crates/, both drop tests green on final tree, modified-file set matches phase scope. Process lesson recorded: mutation probes by parallel gate agents on the shared tree can expose sibling reviewers to inconsistent states — future gates must serialize mutation probes (coordinator-run post-gate, or single designated prober).
- 2026-06-12T16:30 — ROUND 7 (focused gate, user-authorized at arbitration #2; code-reviewer + acceptance-verifier + judges #15 #16 on the R7 decline-gate fixes). 3 PASS / 1 BLOCK. code-reviewer PASS: R6 ISSUE-A + ISSUE-B both confirmed resolved via 6 adversarial inputs (leading wait, nested pre-pair wait, post-pair suspending stmt/expr-arg, post-pair pure-compute correctly still FIRES with output 144, ISSUE-B repro declines 55/55/89); concerns logged (stale prune comment emit.rs:2613, spike-on oracle is manual-only, reload machinery unreachable). acceptance-verifier PASS: all 7 ACs MET on live runs (17 fixtures byte-identical, timing 1283ms<1600ms, 382 driver + 31 codegen + 9 runtime tests green, block_on grep clean in production paths, drop-witness mutation-verified); critical coverage note matches judge #15. judge #16 PASS: entrypoint+post-pair gate conservatively correct — `background worker()` post-pair admitted and harmless (own task, own frame, no ISSUE-B path), R6 repro declines byte-identically, no admitted program reaches emit_suspending_call_inline_poll from post-pair.
- 2026-06-12T16:30 — [R7 SILENT-NARROWING] deviation-judge #15 (identity 76cbd9f): BLOCK. The post-pair gate's `stmt_contains_wait` clause also catches `wait sleep(0)` INTRINSIC waits — sleep uses the inline-poll path via ynz_rt_async_sleep_create/poll and embeds NO child sub-frame, so it poses zero ISSUE-B aliasing risk, yet it declines the group. Consequence (IR-verified): only fixtures (a)-(f) fire the spike; (g)-(q) — 11 of 17 — silently decline (zero ynz_rt_spawn_blocking_joinable call sites in their IR); spike_reload_cpu_results_from_frame (emit.rs:3572-3574) + wait branches (emit.rs:3744/3787-3803) are unreachable in the admitted envelope — the reload machinery built across deviations #9/#14 has zero firing-fixture coverage; fixture (g)/(h)/(n) headers + deviation #15 record factually overstate what they test. All fixtures stay green because sequential fallback is always correct (the silent-narrowing pattern the round was primed to detect). Judge-named narrow fix: strip `stmt_contains_wait` from the POST-PAIR gate only (3 sites: emit.rs:6571/6748/6852), keep `stmt_contains_suspending_call` — sleep is already excluded from that predicate via the M2_MAY_BLOCK_INTRINSICS guard at emit.rs:3264, so (g)/(h)/(i)/(j)/(n) re-admit while the real ISSUE-B class (user-defined suspending callees) still declines. ISSUE-A is pre-pair territory; the pre-pair gate keeps `stmt_contains_wait` unchanged. Round cap exceeded → arbitration #3 to Patrick (round-8 one-clause fix vs accept narrowed envelope with honest documentation).
- 2026-06-12T16:35 — Post-R7 tree verification: delegation intact at runtime.rs:551, zero probe markers, 9/9 m3d runtime tests green (both drop-probes), no stray .ll/binary artifacts, untracked set = 17 fixtures + plan + scratch only.
- 2026-06-12T17:03 — ROUND 8 (user-authorized at arbitration #3: "Round 8: one-clause fix (Recommended)"). Executor applied judge #15's named fix exactly: post-pair gate predicate narrowed to `stmt_contains_suspending_call` only at all three gate sites (emit.rs:6574/6752/6940); pre-pair gates unchanged; stale prune comment at emit.rs:2613 rewritten; fixture headers verified. FIRES table (executor IR-grep, independently re-verified by R8 acceptance-verifier): (g)/(h)/(i)/(j)/(n) 0→2 `call @ynz_rt_spawn_blocking_joinable` instructions each; (m)/(p)/(q) still 0 (decline). Zero new deviations. Focused re-gate (authorized scope): **judge #15 PASS** (fresh adversarial inputs: `wait __testFallibleAsync(false)` post-pair — the OTHER M2_MAY_BLOCK_INTRINSICS member, distinct synchronous-inline codegen path through arm 4426/lower_stmt vs sleep's emit_wait_point — produced correct spike results with and without a following sleep; orphan state slot patched by terminator loop at emit.rs:3652-3667, LLVM verifies; `print(suspendingFn())` arg-position suspending call correctly declines via recursive arg check at emit.rs:3273-3275; Stmt::If recursion identical between both predicates — no structural gap). **acceptance-verifier PASS** (all 4 ACs MET on own live runs: timing 1218ms vs 1780ms < 1600ms; saturation_600_joins green; drop-witness tests green; own IR-level FIRES verification confirms reload machinery now live-exercised — fixtures (h)/(n) fire AND complete post-join waits byte-identically; cargo test --workspace 1924 passed / 0 failed after one harness-race flake re-ran clean; clippy -D warnings clean). R7 silent-narrowing RESOLVED. Phase 0 gate fully green.

---

#### Phase 0 Decision Record (Step 4 — locked 2026-06-11)

**Status**: COMPLETE — All 5 steps done (2026-06-11, devcontainer, LLVM 18). Steps 1 + 4 landed in the prior WSL-host session; Steps 2, 3, 5 completed in the devcontainer continuation session.

**Runtime shims shipped** (`crates/ynz-runtime/src/runtime.rs`):
- `YnzCpuResult([i64; 2])` — the 16-byte POD return ABI (repr C, Copy)
- `CpuJoinHandle(tokio::task::JoinHandle<YnzCpuResult>)` — heap-boxed handle newtype (pub(crate))
- `ynz_rt_spawn_blocking_joinable(fn_ptr, ctx_ptr, ctx_size) -> *mut u8` — copies ctx bytes (FrameDropGuard, mirrors ynz_rt_spawn_blocking discipline), spawns on blocking pool, returns Box::into_raw(CpuJoinHandle)
- `ynz_rt_join_poll(handle, waker_ctx, result_out) -> i32` — `extern "C-unwind"` (permits resume_unwind to cross the frame), casts waker_ctx to `&mut Context<'_>`, pins and polls JoinHandle; Pending=1, Ready(Ok)=writes 16 bytes + drops Box + returns 0, Ready(Err(panic))=resume_unwind
- `ynz_rt_join_handle_free(handle)` — drops Box (detaches); null-safe; called by frame drop shim when parent cancelled mid-join

**Unit tests** (`crates/ynz-runtime/src/lib.rs`, module `m3d_join_shims`):
- `value_roundtrip_int` — [42, 0] survives ABI
- `value_roundtrip_saturation` — [i64::MIN, i64::MAX] survives ABI (decimal128 lo/hi path)
- `ctx_copy_contents_reach_child` — ctx bytes faithfully copied and visible to child
- `drop_detach_no_uaf` — ynz_rt_join_handle_free drops without UAF; child runs to completion (Arc<AtomicBool> witness)
- `panic_reraises_in_parent` — ynz_rt_join_poll re-raises via resume_unwind (extern "C-unwind" allows propagation); catch_unwind in test catches the original panic payload

**Locked decisions**:

1. **Result ABI**: `YnzCpuResult = [i64; 2]` (16 bytes, C repr, Copy).
   Mapping: int/bool → [bits, 0]; float → [f64_bits as i64, 0]; string/array/map → [heap_ptr as i64, 0]; number/decimal128 → [lo_word, hi_word]; T errors → [err_tag, ok_bits].
   Shape and Shape-errors returns are declined by the promotion pass (WideValueSuspendingReturn decline rule).

2. **Poll ABI**: `ynz_rt_join_poll` is `extern "C-unwind"` — allows `resume_unwind` to propagate from within its own body. The codegen-emitted SM resume functions are `extern "C"` (not `extern "C-unwind"`); an unwind that escapes `ynz_rt_join_poll` back into an SM resume function will abort the process at that ABI boundary (RFC 2945). Full end-to-end `C-unwind` propagation — resume functions emitted as `extern "C-unwind"` so unwind reaches Tokio's `catch_unwind` — is a P1 deliverable. `ynz_rt_join_poll` is called directly from `SpawnStateFnFuture::poll` (pure Rust, no C boundary) in the unit test path, where the `extern "C-unwind"` ABI is exercised correctly.

3. **Waker forwarding**: `waker_ctx: *mut u8` is a type-erased `&mut Context<'_>` from the enclosing SM's poll call — identical discipline to `ynz_rt_async_sleep_poll`. No fabricated wakers. The JoinHandle<T> implements Future and is polled with the real waker, so Tokio registers the correct waker and wakes the SM when the blocking task completes.

4. **Ctx-copy discipline**: `ynz_rt_spawn_blocking_joinable` copies `ctx_size` bytes to a heap buffer via `FrameDropGuard` (same as `ynz_rt_spawn_blocking` at runtime.rs:170-181). The child owns its copy; the parent frame may be dropped at any time without dangling the child's args (UAF-on-cancellation prevention from Research Finding 3).

5. **Handle ownership**:
   - Spawn → Box::into_raw → caller stores in frame handle slot
   - Poll Ready → ynz_rt_join_poll drops Box (result already written to frame result slot)
   - Poll Pending → handle unchanged; SM saves resume_point, yields Pending
   - Frame drop (cancellation) → ynz_rt_join_handle_free drops Box; detaches task

6. **Panic re-raise**: `Ready(Err(join_err))` → `resume_unwind(join_err.into_panic())`. Non-panic JoinError (abort) → `panic!` with clear message. The Box intentionally leaks on the panic path (bounded: one per panicking child) — matches Tokio's own JoinHandle panic propagation behavior.
   **Spike scope note**: in the spike, `fn_ptr` is `extern "C" fn`, so a Yinz div-by-zero panic (which calls `process::abort()` from the child thread) aborts the process at the `extern "C"` ABI boundary — BEFORE Tokio's internal `catch_unwind` can form a `JoinError`. The `resume_unwind` branch in `ynz_rt_join_poll` is validated at the Rust unit-test level only (`panic_reraises_in_parent` test uses a pure-Rust panic that crosses `extern "C-unwind"` correctly). The spike fixtures (e)/(f) pass because div-by-zero happens to abort the process on both the sequential and parallel paths, not because the re-raise contract fires end-to-end. P1 production hardening changes `fn_ptr` to `extern "C-unwind"` — that ABI change is what enables the re-raise path to fire end-to-end; the `catch_unwind` wrapper added in the F1 fix is the other half of that contract.

7. **No synchronous join**: poll returns `Poll::Pending` when the child hasn't finished; the SM suspends. No `block_on`, no `thread::park`, no spin-wait anywhere in the join path. The Tokio task harness registers the real waker; the blocking pool signals completion via `JoinHandle`'s internal channel, which wakes the SM.

8. **Promotion fixpoint algorithm** (locked from Research Findings 7–9) — Cancellation cleanup: spike frame handle slots are freed by `cleanup_spike_cpu_handles` (called from `SpawnStateFnFuture::drop` step 1.5). The discriminator at frame offset 4 (`SPIKE_FRAME_MAGIC`) gates the cleanup so normal frames are never misread. `ynz_rt_join_handle_free` is declared and unit-tested (`drop_detach_no_uaf`) but is called via the discriminator path in `SpawnStateFnFuture::drop`, not by spike codegen directly — spike codegen writes handle pointers into fixed slots at spawn time; `Drop` reads them and frees. Evidence: `discriminator_drop_frees_spike_handles` + `spawn_state_fn_future_drop_before_first_poll_frees_handles` tests.
   (a) Intrinsic-rooted may-block fixpoint: mark all `sleep`/`http`/I/O-equivalent calls may-block; propagate transitively bottom-up through the call graph (monotone, terminates at fixpoint).
   (b) Bottom-up promotion pass: in deterministic call-graph order (post-order DFS), for each function, identify independent pure-CPU statement pairs that meet ALL: both callees are NOT may-block, return type maps to YnzCpuResult, not in a cycle, no SM guard fires on either call, `--no-auto-parallel` not set.
   (c) Guard-probe transitive rollback loop: after promoting a callee, re-run the SM suspension guards on every caller of that callee; if any guard fires on previously-compiling code, roll back the promotion (decline silently — no new compile error). Repeat until fixpoint.

9. **Decline rules** (SM-promotion declines when any of these match):
   - Callee return type is Shape or `Shape errors` (WideValueSuspendingReturn)
   - `ShadowsCrossingLocal`, `UnsupportedCrossingLocalType`, `WideValueSuspendingReturn`, or `OwnershipConflictAtSuspend` guard fires on the candidate pair
   - Callee is in a call cycle (recursive group — inter-procedural fixpoint excludes cycles)
   - `--no-auto-parallel` flag set
   - `--kernel` mode (no blocking pool)
   - Either callee is may-block (promotes to I/O SM instead, existing path)

   **Spike admission envelope** (consolidated, locks the spike's proven-safe range; P3 lifts these via typeck analysis + frame_layouts integration):
   - Entrypoint function only (`f.name == "entrypoint"`) — non-entrypoint spike hosts have their frame sized by `emit_suspending_call_heap_boxed` reading the pre-spike `frame_layouts` entry, which is 32 bytes too small (missing the 48-byte spike reserve), causing heap corruption.
   - Zero function parameters — param slots start at FRAME_HEADER_SIZE = byte 32, the same offset as `SPIKE_HANDLE_0_OFFSET`; a param reload would overwrite the handle pointer.
   - Single adjacent pair of non-suspending int-returning direct calls (`second_idx == first_idx + 1`).
   - Exactly one arg per call, `IntLit` or `Ident` only — the trampoline ctx holds one i64; zero-arg and multi-arg callees are not packable.
   - No data-dependency between pair members — the second call's argument must not reference the first call's bind name (evaluated at spawn time before the first result is available).
   - No post-pair rest statement assigns a CPU-result bind name at any nesting depth — the upfront-prune approach left the initial read sourcing from an unpopulated crossing slot; admission decline is the correct fix.
   - No pre-pair statement contains a `wait` or suspending call — a wait in pre_stmts advances `current_state` and positions the builder in a new `post_wait_bb`; the subsequent spawn block has no predecessor branch from that block, leaving `post_wait_bb` without a terminator (LLVM verifier crash).
   - No post-pair statement contains a `wait` or suspending call — after the join, a suspending post-pair callee would be lowered with its child sub-frame embedded at the pre-spike offset (computed before the 48-byte spike reserve). For a 0-local entrypoint that offset equals `SPIKE_RESULT_0_OFFSET` (byte 48), causing the callee's `resume_point` write to alias the joined result (silent wrong value). Routing through `emit_suspending_call_heap_boxed` is not viable because the spike host's `recursion_slot` is `None` (the callee appears in `children`, not in `recursion_slot`), so the heap-boxed path's re-entry code uses the original `child_frame` SSA value across basic blocks, violating LLVM SSA dominance ("Instruction does not dominate all uses"). Declining at the gate routes the whole body through sequential lowering, which is always correct. P3 integrates the spike reserve into `frame_layouts` so embedded sub-frames carry the correct offset and the restriction is lifted.

10. **Mixed groups** (CPU child + I/O sleep child sharing one continuation): the CPU JoinHandle slot and the sleep handle slot are independent fields in the frame. The inline-poll group join loop polls each independently. CPU-finishes-first is the highest-risk case: the Ready join-poll must write result bytes to the CPU result slot and null the handle slot WITHOUT touching the still-Pending sleep handle slot. This is naturally correct: each poll returns its own result independently. **Spike finding (fixture g) — RESOLVED in fix round (2026-06-11)**: the spike's original mixed-body codegen failed with "continuation state 3 out of range" because `spike_extra_states` was set to 1 (accounting only for spawn=0 and poll=1) instead of 2 (spawn=0, poll=1, plus one additional slot for the subsequent sleep continuation). Fixed by: (a) changing `spike_extra_states` to 2, (b) returning `Vec<(String, u64)>` crossing results from `emit_cpu_group_spawn_join`, (c) adding `spike_reload_cpu_results_from_frame` which reloads frame slots into fresh allocas after each subsequent suspension. Fixture (g) now passes: output `55\n89\ndone\n`, exit 0 with `YNZ_M3D_SPIKE=1`. The per-handle independent-poll design is correct; the fix was purely a state-index accounting and cross-suspension reload gap. P3 production lowering generates the canonical unified state table.

**Environment blocker** (resolved): Steps 2/3/5 required LLVM 18. Host (Debian bookworm, LLVM 15) was unworkable; devcontainer (Ubuntu 24.04, LLVM 18) was used for the continuation session.

#### Phase 0 Step 5 — Accept/Reject Verdict (locked 2026-06-11)

**ACCEPT. Proceed to Phase 1.**

**Timing numbers** (fixture c, `v0_3_m3d_spike_c_timing.ynz`, fib(40)/fib(41) on devcontainer):
- Sequential (spike off): ~1795 ms wall-clock (representative run; earlier run measured ~1913 ms)
- Parallel (spike on): ~1256 ms wall-clock (representative run; earlier run measured ~1317 ms)
- Overlap factor: 1256ms < 1600ms threshold (1.6× sequential-each = two ~1.0s workloads in under 1.6s) ✓
- Output byte-identical both paths: `102334155\n165580141\n`, exit 0 ✓

**Gate conditions — all satisfied**:
1. The simplest distinct-callee case (fixture a) works without any synchronous join — poll-based suspension drives the full spawn→suspend→resume→result-bind cycle. ✓
2. The timing overlap materializes (1317ms < 1600ms threshold). ✓
3. No `block_on`, `Handle::block_on`, `thread::park`, or spin-wait appears in any spike code path (grep confirmed). ✓
4. Pool saturation (600 concurrent joins, fixture d) queues without deadlock — the poll-based join structurally cannot deadlock (joiner is suspended, not parked on a thread). ✓
5. Panic re-raise (fixture e) and cancellation-detach (fixture f) behave identically to sequential execution. ✓ (Process-level coincidence: Yinz's div-by-zero calls `process::abort()` from the child thread, which aborts identically on both sequential and parallel paths. The `resume_unwind` end-to-end contract is validated at Rust unit-test level only — `panic_reraises_in_parent` tests the ABI path correctly; the spike trampoline uses `extern "C"` not `extern "C-unwind"`, so the re-raise branch is unreachable through the spike fixtures. P1 production hardens the trampoline ABI to close this.)
6. Same-callee case (fixture b) produces distinct correct values per invocation (per-invocation ctx keying, not callee-name keying). ✓

**Spike limitation — RESOLVED in fix round (2026-06-11)**: the spike's continuation-state table gap (fixture g "continuation state 3 out of range") was fixed by correcting `spike_extra_states` from 1 to 2 and adding cross-suspension result reloading. Fixture (g) now produces `55\n89\ndone\n`, exit 0 with `YNZ_M3D_SPIKE=1`. No remaining known spike limitations that block Phase 0 acceptance.

#### Phase 0 Session Handoff (written 2026-06-11 by the /execute-plan coordinator — local WSL chat → devcontainer chat)

The original chat ran on the WSL host and hit the LLVM 18 environment blocker above. Chats do not transfer into the devcontainer; the working tree DOES (same mounted workspace). Everything a fresh chat needs is in this file + the scratch file. **Nothing is committed yet** — all Phase 0 work is uncommitted on `feat/m3d-cpu-parallelization`. `BASE` for the Phase 0 diff = `plan_base` front-matter (`8b99ac9`).

**Execution settings already chosen by Patrick (do not re-ask)**: phase-by-phase mode, in-place on `feat/m3d-cpu-parallelization` (no worktree), Phase 0 first.

**State at handoff**:
- DONE — Step 1 (runtime shims in `crates/ynz-runtime/src/runtime.rs` +292 lines; re-exports + 5 unit tests in `crates/ynz-runtime/src/lib.rs` +302 lines) and Step 4 (Decision Record above). Verified on host: `cargo test -p ynz-runtime` → 96 green; `cargo clippy -p ynz-runtime -- -D warnings` clean.
- NOT DONE — Step 2 (`YNZ_M3D_SPIKE=1`-gated promotion+lowering path in `crates/ynz-codegen/src/emit.rs`), Step 3 (spike fixtures (a)–(g) via `./target/debug/ynz run`), Step 5 (accept/reject verdict + timing numbers).
- Deviations so far (D_count = 3: 1 scope, 2 approach) persisted verbatim with identity hashes to `.claude/plans/scratch/v0-3-m3d-cpu-parallelization-phase0-deviations.md`. The completed-portion list is PARTIAL — the resumed executor may add deviations from Steps 2/3/5; the resuming coordinator must merge + overwrite the scratch file before the reviewer fan-out (per /execute-plan 3.d.0).
- Executor follow-up observation parked for P1: `CpuJoinHandle` Box intentionally leaks on the panic path (matches Tokio's own JoinHandle panic behavior, documented in the doc comment); P1 evaluates `ManuallyDrop` + explicit free before `resume_unwind`.

**Resume instructions for the new chat (inside the devcontainer)**:
1. Claim the slug (`/init-chat v0-3-m3d-cpu-parallelization` or the write-slug canonical writer), then run `/execute-plan v0-3-m3d-cpu-parallelization`.
2. Step 2 resume detection will classify Phase 0 as partial-state (working-tree changes + scratch file = Case A). IMPORTANT: the executor work is INCOMPLETE — do NOT jump straight to reviewers. First fresh-spawn a `plan-executor` with resume context: "Steps 2, 3, 5 of Phase 0 remain; Step 1 + Step 4 already landed in the working tree (see `git status` + this handoff) — do not re-do them." Devcontainer has LLVM 18; `cargo build --workspace` works there.
3. After the executor completes (including the Step 5 accept/reject verdict — HALT the milestone if the gate fails), merge its new deviations into the scratch file, then run the full 5-reviewer + per-deviation-judge fan-out on the COMPLETE Phase 0 diff (`git diff 8b99ac9` — working tree vs BASE, porcelain will be dirty).
4. Phase-by-phase mode: pause for Patrick's OK before committing Phase 0 and before starting Phase 1.

### Phase 1: Runtime ABI — production joinable spawn + poll + drop shim
**PR scope**: The three production C-ABI runtime functions with full doc comments, runtime-crate unit tests, and `runtime_decls.rs` declarations. No codegen consumer yet.
**Branch**: `feat/m3d-cpu-parallelization`
**Flag**: N/A
**Est. lines**: ~450
**Ships via**: `/pr`
**Objective**: `ynz_rt_spawn_blocking_joinable`, `ynz_rt_join_poll`, `ynz_rt_join_handle_free` hardened to production quality from the spike prototypes: init/shutdown-ordering failure modes (mirroring `ynz_rt_spawn`'s discard-with-warning paths at `runtime.rs:617-628`), panic capture → re-raise contract, waker forwarding identical to `ynz_rt_async_sleep_poll`'s discipline, handle ownership rules documented (Ready consumes; drop-shim detaches + frees).
**Why this phase exists**: the runtime ABI is the foundation P3's codegen emits calls against; landing it standalone keeps the P3 diff reviewable and lets runtime unit tests pin the contract before LLVM IR depends on it.
**Current-state anchors**:
- `crates/ynz-runtime/src/runtime.rs:591` — `ynz_rt_spawn` (error-path patterns to mirror)
- `crates/ynz-runtime/src/runtime.rs:89` — ctx-copy helper (reused for arg copies)
- `crates/ynz-codegen/src/runtime_decls.rs` — extern declarations consumed by codegen
**Files (expected scope)**: `crates/ynz-runtime/src/runtime.rs`, `crates/ynz-runtime/src/lib.rs`, `crates/ynz-codegen/src/runtime_decls.rs`.
**Deviation rule**: (same as Phase 0)
**Steps**:
1. Finalize `YnzCpuResult` = 16-byte POD; document which Yinz return classes map to it (int/bool/float/number/string/array/map/`T errors`) and that Shape returns are out of contract (declined upstream).
2. `ynz_rt_spawn_blocking_joinable`: spawn via `Handle::try_current()` → fallback RUNTIME-mutex path (exact same ladder as `ynz_rt_spawn:591`); closure owns ctx + arg-copies, frees them after the call returns; returns boxed handle ptr. Pre-init/post-shutdown: return null + warning (caller treats null handle as "run inline sequentially" — codegen guards this in P3? NO — codegen always runs after `ynz_rt_init` in generated main; null only occurs in hand-written misuse; document + poll on null = abort with message).
3. `ynz_rt_join_poll(handle, waker_ctx, result_out) -> i32`: poll the `JoinHandle` future with the forwarded real waker; Pending→1; Ready(Ok)→write 16 bytes, drop box, 0; Ready(Err panic)→re-raise (resume_unwind) so the Tokio task wrapper of the PARENT surfaces it exactly as a sequential panic would.
4. `ynz_rt_join_handle_free(handle)`: drop the box (detach). Idempotence rules documented (never called after a Ready poll — slot nulled, mirroring sleep-handle discipline).
5. Runtime unit tests: spawn+join value roundtrip (all 16-byte payload shapes — with an EXPLICIT i128/decimal128 alignment assertion: the `result_out` write path and the frame result slot must satisfy 16-byte alignment; a misaligned i128 store is a silent-wrong-value/SIGBUS class), saturation queueing (≥600 concurrent), drop-detach (task completes after handle freed; ctx freed by closure — asserted via allocation counter), panic surfacing, pre-init discard.
**Acceptance criteria**:
- [x] All three functions exported with `#[no_mangle]` C ABI + doc comments covering Flow/Failure modes/Side effects/Safety (house pattern in runtime.rs)
  - Evidence: acceptance-verifier R4 (live): `ynz_rt_spawn_blocking_joinable` (runtime.rs:~1100, `#[no_mangle] extern "C"`), `ynz_rt_join_poll` (~1247, `extern "C-unwind"`), `ynz_rt_join_handle_free` (~1339, `extern "C"`) — each carries # Flow / # Failure modes / # Side effects / # Safety (+ # Idempotence on free), matching the runtime.rs house pattern; all three `#[no_mangle]`. Big-O lines added inside # Side effects on all 5 concurrency FFI fns for cluster consistency (Patrick-directed).
- [x] Runtime unit tests cover: value roundtrip per payload class, saturation, drop-detach with alloc accounting, panic re-raise, pre-init/post-shutdown discard
  - Evidence: acceptance-verifier R4 ran `cargo test -p ynz-runtime --lib m3d_join_shims` (live). (1) roundtrip per class: value_roundtrip_int/float/ptr/decimal128_aligned(asserts slot%16==0 + aligned i128 read)/error_pair; (2) saturation: saturation_600_joins (640 joins > 512 pool); (3) drop-detach with REAL alloc accounting: handle_free_detaches_and_frees_box (Box free_count==1 drop-probe) + joinable_spawn_frees_ctx_copy_exactly_once (ctx free_count==1 through the REAL ynz_rt_spawn_blocking_joinable + FrameDropGuard); (4) panic: panic_reraises_in_parent (resume_unwind); (5) pre-init: spawn_before_init_returns_null + post-shutdown: spawn_after_shutdown_returns_null (tests/m2_spike.rs). All green.
- [x] `runtime_decls.rs` declares all three for codegen consumption
  - Evidence: acceptance-verifier R4: runtime_decls.rs declares ynz_rt_spawn_blocking_joinable / ynz_rt_join_poll / ynz_rt_join_handle_free as FunctionValue fields + declare_fn wiring (signatures match runtime); P1 diff de-spiked the comments only. Workspace links clean (no missing-symbol errors).
- [x] Zero behavior change to existing exports (full workspace suite green)
  - Evidence: acceptance-verifier R4 live `cargo test --workspace` → 1958 passed / 0 failed (all crates); `cargo clippy --workspace -- -D warnings` clean. All production edits are doc-comment-only or `#[cfg(test)]`-gated (FrameDropGuard.free_probe / CTX_FREE_PROBE / arm_ctx_free_probe / CpuJoinHandle::set_drop_probe) — zero release-build surface change.
**Quality gate**:
- [x] No `block_on`/park anywhere in the new code — code-reviewer + design-compliance R4 grep clean in production paths; the only `block_on` in the diff is in a `#[tokio::test]` panic harness.
- [x] Waker forwarding matches the sleep-poll discipline (no fabricated wakers) — `ynz_rt_join_poll` polls JoinHandle with the forwarded `&mut Context` (cast from waker_ctx), identical to ynz_rt_async_sleep_poll; verified code-reviewer + plan-adherence R4.
- [x] Ownership of ctx/handle/result documented at every transfer point — doc sections + the three-state handle lifecycle; plan-adherence R4 MET.
- [x] Follows existing runtime.rs doc-comment + error-path conventions — Handle::try_current()→RUNTIME-mutex ladder mirrors ynz_rt_spawn; null+warning on pre-init/post-shutdown; plan-adherence R4 MET.
**Verification**: `cargo test -p ynz-runtime` green incl. new tests; `cargo clippy --workspace -- -D warnings`; `cargo test --workspace` green.

**Phase Review Gates**:
- [x] code-reviewer: PASS 2026-06-12T (R4 — Big-O accuracy + ctx-free probe layout-safe, mutation-verified; M2-HALT clean)
- [x] rules-compliance-reviewer: PASS 2026-06-12T (R4 — R3 Big-O BLOCK resolved + deferral-tracked citation closes the Concern; 1 accepted warning-advisory: same-deferral adjacency in a body comment)
- [x] plan-adherence-verifier: PASS 2026-06-12T (R4 — Steps 1-5 MET; m2_spike.rs touch authorized+minimal; banned-phrase grep clean)
- [x] acceptance-verifier: PASS 2026-06-12T (R4 — all 4 ACs MET on live runs; 1958/0; clippy clean)
- [x] design-compliance-reviewer: PASS 2026-06-12T (R4 — no [locked]-doc contradiction; no-synchronous-join invariant intact)
- [x] Committed: 23f4e81

**Findings Log**:
- 2026-06-12 — coordinator tier-classification (no Executor tier field): Phase 1 → complex (trigger: "production C-ABI runtime functions … panic capture → re-raise contract, waker forwarding" + M2-HALT-corpse adjacency). Executor dispatched at Opus.
- 2026-06-12 — Phase 1 BASE = 6a183d2 (chore commit isolating Phase-0 /learn bookkeeping; clean boundary so the Phase 1 diff is pure runtime-ABI).
- 2026-06-12 — coordinator probes (pre-gate), all REFUTED: (1) scope — `git diff --name-only 6a183d2` → exactly the 3 expected code files (plan-file change is coordinator writeback). (2) drop-detach contract — `handle_free_detaches_and_frees_box` asserts `free_count == 1` via per-handle drop-probe (real Box-FREE ledger, not an Arc<bool> 'ran' witness — closes the Phase-0 5-round false-contract trap). (3) no-block_on — production diff grep clean. Routing: proceeding to full 5-reviewer fan-out (D_count=0).
- 2026-06-12 — ROUND 1 gate (D_count=0; 5 reviewers). 2 PASS / 3 BLOCK. PASS: code-reviewer (M2-HALT corpse clean; drop-test asserts real Box-FREE; doc honesty holds; 2 non-blocking concerns — pre-existing clippy dup-attr warning in out-of-diff m2_runtime.rs integration test, and the load-bearing "no unit test calls ynz_rt_init" assumption behind spawn_before_init_returns_null), design-compliance (no synchronous join in any production path; CPU routing/detach/panic-reraise all match design/no-function-coloring.md; no function coloring). 3 BLOCK → 4 distinct findings:
- 2026-06-12 — rules-compliance round 1: BLOCK. [crates/ynz-runtime/src/lib.rs:2960] `spawn_join_collect` is a Tier-2 test helper with a poll `loop {}` and no Big-O annotation — violates comments.md:361 (Big-O on Tier 2+). (Cleared: 200ms sleep = codebase convention w/ provenance at lines 3164/3295/3522/3648; C-unwind "later phase" deferral language = plan-tracked exemption.)
- 2026-06-12 — plan-adherence round 1: BLOCK. [crates/ynz-runtime/src/lib.rs:3172] Step 5 silent deviation — `handle_free_detaches_and_frees_box` constructs CpuJoinHandle directly via `CpuJoinHandle::new`, BYPASSING `ynz_rt_spawn_blocking_joinable`, so there is no ctx copy under test; Step 5's "ctx freed by closure — asserted via allocation counter" is unmet (Box-free is proven; ctx-free is not). Cross-flag: post-shutdown discard branch (runtime.rs:1086-1090) untested — only pre-init covered. Steps 1-4 MET; scope clean (no emit.rs/state_machine.rs).
- 2026-06-12 — acceptance-verifier round 1: BLOCK (OVERALL). 2 MET (runtime_decls declares all three; cargo test --workspace 1956/0 + clippy clean — verifier's own live run, zero regressions). 2 WEAK: (AC1) `ynz_rt_join_handle_free` (runtime.rs:~1287) has only `# Idempotence` + `# Safety` — missing `# Flow`, `# Failure modes`, `# Side effects` of the four-section house pattern; (AC2) post-shutdown discard branch has zero test coverage (only pre-init `spawn_before_init_returns_null`). Verifier-suggested fix: `spawn_after_shutdown_returns_null` (init→shutdown→spawn→assert null), likely in tests/ integration binary since ynz_rt_init/shutdown are integration-only.
- 2026-06-12 — coordinator probes (fix-loop R1, all LANDED): FIX1 Big-O present at lib.rs:2960; FIX4 all 5 house-pattern headings (Flow/Failure modes/Side effects/Idempotence/Safety) on ynz_rt_join_handle_free; FIX2 `joinable_spawn_frees_ctx_copy_exactly_once` passes (drives real ynz_rt_spawn_blocking_joinable, asserts ctx_free_count==1, executor mutation-proven RED on neutered probe); FIX3 `spawn_after_shutdown_returns_null` passes in tests/m2_spike.rs (executor mutation-proven RED on faked non-null). Re-spawning full gate on updated diff.
- 2026-06-12 — SCOPE NOTE (coordinator-authorized, not a silent deviation): FIX 3 added one test fn to existing `crates/ynz-runtime/tests/m2_spike.rs`, outside Phase 1's declared 3-file scope. Authorized by coordinator's FIX-3 instruction (post-shutdown branch requires ynz_rt_init/shutdown which are integration-binary-only; instruction preferred extending an existing integration file over a new one). Executor reported D_count=0 (rationalized as existing-file extension). Coordinator surfaces it explicitly; plan-adherence asked to confirm the touch is minimal (one test fn, nothing else in m2_spike.rs changed). No deviation-judge spawned — coordinator-directed, exactly-as-wide-as-needed.
- 2026-06-12 — ROUND 2 gate (D_count=0). 4 PASS / 1 BLOCK. PASS: code-reviewer (ctx-free probe test-gated+layout-safe+mutation-verified to bite on a real leak; 2 non-blocking concerns: probe-after-free coupling is not a realistic regression surface, deferral-tracking cross-flag = plan-tracked), design-compliance (no synchronous join; new poll-loops yield via tokio sleep/yield_now; post-shutdown .join() is a raw OS-thread join returning null synchronously — M2-HALT clean), plan-adherence (both R1 BLOCKs closed cold; m2_spike.rs touch minimal + coordinator-authorized; banned-phrase grep clean), acceptance-verifier (all 4 ACs MET on live runs: ynz_rt_join_handle_free has all 5 house headings; post-shutdown + ctx-free pinned with real counters; cargo test --workspace all-green, clippy clean).
- 2026-06-12 — rules-compliance round 2: BLOCK → COORDINATOR ADJUDICATION (CLAUDE.md Rule 11 verify-real-then-fix): NOT CONFIRMED. Finding: [runtime.rs:1310] ynz_rt_join_handle_free gained Tier-3 sections (Flow/Failure modes/Side effects via FIX 4) but lacks `Time: O(1) Space: O(1)` per comments.md:345/361. Verification probe (coordinator, read-only): 0 of 5 FFI functions in runtime.rs carry a Time:/Space: line (ynz_rt_spawn_blocking_joinable, ynz_rt_join_poll, ynz_rt_spawn_blocking, ynz_rt_spawn, ynz_rt_async_sleep_poll all omit it — grep -cE Time: O\( → 0). Grounds for NOT-CONFIRMED: (1) Hard Rule 7's actual trigger is loops/recursion/accumulating-data — ynz_rt_join_handle_free is an O(1) single-statement box-drop with none; (2) the "house pattern in runtime.rs" the AC explicitly references is 0/5 FFI fns with Big-O; (3) round-1 rules-compliance PASSED this same Tier-3 FFI function (Idempotence+Safety) without flagging missing Big-O — the function's tier did not change in R2, only 3 headings were added; (4) complying would put Big-O on 1 of 5 siblings = a NEW inconsistency, or obligate retrofitting all 5 (scope creep in a runtime-ABI phase). Routed to Patrick for overrule at the commit prompt (NOT the fix loop), per the plan's Phase-0 R4/R5 adjudication precedent. No-progress check: R1 hash [lib.rs:2960] ≠ R2 hash [runtime.rs:1310] → genuine progress, not a thrash.
- 2026-06-12 — PATRICK DECISION on the rules adjudication: NOT overrule — chose "Comply across all 5 FFI fns + re-gate" (strict letter-compliance done consistently to avoid a 1-of-5 inconsistency; explicitly accepted pulling the non-M3d sibling FFI fns into the diff). Fix round 2 (round-3 gate): add accurate per-function `Time:/Space:` doc lines to the 5 concurrency-runtime FFI fns — ynz_rt_spawn_blocking_joinable, ynz_rt_join_poll, ynz_rt_join_handle_free (M3d), ynz_rt_spawn/ynz_rt_spawn_blocking, ynz_rt_async_sleep_poll (siblings). Per-function accuracy required (spawn family copies ctx = O(n); poll/free = O(1)). Touching the sibling fns is Patrick-authorized consistency compliance (same-file, in declared scope) — not silent creep.
- 2026-06-12 — coordinator probe (fix-loop R2, LANDED): runtime.rs Time: doc lines 0→6; all 5 named FFI fns carry a Big-O line (ynz_rt_spawn computed O(1) — frame moved not copied; spawn-blocking family O(n) ctx-copy; poll/free/sleep-poll O(1)). Workspace 1958/0, clippy/fmt clean. Re-spawning round-3 gate (delta = 6 doc lines only).
- 2026-06-12 — ROUND 3 gate (D_count=0). 4 PASS / 1 BLOCK. PASS: design ("delta inert"), plan-adherence (doc-only; Steps 1-5 intact; sibling touches authorized), code-reviewer (Big-O accuracy verified vs bodies — spawn-MOVES vs spawn-blocking-COPIES distinction dead-accurate; 1 non-blocking concern re the SM-promotion-phase comment), acceptance-verifier (all 4 ACs MET on live run, 1958/0, clippy clean). rules-compliance R2 finding (ynz_rt_join_handle_free Big-O) RESOLVED. BLOCK: rules-compliance round 3 — NEW finding [lib.rs joinable_spawn_frees_ctx_copy_exactly_once]: poll-loop test fn lacks Big-O (comments.md:361). CONFIRMED-real + consistency-aligned (sibling poll-loop helper spawn_join_collect already carries Big-O). Plus warning-severity Concern: ynz_rt_join_poll "SM-promotion lowering phase" deferral comment — asks coordinator to confirm plan-tracked.
- 2026-06-12 — coordinator confirms the deferral IS plan-tracked: Phase 3 ("Codegen — SM-promotion lowering + CPU children") IS the SM-promotion lowering phase; the end-to-end C-unwind resume-fn ABI is logged as a P1 carry-forward in the plan status (line 28). Exemption satisfied. Round 4 will add an inline `// deferral-tracked:` citation to close the warning permanently.
- 2026-06-12 — coordinator precise probe (read, not crude grep): the ONLY new loop-containing fn lacking Big-O is joinable_spawn_frees_ctx_copy_exactly_once. echo_ctx_first_word (reads one i64) + returns_float/ptr/decimal128/error_pair (return a constant) are loop-free trampolines (Tier 1, no Big-O needed); saturation_600_joins is pre-existing (committed dcc1432, not in the 6a183d2 diff). Round 4 fix is bounded to 1 Big-O line + 1 deferral citation — rules cannot find a 4th.






### Phase 2: Typeck — independence relocation, worth-it analysis, promotion query with decline rules
**PR scope**: Move the independence partition into `ynz-typeck`; add the `does_real_work` fixpoint + `FunctionSig` field with cross-module propagation; implement the promotion query (suspend-set extension) with the full decline-to-promote rule set and transitive guard rollback. No codegen change yet — promoted functions are computed but codegen consumes them only in P3 (suspend-set extension is feature-gated off until P3 wires the lowering, so P2 ships zero behavior change).
**Branch**: `feat/m3d-cpu-parallelization`
**Flag**: N/A
**Est. lines**: ~900 (≈300 is the relocation diff)
**Ships via**: `/pr`
**Objective**: One typeck-owned source of truth for "which functions are state machines and which statement groups parallelize" that guards, inlay hints, cross-module `FunctionSig.suspends`, and codegen all read — per the registry's existing "hint and binary always agree" contract.
**Why this phase exists**: Research Findings 4 + 5 — promotion computed anywhere else either breaks the hint/binary agreement or lets guards fire as errors on previously-valid code. The relocation must land BEFORE feature logic so the move itself is a reviewable no-behavior-change diff.
**Current-state anchors**:
- `crates/ynz-codegen/src/independence.rs:126` — `partition_independent_groups` (moves; deps are only `ynz_ast` + `ynz_typeck` types)
- `crates/ynz-codegen/src/emit.rs:3473` — the codegen call site (re-pointed to the typeck re-export)
- `crates/ynz-typeck/src/may_block.rs:96` — `analyze` (the fixpoint the promotion pass extends; `calls_may_block_intrinsic` plumbing at `may_block.rs:108,168`)
- `crates/ynz-typeck/src/signatures.rs:14,38` — `FunctionSig` / `suspends` (new sibling field `does_real_work`)
- `crates/ynz-typeck/src/check.rs:497,560` — suspending-function guard sites (the predicates the promotion probe reuses)
- `crates/ynz-driver/src/main.rs:81,210,219` — `--no-auto-parallel` plumbing (promotion must respect it)
- `crates/ynz-typeck/src/check.rs:142-186` — kernel-mode flag (promotion disabled under it)
**Files (expected scope)**: `crates/ynz-typeck/src/independence.rs` (new home), `crates/ynz-typeck/src/may_block.rs`, `crates/ynz-typeck/src/signatures.rs`, `crates/ynz-typeck/src/check.rs`, `crates/ynz-typeck/src/queries.rs`, `crates/ynz-typeck/src/exports.rs`, `crates/ynz-codegen/src/independence.rs` (deleted/re-export), `crates/ynz-codegen/src/emit.rs` (import path only).
**Deviation rule**: (same as Phase 0)
**Steps**:
1. **Relocation commit (isolated)**: move `independence.rs` to `ynz-typeck` verbatim (module path + imports only), unit tests move with it; `ynz-codegen` imports from typeck. Full suite green — zero behavior change.
2. `does_real_work` fixpoint beside `may_block::analyze`: a function does real work iff its body contains a loop (`while`/`for`) or it participates in recursion (self or mutual), transitively through calls. New `FunctionSig.does_real_work: bool`, set in the same pre-pass→analysis flow as `suspends` (`signatures.rs:26-38` pattern); propagated cross-module through `module_signatures_query`/`exports.rs` exactly as `suspends` is.
3. Class-aware candidacy in the partition: a group member is EITHER a suspending call (existing criteria, same-callee restriction KEPT) OR a CPU call — non-suspending direct ident call whose callee `does_real_work`, passes the type-based write-effect floor, returns a supported class (NOT Shape/Shape-errors), and is not the enclosing function itself. Same-callee allowed for CPU members (per-invocation identity = statement index). Mixed groups permitted.
4. Promotion query (salsa-tracked): after the intrinsic-rooted may-block fixpoint, walk functions bottom-up in deterministic order; a non-suspending function containing ≥1 CPU-parallelizable group (≥2 members after partition) becomes promotion-candidate; extend the effective suspend set; iterate until stable.
5. Guard-probe transitive rollback: for the candidate set, probe EVERY function that becomes newly-SM (candidates + their transitive callers) against all suspension-guard predicates (`ShadowsCrossingLocal`, `UnsupportedCrossingLocalType`, `WideValueSuspendingReturn`, `MutualSuspensionCycle`, `FixedArrayIterWithWait`, `StoredRangeWithWait`, `ExpressionIterWithWait`, `ArrayShapeRuntimeFieldWithWait`). Any violation → remove the promotion(s) whose propagation caused it; repeat until no violations (monotone downward, converges). Functions in call cycles are never promoted (mutual-suspension prevention); self-recursion is fine (existing heap-boxed child frame support).
6. Gating: promotion disabled under `kernel_mode` and `--no-auto-parallel` (both checked at the query entry).
7. Tests: every guard shape from Research Finding 4 as a fixture that compiled pre-M3d — assert it STILL compiles with zero new diagnostics and is NOT promoted; promotion-positive cases (clean 2-member group → promoted, suspend set extended, callers promoted transitively); cross-module `does_real_work` propagation; cycle decline; `--no-auto-parallel` + kernel decline; determinism (two runs, identical promotion sets).
**Acceptance criteria**:
- [x] Relocation commit is verbatim-move + import-path-only (reviewable as such) with full suite green before any feature logic lands
  - Evidence: commit `724a765` ("phase 2 step 1 — verbatim relocation of independence.rs to ynz-typeck") is the SOLE committed object in `git log 23f4e81..HEAD`; `git show 724a765:crates/ynz-typeck/src/independence.rs` has ZERO feature symbols (MemberClass/partition_groups_classified absent); `emit.rs` diff is import-path-only (re-pointed to `ynz_typeck::independence`); feature logic stays uncommitted in queries.rs (lands after). 20 promotion_tests green live at the relocation boundary. (acceptance-verifier R4)
- [x] `does_real_work` propagates cross-module (fixture: importer parallelizes calls to an imported loop-containing callee)
  - Evidence: `queries::promotion_tests::imported_real_work_callee_promotes_importer` — two-file salsa project (`lib.ynz` exports `heavy()` with a `while` loop; `entrypoint.ynz` calls `heavy(20)`/`heavy(21)`), calls `cpu_promotion_query`, asserts `promoted.contains("entrypoint")`. Live: `ok`. (acceptance-verifier R4)
- [x] Every guard-shape fixture compiles with IDENTICAL diagnostics pre/post promotion logic and declines silently
  - Evidence: all 8 guards have decline fixtures, 20 promotion_tests pass live. 5 valid-but-unpromotable guards assert `!promoted && !has_errors` (`wide_shape_return_*`, `nested_shape_crossing_local_*`, `param_nested_let_shadow_*`, `array_shape_runtime_field_crossing_*`, `mutual_cycle_member_never_promotes`). 3 for-wait guards (StoredRange/FixedArrayIter/ExpressionIter) — whose shapes ERROR pre-M3d via check_function's pre-existing M2/M3 guards (check.rs:584-650), so `!has_errors` is structurally unconstructible (judge #10 R4: probe-fire ⟺ baseline-error coupling is TOTAL — both call the identical `find_*` helpers under the same `has_explicit_waits` gate) — assert `!promoted` (non-vacuous: real fib-pair candidate reaches the probe + declines via rollback, proven by the `wait_free_for_loop_does_not_block_promotion` mutation control) + `error_whats` shows exactly the pre-existing guard message and NEITHER sibling (M3d introduced ZERO NEW diagnostics). (acceptance-verifier + judge #10 + code-reviewer + plan-adherence, all R4)
- [x] Promotion query is deterministic and respects kernel-mode + `--no-auto-parallel`
  - Evidence: `promotion_is_deterministic_across_runs` (two runs, identical set), `kernel_mode_declines_all`, `no_auto_parallel_flag_declines_all`, `salsa_query_respects_no_auto_parallel_env` (ENV_LOCK-serialized) — all pass live. Shared `no_auto_parallel_env()` (`.is_ok_and(|v| v=="1")`) read by BOTH typeck + codegen (emit.rs:727) so the gate can't drift. (acceptance-verifier R4)
- [x] P2 ships zero default behavior change (suspend-set extension not yet consumed by codegen — full fixture corpus byte-identical)
  - Evidence: coordinator pre-gate probe + acceptance-verifier R4: `git grep -nE 'cpu_promotion_query|compute_cpu_promotions|PromotionOutput|does_real_work' -- crates/ynz-codegen/src crates/ynz-driver/src crates/ynz-lsp/src` → ZERO matches; `emit.rs` diff is import re-path + `no_auto_parallel_env()` unification only; `PromotionOutput` docblock states "computed but UNCONSUMED"; codegen SM-lowering still reads only `CheckOutput::suspends_set`. Full workspace green. (acceptance-verifier R4)
**Quality gate**:
- [x] No parallel implementation: partition has ONE home (typeck); codegen imports it — verbatim relocation `724a765`; codegen `emit.rs` imports from `ynz_typeck::independence` (code-reviewer + judge #8/#9 R1-R4)
- [x] Probe predicates REUSE the guard implementations (`check.rs`) — not re-derived copies — `suspension_guards_fire_for_fn` calls the same `param_has_nested_let_shadow`/`find_*`/`crossing_local_names` helpers `check_function` uses; `resolve_type_for_guard_free` is DRY delegation (judge #4 R1-R2, code-reviewer)
- [x] Salsa-tracked queries (incremental-safe); no global mutable state — `cpu_promotion_query` is `#[salsa::tracked(lru=64, cycle_fn=…)]`; `cpu_callees_by_candidate` is a local (design-compliance + judge #5)
- [x] No `unwrap()` on fallible paths; clippy clean — production `cargo clippy --workspace -- -D warnings` clean; test `.expect()` only (rules-compliance R1-R4)
**Verification**: `cargo test -p ynz-typeck` + `cargo test --workspace` green; cross-impl consistency harness on full corpus (must be trivially green since codegen unconsumed); determinism check run twice.

**Phase Review Gates** (4 gate rounds; each line = latest verdict, full per-round history in Findings Log):
- [x] code-reviewer: PASS 2026-06-13T03:45 (R4; R1 BLOCK→fixed)
- [x] rules-compliance-reviewer: PASS 2026-06-13T03:45 (R4; R1/R2/R3 Big-O BLOCKs→fixed)
- [x] plan-adherence-verifier: PASS 2026-06-13T03:45 (R4; R1/R2 scope+Step7 BLOCKs→fixed)
- [x] acceptance-verifier: PASS 2026-06-13T03:45 (R4, 5/5 ACs MET; R1 AC1 WEAK→fixed)
- [x] design-compliance-reviewer: PASS 2026-06-13T03:45 (R1-R4, no `block_on`/coloring)
- [x] deviation-judge #1 (scope: completion.rs): PASS — LSP completion path blind to `does_real_work`
- [x] deviation-judge #2 (scope: hover.rs): PASS — hover renderer blind to `does_real_work`
- [x] deviation-judge #3 (scope: tests/check.rs kernel helper): PASS — empty-set init structurally correct
- [x] deviation-judge #4 (approach: probe param-shadow predicate): PASS — predicate matches check_function exactly (R2 fix)
- [x] deviation-judge #5 (approach: per-candidate CPU-callee seed): PASS — over-rollback closed + true-positive preserved (R1 BLOCK→fixed)
- [x] deviation-judge #6 (approach: kernel_mode=false query entry): PASS — faithful deferral; P3 carry-forward (wire real --kernel signal)
- [x] deviation-judge #7 (scope: typeck/lib.rs re-exports): PASS — over-exposed `compute_cpu_promotions` narrowed to `pub(crate)` (R2 BLOCK→fixed)
- [x] deviation-judge #8 (scope: codegen/lib.rs dereg): PASS — one-line mirror, zero stranded consumers
- [x] deviation-judge #9 (scope: resolve_import.rs propagation): PASS — the ONLY correct propagation home (exports.rs can't access CheckOutput)
- [x] deviation-judge #10 (approach: for-wait decline fixtures): PASS — `!promoted`+zero-new-diagnostics contract honest; probe-fire⟺baseline-error coupling proven total (R3 BLOCK→fixed)
- [x] Committed: dcb32af (feature work; stacks on relocation 724a765)

**Findings Log**:
- 2026-06-13T00:22 — coordinator tier-classification (no Executor tier field): Phase 2 → complex (trigger: "`does_real_work` fixpoint + salsa-tracked promotion query + transitive guard-probe rollback" — type-system internals + novel-algorithm-no-pattern). Executor dispatched at Opus. Note: code-reviewer also runs at Opus this phase (same model tier as executor); the 4 Sonnet verifiers + judges retain cross-model coverage. Informational, not a gate.
- 2026-06-13T00:30 — coordinator probe (pre-gate): suspicion that promotion query/`does_real_work` might have a production consumer in codegen/driver/lsp (would break the "zero behavior change" AC). Command: `git grep -nE 'cpu_promotion_query|compute_cpu_promotions|PromotionOutput|does_real_work' -- crates/ynz-codegen/src crates/ynz-driver/src crates/ynz-lsp/src`. Result: REFUTED (no matches; new promotion API confined to ynz-typeck). Routing: proceeding to full fan-out unchanged.
- 2026-06-13T01:25 — **Round 1 gate: 5 PASS / 6 BLOCK** (5 reviewers + 6 deviation-judges). Tree-integrity check: code blobs byte-identical to pre-fan-out snapshot (only `.md` bookkeeping changed via radar hook) — verdicts produced against correct tree. No Planned RED Repros declared → no carve-out; all BLOCKs routed to fix loop.
  - design-compliance: PASS (no `block_on`/coloring; both pre-approved M3b divergences confirmed real tradeoffs).
  - deviation-judge #1 (scope/completion.rs): PASS. #2 (scope/hover.rs): PASS. #3 (scope/check.rs kernel helper): PASS. #4 (approach/probe guard-set): PASS (7 guards covered, MutualSuspensionCycle pre-filtered at candidacy; `resolve_type_for_guard_free` is DRY delegation not parallel copy). #6 (approach/kernel_mode=false): PASS (faithful deferral — mirrors pre-existing `check_query`, no `--kernel` signal exists yet, wiring point documented).
  - **deviation-judge #5 (approach/CPU-callee seeding): BLOCK** — global seed of `cpu_callees` into `suspending_fns` (queries.rs:808-811) over-rolls-back a SOUND candidate. Proven trace: non-candidate caller `wrapper` calls CPU callee `fib` in a subexpr (`let x = fib(10)+1`) + calls candidate `caller`; `fib` globally "suspending" → subexpr guard fires on `wrapper` → rollback walk removes `caller`. False-negative. FIX: scope the seed per-candidate (`HashMap<candidate→callee_set>`, augment `suspending_fns` inside the probe loop only when `candidates.contains(&f.name)`).
  - **code-reviewer: BLOCK (2)** — (1) [check.rs:7626-7634] guard probe calls WRONG shadow predicate `find_shadow_in_stmts` (needs top-level `let <param>`) vs `check_function`'s `param_has_nested_let_shadow` (nested in any block); EMPIRICALLY PROVEN silent-compile-break (host promotes `{"host"}` while check_function fires `ShadowsCrossingLocal`). FIX: probe must call `param_has_nested_let_shadow` (make `pub(crate)`); add param-nested-shadow decline test. (2) [queries.rs:131] `--no-auto-parallel` env gate parity lie: promotion reads `.is_ok()` (any value) vs codegen `.is_ok_and(|v| v=="1")`; docstring falsely claims "same signal codegen reads"; split-brain in P3 when var=`0`/``. FIX: extract ONE shared helper both call (preferred per no-duct-tape #7), fix docstring.
  - **rules-compliance: BLOCK (9 Big-O)** — comments.md:361 Big-O on Tier-2+ fns: 5 new (`partition_groups_classified`, `flush_classified`, `recursive_members`, `block/expr/stmt_calls_imported_name` cluster, `block/stmt_contains_loop`) + 4 moved (`partition_independent_groups`, `collect_ident_refs`, `build_write_effect_summary`, `stmts_are_independent`). Moved-fn annotations go in the FEATURE commit, not the verbatim-relocation commit.
  - **plan-adherence: BLOCK (5)** — 3 undocumented scope touches (`lib.rs` +`pub mod`/re-exports; `codegen/lib.rs` −`pub mod`; `resolve_import.rs` cross-module propagation — note plan named `exports.rs` but landed in `resolve_import.rs::load_export_table`); 6 guard-shape decline fixtures missing (only WideValueSuspendingReturn + nested-shape present); relocation not an isolated reviewable commit.
  - **acceptance-verifier: BLOCK (AC1 WEAK)** — relocation isn't a standalone verbatim commit; `independence.rs` has ~225 lines of Step 3 feature logic baked into the same uncommitted blob. AC2/AC3/AC4/AC5 all MET (live-run verified: `imported_real_work_callee_promotes_importer`, the two decline tests asserting `!promoted && !has_errors`, determinism + kernel + no-auto-parallel decline tests, zero codegen consumption).
- 2026-06-13T01:30 — **Round 1 fixes applied** (Opus executor; partial-then-completed across the interruption). Both PROVEN bugs fixed with RED-repro-first: param-shadow predicate swapped to `param_has_nested_let_shadow` (matches `check_function`); per-candidate CPU-callee seed map (`cpu_callees_by_candidate`) replaced the global seed. Shared `no_auto_parallel_env()` helper (codegen + typeck both call it). 9 Big-O added. 5 missing guard fixtures added. 3 scope deviations documented (#7/#8/#9). Relocation split into its own verbatim commit `724a765`; feature work left uncommitted for the phase-boundary commit. Coordinator structural probe confirmed all fixes landed before re-gate.
- 2026-06-13T02:05 — **Round 2 re-gate: 8 PASS / 3 BLOCK** (5 reviewers + judges #4/#5/#6/#7/#8/#9 re-fired; #1/#2/#3 carried round-1 PASS — hunk-file blobs unchanged). Tree-integrity exact match pre/post. Round-1 BLOCKs all verified RESOLVED.
  - PASS: code-reviewer (both round-1 fixes traced clean), acceptance-verifier (all 5 ACs MET incl. AC1 on committed `724a765`), design-compliance, judge #4 (param-shadow predicate matches `check_function` across 6 strategies), judge #5 (per-candidate fix closes over-rollback AND preserves true-positive), judge #6 (kernel gate untouched), judge #8 (codegen dereg clean, 0 stranded consumers), judge #9 (`resolve_import.rs` is the ONLY correct propagation home — `exports.rs` structurally can't access `CheckOutput`).
  - **rules-compliance: BLOCK (6 NEW Big-O)** — round-2 changes surfaced Tier-2+ fns missing `comments.md:361` Big-O: `walk_cpu_groups` (queries.rs:885), `function_has_cpu_group` (935), `collect_cpu_callees` (959), `candidates_reachable_from` (990); plus 2 moved fns `call_site_write_set` (independence.rs:730), `collect_ident_names` (632). Different fns from round 1 → progress.
  - **judge #7: BLOCK (over-exposed pure core)** — FIX-2 re-exported the 10-arg `compute_cpu_promotions` (queries.rs:694) as crate-`pub` (lib.rs:78) with ZERO external callers; its raw `kernel_mode`/`no_auto_parallel` bool args let a future P3 caller bypass the salsa-query gate (silent promotion in a `--kernel` build). FIX: `pub fn`→`pub(crate) fn` + drop from lib.rs:78 (intra-crate tests use `super::*`; the 3 legit re-exports `cpu_promotion_query`/`no_auto_parallel_env`/`PromotionOutput` stay `pub`).
  - **plan-adherence: BLOCK (1 — undocumented Step-7 approach divergence)** — `for_with_wait_guards_structurally_cannot_fire_via_cpu_promotion` asserts `promoted.contains(...)` not Step-7's `!promoted`, undocumented. Coordinator assessment (LATER OVERTURNED — see R3 judge #10): believed the divergence CORRECT (structural impossibility). FIX applied R2: documented as approach deviation #10 + strengthened comment.
- 2026-06-13T02:25 — **Round 2 fixes applied**: 6 Big-O added; `compute_cpu_promotions` `pub`→`pub(crate)` + dropped from lib.rs re-export; deviation #10 documented (structural-impossibility) + test comment strengthened. Coordinator probe confirmed all landed.
- 2026-06-13T02:55 — **Round 3 re-gate: 8 PASS / 2 BLOCK** (5 reviewers + judges #5/#6/#7/#10; #1/#2/#3/#4/#8/#9 carried). Tree-integrity exact match. R2 BLOCKs verified resolved (judge #7 over-exposure closed; plan-adherence accepted deviation #10).
  - PASS: code-reviewer, acceptance-verifier (5 ACs MET, `pub(crate)` strengthens AC5), design-compliance, plan-adherence (deviation #10 rationale verified accurate by its own source reads — but see judge #10), judge #5 (seed logic byte-identical), judge #6 (kernel gate intact), judge #7 (over-exposure CLOSED — `pub(crate)`, no stranded consumer).
  - **judge #10: BLOCK (rationale OVERTURNED)** — the deviation-#10 "structural impossibility" claim is FALSE. `wait <non-suspending-fn>()` is warning-only (check.rs:2564, compiles) → `block_contains_wait=true` (`has_explicit_waits`) but NOT in `base_suspends` (transitive `suspends` predicate diverges from local `contains_wait`). Such a fn PASSES the candidacy gate, reaches the probe, and `StoredRangeWithWait` fires → declines. So the 3 for-wait guards ARE reachable via the CPU-promotion probe and a `!promoted` fixture is NON-vacuous. Coordinator verified the crux (warning-only path at check.rs:2564-2589). FIX: add real `!promoted` decline fixtures for StoredRangeWithWait / FixedArrayIterWithWait / ExpressionIterWithWait using `wait <non-suspending-fn>()` shapes (per judge #10's narrower fix); keep the non-interference test; correct deviation #10. Plan-adherence's original R2 instinct (Step 7 needs real `!promoted` fixtures) was right.
  - **rules-compliance: BLOCK (1 — `cpu_promotion_query` Big-O)** — the salsa wrapper (queries.rs:617) has merge loops + delegates `O(C·F²·E)` but its docblock lacks `Time:`/`Space:`. Coordinator sweep confirms this is the LAST genuine new-production Tier-2+ gap (one-liners/test-fns/trivial-stubs/pre-existing excluded). FIX: add `Time: O(C·F²·E) Space: O(F)` to the docblock.
- 2026-06-13T03:10 — **Round 3 fix executor BLOCKED (verify-first caught a bad prescription).** FIX 2 (cpu_promotion_query Big-O) DONE. FIX 1: executor ran the judge's repro before building and found `!promoted && !has_errors` UNSATISFIABLE for the 3 for-wait shapes. Paper-trace: all 3 are NOT in base_suspends (reach the probe, `!promoted` non-vacuous — judge #10 correct) BUT error at baseline (the explicit `wait` trips check_function's PRE-EXISTING M2/M3 for-wait guards at check.rs:584-650, same `has_explicit_waits` gate as the probe). No shape decouples probe-fire from baseline-error.
  - **Coordinator adjudication (testing-contract, facts settle it — not escalated):** Step 7 targets shapes "that compiled pre-M3d"; the for-wait shapes never compiled pre-M3d (already-erroring), so the honest satisfiable contract is **`!promoted` + "M3d introduced ZERO NEW diagnostics"** (assert the SPECIFIC pre-existing for-wait error is present + unchanged — NOT `!has_errors`). This exercises the real probe-decline path, matches the AC ("identical diagnostics pre/post promotion logic and declines silently"), and beats dropping the fixtures (which would leave the probe's for-wait guards with no regression test). Re-spawning executor with this decision. The non-interference test stays (renamed, false "structurally cannot fire" comment corrected); deviation #10 rewritten to the TRUE situation (guards reachable, shapes error at baseline, `!has_errors` impossible).

### Phase 3: Codegen — SM-promotion lowering + CPU children in the group poll
**PR scope**: Promoted functions lower through the SM path; the group poll gains the CPU-child class (spawn at group entry, join-poll, result bind, panic re-raise); frame layout gains per-member handle + result slots; trampolines emitted; drop shim frees live handles. The feature goes LIVE in default builds here.
**Branch**: `feat/m3d-cpu-parallelization`
**Flag**: N/A
**Est. lines**: ~1100
**Ships via**: `/pr`
**Objective**: End-to-end working PURE-CPU parallelization on real fixtures: distinct-callee, same-callee, all supported return classes, inside `if`/`match` arms, under both modes (`--no-auto-parallel` sequential-identical). [CORRECTED 2026-06-14: "mixed CPU+I/O groups" and "single loop-iteration bodies" REMOVED from this objective — see Scope Boundary below; they were plan over-reach vs the roadmap.]
**Scope Boundary (capabilities — added 2026-06-14 to correct a plan-vs-roadmap over-reach):**
- **IN scope (this milestone owns per the roadmap Capability Ledger):** pure-CPU statement parallelization — distinct-callee, same-callee, return-class matrix, `if`/`match`-arm placement, promoted-caller chains, worth-it heuristic, panic/cancel robustness, gate lifts (zero-param/N>2/wait-barrier). This is "Pure-CPU Statement Parallelization."
- **OUT of scope (owned elsewhere — the M3d plan must NOT deliver these; it locks safe DECLINEs instead):**
  - **Mixed CPU+I/O overlap** (one CPU child + one I/O `wait` child in one group) → **M3g** (`v0-3-m3g-mixed-cpu-io-overlap`). It is the FUSION of M3b's I/O inline-poll path + M3d's CPU join-poll path; sub-slice 4c verify-first proved it is milestone-sized (deadlocks without a shared continuation). M3d locks the DECLINE (`v03_m3d_mixed_cpu_io_group_declines` + `..._nested_group_with_outer_wait` + `..._nested_group_with_suspending_callee`, all 0 spawns / byte-identical). Full machinery in `.claude/todos.md` "mixed CPU+I/O overlap" residual.
  - **Loop-body CPU groups** (a CPU group inside any `for`/`while` body) → future loop-placement-matrix slice. DECLINED in 4b (needs multi-level synthetic-index reservation). Locked by `v03_m3d_nested_for_body_group` / `..._for_under_if_group` / `..._inner_nested_for_group`.
  - **Multi-group ≥2** (two-or-more CPU groups in one function) → future per-group-slot work. DECLINED in 4b. Locked by `v03_m3d_nested_multi_group` / `..._nested_group_before_top`.
  - Unsupported return-class FIREs (`number errors`/wide-EC UAF, `maybe`, `fixed`, `Shape`/`Shape errors`, union/dynamic/range/options) — declined consistently; some are M3f-family correctness fixes.
**Why this phase exists**: this is the milestone's working core — everything before it de-risked the mechanism; everything after it is teaching + adversarial hardening.
**Current-state anchors**:
- `crates/ynz-codegen/src/emit.rs:1847` — SM dispatch (promoted fns flow here automatically once the P2 suspend set is consumed)
- `crates/ynz-codegen/src/emit.rs:2063` — `lower_function_with_waits`
- `crates/ynz-codegen/src/emit.rs:254` — `build_frame_layouts_with_resolver` (handle/result slot layout extension; keyed (group, member-index), NOT callee name)
- `crates/ynz-codegen/src/emit.rs:6061` — `emit_independent_group_poll` (CPU-child arm)
- `crates/ynz-codegen/src/emit.rs:3473` — partition consumption in `lower_sm_block`
- `crates/ynz-runtime/src/runtime.rs:89` — ctx/arg-copy helper discipline (BgArgDropEntry)
**Files (expected scope)**: `crates/ynz-codegen/src/emit.rs`, `crates/ynz-codegen/src/state_machine.rs`, `crates/ynz-codegen/src/runtime_decls.rs` (if signatures shift), `crates/ynz-driver/tests/fixtures/` + `crates/ynz-driver/tests/integration.rs`.
**Deviation rule**: (same as Phase 0)
**Steps**:
1. Wire P2's extended suspend set into codegen's `cg.suspend_set` (the P2 gate flips on). Promoted functions route through `lower_function_with_waits` unchanged — verify the no-explicit-`wait`, no-sleep-child case lowers cleanly (frame with zero sleep sub-frames, ≥1 CPU group).
2. Frame layout: for each Parallel group with CPU members, allocate per-member (handle slot: i64 ptr; result slot: 16 bytes) keyed (group-id, member-index). Composed-frame size math extended; alloc still once per task tree.
3. Trampoline emission: per (callee, arg-signature) a C-ABI `extern fn(ctx: *mut u8) -> YnzCpuResult` that unpacks args from ctx (scalars by value; heap args from copied pointers), calls the plain compiled function, packs the return into 16 bytes.
4. Group-entry spawn: build ctx (arg evaluation happens BEFORE any spawn, in source order — argument expressions are not parallelized), copy heap args via the BgArgDropEntry-equivalent for joined children, call `ynz_rt_spawn_blocking_joinable`, store handle in the frame slot.
5. CPU-child arm in `emit_independent_group_poll`: per poll visit, `ynz_rt_join_poll(handle, waker_ctx, result_slot_ptr)`; Pending → mark outstanding; Ready → null the handle slot, bind via `load_sm_return_value_typed`-equivalent read from the 16-byte result slot through the existing `bind_sm_result_and_flush` (including the i64→i1 bool truncation discipline from the M3f fix at `emit.rs:5702-5726`).
6. Drop shim: frame drop frees any non-null handle slots via `ynz_rt_join_handle_free` (mirrors sleep-handle cleanup), then arg-copy entries.
7. Mixed groups: the poll loop dispatches per-child class (embedded-frame inline poll vs join poll) — one continuation state for the whole group, same as today.
8. Integration fixtures (all via `./target/debug/ynz run`, each with a `--no-auto-parallel` twin assertion): distinct-callee int; same-callee int (distinct values!); float/number/string/array/map/`T errors` returns; mixed CPU+I/O (DECLINE — 0 spawns, byte-identical; FIRE is M3g, not M3d); group inside `if` arm; group inside `match` arm; group inside a single for-iteration body (DECLINE — 0 spawns; loop-body FIRE deferred to a future loop-placement slice); explicit `wait` barrier splits a would-be group; trivial leaf callee NOT spawned (worth-it respected — assert via IR or debug output); promoted-caller chain (caller of promoted fn is SM, output correct).
**Acceptance criteria**:
- [x] All step-8 fixtures pass with byte-identical default vs `--no-auto-parallel` output
  - Evidence: 48 m3d integration tests green (code-reviewer + plan-adherence live-confirmed). Shared FIRE helper asserts ==N `ynz_rt_spawn_blocking_joinable` spawns + byte-identical default-vs-`--no-auto-parallel` + alloc=free; DECLINE helper asserts ==0 spawns + byte-identical. Full return-class matrix (int/bool/float/number/string/array/map/`int errors`) + if/match-arm + N>2 all covered. [completion gate 2026-06-15]
- [x] Same-callee fixture binds DISTINCT correct values (the per-invocation keying proof)
  - Evidence: `v03_m3d_same_callee_{int,bool,float,number,string,array,map,int_errors}_distinct_values` ×8 all green; per-(group,member-index) keying proven in IR (distinct member-index trampolines `__ynz_spike_trampoline_*_0/_1` calling the same callee through separate ctxs). [completion gate 2026-06-15]
- [x] Timing fixture: two heavy CPU calls complete in measurably less than sequential sum
  - Evidence: LIVE coordinator measurement on `v0_3_m3d_spike_c_timing.ynz` (fib(40)+fib(41)) — PARALLEL 1132 ms vs SEQUENTIAL (`YNZ_NO_AUTO_PARALLEL=1`) 1670 ms, 5-run avg each (~32% faster; ≈ max(child) not sum(children)). Output byte-identical both modes. [completion gate 2026-06-15, acceptance-runs-not-surveys: graded from a live run, not the FIRE-only CI gate]
- [x] alloc=free on every new fixture (frames, ctx, handles, arg copies)
  - Evidence: FIRE/DECLINE helpers assert alloc==free via `ynz_run_with_alloc_counter` on every m3d fixture (code-reviewer + plan-adherence confirmed; cancellation cleanup mutation-proven alloc=free at N=2/N=3 in 4d). [completion gate 2026-06-15]
- [x] Full existing corpus green — zero regressions (`cargo test --workspace`)
  - Evidence: plan-adherence live run = 115 suites / 0 failed. Coordinator run = 2049 passed / 1 failed where the lone fail is `v03_m3e_alias_local_name_collision_runs_correctly` — the pre-existing M3e `background` load-flake (todos:178): NOT in the Phase-3 diff (M3e file, untouched), passes 3/3 in isolation (verified this session), explicitly not auto-parallel-related (auto-parallel never groups `Expr::Background`). Zero regressions FROM Phase 3. [completion gate 2026-06-15]
**Quality gate**:
- [ ] No synchronous join anywhere (diff grep)
- [ ] Slot I/O routes through the canonical helpers (`flush_var_slot_to_frame`/`reload_params_from_frame`/`bind_sm_result_and_flush`) — corpse guard (a) from M3b respected
- [ ] No flat-scan re-derivation — partition consumed from typeck only (corpse guard (b))
- [ ] Arg evaluation order = source order, pre-spawn (no observable reorder of argument side effects)
**Verification**: `cargo test --workspace`; cross-impl harness over full corpus; `valgrind`/alloc-counter run on the new fixtures per existing M3a/M3b practice.

**Phase Review Gates** (cumulative completion gate, BASE `dcb32af`, scoped diff `crates/ examples/pirates-roster/ Cargo.toml Cargo.lock cspell.json`):
- [x] code-reviewer: PASS 2026-06-15T15:59Z (Opus; 3 non-blocking concerns — all_variants count-assert, stale catch-all comment indep.rs:685, pre-existing ynz-lsp test warnings; frame-ABI single-source + completeness-gate-proven-non-vacuous + N>2 no-alias + R2 read-after-join lock all confirmed)
- [x] rules-compliance-reviewer: PASS 2026-06-15T15:59Z (Opus; no violations; `v0.3-M3d:` prefix informational only per standing ruling)
- [x] plan-adherence-verifier: PASS 2026-06-15T15:59Z (Opus; all 8 steps MET, deferrals DECLINE-locked + tracked, every deviation rationale banned-phrase-clean; 1 non-blocking concern — foreign-provenance ynz-registry test edits bled in from docs commit 93506c0, not M3d work)
- [x] acceptance-verifier: PASS 2026-06-15T15:59Z (COORDINATOR-SUBSTITUTED live-run — agent malformed 4-5× this session on the long suite per standing authorization; all 5 ACs MET with live evidence incl. the 1132ms-vs-1670ms timing measurement)
- [x] design-compliance-reviewer: PASS 2026-06-15T15:59Z (Opus; M2-HALT class dead — poll-and-yield join, lone `block_on` is pre-existing entrypoint driver, codegen throws not bridges, zero coloring; both recorded divergences carry real named costs)
- [x] Deviation judges (~18, COORDINATOR-SUBSTITUTED inline per Patrick spend-constraint — logged honesty caveat: less independent than fresh agents, but every deviation was already fresh-judge-vindicated at its sub-slice gate + the cumulative code-reviewer owns cross-cutting surface): ALL PASS. DECLINE-narrowings (4b for-body, 4b/4c nested-in-suspending, 4c mixed-CPU+I/O, 4e pre-pair-wait) safe-by-construction (sequential=byte-identical, 0-spawn-locked). Corruption-surface FIREs (slice1 trampoline pack-deref, 4e param-host byte-32, 4e DRY consolidation, 4d/4e frame-ABI const churn) each per-class byte-identical-vs-oracle fixture-proven; param-host walk verified by hand (exhaustive match no `_=>`, loop-local `body_reads`, conservative direction). Scope deviations (cpu_result_abi predicate in typeck, maybe-fixture determinism-exclusion, asymmetric dead-arm removal, s-fixture sweep, queries.rs 0-diff revert) all within front-matter glob / non-change / cosmetic.
- [x] Committed: 09a810d (gate writeback; Phase-3 code BASE remains dcb32af → HEAD e00ddca across the 8 sub-slice commits)

**Findings Log**:
- 2026-06-15 — **★ PHASE-3 CUMULATIVE COMPLETION GATE: ALL LANES PASS ★** (this session). 4 Opus reviewer agents (code-reviewer / rules-compliance / plan-adherence / design-compliance) ALL PASS; acceptance + ~18 deviation-judges COORDINATOR-SUBSTITUTED (spend-constraint, logged caveat) ALL PASS; coordinator HALT-class no-sync-join probe REFUTED (clean). Tree-integrity HELD — all 5 code blobs (emit/queries/independence/runtime/abi) byte-identical to the pre-gate snapshot after code-reviewer's plant/restore completeness-gate probe (restored clean). 5 ACs ticked above with live evidence (incl. live 1132ms-parallel-vs-1670ms-sequential timing). Corpus: 115 suites 0-failed (plan-adherence) / coordinator 2049-1 where the 1 = tracked isolation-passing M3e `background` flake (todos:178, not Phase-3). Non-blocking carry-forwards (NOT gate blockers — surfaced to Patrick for an optional pre-commit polish): code-reviewer C1 `all_variants` count-assert (low-risk, parity_case compile-break catches), C2 stale "catch-all" comment at independence.rs:685, plan-adherence foreign-provenance registry-test edits from docs commit 93506c0. **⚠️ UNRELATED working-tree anomaly observed mid-gate** (NOT Phase-3, NOT staged): `.devcontainer/*` deleted + root `Dockerfile`/`docker-compose.yml`/`.dockerignore` added + `CLAUDE.md` modified — appeared during the gate (pre-gate tree was clean); flagged to Patrick, left untouched. **PENDING: Patrick commit go-ahead** (review-before-commit) → then tick `Committed:` + STOP at Phase 3→4 boundary (authorized pause).
- 2026-06-13 — coordinator tier-classification (no Executor tier field): Phase 3 → complex (trigger: codegen/IR/ABI/frame-layout/state-machine internals — SM-promotion lowering + CPU children in the group poll). Executor dispatched at Opus. code-reviewer also runs at Opus this phase; the 4 Sonnet verifiers + judges retain cross-model coverage. Informational, not a gate.
- 2026-06-13 — **PROCESS DECISION (Patrick)**: Phase 3 is corpse-adjacent silent-wrong codegen (~1100 lines, every spike restriction lifted at once). First executor invocation BLOCKED on delivery process (refused to emit a survey-graded open-loop diff per acceptance-runs-not-surveys + Silent Envelope Narrowing corpse). Patrick chose **incremental live-verified slices, ONE Phase-3 commit at the boundary**. Slices: (1) FrameLayout CPU-slot extension + production trigger wiring; (2) distinct-callee int lowering through bind_sm_result_and_flush; (3) same-callee + return-class matrix; (4) mixed CPU+I/O + if/for-body + restriction-lift. Coordinator gates each slice's cumulative diff with live evidence; no per-slice commit.
- 2026-06-13 — **Slice 1 landed** (Opus executor): production trigger replaces YNZ_M3D_SPIKE env gate (cpu_promotion_query unioned into suspend set in codegen_query + frame_layouts_query); FrameLayout gains cpu_group_slots (computed offsets). Live evidence: cargo test --workspace 0 failed (corpus green); 4 non-promoted fixtures IR byte-identical pre/post (zero-cost invariant); only 4 fixtures promote (typeck-promoted ⊆ spike-admitted — no half-built path reachable); promoted fixtures byte-identical vs oracle; FIRE confirmed (2 spawn calls, layout-derived offsets 32/40/48/64); clippy/fmt clean. 2 approach deviations (#1 SPIKE_* constants kept as documented fallback; #2 union in both query boundaries). 0 scope deviations.
- 2026-06-13 — coordinator probe (pre-gate, slice 1): suspicion of a new synchronous-join primitive (corpse-adjacency, milestone-HALT-class). Command: `git diff dcb32af -- crates/ynz-codegen/src crates/ynz-runtime/src | grep '^+' | grep -iE 'block_on|thread::park|spin_wait|busy.?wait|\.join\(\)'`. Result: REFUTED (empty). Routing: proceeding to full fan-out unchanged.
- 2026-06-13 — **Slice 1 gate Round 1: 4 PASS / 3 BLOCK** (5 reviewers + 2 deviation-judges). PASS: design-compliance (no coloring, no sync-bridge; M2-HALT corpse stays buried), plan-adherence (Steps 1–2 met, scope clean, both deviations sound), acceptance-verifier (live: cargo test --workspace 1986/0, 4 promoted fixtures byte-identical vs oracle, zero-cost invariant holds by construction), deviation-judge #1 (SPIKE_* fallback can never diverge from computed layout for any currently-admitted input — dead-but-defensive). No Planned RED Repros declared → no carve-out; all BLOCKs route to fix loop.
  - **rules-compliance: BLOCK (1)** — [emit.rs:496] `cpu_slot_reserve_slots` divides by bare `8` where `CPU_HANDLE_SLOT_BYTES` (defined emit.rs:455, used everywhere else) belongs. DRY/magic-constant. One-token fix.
  - **code-reviewer (Opus): BLOCK (2)** — (1) [emit.rs:664 `compute_frame_size`] omits the `cpu_reserve_bytes` that sibling `build_frame_layouts` (emit.rs:393) adds → the `sizes` memo (read as `child_frame_size` when a parent embeds a callee) is 48 bytes short for any spike fn. Dual-frame-size corpse pattern; defused TODAY only by the entrypoint-only gate (spike fn never a child) — slice 2 relaxing that gate → parent under-allocates 48 bytes → child handle/result region aliases parent's next sub-frame → silent heap corruption. Right-design-now. (2) [emit.rs:467,489] ZERO unit tests on net-new `build_cpu_group_slots`/`cpu_slot_reserve_slots` (Tier-A invariant targets, now firing in production builds) — violates gated-path-fire-assertions + plan's own "spike-on oracle manual-only → needs CI gate" carry-forward.
  - **deviation-judge #2: BLOCK (1)** — Deviation #2's union of the FULL promotion set into the suspend_set that `spike_cpu_candidates` reads POISONS host admission for nested CPU groups. Confirmed in code: `compute_cpu_promotions` (typeck queries.rs:62-80) promotes ANY fn with a CPU group, no name filter → nested program (`fib` leaf; `work`=fib(n)+fib(n+1); `entrypoint`=work(40)+work(41)) promotes `{work, entrypoint}`; codegen `spike_cpu_candidates(entrypoint, {work,entrypoint})` sees `work` in suspend_set → callee filter `!suspend_set.contains("work")` excludes it (emit.rs:6611) → entrypoint's group DECLINES. typeck approved the parallelism; codegen silently drops it → defeats outer-group parallelism + sets up Phase-4 hint/binary disagreement (breaks the milestone's load-bearing "hint and binary always agree" contract). The removed internal scan ran the gate with the un-unioned base set, which is why it was correct.
  - **Carry-forwards logged for slice 2+** (out of slice-1 scope, NOT fixed this round): judge #1 + code-reviewer concern — runtime `cleanup_spike_cpu_handles` hardcodes handle offsets `[32,40]` so N>2-member groups leak on cancellation; `FRAME_HEADER_SIZE` (codegen crate) and `SPIKE_HANDLE_0_OFFSET=32` (runtime crate) are independent constants with NO cross-crate assertion linking them (silent corruption if header size ever shifts). Code-reviewer's optional `const _: () = assert!` binding codegen↔runtime offsets would close the second cheaply.
- 2026-06-13 — **TREE-INTEGRITY ANOMALY (logged, not correctness-compromising)**: pre-fan-out snapshot of `emit.rs` blob was `07397d3b`; post-gate it is `de434f0f` (mtime 14:18, ~30 min into the gate — a read-only reviewer mutated the shared tree, the failure mode the `reviewer-judge-read-only-git` memory warns about; prime suspect code-reviewer running a mutation probe directly). Coordinator verified the mutation did NOT invalidate verdicts: all 3 BLOCK findings confirmed still present on the current tree (bare-8 at emit.rs:496 intact; compute_frame_size still has no reserve; zero offset-fn tests), and the diff magnitude (225 ins/62 del across 3 files) matches the executor's reported slice-1. No finding erased, no slice-1 logic altered. Did NOT stash-reset (would destroy the executor's verified work); the fix round overwrites emit.rs anyway and the round-2 gate re-snapshots. `queries.rs` blob unchanged (`18ab6aff`).
- 2026-06-13 — **Slice 1 Round 2 fixes applied** (Opus executor) — all 3 Round-1 BLOCKs resolved, live-verified:
  - **FIX 1 (rules-compliance)**: `cpu_slot_reserve_slots` bare `/ 8` → `/ CPU_HANDLE_SLOT_BYTES` (emit.rs). One-token DRY fix.
  - **FIX 2 (code-reviewer #1, load-bearing — dual-frame-size corpse)**: factored the CPU-reserve computation into ONE shared helper `cpu_group_slots_and_reserve(f, typed, suspend_set)` that BOTH `build_frame_layouts_with_resolver` AND `compute_frame_size` now call — single source of truth, so the `sizes` memo (read as `child_frame_size` when a parent embeds a spike callee) can never under-allocate by the 48-byte reserve. Additionally bound the two paths with a `debug_assert_eq!` (layout `total_size` == `compute_frame_size` memo for every CPU fn) as a belt-and-suspenders divergence tripwire. Verified: all promoting fixtures build clean in debug mode (assert active) → the two paths agree.
  - **FIX 3 (code-reviewer #2, gated-path-fire-assertions)**: 5 net-new unit tests on `build_cpu_group_slots`/`cpu_slot_reserve_slots` in emit.rs `#[cfg(test)] mod tests` — pin two-member ABI offsets (handles 32/40, results 48/64), three-member generalization (handles 32/40/48, results 56/72/88), zero-member empty, reserve=6 for two-member, reserve=0 for empty. `cargo test -p ynz-codegen` → 8 emit-mod tests green.
  - **FIX 4 (deviation-judge #2, union poisoning)**: replaced the Round-1 full-promotion-set union with a reconciliation — new `pub fn spike_host_subset(typed, base_suspend_set, promoted)` probes each promoted fn with `spike_cpu_candidates` against the BASE (I/O-only) suspend set, returning only the subset codegen can actually spike-HOST this slice. BOTH `frame_layouts_query` and `codegen_query` now union only `spike_hosts`, not `promotion.promoted`. A promoted-but-unhosted inner callee (`work`) thus neither becomes an SM (no trampoline mismatch) nor poisons the host's callee-eligibility filter (entrypoint's group no longer silently declines). Verify-first: new nested-group fixture `v0_3_m3d_spike_r_nested_groups.ynz` (`fib` leaf; `work`=fib(n)+fib(n+1); `entrypoint`=work(5)+work(6)) + integration test `v03_m3d_nested_groups_byte_identical_and_fires` — LIVE: default and `YNZ_NO_AUTO_PARALLEL=1` byte-identical (`13\n21\n34`, exit 0); IR-grep confirms entrypoint's group FIRES with **2** `call @ynz_rt_spawn_blocking_joinable` instructions (NOT 0 — poisoning gone), trampolines `__ynz_spike_trampoline_entrypoint_work_{0,1}`; `work` emits as plain `define i64 @work` (slice-2 carry-forward: its own inner fib group runs sequentially this slice, the documented acceptable residual). The optional cross-crate const-assert binding SPIKE_* offsets to the runtime constants was SKIPPED (runtime consts private + no ynz-codegen→ynz-runtime dependency = a reach; the cross-crate offset gap stays a logged slice-2 carry-forward).
  - **Full verification (live)**: `cargo test --workspace` **1992 passed / 0 failed** (baseline 1986 + 6 new tests); 4 prior promoting fixtures (a_distinct/b_same_callee/c_timing/d_saturation) re-confirmed byte-identical both modes AND still FIRE (a/b IR-grep = 2 spawns each); cross-impl determinism harness green (new fixture in corpus, deterministic); `cargo clippy --workspace -- -D warnings` clean; `cargo fmt --check` clean; no-block_on grep on `git diff dcb32af` REFUTED (empty). Deviation #2 union logic REWORKED (prior judge identity hash 2df0f05 invalidated → re-judge required); Deviation #1 unchanged (Round-1 judge #1 PASS still applies). 0 scope deviations.
- 2026-06-13 — **Slice 1 gate Round 2: 5 PASS / 2 BLOCK** (5 reviewers + 2 deviation-judges; all carried an explicit read-only/no-mutation-probe guard this round — tree-integrity held, all 3 code blobs unchanged pre/post). PASS: rules-compliance (FIX 1 airtight, all 5 new Tier-2+ fns carry Big-O, debug_assert is a real tripwire, every graveyard corpse check clean), plan-adherence (R1 fixes met, scope clean; minor: a doc-link sweep touched `m2_state_machine_integration.rs` — 1-line comment, de minimis, scratch "None" imprecise), design-compliance (spike_host_subset is compile-time HashSet filter, no coloring/bridge regression), acceptance-verifier (LIVE: 1992/0, 4 prior fixtures byte-identical, nested fixture fires exactly 2 spawns + `work` plain non-SM, 5 offset tests green), deviation-judge #1 (FIX 2 shared helper makes the two size paths structurally identical by construction — mutual-recursion/three-fn-chain/zero-member all agree). **Both R1 BLOCKs CONFIRMED resolved.** No carve-out (no Planned RED Repros).
  - **code-reviewer (Opus): BLOCK (1, NEW miscompile from FIX 4)** + **deviation-judge #2: BLOCK (same finding, independent fuller trace)** — `spike_host_subset` is called with DIFFERENT base suspend sets at its two query boundaries: `frame_layouts_query` (queries.rs:113) passes `effective_suspend_set` (= `check.suspends_set` ∪ imported-suspending names via `build_effective_suspend_set`), `codegen_query` (queries.rs:319) passes bare `check.suspends_set` (imported names ABSENT) and builds `suspends_with_promotions` from it too. For an `entrypoint()` with a local adjacent CPU pair + a post-pair call to an IMPORTED suspending fn: `spike_cpu_candidates`'s post-pair decline gate (`stmt_contains_suspending_call`) fires in frame_layouts_query (sees the import → declines → reserve=0 but sizes the frame WITH the imported child sub-frame) but NOT in codegen_query (doesn't see it → admits as spike host → the spike `frame_bytes` formula at emit.rs:2451 OMITS the imported child sub-frame) → heap block under-allocated by exactly the imported callee's frame size → child frame written past the end = silent heap corruption (the exact class the entrypoint-only gate exists to prevent). FIX 2's `debug_assert` cannot catch it (both its compared quantities derive from frame_layouts_query/the effective set; the divergence is against what `lower_function_with_waits` actually allocates). Reachable now (Phase-2 `imported_real_work_callee_promotes_importer` proves entrypoint can call imported callees); untested (no multi-file m3d fixture) so 1992/0-green doesn't refute it. Coordinator triple-confirmed via direct queries.rs read. **FIX (both reviewers agree)**: build ONE canonical effective suspend set used by BOTH query boundaries' `spike_host_subset` probe AND the `suspends_with_promotions` set handed to `emit_artifact` (so the emit-time re-probe at emit.rs:2321 also sees imported suspending names) + add a multi-file fixture (entrypoint + local CPU pair + post-pair imported-suspending call) asserting both paths agree → host declined in both → 0 spawn calls, sequential, byte-identical both modes. This is a NEW bug introduced by Round-1's FIX 4 (different file:line set than Round 1 → genuine progress, no-progress check does not trip).
- 2026-06-13 — **Slice 1 Round 3 fixes applied** (Opus executor) — the single cross-boundary BLOCK (code-reviewer + judge #2, same finding) resolved as both reviewers prescribed; live-verified:
  - **FIX (single source of truth)**: `codegen_query` now builds `effective_suspend_set = build_effective_suspend_set(&check.suspends_set, &sig_output.imported_fns)` (the SAME canonical computation `frame_layouts_query` already used), probes `spike_host_subset` against `&effective_suspend_set` (was bare `&check.suspends_set`), and builds the `suspends_with_promotions` set handed to `emit_artifact` from the effective set (so the emit-time re-probe `lower_function_with_waits → spike_cpu_candidates` also sees imported suspending names). Both query boundaries + the emit-time re-probe now read ONE canonical effective set — the two frame-sizing decisions are in lock-step by construction. `queries.rs:325-339`.
  - **REACHABILITY FINDING (verify-first, live — IMPORTANT, contradicts the BLOCK's "reachable now" claim)**: the BLOCK asserted the corruption is reachable today. LIVE probing (registering the multi-file fixture + calling `cpu_promotion_query` directly) shows it is NOT independently reproducible as corruption in the current slice envelope: for ANY `entrypoint` with a CPU group AND a post-pair imported-suspending call, typeck's `cpu_promotion_query` guard-probe rollback DECLINES `entrypoint` (promotion set = `{}` — verified across 4 shape variants: print-before-wait, no-crossing, bare-call, wait-call). With an empty promotion set, `spike_host_subset` never probes that function at EITHER boundary, so the bare-vs-effective asymmetry produces NO divergent admission — it is MASKED by the guard-probe. The RED-vs-GREEN attempt confirmed it: under the reverted bare-set codegen_query the fixture still ran `55\n89` clean (no corruption observed) AND emitted 0 spawns. So the fix lands as a SINGLE-SOURCE-OF-TRUTH correctness fix removing a real latent asymmetry (so the masking can never silently flip to corruption if a future slice relaxes the entrypoint-only gate or the guard-probe), NOT as a fix for a live-reproducing miscompile. The reviewers' static trace was sound about the asymmetry but did not account for the typeck guard-probe gating it upstream. Honesty preserved per verification.md — the fixture/test assert the consistency invariant + correct decline, not a live corruption→clean flip.
  - **VERIFY-FIRST artifacts**: new multi-file fixture `crates/ynz-driver/tests/fixtures/v0_3_m3d_spike_s_imported_suspending_after_pair/` (yinz.toml + io_lib.ynz exporting suspending `ioWork` + entrypoint with `let a=fib(10)`/`let b=fib(11)` then `wait ioWork()`). Codegen-crate regression test `imported_suspending_after_pair_declines_consistently_across_boundaries` (frame_layouts_query.rs): asserts (1) entrypoint emits **0** `call @ynz_rt_spawn_blocking_joinable` (declined) and (2) `spike_host_subset` returns the SAME host set probed against bare vs effective sets (agreement by construction). Driver integration test `v03_m3d_imported_suspending_after_pair_byte_identical_and_clean` (integration.rs): default and `YNZ_NO_AUTO_PARALLEL=1` byte-identical (`55\n89`, exit 0, clean — 20/20 repeated runs clean; no valgrind in env, the project's alloc-harness is unit-level and the declined path allocates no spike frame to leak).
  - **Full verification (live)**: `cargo test --workspace` **1994 passed / 0 failed** (baseline 1992 + 2 new tests); 4 prior promoting fixtures (a_distinct/b_same_callee/c_timing/d_saturation) re-confirmed in PRODUCTION (no env) byte-identical both modes AND still FIRE (each IR-grep = 2 spawns); nested fixture `v03_m3d_nested_groups_byte_identical_and_fires` still fires exactly 2 spawns + byte-identical; `cargo clippy --workspace -- -D warnings` clean; `cargo fmt --check` clean; no-block_on/thread-park grep on `git diff dcb32af` REFUTED (empty). Deviation #2 hunks moved (codegen_query block 306-333 → 325-339, now effective-set; prior identity hash invalidated → re-judge required). Deviation #1 unchanged. 0 scope deviations. The `m2_state_machine_integration.rs` doc-link change cited by plan-adherence as a minor scope touch is already committed under docs-relocation commit 93506c0, not in this changeset.
- 2026-06-13 — **BASE/diff-form change for Round 3 gate**: the user committed two unrelated DOCS commits on top of Phase 2's dcb32af mid-execution (`93506c0` docs relocation incl. the `m2_state_machine_integration.rs` doc-link, `7d3f38b` GUI design doc). HEAD is now `7d3f38b`. The slice-1 code work is all UNCOMMITTED, so the clean gate diff is `git diff HEAD` (scoped to crates/ynz-codegen/src + crates/ynz-driver) — excludes the docs commits AND a pre-existing prior-session `.claude/rules/vue-website.md` WIP (NOT m3d; excluded from gate and from the eventual slice commit per no-git-add-all). This resolves plan-adherence's R2 `m2_state_machine_integration.rs` flag (it's the user's commit, not the slice's). The Phase-3 final-commit BASE remains dcb32af for SHA bookkeeping.
- 2026-06-13 — **Slice 1 gate Round 3 dispatched** (5 agents re-fired with read-only/no-mutation guard: code-reviewer + judge #2 re-fire the cross-boundary fix; rules-compliance + plan-adherence + acceptance-verifier delta-focus). **Carried R2 PASS**: deviation-judge #1 (Deviation #1 hunks in emit.rs — blob unchanged this round `fc00b31f`, identity hash + hunk files untouched) and design-compliance (the fix is pure compile-time effective-suspend-set construction — zero bridge/coloring surface, cleared R2). Coordinator instructed code-reviewer + judge #2 to probe the UN-masked shape (a second function promoting while entrypoint has CPU-pair + post-pair-imported-wait) the executor's masking analysis didn't cover — the fix should close the asymmetry for ALL shapes regardless of the current guard-probe masking. R3 tree-integrity snapshot recorded (queries.rs 9dbe2118, emit.rs fc00b31f, integration.rs e919f159, frame_layouts_query.rs faa8ea18).
- 2026-06-13 — **Slice 1 gate Round 3: 7/7 PASS, no BLOCKs** (5 fresh + 2 carried). PASS: code-reviewer (asymmetry genuinely closed; CONFIRMED the fix is load-bearing on the un-masked shape — a second fn promoting → m3d_spike=true → OLD bare-set corrupts, NEW effective-set declines consistently), deviation-judge #2 (R2 BLOCK killed; both boundaries + emit-time re-probe read one canonical effective set; found one NEW residual asymmetry — entrypoint self-recursion post-pair — traced BENIGN: over-allocation not under, dead spike emission, no corruption), plan-adherence (Steps 1–2 met, rationales phrase-clean), rules-compliance (no violations), acceptance-verifier (LIVE: 1994/0, a/b/nested fire 2 spawns, s-fixture 0 spawns + `55\n89` clean both modes, all new tests green), + carried judge #1 + design-compliance. Tree-integrity held (all 4 code blobs unchanged pre/post). **The cross-boundary fix is verified correct.**
  - **Code-reviewer 3 non-blocking Concerns → routed to Round 4 (coordinator decision: Rule 11 + no-duct-tape — confirmed corpse-adjacent findings get fixed at the cheapest moment, not deferred into slice 2 where a contributor reads the lie first):** (C1) [emit.rs:6785+] `spike_host_subset`'s param is named `base_suspend_set` and its doc says "evaluated against the BASE (I/O-only) set, never the promotion-unioned set" — but BOTH callers now pass `effective_suspend_set` (that IS the R2/R3 fix). The contract actively contradicts the callers → a slice-2 author could "correct" a caller back to the base set and walk into the heap-corruption ditch. CONFIRMED real (coordinator read emit.rs:6787 `base_suspend_set: &SuspendSet`). FIX: rename param to `suspend_set`, rewrite the doc to say the EFFECTIVE set + WHY (imported-suspending callees must be visible to the post-pair decline gate so both boundaries size identically). (C2) [queries.rs:104] stale comment same contradiction — fix in same pass. (C3) [frame_layouts_query.rs] the `hosts_bare == hosts_effective` assertion is TAUTOLOGICAL for the masked fixture (promotion empty → spike_host_subset short-circuits, never reads the suspend-set arg) — can't catch a C1-style regression. FIX: add a direct unit test calling `spike_host_subset` with hand-built `promoted={"entrypoint"}` + the pair+post-pair-imported-wait typed module, asserting bare-set ADMITS / effective-set DECLINES (exercises the divergence the masking hides).
  - **Slice-2 carry-forward logged (NOT fixed — benign + slice-2 territory):** judge #2's entrypoint-self-recursion-post-pair residual (emit-time `suspends_with_promotions` contains `"entrypoint"` but `spike_host_subset`'s probe set didn't → emit-time declines while probe admitted → 48-byte OVER-allocation, dead spike emission, no corruption). When slice 2 generalizes the host gate, tighten so the probe set and the emit-time set agree to eliminate even benign over-allocation.
- 2026-06-13 — **Slice 1 Round 4 (code-reviewer's 3 R3 concerns) applied + confirmed GREEN.** Executor (Opus): C1 renamed `spike_host_subset`'s param `base_suspend_set` → `suspend_set` + rewrote its doc to state the EFFECTIVE set + WHY (isolated diff confirms the sole code-line change is the param identifier — zero logic); C2 fixed the queries.rs frame_layouts_query "BASE" comment → effective; C3 added direct non-tautological test `spike_host_subset_bare_admits_effective_declines_on_imported_post_pair` (hand-built `promoted={"entrypoint"}` + divergence shape, asserts bare ADMITS / effective DECLINES — MUTATION-PROVEN non-vacuous: flipping each assertion fails); judge-#2 benign-residual carry-forward note added in-code at both seams. Live: `cargo test --workspace` **1995/0** (R3 1994 + 1 new test), 8/8 frame_layouts_query tests incl. the new direct test, a/b/nested still fire 2 spawns, s-fixture still 0 spawns byte-identical, clippy/fmt clean. **Code-reviewer R4 scoped-confirm: PASS** ("all three concerns nailed shut, behavior-neutral"). Carried R3 PASS for the other 5 lanes (rename/doc/additive-test don't touch the logic they validated). Coordinator probe confirmed rename landed + doc fixed + new test passes live + tree-integrity held (R4 snapshot emit.rs b884b990, queries.rs ceb000b9, frame_layouts_query.rs 10cd588f). Out-of-scope carry-forward noted: the `.ynz` fixture-file HEADERS still say "BASE suspend set" (same lie, but in fixture comments that drive no code decision — slice-2 sweep).
- 2026-06-13 — **SLICE 1 COMPLETE & VERIFIED GREEN (4 gate rounds).** Slice 1 = Phase 3 Steps 1–2: production trigger (`cpu_promotion_query` replaces the `YNZ_M3D_SPIKE` env gate, unioned via the canonical effective suspend set into both query boundaries + the emit-time re-probe) + `FrameLayout` CPU-slot extension (`(group,member-index)`-keyed handle/result slots via the shared `cpu_group_slots_and_reserve` single-source helper feeding both `build_frame_layouts` and `compute_frame_size`). Round history: R1 (3 BLOCK: bare-8, compute_frame_size 48-byte dual-size corpse, zero offset tests, union-poisoning) → R2 (1 BLOCK: NEW cross-boundary probe-set-mismatch miscompile from R1's fix) → R3 (7/7 PASS + 3 concerns) → R4 (concerns nailed, confirmed). Corpus 1995/0; all corpse guards (memo-ordering, flat-scan, silent-envelope-narrowing) respected; no synchronous join anywhere; tree-integrity verified each round (one mid-R1-gate reviewer mutation logged + verified non-compromising). **Per Patrick (2026-06-13): committing slice 1 as a VERIFIED CHECKPOINT (mid-phase, deviation from "one commit at phase boundary" — approved given the 4-round token spend + the twice-observed mid-gate tree-mutation risk), then STOPPING per "just Phase 3, then stop". Slices 2–4 (return-class matrix, same-callee, mixed CPU+I/O, restriction-lift) resume in a future session from this clean committed base.** Phase 3 Phase Review Gates + ACs remain UNTICKED — they gate on the FULL phase (slices 2–4 pending); slice-1 gate verdicts live here in the Findings Log.
- 2026-06-13 — **SLICE RE-CUT (coordinator, transparent — NOT silent narrowing).** Reconnaissance for slice-2 dispatch found the line-657 slice boundaries drifted: slice 1 already production-triggered the spike lowering, and the CPU-child bind arm (`emit.rs` ~6060) already handles int, ErrorsCapable (`T errors`), and Shape-crossing. A literal "slice 2 = distinct-callee **int** lowering" is therefore near-trivial. ALSO found the real gap: the spike int fixtures (`a_distinct`/`c_timing`/`d_saturation`) exist as files but are NOT referenced in any integration test — only `r` (nested) + `s` (imported-suspending) are CI-gated (the "spike-on oracle manual-only" gap the R1 code-reviewer flagged). Re-cut to non-trivial, verifiable chunks (within Patrick's "do 2–4 adaptively, pause if as big as slice 1" authorization): **Slice 2 = distinct-callee return-class MATRIX** (int/float/number/string/array/map/`T errors`) — each return class CI-gated with FIRE (IR-grep ≥1 `ynz_rt_spawn_blocking_joinable`) + byte-identical default-vs-`--no-auto-parallel` + alloc=free; fix any class that does not yet bind correctly (verify-first vs oracle). **Slice 3 = same-callee distinctness across the matrix + worth-it/trivial-leaf-not-spawned.** **Slice 4 = mixed CPU+I/O + `if`/`match` arms + single-for-body + panic re-raise + cancel + full restriction-lift (feature fully live).** Gate BASE for slice 2 = HEAD (`a63d2c9`) so the gate diff scopes to slice-2's uncommitted work only (excludes the unrelated docs commit on top of slice 1, per the slice-1 `git diff HEAD` precedent at line 679). Phase-3 final-commit BASE remains dcb32af for SHA bookkeeping.
- 2026-06-13 — **Slice 2 executor (Opus) DONE**: distinct-callee return-class matrix. `return_type_fits_cpu_result_abi` lifted from int-only to int/bool/float/number/string/array/map/`T errors`; trampoline pack dispatches on LLVM value-kind + `callee_returns_bare_number`; join-bind routes through `load_sm_return_value_typed` + `bind_sm_result_and_flush`. 9 CI tests (6 new classes + 3 promoted int spike fixtures a/c/d) via shared `m3d_assert_fires_byte_identical_alloc_free` (asserts ==2 spawns + byte-identical + alloc=free). Corpus 2004/0. Caught + fixed a real silent-wrong `number` bind verify-first (parallel read 0.0, sequential 6.0 — trampoline packed the heap ptr bits instead of dereferencing). 1 approach deviation (#1, trampoline pack dispatch), 0 scope deviations.
- 2026-06-13 — coordinator probe (pre-gate, slice 2): no-synchronous-join (HALT-class). Command: `git diff a63d2c9 -- crates/ynz-codegen/src crates/ynz-runtime/src crates/ynz-typeck/src | grep '^+' | grep -iE 'block_on|thread::park|spin_wait|busy.?wait|\.join\(\)'`. Result: REFUTED (empty). Routing: proceeding to full fan-out.
- 2026-06-13 — **Slice 2 gate Round 1: 2 PASS / 4 BLOCK** (5 reviewers + 1 deviation-judge). Tree-integrity HELD (all 4 code blobs unchanged pre/post — reviewers respected read-only). No Planned RED Repros → no carve-out; all BLOCKs route to fix loop. PASS: **acceptance-verifier** (LIVE: all 6 slice-2 contract items MET from its own runs — number 6.0/15.0 not 0.0, FIRE non-vacuous (sequential IR = 0 spawns fails the ==2 gate), alloc=1/free=1, int spikes CI-gated, 2004/0); **design-compliance** (poll-based throughout, lone `block_on` is the pre-existing entrypoint driver, same-callee lifting is the recorded `design/concurrency.md` divergence — no new divergence/coloring/bridge).
  - **deviation-judge #1: BLOCK (number-errors UAF, corpse-class)** — a non-SM `number errors` callee is admitted by `return_type_fits_cpu_result_abi` (emit.rs:6622 — `ErrorCapable{inner:Number}` slips the `!matches!(Named)` gate) but returns its ok-word as a POINTER TO A STACK ALLOCA (emit.rs:10884 + 9195 `ptr_to_int` of stack i128). The deviation's deref fix is only in the trampoline `PointerValue` arm; `number errors` arrives via the `StructValue` arm, which packs the dead-stack ptr; join-bind `bind_sm_result_and_flush`→`is_number_errors_callee`→`int_to_ptr`→`build_load i128` derefs it after the callee frame is dead → **use-after-free** on the decimal bits. Bare `number` is safe (its ABI ptr is heap-stable); `number errors` is not. Root cause: {i64 err-word + i128 ok-value} = 24 bytes > the 16-byte YnzCpuResult slot, so the ABI smuggles a pointer that the spike path never heap-stabilized.
  - **code-reviewer (Opus): BLOCK (typeck↔codegen gate-parity, SAME contract Slice-1 judge #2 BLOCKed on)** — the machinery is solid (live-verified bignum/bool/empty-array round-trip), but codegen `return_type_fits_cpu_result_abi` (emit.rs:6599) and typeck `returns_cpu_supported_class` (independence.rs:270) admit DIFFERENT class sets while both doc-comments claim they "always agree." Typeck's `_ => true` admits `maybe`/`union`/`dynamic`/`range`; codegen declines them. Live-reproduced: a `maybe<int>` distinct-callee CPU pair → typeck promotes, codegen emits 0 spawns → silently sequential → Phase-4 `parallel_groups` hint (reads typeck) will mark it parallel while it runs sequential. The "always agree" doc is false-as-written (no-duct-tape #8). Non-blocking concerns: `int_errors` only tests the success path (error path source-unreachable this slice — no raise primitive); `fixed` admitted by codegen but never fires (dead admission + separate pre-existing fixed-return bug).
  - **rules-compliance: BLOCK (2)** — (1) [emit.rs:15028] `callee_returns_bare_number` was inserted between the `errors_capable` doc-comment and its owner `is_errors_capable_fn`; Rust `///` attaches to the following item, so the new fn now carries a doc claiming it "checks errors_capable" when it checks `number` return type (comments.md durable-not-changelog + structural). (2) 5 fixture comments leak LLVM/runtime jargon into user-facing `.ynz` examples (`f64`, `i64`, `{i64,i64}`, `ErrorsCapable`, `heap pointer`, `parallel-join trampoline`, `ABI`, `pointer word`) — vocabulary.md violation (a Yinz user only knows int/float/number, never pointers — Golden Rules 3/4).
  - **plan-adherence: BLOCK (1)** — [emit.rs:6592] `bool` is admitted by the ABI gate with a dedicated `build_int_z_extend` pack path (emit.rs:7498) but has NO fixture/CI test (gated-path-fire-assertions: admitted path, distinct codegen, zero coverage). Add a bool test or document int/bool ABI grouping with proof. (Scope verified clean — 0 deviations confirmed; Deviation #1 rationale phrase-clean.)
  - **CONVERGENT ROOT CAUSE**: judge #1 (number-errors UAF) and code-reviewer (maybe/union parity) are the SAME defect class — two return-class gates that don't agree on the admitted set. Clean fix = ONE shared CPU-supported-return-class predicate (single source of truth) used by BOTH typeck + codegen, declining everything neither can safely parallelize.
- 2026-06-13 — **number-errors resolution (Patrick: "right long-term answer per design doc / rules / plan"; coordinator reasoned to it):** the governing rule is the plan's own Anti-Pattern Callout **"Hotfix that isn't"** (line 203) — pre-existing/base-codegen bugs discovered during M3d are DECLINED-AROUND (decline-to-promote) and tracked on their existing roadmap/todos track, NOT fixed inside M3d. The `number errors` UAF is the **parallel-path manifestation of the wide-value-EC staging-slot family** already tracked at `.claude/todos.md:180` (sequential copy-on-bind shipped in M3f) + the `ec-wrapper-collect-on-completion` deferral (registry/features.toml). Its root fix (heap-stabilize the i128 ok-word so it survives a worker thread, mirroring bare-`number`'s SM wrapper at emit.rs:3161-3187) is a return-ABI change broader than M3d — same footing as the `-> Shape` return bug the plan cites as the decline-around precedent. **RESOLUTION: decline `ErrorCapable{inner: Number}` (and any wide-value-EC inner) from CPU promotion** via the shared predicate (both gates), lock with a decline-test, track the parallel-path manifestation in todos. This is NOT a documented-scope narrowing: the matrix's `T errors` cell stays green via its `int_errors` representative; only the unsafe wide inner declines, exactly as the auto-promotion rule's first-class decline + the Hotfix-that-isn't callout mandate. Tracked: `.claude/todos.md` (appended 2026-06-13).
- 2026-06-13 — **Slice 2 Round 2 dispatched** (Opus executor): consolidated fix of all 4 R1 BLOCKs + non-blocking concerns. (1) ONE shared CPU-supported-return-class predicate used by BOTH typeck `returns_cpu_supported_class` + codegen `return_type_fits_cpu_result_abi` — declines `maybe`/`union`/`dynamic`/`range`/non-(array|fixed|map) Generic AND `ErrorCapable{inner: Number}`/wide-EC; truthful doc-comments (no false "always agree"); gate-parity invariant test. (2) decline-tests: number-errors pair + maybe<int> pair → 0 spawns, sequential byte-identical. (3) bool fixture+test (FIRES byte-identical). (4) emit.rs:15028 doc-comment relocation. (5) 5 fixture-comment jargon scrub → Yinz vocabulary. (6) int_errors fixture header note (error path source-unreachable this slice). Verify-first: confirm sequential number-errors correctness (M3f copy-on-bind) before declining the parallel path.
- 2026-06-13 — **Slice 2 Round 2 DONE & VERIFIED** (Opus executor). Single source of truth: typeck `cpu_result_abi_supports` (resolved `Type`, `pub`) — codegen's AST-level `return_type_fits_cpu_result_abi` is its documented mirror over the un-resolved AST `Type`; the two are LOCKED in agreement by `emit::tests::cpu_result_abi_gate_parity` (resolves a representative spread incl. maybe/union/range/number-errors/fixed/shape and asserts both verdicts agree byte-for-byte). The shared-fn route over a single enum was rejected (would require threading resolved sigs through the slice-1-hardened frame-size path — ~3 helpers + 4 callers + the dual-frame-size machinery, high regression risk); the cross-asserting-test route is the prompt-sanctioned alternative for a genuine two-enum boundary, and the test CAUGHT a real divergence during development (typeck still admitted `fixed` after a first pass — fixed). Admit set: int/bool/float/number/string/array/map + `T errors` with safe inner (int/bool/float/string/array/map). Declines: maybe/union/dynamic/range/options/non-(array|map) Generic, `Shape`/`Shape errors`, **`number errors`/wide-EC** (UAF — decline-around per "Hotfix that isn't", tracked todos:188), **`fixed`** (verify-first: a `fixed<int>` distinct pair emits 0 spawns — dead admission + a separate pre-existing `fixed`-return base bug where `.count()`==0 in both modes; declined consistently in both gates, tracked). FIX 2 decline-tests: `number_errors` pair → 0 spawns + byte-identical `6.0/10.0` (verify-first confirmed sequential M3f copy-on-bind correctness before relying on decline); `maybe` pair → 0 spawns + IR byte-identical between modes (asserted at IR level, NOT stdout — two adjacent `maybe<int>` binds hit a tracked pre-existing base bug producing non-deterministic runtime output, orthogonal to auto-parallel, IR identical both modes; tracked todos + excluded from the corpus determinism harness with a documenting WHY). FIX 3 bool fixture+test FIRES 2 spawns byte-identical (`true/true`). FIX 4 doc-comment relocated (the `errors_capable` doc now precedes `is_errors_capable_fn`; only the `number` doc precedes `callee_returns_bare_number`). FIX 5 all 6 return-class fixture headers scrubbed to plain Yinz vocab (no f64/i64/{i64,i64}/ErrorsCapable/decimal128/pointer/trampoline/ABI/heap). FIX 6 int_errors header note (error case source-unreachable this slice). **Live verification**: full matrix — float/number/string/array/map/int_errors/bool + int spikes (a/c/d) all FIRE 2 spawns + byte-identical (no regression from refactor); number_errors/maybe DECLINE 0 spawns. `cargo test --workspace`: 1997 passing in the run captured (one pre-existing load-flake `v03_m3e_alias_local_name_collision_runs_correctly` — todos:176, passes in isolation, M3e `background` fixture untouched by this work); 12 m3d return-class tests + gate-parity unit test all green in isolation. CI-equivalent `cargo clippy --workspace -- -D warnings` clean (exit 0); `cargo fmt --check` clean. No-synchronous-join HALT-class probe REFUTED (empty) on the diff. **SCOPE deviation**: touched `crates/ynz-typeck/src/independence.rs` (within the plan's front-matter `files:` but beyond slice-2's emit/fixtures/integration working set) — required to host the single-source-of-truth predicate. 0 new approach deviations (matches the plan's "ONE shared predicate" mandate). Slice-1 approach deviation #1 (trampoline pack dispatch) unchanged.

### Phase 4: Teaching surface + docs + demo
**PR scope**: `parallel_groups` registry domain + LSP inlay pass + hover text; `wait_on_non_may_block` reword; VSCode bump + screenshot; pirates-roster CPU section; design/spec doc updates; error-gallery considered-note.
**Branch**: `feat/m3d-cpu-parallelization`
**Flag**: N/A
**Est. lines**: ~600
**Ships via**: `/pr`
**Objective**: The full teaching surface mandated by roadmap Constraint "Full teaching surface ships in the same milestone": every compiler scheduling decision this milestone makes is visible in the IDE with WHAT/WHAT-INSTEAD/WHY, the demo shows it in context, and the docs describe the locked model.
**Why this phase exists**: invisible auto-promotion is magic; magic is anti-Yinz (`.claude/rules/auto-promotion.md` Banned Anti-Pattern 1). This phase also closes the M3b-era gap where parallel groups had NO inline IDE surface (design/concurrency.md "IDE Execution Plan — Non-Negotiable").
**Current-state anchors**:
- `registry/features.toml:2086` — `background_routing` entry (format precedent for the new domain)
- `registry/features.toml:1553` — `wait_on_non_may_block` why_template (reword target)
- `crates/ynz-typeck/src/inlay_hint_passes.rs:1495` — `background_routing_hints` (pass pattern to mirror; new pass consumes the typeck partition directly)
- `crates/ynz-lsp/` — inlayHint handler wiring (mirror the M3b domain hookup)
- `examples/pirates-roster/entrypoint.ynz` — demo growth point
**Files (expected scope)**: `registry/features.toml`, `crates/ynz-registry/` (regenerated constants), `crates/ynz-typeck/src/inlay_hint_passes.rs`, `crates/ynz-lsp/src/`, `tooling/vscode-ynz/` (version bump + screenshot), `examples/pirates-roster/entrypoint.ynz`, `examples/primantis-orders/v0_3_m3d_errors.ynz` (considered-note header), `design/concurrency.md`, `spec/concurrency.md`.
**Deviation rule**: (same as Phase 0)
**Steps**:
1. Registry: `[[muted_hint_domain]] parallel_groups` (Informational; example_hint_rendered like `// runs at the same time as line 12 — separate core` for CPU members / `// runs at the same time as line 12` for I/O members — final wording jargon-audited, no "thread"/"spawn"/"pool" leakage per vocabulary.md).
2. Inlay pass `parallel_group_hints` consuming the typeck partition (the same query codegen reads — agreement guaranteed by construction); hover: WHAT (these statements overlap because no data flows between them), WHAT-INSTEAD (write `wait` before the second call to force order), WHY (contextual — names the actual bindings/lines).
3. Reword `wait_on_non_may_block` why_template: explicit `wait` on a CPU-bound callee is an ordering barrier (prevents overlap with surrounding independent statements); update the fixture snapshots that carry the old text.
4. `design/concurrency.md`: new "CPU Statement Parallelization (M3d)" section (promotion + decline rules verbatim from the P0 decision record, panic re-raise, cancellation detach constraint, worth-it proxy, same-callee scope amendment to the M3b divergence entry). `spec/concurrency.md`: HS-grad-tone subsection.
5. `examples/pirates-roster/entrypoint.ynz`: CPU multicore section (realistic two-stat crunch over the roster) with comments pointing at the hint; insta snapshot. `examples/primantis-orders/v0_3_m3d_errors.ynz`: header note — M3d ships no new compile-error classes (decline is silent by design); file exists so the per-milestone gallery convention holds.
6. VSCode extension: version bump, `cpu-parallel-hints.png` screenshot, hover-doc text carried via registry regeneration.
**Acceptance criteria**:
- [ ] `parallel_groups` hints fire on the pirates-roster demo section in a live LSP session (screenshot evidence)
  - Evidence: (filled at phase completion)
- [ ] Hint/binary agreement: the inlay pass and codegen consume the SAME partition query (cited import paths)
  - Evidence: (filled at phase completion)
- [ ] `wait_on_non_may_block` reworded + snapshots updated; jargon_audit green over all new user-facing text
  - Evidence: (filled at phase completion)
- [ ] `design/concurrency.md` + `spec/concurrency.md` sections landed; M3b divergence entry amended
  - Evidence: (filled at phase completion)
- [ ] VSCode version bumped, screenshot added, registry constants regenerated
  - Evidence: (filled at phase completion)
**Quality gate**:
- [ ] Hint placement category is Informational ONLY (no mixed surfaces — inference.md consistency rule)
- [ ] WHY text is contextual (names actual lines/bindings), not generic
- [ ] Registry entry precedes code consuming it (feature-registry.md discipline)
**Verification**: `cargo test --workspace` (snapshot suites green); manual LSP session against the demo; jargon audit test green.

**Phase Review Gates**:
- [ ] code-reviewer: <verdict + ISO timestamp>
- [ ] rules-compliance-reviewer: <verdict + ISO timestamp>
- [ ] plan-adherence-verifier: <verdict + ISO timestamp>
- [ ] acceptance-verifier: <verdict + ISO timestamp>
- [ ] design-compliance-reviewer: <verdict + ISO timestamp>
- [ ] Committed: <commit SHA>

**Findings Log**:
_(empty)_

### Phase 5: Adversarial gate + perf spike + verification sweep
**PR scope**: The mandatory adversarial gate (danger matrix + stress + cancellation/panic hostile fixtures), the worth-it perf calibration spike with recorded evidence, the cross-impl consistency run over the entire corpus, compile-time budget check, and the Step-10 verification sweep (TODO sweep, quality checklist, cumulative review).
**Branch**: `feat/m3d-cpu-parallelization`
**Flag**: N/A
**Est. lines**: ~700 (mostly fixtures + tests)
**Ships via**: `/pr`, then `/release` for `v0.3.0-m7`
**Objective**: Prove the milestone hostile-input-clean before release. The roadmap marks this gate MANDATORY: the CPU join is M2-HALT-corpse-adjacent.
**Why this phase exists**: M3a P2's shadow saga (6 fix-rounds, round 6 a silent miscompile) and M3e's adversarial round (3 live SIGILLs found) prove this codegen area is whack-a-mole-prone; a structured matrix catches what feature-development testing doesn't.
**Current-state anchors**:
- M3e's 19-fixture danger-matrix pattern (plans/done/v0-3-m3e — structure precedent)
- `crates/ynz-driver/tests/integration.rs` — fixture harness
**Files (expected scope)**: `crates/ynz-driver/tests/fixtures/`, `crates/ynz-driver/tests/integration.rs`, this plan file (evidence write-back), `design/concurrency.md` (calibration constant note if the spike adjusts the proxy).
**Deviation rule**: (same as Phase 0). Any bug found routes to a fix commit within this phase if it's an M3d defect; CONFIRMED pre-existing bugs get the M3f treatment — tracked loudly on the roadmap/todos, not silently fixed here.
**Steps**:
1. Danger matrix fixtures (every cell via real `ynz run`, multiple runs to catch garbage reads, alloc=free): return classes {int, bool, float, number, string, array, map, int errors, number errors} × {distinct-callee, same-callee} × {CPU-only group (FIRE), mixed group (DECLINE in M3d — the FIRE is M3g; here the cell asserts 0 spawns + byte-identical)} × position {top-level, if-arm, match-arm (FIRE), single loop-iteration body (DECLINE — 0 spawns)} — pruned to the ~25 highest-risk cells with the pruning rationale recorded (full cross-product is ~288; pruning keeps every unique mechanism pairing). NOTE 2026-06-14: mixed-group and loop-body cells test the locked DECLINE (0 spawns), not a FIRE — those FIREs are M3g / a future loop-placement slice per the Phase-3 Scope Boundary.
2. Hostile fixtures: blocking-pool exhaustion (recursive promoted fn fanning out groups, total spawns ≫ 512 — must complete); cancellation mid-join (parent task cancelled while ≥1 CPU child outstanding — no UAF, no leak, detached child completes harmlessly); panic-in-child (re-raise identical to sequential); CPU child that itself calls `background` (spawn-from-blocking-pool-thread path); group whose members finish in reverse order — INCLUDING the mixed variant where the CPU child completes before the I/O child suspends (shared continuation state, different child classes, out-of-declaration-order completion); a SELF-RECURSIVE promoted function containing a CPU group (recursion heap-boxing × per-member handle/result slots intersecting — the two allocation mechanisms the frame-composition decision treats separately); a worth-it FALSE-NEGATIVE leaf (heavy straight-line callee with no loop/recursion — assert the decline is silent and output identical, confirming the proxy is a perf gate, never a correctness gate); zero-iteration and 10k-iteration loop bodies containing groups; deep promoted-caller chains (A→B→C all promoted by propagation).
3. Worth-it perf spike (CI machine): measure spawn+join overhead vs workload size; confirm the loop/recursion proxy leaves no pathological small-workload regression (a loop of 3 iterations spawning) — if it does, add a calibrated constant (e.g. minimum estimated iteration weight) with benchmark evidence recorded here + design doc, same protocol as auto-SoA SIZE_THRESHOLD.
4. Cross-impl consistency: EVERY `examples/` program + EVERY `crates/ynz-codegen/tests/` + driver fixture, default vs `--no-auto-parallel`, byte-identical stdout/stderr/exit.
5. Compile-time budget: `ynz build` wall-clock on pirates-roster, pre vs post — <10% increase (roadmap target).
6. Step-10 sweep: TODO/FIXME/phase-comment grep; todos.md cross-check; shortcut detection; quality checklist with evidence; plan-file final persistence; cumulative Opus reviewer fan-out per the protocol.
**Acceptance criteria**:
- [ ] Danger matrix green (cell list + pruning rationale recorded in the PR)
  - Evidence: (filled at phase completion)
- [ ] All hostile fixtures green incl. exhaustion, cancellation, panic, reverse-completion
  - Evidence: (filled at phase completion)
- [ ] Perf evidence recorded: overlap speedup numbers + worth-it calibration verdict
  - Evidence: (filled at phase completion)
- [ ] Cross-impl consistency green over the ENTIRE corpus
  - Evidence: (filled at phase completion)
- [ ] Compile-time increase <10% on pirates-roster (numbers recorded)
  - Evidence: (filled at phase completion)
- [ ] Step-10 sweep complete; cumulative reviewer fan-out PASS
  - Evidence: (filled at phase completion)
**Quality gate**:
- [ ] No fixture asserts interleaving-dependent stdout (aggregate/post-join discipline)
- [ ] Multiple-run garbage detection applied to every matrix cell touching frame slots
- [ ] Any pre-existing bug found is tracked loudly (roadmap/todos), not silently patched or silently skipped
**Verification**: full matrix + hostile suite via `cargo test --workspace`; timing/perf numbers in PR + plan; `/release` readiness check (Cargo.toml bump to `0.3.0-m7`, CHANGELOG, VSCode `.vsix` pair incl. `yinz-latest.vsix` clobber).

**Phase Review Gates**:
- [ ] code-reviewer: <verdict + ISO timestamp>
- [ ] rules-compliance-reviewer: <verdict + ISO timestamp>
- [ ] plan-adherence-verifier: <verdict + ISO timestamp>
- [ ] acceptance-verifier: <verdict + ISO timestamp>
- [ ] design-compliance-reviewer: <verdict + ISO timestamp>
- [ ] Committed: <commit SHA>

**Findings Log**:
_(empty)_

## Quality Checklist (verify at completion)
- [ ] All inputs validated (N/A web-style — compiler: all new analyses conservative-correct, decline on uncertainty)
- [ ] No synchronous join anywhere in the milestone diff (grep evidence)
- [ ] Promotion never errors: guard-shape corpus identical diagnostics pre/post
- [ ] Cross-impl consistency: full corpus byte-identical default vs `--no-auto-parallel`
- [ ] alloc=free on all new fixtures
- [ ] Teaching surface complete: registry + LSP + hover + VSCode + demo + docs
- [ ] Tests: happy path + error cases + edge cases (danger matrix + hostile fixtures)
- [ ] Existing tests still pass (full workspace, 1929+ baseline)
- [ ] Types complete (no `unwrap()` on fallible paths, clippy `-D warnings` clean)
- [ ] Follows existing codebase conventions (corpse guards a+b, canonical slot helpers, doc-comment house style)
- [ ] Every phase received all-reviewer + all-judge PASS before committing
- [ ] Final cumulative reviewer sweep passed (Step 10f)
- [ ] Plan-file acceptance-criteria checkboxes accurate across all phases
- 2026-06-13 — **Slice 2 gate Round 2: 5 PASS / 3 BLOCK** (5 reviewers + 3 deviation-judges). Tree-integrity HELD (all 4 code blobs unchanged pre/post). No-progress check: R2 BLOCK file:line sets are entirely disjoint from R1's → genuine progress. PASS: **code-reviewer** (R1 number-errors UAF corpse confirmed DEAD — declines in both gates, locked by regression test; trampoline reuses the canonical `load_sm_return_value_typed`+`bind_sm_result_and_flush` decoder, no wide-bits path survives), **acceptance-verifier** (LIVE: declines=0 spawns IR-confirmed, number_errors sequential `6.0/10.0` no-clobber, bool fires 2, full matrix byte-identical, gate-parity test passes, corpus 2008/0), **design-compliance** (pure compile-time predicate, silent sequential declines, no new divergence, number-errors decline-around is clean "Hotfix that isn't"), **deviation-judge (trampoline re-fire — R1 BLOCK now cleared)** (UAF vector gone; every admitted EC class string/array/float/bool-errors normalizes its ok-word to i64 in the SM wrapper before the trampoline sees a StructValue → pack sound), **deviation-judge (cross_impl harness-exclusion)** (laser-cut exclusion; any auto-parallel regression trips the IR-level `count_spawn_calls==0` guard before the determinism harness matters).
  - **deviation-judge (typeck shared-predicate): BLOCK + code-reviewer Concern 1 (same root)** — the `cpu_result_abi_gate_parity` test's `fixed`/`array`/`map` rows use `Resolved::Generic{name}` variants the typeck resolver NEVER emits for return types (it emits `Type::BuiltinFixed`/`BuiltinArray`/`BuiltinMap` — verified check.rs:3813/3827/3836). Both gates decline/admit consistently TODAY (no live bug), but the parity LOCK is phantom for the three collection variants: a future change admitting `BuiltinFixed`/etc. in one gate would pass the test unchanged → silently re-opens the hint/binary contract. The `Generic{name:"array"|"map"}` admit arm is dead for return classification. FIX (R3): parity-test rows → real `Resolved::Builtin*` variants + EC-inner rows; remove the dead `Generic{array|map}` arms (verify-first confirmed dead).
  - **rules-compliance: BLOCK (5 single-line)** — both R1 BLOCKs verified FIXED (doc relocation correct, all 9 `.ynz` fixtures jargon-free incl. the 3 new ones). New: (1) [emit.rs:15058] `callee_returns_bare_number` missing Big-O despite `.iter().any()` loop (comments.md Hard Rule 7); (2-5) [integration.rs:5297/5319/5332/5448] four changelog-style WHY comments ("Promotes the previously manual-only", "Before Slice 2", "Before the shared predicate") → durable-not-changelog. FIX (R3): add Big-O + rewrite 4 WHYs to durable invariants.
  - **plan-adherence: BLOCK (coordinator-attributable, NOT executor defects)** — (1-3) the 3 new R2 fixtures (bool/maybe/number_errors) were untracked → absent from `git diff` (coordinator only `git add -N`'d the original 6 before R1). Reviewers read them directly (rules+code confirmed content), so review coverage held; the real action was staging them — DONE (coordinator `git add -N` 2026-06-13). (4) `cspell.json` +deref/derefs is COORDINATOR bookkeeping for the plan-file prose flag, not executor scope creep — executor's "0 scope deviations" stands.
  - **R2 non-blocking concerns tracked**: code-reviewer Concern 2 (gate parity is hand-listed not compile-forced exhaustive — SAFE via decline-by-default, hardening tracked todos:M4); code-reviewer Concern 3 + reproduction (the `maybe<int>` base-codegen garbage bug — already tracked, decline-around confirmed orthogonal). plan-adherence cspell misattribution noted above.
- 2026-06-13 — **Slice 2 Round 3 (parity-fidelity + 5 comments) applied + gate Round 3: 4 PASS / 3 BLOCK** (5 reviewers + 2 judges; cross_impl judge CARRIED R2 PASS — file blob unchanged 564e8599). Tree-integrity HELD. R3 executor: parity-test rows for fixed/array/map → real `Resolved::BuiltinFixed`/`BuiltinArray`/`BuiltinMap` (+ EC-inner Builtin* rows); removed the dead `Generic{array|map}` arm from the TYPECK gate ONLY (kept in codegen — AST-vs-resolved domain distinction; R3 Approach Deviation #1, scratch-documented); Big-O on `callee_returns_bare_number`; 4 changelog WHYs rewritten. PASS: **code-reviewer** (mutation-proved the parity test now catches one-sided changes on BOTH codegen bare-return AND EC-inner sides — no blind spot; flagged that the typeck `_=>true`→`_=>false` inversion is a real hint-side correctness IMPROVEMENT, not merely behavior-preserving — it closes a latent hint/binary false-positive for maybe/union/range/etc.), **design-compliance** (zero semantic change, dead-arm removal behavior-preserving, no coloring/bridge), **acceptance-verifier** (LIVE 2008/0, matrix intact, parity test passes exercising real Builtin*), **deviation-judge trampoline re-fire** (pack logic byte-identical — zero `-` lines in trampoline region; R3 was doc + additive test rows only).
  - **deviation-judge (parity/asymmetric): BLOCK** — R3 added an EC-inner parity row for `array<int> errors` (BuiltinArray) but OMITTED the symmetric `map<int,int> errors` (BuiltinMap). The codegen EC-inner gate handles `"array"|"map"` in one `matches!` arm; the typeck gate handles BuiltinArray + BuiltinMap as TWO separate arms (independence.rs:342-343). A one-sided drop of `"map"` from the codegen EC-inner arm → `map<,> errors`: codegen=false/typeck=true → divergence, but NO row exercises that pair → test passes silently (same phantom-coverage class, EC-inner level). FIX (R4): add EC-inner rows for EVERY safe inner the gate admits (float/bool/string/map errors → admit) so every arm is pinned — durable, not whack-a-mole.
  - **rules-compliance: BLOCK (2 changelog)** — both R1+R2 BLOCKs verified FIXED (Big-O present; "Before Slice 2"/"Before the shared predicate" gone). NEW: (1) [emit.rs:16772] R3 INTRODUCED "── DECLINE side — cases that previously shipped a disagreement ──" (banned `previously`); (2) [integration.rs:5309-5311] the c_timing "Promotes the manual-only `c_timing` spike fixture into CI" survived (R3 softened "previously" but kept the changelog structure). FIX (R4): rewrite both to durable invariants; NO new changelog phrasing.
  - **plan-adherence: BLOCK (2)** — (1) [integration.rs:5311] same c_timing changelog straggler (corroborates rules); (2) R3 Approach Deviation #1 (asymmetric removal) was in the executor REPORT + reviewed by code-reviewer/judges but NOT written to the scratch file (coordinator bookkeeping gap) — RESOLVED 2026-06-13 (coordinator added the R3 Approach Deviation #1 entry to the scratch).
  - **Assessment**: all 3 R3 BLOCKs are non-corpse (test-completeness + comment durability); the codegen logic is sound (every R1 corpse bug closed, design/code-reviewer/acceptance/judges all PASS the actual code). Slice 2 is now AS HEAVY as slice 1 (entering R4 = 4 rounds). Per Patrick's "pause at boundary if big" — coordinator will STOP and surface the full slice-2 result for review + commit decision once R4 goes green.
- 2026-06-13 — **Slice 2 Round 4 (4 EC-inner parity rows + 4 comment rewrites) applied + gate Round 4: 4 PASS / 1 BLOCK** (5 reviewers + 1 judge; design-compliance + trampoline-judge + cross_impl-judge CARRIED prior PASS — surfaces provably untouched by a comment+test-row delta). Tree-integrity HELD. R4 executor: added parity rows for float/bool/string/map errors (admit), fixed the 2 changelog comments + 2 MORE the anti-changelog self-check caught (integration.rs:5346 "This was a real bug...", emit.rs:7922 "before"→"below the frame base"); anti-changelog grep EMPTY; mutation-proved the map row (drop "map" from codegen → parity FAILS). PASS: code-reviewer (independently mutation-tested the map row; gates byte-identical, no residue), rules-compliance (anti-changelog full sweep zero hits, all corpses NO-MATCH), plan-adherence (both R3 BLOCKs dead, scratch documents R3 ApproachDev#1, rationales clean), acceptance-verifier (LIVE 2008/0, full matrix fire/decline verified).
  - **deviation-judge (parity): BLOCK** — R4 pinned all 6 ADMIT EC-inner arms + 3 DECLINE representatives (fixed/number/shape) but the declined `maybe<int> errors` (Maybe inner) has NO parity row → a one-sided `AstType::Maybe=>true` edit to the codegen EC-inner gate would slip the test (no row exercises it). `maybe<int> errors` is a valid parseable return type. Judge also flagged dynamic/range/union errors as likely-unpinned. This is the 2nd consecutive parity-completeness BLOCK (R3=map, R4=maybe) — the decline side is `_=>false` (catch-all), so row-pinning is bounded-but-incremental.
  - **Coordinator R5 decision**: pin EVERY reachable declined EC-inner `Type` variant at once (Maybe/Dynamic/Range/Union/Options/Sensitive/user-Generic — verified parseable as `-> <T> errors`), each expected=false, ending the whack-a-mole definitively. The compile-forced exhaustive-match (code-reviewer R2 Concern 2) does NOT clear this BLOCK (it forces NEW-variant classification, not one-sided-edit detection — only rows catch that) and stays M4-tracked (todos; decline-by-default is safe meanwhile). Note: this slice has now EXCEEDED slice-1's 4 rounds; per Patrick's "pause if big", coordinator STOPS after R5 goes green and surfaces the full slice-2 result for review + commit decision.
- 2026-06-13 — **Slice 2 Round 5 (7 additive decline parity rows) applied + gate Round 5: 3 PASS / 1 BLOCK** (4 reviewers + 1 judge; plan-adherence + design-compliance + trampoline-judge + cross_impl-judge CARRIED prior PASS — surfaces provably untouched by additive test rows). Tree-integrity HELD; gate logic byte-identical (mutation-reverted, verified by coordinator + code-reviewer); anti-changelog EMPTY; corpus 2008/0. R5 added decline rows for Maybe/Dynamic/Union/Range/Options/Sensitive/user-Generic with a full reachability table. PASS: code-reviewer (rows non-vacuous — independently mutation-tested; pairings verified vs real resolver), rules-compliance (additive ratchet clean, anti-changelog empty), acceptance-verifier (slice-2 contract MET live 2008/0; its 2 "MISSING" ACs are the Phase-3 same-callee [Slice 3] + timing [Slice 5] items, correctly out of slice-2 scope — NOT defects).
  - **deviation-judge (parity): BLOCK** — `nothing errors` (`ErrorsCapable{Nothing}`) is unpinned. Executor marked Nothing-as-EC-inner "unreachable" but it's production-handled (`check.rs:1074` dedicated comment for `-> nothing errors`; `emit.rs:10904` dedicated codegen branch). A one-sided `AstType::Nothing=>true` admit would escape. One-row fix — BUT this is the 3rd consecutive parity-completeness BLOCK (R3 map → R4 maybe → R5 nothing-errors), revealing the row-based decline-side test structurally invites whack-a-mole.
  - **COORDINATOR ESCALATION TO PATRICK**: slice-2 code green since R2; last 3 rounds are parity-test completeness hardening on the load-bearing hint/binary-agree contract. Surfacing the close-out fork (durable exhaustive-forced restructure vs ship-safe-residual + M4 track vs one-more-row) for a decision — round 6 territory, exceeds slice-1's 4 rounds.
- 2026-06-14 — **Slice 2 Round 6 (DURABLE FIX: compile-forced exhaustive parity) + gate Round 6: ALL PASS on slice-2 scope** (5 reviewers + judge-parity; design-compliance + trampoline-judge + cross_impl-judge carried). Tree-integrity HELD (emit.rs blob unchanged pre/post). R6 restructured `cpu_result_abi_gate_parity` into a compile-forced exhaustive classifier `parity_case` over all 21 `ynz_typeck::types::Type` variants (NO `_` arm → a future variant is an E0004 build error until classified), running BOTH bare and `errors`-wrapped gate assertions per Reachable variant. Reachability re-audited + CORRECTED (verify-first): Nothing/MapEntry/Error → Reachable; only TypeParam (generic fns route to GenericFnTable, never the gated SignatureTable.fns) + ErrorsCapable (nested `T errors errors` parser-blocked) → Unreachable.
  - PASS: **deviation-judge (parity)** — empirically + by-trace verified exhaustiveness is compiler-forced, both Unreachable marks correct, one-sided-edit catch holds for Sensitive/Union; **"this line of findings is closed"** (the R3→R4→R5 whack-a-mole permanently ended); **code-reviewer** (empirically yanked the Bool arm → E0004 fired; hand-traced all 21 variants both gates; strictly-stronger coverage, no test-weakening, no mutation residue); **rules-compliance** (additive, anti-changelog empty, all corpses clear, satisfies new testing.md §Exhaustive-Coverage on all 3 axes); **plan-adherence** (directive delivered, TEST-ONLY scope, reachability correction surfaced loudly, rationales clean).
  - **acceptance-verifier: "BLOCK" = MID-PHASE-AC ARTIFACT, NOT a slice-2 defect** (verifier's own words). It graded the FULL Phase-3 AC block (lines 630/632/636) where same-callee (Slice 3) + mixed/restriction (Slice 4) are MISSING/WEAK BY DESIGN. The slice-2 CONTRACT is fully MET: parity test exhaustive (1 passed, no `_` arm), full return-class matrix FIRES (2 spawns) + DECLINES (0) byte-identical, timing MET (c_timing CI-gated, prior 1218ms vs 1780ms), alloc=free on all 9 FIRE fixtures, **corpus 2008/0** (live `--no-fail-fast`; the 1 fail-fast "failure" was the documented `v03_m3e_alias` contention flake — todos:176, passes in isolation). The 3 acceptance "Required Fixes" are all "Slice 3/4 must deliver X" — future-slice work the re-cut deferred, NOT slice-2 actionable. Phase-3 ACs get ticked at PHASE completion, not slice.
  - 2 non-blocking concerns tracked: `all_variants` runtime list hand-maintained (todos, M3d-P3-R6 — bounded residual, load-bearing classifier IS compiler-forced); `precision <= 34` pre-existing codebase-wide literal (not a regression).
- 2026-06-14 — **★ SLICE 2 COMPLETE & VERIFIED GREEN (6 gate rounds) ★.** Slice 2 = distinct-callee return-class MATRIX (int/float/bool/number/string/array/map + `T errors` with safe inner), each CI-gated with FIRE (2 spawns) + byte-identical default-vs-`--no-auto-parallel` + alloc=free. Round history: R1 (4 corpse-class BLOCK: number-errors UAF, typeck↔codegen hint/binary parity break, doc-misplacement, bool-gap) → R2 (NEW cross-boundary fix + shared single-source-of-truth predicate `cpu_result_abi_supports`; 1 BLOCK: phantom-Generic parity rows) → R3 (parity fidelity; 1 BLOCK: map EC-inner gap) → R4 (5 comment + map row; 1 BLOCK: maybe EC-inner gap) → R5 (7 decline rows; 1 BLOCK: nothing-errors gap) → R6 (DURABLE compile-forced exhaustive parity — Patrick-approved; whack-a-mole permanently closed). **Both corpse bugs dead**: the `number errors` use-after-free (declined-around per "Hotfix that isn't", tracked todos) + the load-bearing "IDE hint and emitted binary always agree" contract (now machine-enforced exhaustive). Corpus 2008/0; no synchronous join; all graveyard corpses respected; tree-integrity verified every round. **Phase 3 Phase Review Gates + ACs remain UNTICKED** — they gate the FULL phase (slices 3 [same-callee + worth-it] + 4 [mixed CPU+I/O, if/match/for-body, restriction-lift] pending). Slice-2 gate verdicts live here in the Findings Log. **Per Patrick "pause at boundary if as big as slice 1" (slice 2 = 6 rounds, exceeded slice 1's 4): coordinator STOPS, surfaces for review + commit decision.**
- 2026-06-14 — **Slice 2 committed as verified checkpoint: `d8bef4e`** (Patrick-approved). Explicit-path stage (no -A): emit.rs, independence.rs, integration.rs, cross_impl_consistency.rs, 9 return-class fixtures, plan, scratch, todos, cspell. Excluded: a stray `rust-toolchain.toml` 2-blank-line tool-noise change (not slice-2, left uncommitted). **Slices 3 (same-callee + worth-it) + 4 (mixed CPU+I/O, if/match/for-body, restriction-lift) resume from BASE `d8bef4e` in a future session.** Phase-3 Phase Review Gates + ACs still UNTICKED (gate the full phase). Per Patrick: STOP for this session.
- 2026-06-14 — **Slice 3 executor (Opus) DONE**: same-callee distinctness across the return-class matrix + worth-it/trivial-leaf-not-spawned. Gate BASE = `d8bef4e`. **Test-only diff** (no source change to emit.rs/state_machine.rs/independence.rs) — verify-first confirmed same-callee per-(group,member-index) keying (Slice 1) + worth-it production-trigger wiring already correct on base; no class binds wrong, no fix needed. 10 new fixtures (+ integration.rs +101): 8 same-callee (int/bool/float/number/string/array/map/`int errors`) each FIRE 2 spawns + DISTINCT correct values + byte-identical + alloc=free; `worth_it_trivial_leaf_inline` DECLINES (0 spawns) + `worth_it_loop_overlaps` FIRES (2) — identical entrypoint shape, callee loop-presence the sole FIRE/DECLINE variable. Corpus 2018/0. 0 scope deviations, 0 approach deviations.
- 2026-06-14 — coordinator probe (pre-gate, slice 3): no-synchronous-join (HALT-class). Command: `git diff d8bef4e -- crates/ynz-codegen/src crates/ynz-runtime/src crates/ynz-typeck/src | grep '^+' | grep -iE 'block_on|thread::park|spin_wait|busy.?wait|\.join\(\)'`. Result: REFUTED (empty — no source diff in those crates at all). Routing: proceeding to full fan-out.
- 2026-06-14 — coordinator probe (pre-gate, slice 3): "diff is test-only" claim. Command: `git diff --stat d8bef4e -- 'crates/ynz-codegen/src/*' 'crates/ynz-runtime/src/*' 'crates/ynz-typeck/src/*'`. Result: REFUTED suspicion / CONFIRMED claim (empty — zero source-file changes; only integration.rs +101 + 10 untracked fixtures). Routing: proceeding to full fan-out unchanged (clean probe never shrinks the gate).
- 2026-06-14 — coordinator probe (pre-gate, slice 3): worth-it DECLINE genuinely the worth-it gate (not accidental decline). Command: `cat` both worth-it fixtures + compare. Result: REFUTED suspicion / CONFIRMED claim — `trivial_leaf_inline` and `loop_overlaps` have IDENTICAL entrypoint shape (two distinct-callee int calls + two prints); the sole difference is callee body (straight-line arithmetic vs `while` loop), so loop/recursion presence is exactly what flips FIRE↔DECLINE → isolates `does_real_work`. Routing: proceeding to full fan-out unchanged.
- 2026-06-14 — **Slice 3 gate Round 1: 4 PASS / 1 BLOCK** (5 reviewers; D=0, no judges). Gate BASE = d8bef4e, diff `git diff d8bef4e -- crates/` (test-only: integration.rs +101 + 10 intent-to-added fixtures). PASS: **code-reviewer (Opus)** (empirically verified per-(group,member-index) keying in IR — distinct member-index trampolines `__ynz_spike_trampoline_entrypoint_sumTo_0/_1` both calling `@sumTo` through separate ctxs; FIRE non-vacuous 2-default/0-nopar; worth-it contrast isolates loop-presence; test-only claim honest), **plan-adherence** (all 8 same-callee classes + worth-it pair present + CI-gated, scope clean, no class silently dropped, test-only outcome plan-sanctioned), **design-compliance** (all assertions Model-A order-independent, same-callee fixtures embody member-index-keying divergence, no [locked] contradiction), **acceptance-verifier** (LIVE: 10 fixtures byte-identical both modes, 8 same-callee outputs arithmetically distinct, FIRE IR-verified 2/0, alloc=free, 1958 workspace tests green; timing AC MISSING but out-of-slice-3-scope by design). No Planned RED Repros → no carve-out; BLOCK routes to fix loop.
  - **rules-compliance: BLOCK (2)**:
    - **(1) [enumerable-domain] [integration.rs:~5508-5601]** — the 8 same-callee `#[test]`s hand-list the FIRE return classes with NO compile-forced exhaustiveness on the same-callee RUNTIME-CORRECTNESS axis (the R6 `parity_case` in emit.rs is compile-forced but covers the CLASSIFICATION axis only — FIRE-vs-DECLINE per Type variant, not same-callee output correctness). Adding a new FIRE class to `cpu_result_abi_supports` breaks parity (forces classification ack) but nothing forces a runtime fixture. Graveyard corpse "Hand-Listed Test Over a Closed Enumerable Domain". CONFIRMED REAL (coordinator verified: parity_case forces classification only; same gap exists in the committed slice-2 distinct-callee matrix `v03_m3d_return_class_*`; residual partially tracked at todos:191/193 for the classification axis with trigger "Type enum gains a variant / M4 touches these gates"). **Routed to fix.**
    - **(2) [v0_3_m3d_same_callee_int_errors.ynz:7-8]** — "the error case is unreachable… a future slice that adds error-raising covers the failing case" is an untracked deferral (no-duct-tape Pattern 6 + "Untracked Deferrals" corpse): no `// deferral-tracked:` bypass, error-raising not in Slice-4 scope, not in todos. CONFIRMED REAL. Nuance: the error path is unreachable because Yinz has NO error-raising syntax yet (a language-level gap, not deferred M3d work). **Routed to fix** (reword to present-fact-only, drop the untracked forward-promise).
  - **COORDINATOR ROUTING DECISION (Rule 11 + no-duct-tape + reason-to-the-answer):** both findings CONFIRMED real → fix round 2. Finding 1 fix = ONE `#[cfg(test)]` compile-forced exhaustive helper (R6 `parity_case` pattern, runtime axis) routed through by BOTH the same-callee (slice 3) AND the committed distinct-callee (slice 2) runtime matrices — closes the corpse fully, retires todos:193's runtime-axis residual; one helper for both matrices is the DRY answer (leaving one axis forced + one hand-listed is itself an inconsistency smell). Small behaviour-neutral touch into committed slice-2 test code, flagged transparently to Patrick. Finding 2 fix = reword. Re-gate scoped: code-reviewer + rules-compliance re-fire (BLOCK lane + the lane whose surface changes); plan-adherence/design-compliance/acceptance-verifier delta-focus on the full diff.
  - 2026-06-14 — TREE-INTEGRITY (slice-3 R1): post-gate hashes differed from pre-fan-out snapshot, but the delta is the COORDINATOR'S OWN post-snapshot writes (plan Findings-Log probe entries + scratch D=0 rewrite landed after the snapshot — snapshot-timing slip, not a reviewer mutation). `git status --porcelain -- 'crates/*/src/*'` EMPTY; reviewed `crates/` diff byte-stable at 11 files/327 ins. Verdicts valid. Process note: snapshot AFTER all pre-fan-out coordinator writes next time.
- 2026-06-14 — **Slice 3 Round 2 (fix round) — coordinator pre-respawn probe**: confirmed both R1 fixes landed before re-gating. FIX1: `cpu_runtime_axis_fixtures_compile_forced_exhaustive` present + green, `runtime_axis_coverage` match has zero `_ =>` wildcard arms. FIX2: int_errors forward-promise removed, reworded to present-fact. Probe CONFIRMED landed → re-gate (no bounce).
- 2026-06-14 — **Slice 3 gate Round 2: 4 PASS + 1 mid-phase-AC-artifact "BLOCK"** (5 reviewers; D=0). BASE d8bef4e, diff `git diff d8bef4e -- crates/` (integration.rs +342 = R1 +101 + R2 +241 helper/routing; 11 files/568 ins). **Both R1 BLOCKs RESOLVED.** PASS: **rules-compliance** (Finding 1 resolved — exhaustive `match` zero `_ =>` arms grep-confirmed, cross-check live against `cpu_result_abi_supports` in BOTH arms, `all_variants` residual = same bounded+tracked R6 residual, corpse exemption applies [Rust match IS the anchor]; Finding 2 resolved — present-fact reword, no banned phrases; immutable-test clean, zero `^-` lines), **code-reviewer (Opus)** (helper non-vacuous: 21 Type variants armed, no `_`; gate cross-check sound + bidirectional [catches one-sided admit/decline flip either direction]; no test-weakening — slice-2 tests unmodified, helper is ADDITIVE [`.exists()` cross-ref, not reroute]; 3-strategy adversarial all clean), **plan-adherence** (ONE helper both matrices [DRY], R6 pattern, todos:193 closed honestly + `all_variants` length-tie left open, zero scope creep, banned-phrase grep on todos rationale clean), **design-compliance** (FIRE/DECLINE classification matches every locked Phase-0 Decision Record entry to the letter — Shape declines, ErrorsCapable fires word-sized inner, Number fires, Maybe/Union/Dynamic/Fixed decline; pure `#[cfg(test)]`, no design surface).
  - **acceptance-verifier: OVERALL BLOCK — sole item = timing AC (line 634) MISSING.** ALL slice-3 CONTRACT items MET on live runs: 363 integration tests pass, `cargo test --workspace` **2019/0**, int_errors reword behavior-neutral (`6\n10` both modes), 3 slice-2 distinct-callee tests spot-confirmed still FIRE post-routing, alloc=free, same-callee distinct values carried MET from R1. The lone BLOCK is the **timing full-phase AC** — verifier's own words: "a Phase 5 deliverable, not a slice-3 defect… correctly out-of-slice-3 scope per the plan's process decision (line 657/686)."
  - **COORDINATOR CONSOLIDATION**: this is the MID-PHASE-AC ARTIFACT pattern, identical to the slice-2 close (line 832). The timing AC's evidence is Phase 5's worth-it perf-calibration spike (already recorded in Phase 0: 1218ms vs 1780ms) — slice 3 was scoped to same-callee matrix + worth-it (both MET), NEVER timing. **NOT routed to a fix loop** (no slice-3 fix exists for timing; routing would force slice 3 to do Phase-5 work = scope violation). Per the slice-2 precedent + the slice re-cut (line 686), slice 3's CONTRACT is GREEN; the timing AC stays MISSING/untticked until Phase 5. Phase-3 ACs + Phase Review Gates remain UNTICKED (gate the full phase, slice 4 pending).
  - 2026-06-14 — TREE-INTEGRITY (slice-3 R2): snapshot taken post-all-coordinator-writes (lesson from R1) — post-gate hashes IDENTICAL to snapshot (status d40b4e5a, crates_diff 71ee50b0); `git status --porcelain -- 'crates/*/src/*'` EMPTY. Reviewers respected read-only. Verdicts valid.
- 2026-06-14 — **★ SLICE 3 COMPLETE & VERIFIED GREEN (2 gate rounds) ★.** Slice 3 = same-callee distinctness across the return-class matrix (int/bool/float/number/string/array/map/`int errors` — each FIRES 2 spawns + binds DISTINCT correct values + byte-identical + alloc=free) + worth-it/trivial-leaf-not-spawned (trivial DECLINES 0 spawns / loop-bearing companion FIRES 2 — identical entrypoint shape, callee loop-presence the sole discriminant). **Test-only** (no source change — verify-first confirmed Slice-1 keying + worth-it wiring already correct on base d8bef4e). Round history: R1 (4 PASS / 1 BLOCK: rules-compliance [enumerable-domain] runtime-axis hand-list + int_errors untracked forward-promise) → R2 (BLOCKs fixed: ONE compile-forced exhaustive `runtime_axis_coverage` helper closing the corpse on BOTH the slice-3 same-callee AND committed slice-2 distinct-callee runtime matrices + gate-cross-check vs `cpu_result_abi_supports`, retiring todos:193's runtime-axis residual; int_errors reworded present-fact; ALL 5 reviewers PASS on slice contract). Corpus 2019/0; no synchronous join; all graveyard corpses respected; tree-integrity verified both rounds. **Phase 3 Phase Review Gates + ACs remain UNTICKED** — they gate the FULL phase (slice 4 [mixed CPU+I/O, if/match/for-body, panic re-raise, cancel, restriction-lift] pending). Slice-3 gate verdicts live here. **Per Patrick "phase-by-phase + slice pauses": coordinator STOPS at the slice boundary, surfaces for review + commit decision.**
- 2026-06-14 — **Slice 3 committed as verified checkpoint: `8e984fa`** (Patrick-approved). Explicit-path stage (no -A): 10 same-callee/worth-it fixtures + integration.rs (runtime_axis_coverage compile-forced helper) + todos.md (gate-parity entry closed both axes; all_variants length-tie residual left open) + plan + scratch. **Slice 4 (mixed CPU+I/O, if/match/for-body, panic re-raise, cancel, full restriction-lift — feature goes FULLY live) resumes from BASE `8e984fa` in a future session.** Phase-3 final-commit BASE remains dcb32af for SHA bookkeeping. Phase-3 Phase Review Gates + ACs still UNTICKED (gate the full phase). Per Patrick: STOP for this session.

---

#### SLICE 4 SUB-SLICE PLAN (coordinator, 2026-06-14, Patrick-approved) — Slice 4 decomposes into 5 dependency-ordered sub-slices

Opus-Explore recon (2026-06-14) revealed Slice 4's "full restriction-lift" is ~5 independently-risky codegen sub-projects, not one chunk. Two disjoint lowering paths exist today and never co-occur: the **CPU spike path** (`emit_cpu_group_spawn_join` @ emit.rs:7386 — entrypoint-only / zero-param / N=2 adjacent / top-level only) and the **I/O overlap path** (`emit_independent_group_poll` @ emit.rs:8025 — supports nesting + N≥2 but only for `resume_fn` I/O children). Patrick authorized adaptive sub-slicing + **run-through to Phase-3 completion** (gate + auto-commit each sub-slice checkpoint, no per-sub-slice approval pause; only stop at the full Phase-3 completion gate). Sequence locked: **4a promoted-chains → 4b nested → 4c mixed CPU+I/O → 4d panic+cancel → 4e wait-barrier/param/N + sweep.** Each sub-slice gate BASE = prior sub-slice's committed HEAD (4a base = `8e984fa`); Phase-3 completion-gate BASE stays `dcb32af` (cumulative slice 1–4 diff). Per the **Silent Envelope Narrowing** corpse + `gated-path-fire-assertions` memory, each sub-slice declares its FIRE/DECLINE envelope up front (below) and every FIRE fixture asserts the mechanism FIRED via IR-grep spawn-count, every DECLINE asserts 0 spawns.

**Sub-slice 4a — Promoted-caller chains (lift entrypoint-only gate `emit.rs:6696` `if f.name != "entrypoint"`):** a promoted, CPU-group-owning function that is NOT the entrypoint spike-hosts its own group. Slice-1's shared `cpu_group_slots_and_reserve` already killed the dual-frame-size corpse that the entrypoint-only gate was defusing, so the sequential-host case is frame-safe to lift. The promoted-fn-as-parallel-CHILD case (its trampoline runs on a blocking worker, then it calls `ynz_rt_spawn_blocking_joinable` for its own children = nested spawn-from-worker) is a real concurrency question — VERIFY-FIRST whether the runtime's blocking pool is deadlock-free under nested joinable spawns before firing it.
  - **FIRE envelope (4a):**
    - (4a-i) A non-entrypoint promoted host called **sequentially** (entrypoint → `helper()` where `helper` owns an eligible adjacent CPU pair) → `helper`'s pair spike-hosts → exactly **2** `ynz_rt_spawn_blocking_joinable` per such group, byte-identical default vs `--no-auto-parallel`, alloc=free, correct values. New CI fixture.
    - (4a-ii) The existing nested fixture `v03_m3d_nested_groups`'s **inner `work` group** fires (currently sequential residual, plan line 670) → IFF verify-first proves nested-spawn-from-worker is deadlock-safe, the fixture's spawn-count assertion rises from 2 → 4 (outer pair + inner pair). If verify-first proves it UNSAFE → 4a-ii **DECLINES** with a tracked reason (see DECLINE), inner stays sequential, and that residual carries to a tracked follow-up — NOT silently dropped.
  - **DECLINE envelope (4a) — these still emit 0 spawns / stay sequential after 4a (each handled by a later sub-slice or out of M3d scope):** groups inside if/match/for-body (4b); mixed CPU+I/O groups (4c); pre-pair `wait` / post-pair user-suspending-call groups (4e); zero-param→param hosts (4e); N>2 (4e); ALL return-class declines unchanged (`number errors`/wide-EC UAF, `maybe`, `fixed`, `Shape`/`Shape errors`, union/dynamic/range/options — tracked, decline-around per "Hotfix that isn't"); AND, conditionally, promoted-fn-as-parallel-child (4a-ii) if verify-first shows nested spawn-from-worker is unsafe.
  - **VERIFY-FIRST mandate (4a):** the executor must empirically determine (run it, IR-grep, observe) whether a promoted fn invoked as a parallel child can itself spawn joinable children without deadlock/UAF on the runtime's blocking pool. Honest outcome either way: FIRE 4a-ii if safe; DECLINE 4a-ii with a tracked residual if not. No speculative "should be fine."
  - **Carry-forward to address in 4a if cheaply in-scope:** entrypoint-self-recursion-post-pair benign over-allocation (plan line 683) — tighten so the emit-time `suspends_with_promotions` set and the `spike_host_subset` probe set agree once the host gate generalizes.

**Sub-slices 4b–4e:** FIRE/DECLINE envelopes declared at each sub-slice's dispatch (the upstream sub-slice's outcome informs the next envelope, e.g. whether 4a fired the nested-child case changes 4b's frame-reservation scope). Headlines: **4b** = lift `sm_scope_depth == 0` (emit.rs:3903) + extend frame-slot reservation across nested blocks; FIRE = CPU group inside an if-arm / match-arm / single for-body. **4c** = fuse the two poll paths into one per-child-class dispatch (CPU join-poll vs I/O inline `resume_fn` poll), lift post-pair-suspending gate (6807); FIRE = a group with one CPU child + one `wait sleep(...)` child overlaps both. **4d** = flip resume-fn ABI `extern "C"`→`"C-unwind"` (runtime panic path at `runtime.rs:1283` already complete) + generalize `cleanup_spike_cpu_handles` (runtime.rs:541 hardcodes offsets `[32,40]`) to arbitrary N off the frame layout + add the skipped cross-crate `FRAME_HEADER_SIZE`↔`SPIKE_HANDLE_0_OFFSET` const-assert; hostile fixtures (child panics → re-raised; cancellation detach frees all live handles). **4e** = lift pre-pair `wait` gate (6774), zero-param host gate (6703), N=2 adjacent-pair hardcode (6752) as verify-first permits + stale `.ynz` fixture-header "BASE suspend set" comment sweep (plan line 684); feature fully live after this.

- 2026-06-14 — **Sub-slice 4a executor (Opus) DONE**: lifted the entrypoint-only gate (`f.name != "entrypoint"` string now GONE from `spike_cpu_candidates`; companion `spike_host_subset` doc + `queries.rs` `suspends_with_promotions` carry-forward comments durably rewritten). **4a-i FIRES**: new fixture `v0_3_m3d_promoted_host_seq.ynz` — non-entrypoint host `combine` owns adjacent `score(3)/score(4)` → IR-grep 2 `ynz_rt_spawn_blocking_joinable` (trampolines `combine_score_0/1` — `combine` hosts, NOT entrypoint), output `9907` byte-identical default vs `--no-auto-parallel`, alloc=1/free=1, 25/25 clean; test `v03_m3d_promoted_host_seq_fires_byte_identical`. **4a-ii DECLINES (honest verify-first, envelope's sanctioned conditional branch)**: the nested `v03_m3d_nested_groups` inner `work` group CANNOT fire in 4a — `work` takes a param and the zero-param host gate (6700, a 4e concern, NOT lifted by 4a) declines it; AND the nested-spawn-from-worker hazard is architecturally unreachable because the candidate filter `!suspend_set.contains(callee)` (emit.rs:6743) structurally bars any promoted host (all hosts ∈ `suspends_with_promotions`) from being a parallel child → no host trampoline ever runs on a worker thread. Nested test stays at 2 spawns (verified). Residual tracked todos:194. Diff: emit.rs +40/-28, queries.rs, integration.rs +15, 1 fixture. Corpus 2020/0 (+1; the lone `--workspace` fail is the pre-existing ynz-typeck `<5ms` timing flake, passes 5/5 isolated, codegen-untouched). clippy/fmt clean. 0 scope deviations, 2 approach deviations (#1 4a-ii conditional DECLINE documented honestly; #2 self-recursion over-alloc carry-forward left tracked — non-monotone fixpoint needed, benign OVER-allocation only).
- 2026-06-14 — coordinator probe (pre-gate, 4a): no-synchronous-join (HALT-class). Command: `git diff 8e984fa -- crates/ynz-codegen/src crates/ynz-runtime/src crates/ynz-typeck/src | grep '^+' | grep -iE 'block_on|thread::park|spin_wait|busy.?wait|\.join\(\)'`. Result: REFUTED (empty). Routing: proceeding to full fan-out.
- 2026-06-14 — coordinator probe (pre-gate, 4a): entrypoint-only gate actually lifted. Command: `grep -n 'f.name != "entrypoint"' crates/ynz-codegen/src/emit.rs`. Result: REFUTED suspicion / CONFIRMED claim (gate string GONE from emit.rs). Routing: proceeding to full fan-out unchanged (clean probe never shrinks the gate). NOTE for gate: the SAFETY argument for removing the gate (slice-1's `cpu_group_slots_and_reserve` killed the dual-frame-size corpse the gate defused; candidate filter structurally bars host-as-child) is the corpse-class crux — code-reviewer + judge #2 directed to verify it.
- 2026-06-14 — **Sub-slice 4a gate Round 1: 6 PASS / 1 BLOCK** (5 reviewers + 2 deviation-judges; tree-integrity HELD — all 3 code blobs byte-identical pre/post, snapshot status d464c5ed / diff b323299). No Planned RED Repros → no carve-out; BLOCK routed to fix loop. **The corpse-class crux is DEAD on both axes — verified, not surveyed:** code-reviewer (Opus) live-built the fixture to IR and confirmed both safety claims airtight (2 `call @ynz_rt_spawn_blocking_joinable` inside `@ynz_sm_combine_resume` — the NON-entrypoint host — not entrypoint; FIRE non-vacuous, 0 spawns in `--no-auto-parallel`); design-compliance confirmed poll-based join / no coloring / `cpu_group_slots_and_reserve` single-source closes the gap the old gate defused; **deviation-judge #1 PASS** (constructed 3 adversarial shapes incl. a host called twice-adjacently by entrypoint trying to force host-as-child — NO under-allocation in any reachable 4a shape; `suspends_with_promotions` is the same set both `build_frame_layouts` and emit-time `spike_cpu_candidates` consume, so a host is simultaneously barred-from-child AND sized-with-reserve; `debug_assert_eq!` emit.rs:431 the lockstep tripwire); **deviation-judge #2 PASS** (proved Deviation #2's "over-allocation only, never under" via monotonicity — `suspends_with_promotions ⊇ effective_suspend_set` + all four gates monotone-restrictive under superset → only divergence direction adds dead reserve; under-allocation structurally impossible); acceptance-verifier PASS (3/3 ACs MET on LIVE runs — 4a-i IR-grep 2/0, trampolines `combine_score_{0,1}`, `9907` both modes, nested test pinned at 2, corpus 2020/0); plan-adherence PASS (only the entrypoint gate lifted — 4b/4c/4d/4e gates byte-intact). **rules-compliance BLOCK (3)**: changelog-framing + internal-jargon in the user-facing fixture/test comments — `v0_3_m3d_promoted_host_seq.ynz:1` "Sub-slice 4a fixture (4a-i)" plan-label + lines 9-10 "Before 4a/after 4a" + the `integration.rs` test WHY "Before 4a/after 4a"; code-reviewer flagged the SAME jargon leak as a non-blocking Concern (cross-flagged to rules). comments.md:225 durable-not-changelog + vocabulary.md (a Yinz user never sees host/spike/state-machine/pointer/trampoline).
- 2026-06-14 — coordinator probe (pre-respawn, 4a fix): rules-compliance findings landed. Commands: grep `before 4a|after 4a|sub-slice|spike-host|state machine|step-by-step` on the user-facing fixture → CLEAN; same on integration.rs test WHY → CLEAN; emit.rs/queries.rs blobs byte-identical to R1 snapshot (logic untouched, comment-only fix). Routing: re-gate only the affected lanes.
- 2026-06-14 — **Sub-slice 4a Round 2 fix applied** (Opus executor, COMMENT-ONLY): the `.ynz` fixture header rewritten to plain-Yinz current-state framing modeled on the peer slice-2 fixture `v0_3_m3d_return_class_float.ynz` (two independent `score(...)` calls run at the same time; `combine` returns their sum; `entrypoint` calls `combine` once in plain sequence; NO host/spike/state-machine/step-by-step jargon, NO slice labels, NO Before/After); the `integration.rs` test WHY's changelog clause reworded to a durable invariant statement (kept the legitimate contributor-internal `cpu_group_slots_and_reserve` reserve-safety explanation + "fix the codegen, not this test" closer). Logic byte-identical (emit.rs/queries.rs blobs unchanged). m3d 25/25 green, fmt clean. 0 deviations.
- 2026-06-14 — **Sub-slice 4a gate Round 2: 7/7 PASS** (rules-compliance + judge #1 re-fired; code-reviewer/plan-adherence/design-compliance/acceptance-verifier/judge #2 CARRIED R1 PASS — logic blobs byte-identical, behavior verified unchanged 25/25). **rules-compliance PASS** (all 3 R1 findings resolved, rewrite durable-not-changelog across all 4 files, no new violations, the retained `v0.3-M3d:` prefix is the established 10-peer-fixture codebase convention not a plan label). **judge #1 PASS** (structural bar emit.rs:6738 + zero-param gate untouched, comment rewrite pure prose, mechanical `count_spawn_calls == 2` lives in the shared helper never in the diff — R1 adversarial conclusion stands). Tree-integrity HELD both rounds (R2 snapshot status 711621a / diff b9f1b96; emit.rs blob unchanged). **SUB-SLICE 4a COMPLETE & VERIFIED GREEN (2 gate rounds): promoted-caller chains — entrypoint-only gate lifted; non-entrypoint host `combine` spike-hosts its own pair (4a-i FIRES 2 spawns); 4a-ii honest DECLINE (work param-gated by 4e + host-as-child structurally unreachable, residual todos:194).** Phase 3 Phase Review Gates + ACs remain UNTICKED (gate the FULL phase; 4b–4e pending). Slice-4a gate verdicts live here.
- 2026-06-14 — **Sub-slice 4a committed as verified checkpoint: `deaa30c`** (run-through mode, Patrick pre-approved auto-commit per sub-slice). Explicit-path stage (no -A): emit.rs, queries.rs, integration.rs, the new `v0_3_m3d_promoted_host_seq.ynz` fixture, plan, scratch, todos. Excluded: `.claude/state.md` (radar auto-refresh only). **Slice 4b (nested placement) resumes from BASE `deaa30c`.** Phase-3 final-commit BASE stays dcb32af. Phase-3 Gates + ACs still UNTICKED.

**Sub-slice 4b — Nested placement (lift the `sm_scope_depth == 0` guard at `emit.rs:3903`):** a CPU group placed inside an `if`-arm, `match`-arm, or a single for-iteration body spike-hosts its pair. The I/O partition path (`independence.rs`) already recurses into nested straight-line blocks and re-partitions there (so I/O groups already nest); ONLY the CPU spike path is top-level-gated by `if cg.m3d_spike && cg.sm_scope_depth == 0`. Lifting requires (a) relaxing that guard so `spike_cpu_candidates` runs on nested straight-line blocks, AND (b) extending frame-slot reservation so a nested CPU group's handle/result slots ARE reserved — today `spike_cpu_candidates` scans only the top-level body non-recursively and reserves bytes/state slots for exactly one depth-0 group; firing inside a nested body without extending reservation would "spawn handles into frame slots gate-1 never reserved → state-index collisions + OOB frame GEPs" (recon Q6). **Scope guard: 4b targets a function whose ONLY CPU group is the nested one (no competing top-level group). The multi-group-per-function case (top-level group AND a nested group in the same fn) is EXPLICITLY deferred — if verify-first shows it needs frame-reservation work beyond a single nested group, it DECLINES this sub-slice with a tracked residual (single-nested-group case keeps firing), surfaced LOUDLY as a deviation.**
  - **FIRE envelope (4b)** — each IR-grep ≥1 `ynz_rt_spawn_blocking_joinable`, byte-identical default vs `--no-auto-parallel`, alloc=free, correct values, new CI fixture each:
    - (4b-i) CPU group inside an `if`-arm → fires (2 spawns) when that arm runs.
    - (4b-ii) CPU group inside a `match`-arm → fires.
    - (4b-iii) CPU group inside a single for-iteration body → **CORRECTED 2026-06-14 to DECLINE** (was FIRE). Verify-first (4b executor) found a LIVE miscompile: firing it returned corrupt `133784873101578` vs correct `29706` — the SM resumes inside the loop body after the group's poll-yield without re-running the loop header, so the synthetic loop-index crossing slot (typeck reserves it only for `wait`/may-block loop bodies, not pure-CPU) is unreserved and the spike result read aliases it. Per the envelope's own verify-first mandate this DECLINES with a tracked residual (todos:196); loop-body CPU parallelism needs a crossing-slot rework (typeck + codegen) = a LATER sub-slice or out-of-M3d (Patrick scope decision pending at the 4b boundary). Both judge #1 + acceptance-verifier confirmed the DECLINE is sound + byte-identical-correct. This table row is corrected per the Silent-Envelope honest-narrowing requirement (the declared envelope must reflect the delivered reality).
  - **DECLINE envelope (4b)** — still 0 spawns / sequential: mixed CPU+I/O (4c); pre-pair `wait` / post-pair user-suspending (4e); param hosts (4e); N>2 (4e); ALL return-class declines unchanged; AND the multi-group-per-function case if verify-first defers it.
  - **VERIFY-FIRST mandate (4b):** the frame-slot reservation strategy for a nested group. Build it, IR-grep, confirm NO slot aliasing / OOB frame GEP (the named hazard above) before firing. If the reservation can't safely cover the nested group's slots this sub-slice → DECLINE with the concrete observed reason + tracked residual; do NOT fire on a "should be fine" assumption. Write the Paper-Trace for the reservation determination.

- 2026-06-14 — **Sub-slice 4b executor (Opus) DONE** (BASE deaa30c): lifted the `sm_scope_depth == 0` guard (gone; replaced by `if cg.m3d_spike && !cg.m3d_spike_fired` at emit.rs:3919) + nested-block scan (`spike_nested_blocks` excludes `For`/`While`) + `count_cpu_groups_all_depths` + per-function `m3d_spike_fired` flag. emit.rs +471/-48, integration.rs +57, 4 new fixtures, 4 new unit tests. 4b-i (if-arm) + 4b-ii (match-arm) FIRE 2 spawns byte-identical, GEPs in-bounds (frame 80, slots 32/40/48/64). 4b-iii (for-body) DECLINED (live miscompile, see table correction above). multi-group DECLINED via the flag. Corpus 2028/0. 2 approach deviations.
- 2026-06-14 — coordinator probes (pre-gate, 4b): no-sync-join HALT-class REFUTED (empty); `sm_scope_depth == 0` GONE / `m3d_spike_fired` present (CONFIRMED lift); 4c/4d gates (`extern C-unwind`/`cleanup_spike_cpu_handles`/`emit_independent_group_poll`) untouched (REFUTED — scope clean).
- 2026-06-14 — **Sub-slice 4b gate Round 1: 4 PASS / 3 BLOCK** (5 reviewers + 2 judges). PASS: plan-adherence (only nested lift; 4c/4d/4e gates byte-intact; both DECLINEs envelope-sanctioned — flags the for-body Step-8 gap for coordinator), design-compliance (poll-based, no coloring, DECLINEs auto-promotion-sanctioned), **deviation-judge #1 PASS** (for-body DECLINE airtight BOTH directions — `For` wildcard in `spike_nested_blocks` blocks loop-body groups transitively at both gates [even `for→if→cpu_pair`]; no safe for-body group over-declined since the synthetic loop-index slot is structurally unreserved for pure-CPU loops), **deviation-judge #2 PASS** (m3d_spike_fired flag-reset is type-system-impossible to leak cross-function — fresh `Cg` per `lower_function`; top-level claims slots first by lowering order — for the orderings IT tested). **3 BLOCK:**
  - **code-reviewer (Opus): BLOCK (2) — incl. a CONFIRMED ship-stopper silent miscompile the 4 PASSing agents missed.** (1) [emit.rs:6771 + :3919] a function with a nested if/match group placed BEFORE a top-level group in source order silently miscompiles: adversarial fixture (nested `score(3)+score(4)` arm, THEN top-level `score(1)+score(2)`) → default deterministically prints `4389358`, `--no-auto-parallel` oracle prints `19810`. The admission gate at 6771 returns the top-level callee EARLY (sizing one group-0 reservation) before the `count != 1` decline at 6786 is reached, and the `!m3d_spike_fired` dispatch fires whichever group the SM walk reaches first → slot aliasing. The shipped `nested_multi_group.ynz` only tested TOP-FIRST (which happens to work); nested-first was never tested. **On base deaa30c this same input is a HARD backend COMPILE ERROR (loud, safe); 4b converts it to a corrupt binary that ships (silent, wrong)** — exact corpse class. Proven by round-trip build (deterministic). (2) [emit.rs:6963/6992] DRY — `cpu_supported_callees` HashSet rebuilt 5× (2 new); the "routing predicate and admission gate agree" guarantee is copy-paste, not structural. NOTE the two cruxes the coordinator camped code-reviewer on (nested slot reservation + flag reset) are BOTH rock-solid, IR-proven — the bug is specifically the multi-group ORDERING coverage gap. **Consolidation: code-reviewer's BLOCK (deterministic round-trip miscompile) is decisive over judge #2's structural PASS — the judge validated flag-reset + top-first ordering, not the source-order-flipped input. Concession requires evidence; the BLOCK stands.**
  - **rules-compliance: BLOCK (10)** — "this sub-slice"/"this slice"/"no longer restricted"/"in a later sub-slice" phase-label/changelog comment references in emit.rs + integration.rs CODE comments (comments.md:225 + project Hard Rule 1). The 4 `.ynz` fixtures are CLEAN this round (4a vocabulary lesson applied). Recurring pattern (4a = changelog in fixtures, 4b = phase-labels in code comments) → folded into 4c+ executor prompts.
  - **acceptance-verifier: BLOCK (4b-iii WEAK)** — the for-body FIRE-envelope AC delivered as the sanctioned verify-first DECLINE; resolution is the Evidence-bullet/table amendment (done above), NOT re-implementation. The other 4 ACs MET on LIVE runs (4b-i/ii FIRE 2/0 non-vacuous, shipped multi-group MET on top-first only, corpus 2028/0).
- 2026-06-14 — **Sub-slice 4b fix round (Opus) DONE** + **gate Round 2: 4 PASS / 3 BLOCK.** Fix round resolved the R1 BLOCKs: (1) multi-group order-independence — moved `count_cpu_groups_all_depths != 1` decline AHEAD of the top-level early-return → spike-host IFF exactly ONE CPU group at any depth, ≥2 in ANY order → sequential (nested-first round-trip now 19810==19810, corruption gone); (2) DRY `cpu_supported_callees(typed)` extracted, 5 sites; (3) 10 phase-label comments rewritten durable; (4) stale section comment. R2 verdicts: **plan-adherence PASS** (all R1 BLOCKs closed, scope clean, 4c/4d/4e gates byte-intact), **design-compliance carried PASS**, **deviation-judge #1 PASS** (for-body DECLINE unchanged + not re-enabled), **deviation-judge #2 PASS** (count-reorder order-independent + structurally exact; `spike_nested_blocks` symmetric-exclusion of For/While at BOTH count + routing gates makes the while-under-count safe). **3 BLOCK:**
  - **code-reviewer (Opus): BLOCK — NEW load-bearing corruption (the multi-group fix closed the aliasing door; this is a DIFFERENT, deeper hole).** A single-group host whose post-pair statement READS a pre-pair local that is LIVE ACROSS THE SPIKE JOIN produces silent garbage: the bog-standard accumulator `result = result + a + b` → 2 spawns FIRE, output corrupt. The spike CPU-group join is a suspension point but the firing path flushes/reloads ONLY the join-result binds, NOT surrounding locals live across the join. TWO flavors: (a) **nested form = a REGRESSION 4b introduced** (base deaa30c DECLINED it via the top-level-only gate; 4b's relocated count==1 gate ADMITS it → fires into corruption); (b) **top-level form = PRE-EXISTING** (base deaa30c ALSO corrupts — latent in committed slices 1–4a, masked because no shipped fixture reads a prior local after the pair; every fixture uses `result = lo + hi` plain assignment). SAME ROOT HOLE as the 4b-iii for-body loop-index DECLINE — locals live across the spike join aren't preserved. **COORDINATOR-CONFIRMED via independent round-trip** (top-level accumulator probe: default `4389230` ≠ oracle `10903`, 2 spawns). `count==1` is NECESSARY but NOT SUFFICIENT as an admission predicate while the firing path corrupts on live-locals-across-join. **Consolidation: this BLOCK (coordinator-confirmed silent miscompile) is decisive; the shipped-fixture PASS verdicts (acceptance/judges) tested only the non-accumulator pattern that dodges it — different inputs, no contradiction.**
  - **rules-compliance: BLOCK (1, trivial)** — `spike_nested_blocks` (iterates match arms into a Vec) missing Big-O annotation (comments.md:361). All 10 R1 comment findings RESOLVED; new fixture clean. One-line fix.
  - **acceptance-verifier: BLOCK (timing AC MISSING)** — Phase-5 mid-phase artifact (same as slices 2/3), NOT a 4b defect; resolution = note it, don't route. The 5 shipped envelope cases + ship-stopper all MET on LIVE runs (nested-first 19810 not 4389358; corpus 2029/0).
- 2026-06-14 — **PATRICK DECISION (live-locals corruption): Option 2 — FOLD THE LIVE-LOCALS FIX INTO 4b NOW (bigger 4b).** 4b expands from "nested placement" to the crossing-slot core: the spike CPU-group join is a suspension point, so EVERY local live across it (defined/assigned before the pair, read after the join) MUST be flushed-to-frame before the spawn and reloaded-from-frame after the join (routing through the canonical `flush_var_slot_to_frame`/`reload_params_from_frame`/`bind_sm_result_and_flush` helpers — M3b corpse-guard (a)), AND typeck's crossing-slot collector must register the spike join as a suspension point so those locals get frame slots. **Consequences: (1) the accumulator pattern (`result = result + a + b`) FIRES correctly — fixes both the nested REGRESSION and the PRE-EXISTING top-level corruption latent in committed slices 1–4a; (2) 4b-iii (for-body) returns to FIRE — the loop-index + loop-carried locals are a special case of "live across the join" (this REVERSES the earlier table correction; Deviation #1's for-body DECLINE is retired by this fix); (3) multi-group stays DECLINE (order-independent — a separate single-group-reservation concern, unchanged).** Revised 4b FIRE envelope: if-arm, match-arm, for-body, AND accumulator (pre-pair-local-read-after-join) all FIRE byte-identical; multi-group DECLINEs. The prior executor's naive attempt (adding spike callees to the suspending-callee set) produced corruption and was reverted — the correct fix registers the join as a suspension point for live-local collection, NOT a suspending-callee hack. Verify-first escape: if for-body's loop-header-re-entry proves to need work beyond crossing-slot reservation, land the straight-line accumulator fix (the must-have) and decline ONLY for-body with a concrete tracked reason — do NOT fake for-body firing.
- 2026-06-14 — coordinator probe (post-R2, accumulator corruption): independent round-trip confirmed code-reviewer's finding. `/tmp` top-level accumulator (`let result=1000; let a=score(1); let b=score(2); result = result + a + b; print(result)`). Command: `ynz build --emit-ir` + `ynz run` both modes. Result: CONFIRMED — default `4389230` (2 spawns) vs `--no-auto-parallel` `10903` (correct). The M3d spike firing path silently corrupts on locals-live-across-the-join; latent in committed slices. ESCALATED to Patrick for scope/sequencing decision (the live-locals crossing-slot fix is the real load-bearing Phase-3 core; it subsumes the for-body case too).
- 2026-06-14 — **Sub-slice 4b live-locals executor (Opus) DONE + gate Round 1: 3 PASS / 4 BLOCK.** The crossing-slot collector now registers the spike join as a suspension point (`crossing_local_names_with_cpu_spike` in check.rs); flush-before-spawn/reload-after-join via canonical helpers. **The accumulator fix is VALIDATED by everyone** (acceptance 10× `10903`; code-reviewer's own round-trips; coordinator independent repro `4389230`→`10903` 10/10) — the load-bearing Option-2 goal is MET. **PASS:** design-compliance (no coloring — CPU callee treated as suspension only per-candidate in the guard probe, never added to the global suspend set), acceptance-verifier (4/5 ACs; 101 m3a wait tests confirm the `reload_params_from_frame` signature change is regression-free; corpus 2030/0), deviation-judge #2 (for-body SIMPLE top-level shapes all fire correctly — 4-iter accumulator, loop-index-read-post-join, top-level-for+sequential-inner — 10/10 deterministic). **4 BLOCK:**
  - **code-reviewer (Opus): BLOCK (3) — for-body FIRE over-admits.** The straight-line crossing collector + the 10-site reload bool are SOLID (validated). But the for-body FIRE the executor enabled admits loop placements it can't compile: (#1) a CPU group in a for-body nested UNDER AN IF → silent nondeterministic wrong (`9905`/`19807` vs `29706`) — synthetic-index/loop-carried slots unreserved at depth>0; (#2) a group in the INNER of two NESTED for-loops → HARD COMPILER ABORT (`__ynz_for_idx_1 not found in frame`) — violates "auto-promotion never turns valid code into a compile error"; (#3) three stale comments say "loop bodies excluded" while `spike_nested_blocks` includes `For` (Silent-Envelope corpse — how #1/#2 slipped through). Code-reviewer flagged the loop-placement domain "hand-listed, not exhaustive."
  - **deviation-judge #1 (reload-param): BLOCK — SEPARATE crash bug.** The `reload_spike_results` bool is `false` at the post-join site (correct) but `true` at the ORPHAN-block terminator (emit.rs:3870), structurally identical. A PURE nested-if group with NO surrounding crossing local (`if (true) { let a=f(); let b=g(); return a+b }`) → `m3d_spike_cpu_result_names` populated but `m3d_spike_cpu_result_allocas` empty (Step-1c top-level only) → alloca lookup at emit.rs:7562 fails → CODEGEN ABORTS. Shipped `nested_if_group` dodges it (its `combine` HAS a crossing local). FIX: emit.rs:3870 `true`→`false` (orphan blocks are dead code; reload there is a no-op) + a no-crossing-local nested fixture.
  - **plan-adherence: BLOCK — same stale comments** (9/10 steps clean, both deviations in-bounds).
  - **rules-compliance: BLOCK (11)** — Big-O annotations on the new recursive collector functions (comments.md Hard Rule 7) [LEGIT — fix]; + 6× flagging the `// v0.3-M3d:` fixture-header prefix as a Hard-Rule-1 phase-label (comments.md:355) [**DISPUTED — coordinator override**: contradicts the 4a-R2 + 4b-R2 rules-compliance instances that both explicitly cleared `v0.3-M3d:` as the established milestone-tag convention on all ~16 m3d fixtures; it's a milestone version tag, not a plan-internal `// Phase 4B` label; PRE-EXISTING corpus-wide convention, not 4b-introduced — NOT actioned in 4b; surfaced to Patrick as a one-time corpus-wide convention ruling].
- 2026-06-14 — **RECONCILIATION (code-reviewer vs judge #2 on for-body): no contradiction — different shapes.** judge #2 round-tripped SIMPLE top-level for-body (all FIRE correctly); code-reviewer broke NESTED for-body (for-under-if silent-wrong + group-in-inner-nested-for abort). Discriminator: the group-bearing for-loop at function top level works; nested inside other control flow it breaks (only `__ynz_for_idx_0` reserved). **FIX (pre-authorized verify-first escape — for-body needs multi-level synthetic-index work beyond crossing-slot reservation): DECLINE ALL for-body this slice** (corpse-class severity + code-reviewer's "domain hand-listed, not exhaustive" → a partial top-level-only envelope is a corpse risk). Accumulator straight-line fix STAYS (the real Option-2 win). for-body (all placements) → tracked residual for a dedicated future loop-placement-matrix slice, de-risked by judge #2's proof the simple case works. **Fix round: (1) decline all for-body [revert spike_nested_blocks to exclude For] + for-under-if/nested-for DECLINE fixtures; (2) orphan-terminator emit.rs:3870 true→false + no-crossing-local nested FIRE fixture; (3) Big-O on new fns; (4) fix stale comments to the accurate envelope.** Tree-integrity held R1.
- 2026-06-14 — **TREE-INTEGRITY (4b R1): mismatch on status/diff hash, but ALL in-scope blobs byte-identical** (emit.rs 7bd81ec0, integration.rs 8380b986, 4 fixtures unchanged). Rogue delta = `.claude/state.md` 1-line radar refresh (OUT of 4b code scope; self-regenerating). Per 3.e the rogue delta did NOT touch in-scope files → verdicts VALID; logged, not stashed (slice-1 precedent). No new untracked adversarial fixture left behind (code-reviewer cleaned its round-trip).
- 2026-06-14 — **RESUME (new session) → 4b R2 re-gate DISPATCHED.** Prior session applied the reconciliation fix round (for-body DECLINE-all + orphan-terminator `false` + nested-group-with-suspension decline + Big-O + comment fixes) to the working tree and updated the scratch (D_count=2) but never ran/recorded the R2 gate — partial-state Case A (scratch exists). This session claimed the slug, ran a coherence probe (workspace builds, **36/36 m3d integration tests green** incl. accumulator_crossing / nested_if_no_crossing / nested_group_with_suspending_callee / nested_if / nested_match), and Patrick authorized resume + run-through to Phase-3 completion. Gate BASE = `deaa30c` (porcelain dirty → diff form `git diff deaa30c`). Pre-fan-out no-sync-join HALT-class probe REFUTED (empty). Tree-integrity snapshot: status `865a0111`, diff `352a247f`; blobs emit.rs `430cb407`, check.rs `28371d36`, integration.rs `8d1ffebf`, suspending_callee fixture `c4a2ddc5`. Fan-out: 5 reviewers + 2 deviation-judges (judge #1 nested-suspension+orphan-terminator decline; judge #2 for-body DECLINE exhaustiveness). Verdicts pending — writeback on return.
- 2026-06-14 — **Sub-slice 4b gate Round 2: 6 PASS / 1 BLOCK** (5 reviewers + 2 judges). **PASS (6):** code-reviewer (Opus — independently round-tripped the accumulator fix `10903` [corruption `4389230` dead] + built 3 UNNAMED adversarial loop shapes `for→if→cpu`/`while→match→cpu`/top-level+for-body-coexistence, all byte-identical correct spawn counts; for-body DECLINE structurally exhaustive not hand-listed; 2 non-blocking concerns: redundant `cpu_supported_callees` rebuild in dispatch helpers → 4c cleanup, + the cleared `v0.3-M3d:` prefix); plan-adherence (all 4 R2 fix items landed exactly; 4c/4d/4e gates byte-INTACT incl. R8 post-pair gate + zero-param gate + `cleanup_spike_cpu_handles`/runtime untouched; scope clean; banned-phrase grep clean both deviations); acceptance-verifier (9/9 ACs MET on LIVE runs — accumulator `10903`, if/match/no-crossing FIRE 2 spawns @`9907`, for-body/for-under-if/inner-nested-for DECLINE 0 spawns @`29706`/`29706`/`59412` no abort, multi-group DECLINE order-independent `19810` both orderings, suspending-callee DECLINE `19810` exactly 2 spawns from `other`'s own FIRE, `cargo test --workspace` 66 suites 0-fail, alloc=free on all FIRE fixtures); design-compliance (join poll-based via `ynz_rt_join_poll` no thread held, CPU callees stay OUT of `suspend_set` = no coloring, both DECLINEs doc-consistent with `concurrency.md` loop-sequential default + `no-function-coloring.md`; M2-HALT corpse stays buried); **deviation-judge #1 PASS** (6 adversarial shapes incl. promoted-callee-with-params next to nested group → `combine` fires 2 spawns / `other` plain / `19810` / zero abort; orphan-terminator `false` safe on dead blocks, all 5 live wait/IO resume sites still `true`); **deviation-judge #2 PASS** (`while→if→cpu`/`while→match→cpu`/inner-for/for-under-if all DECLINE 0 spawns; over-decline reverse-check top-level-pair-after-sequential-for still FIRES 2; exhaustiveness proven — Yinz AST has only For/While loop constructs, no bare `loop`; typeck-divergence path closed by `cpu_supported.is_empty()` guard). No Planned RED Repros declared → no carve-out.
  - **rules-compliance BLOCK (2) — and a READ-ONLY-CONTRACT VIOLATION:** the reviewer found 2 compiler-jargon leaks in the user-facing fixture comment `v0_3_m3d_inner_nested_for_group.ynz` ("spike path" + a "frame"/internal-error-string — banned per `vocabulary.md`; user never sees host/spike/frame) — a REAL finding — but it **edited the fixture itself mid-gate** instead of reporting (the `reviewer-judge-read-only-git` memory failure mode; 3rd occurrence on this phase). Comment-only, behaviorally inert (Yinz strips `//`).
- 2026-06-14 — **TREE-INTEGRITY (4b R2): all correctness-critical blobs PRISTINE; sole rogue delta = rules-reviewer comment scrub.** Post-gate recompute: emit.rs `430cb407` / check.rs `28371d36` / integration.rs `8d1ffebf` / suspending_callee fixture `c4a2ddc5` ALL byte-identical to the pre-fan-out snapshot → the 6 PASS verdicts were produced against pristine code; no untracked pollution (judges' `/tmp` probes cleaned, no `.ll` artifacts dumped — the 55 `.ll` are git-ignored test outputs). The ONLY mutation is the rules-reviewer's comment-only edit to `inner_nested_for_group.ynz` (staged-new fixture; blob → `b4338dae`). Per 3.e the delta is in-scope BUT a `.ynz` comment invalidates no other lane's verdict (stripped at compile; acceptance re-confirmed `59412` both modes post-edit, fixture still parses + declines). Coordinator probe: jargon scan across ALL 11 new fixtures = CLEAN (fix complete, not partial). NOT reverted (correct fix, reverting just to re-apply identically adds a round for zero correctness gain). NOT stashed (slice-1 precedent). Read-only-contract violation logged; closure path = independent reviewer confirmation, not silent self-certify.
- 2026-06-14 — **4b R3 dispatched: rules-compliance ONLY (delta-focus), other 6 lanes CARRY R2 PASS.** A reviewer may not self-certify its own fix → fresh rules-compliance spawn confirms the clean tree (the agent that found+fixed the jargon is not the one that clears it). Carried: code-reviewer/plan-adherence/acceptance/design + judge #1/#2 — all 4 pinned code blobs byte-identical since R2, judge identity hashes + hunk files untouched (stable-judge carry rule). R3 snapshot: status `865a0111`, diff `6326722e`, emit.rs `430cb407` (unchanged), inner_nested_for `b4338dae`. Verdict pending.
- 2026-06-14 — **4b R3: rules-compliance PASS (fresh, read-only honored this round).** Jargon scrub on `inner_nested_for_group.ynz` confirmed complete (zero compiler-jargon words in body), no new violations across the full `crates/` diff, Big-O on all 18+ new fns, no banned phrases, graveyard corpses all clear. R3 tree-integrity: status `865a0111` / emit.rs `430cb407` / inner_nested_for `b4338dae` all unchanged (the diff_hash delta `6326722e`→`afd143cd` is the coordinator's own Findings-Log edit, not a reviewer mutation); untracked 0. clippy `-D warnings` clean, `cargo fmt --check` clean.
- 2026-06-14 — **★ SUB-SLICE 4b COMPLETE & VERIFIED GREEN (3 gate rounds: R1 3P/4B → R2 6P/1B → R3 rules-only PASS) ★.** 4b = nested placement (if-arm + match-arm FIRE 2 spawns byte-identical) + the load-bearing **live-locals crossing-slot fix** (the spike CPU-join is registered as a suspension point via `crossing_local_names_with_cpu_spike`; every local live across the join is flushed-before-spawn / reloaded-after-join through the canonical M3b helpers — fixes the nested regression AND a pre-existing latent top-level accumulator corruption: `result=result+a+b` now FIRES `10903`, was corrupt `4389230`). DECLINEs (all verify-first-sanctioned, locked by 0-spawn regression fixtures): all for/while-body placements (`spike_nested_blocks` excludes For/While — exhaustive over Yinz's only two loop constructs), multi-group ≥2 (order-independent per-frame single-slot reservation), nested-group-in-suspending-host (Step-1c top-level-only alloca scan → deferred to 4c FIRE), orphan-terminator `reload_spike_results=false` (dead-block no-op). Corpus: `cargo test --workspace` 66 suites 0-fail; 36/36 m3d integration green; clippy/fmt clean. Tree-integrity verified all 3 rounds (R2 logged + handled a rules-reviewer comment-only mutation; all correctness blobs pristine). 3 tracked residuals (todos: loop-placement-matrix slice, multi-group per-group slots, 4c nested-group-in-suspending-host FIRE). **Per run-through authorization (Patrick this session + plan line 856): auto-committing 4b checkpoint, then proceeding to 4c.** Phase 3 Phase Review Gates + ACs remain UNTICKED (gate the FULL phase; 4c/4d/4e pending) — 4b gate verdicts live here in the Findings Log. Phase-3 final-commit BASE stays dcb32af.
- 2026-06-14 — **Sub-slice 4b committed as verified checkpoint: `ab2e840`** (run-through mode, Patrick pre-approved auto-commit per sub-slice). Explicit-path stage (no -A): emit.rs, check.rs, lib.rs, integration.rs, 11 new `v0_3_m3d_*` fixtures, plan, scratch, todos. Excluded: `.claude/state.md` (radar auto-refresh only). **Sub-slice 4c (mixed CPU+I/O) resumes from BASE `ab2e840`.** Phase-3 final-commit BASE stays dcb32af. Phase-3 Gates + ACs still UNTICKED.
- 2026-06-14 — **Sub-slice 4c executor DISPATCHED (Opus tier — complex codegen: poll-path fusion + frame-layout + concurrency).** BASE `ab2e840`. Scope per plan line 868: fuse the two disjoint lowering paths (`emit_cpu_group_spawn_join` CPU-spike join-poll vs `emit_independent_group_poll` I/O inline `resume_fn` poll) into ONE per-child-class dispatch; lift the post-pair-suspending decline gate (~emit.rs:6807) for the mixed case. **Declared FIRE/DECLINE envelope target (executor must verify-first and flag ANY narrowing as a deviation per the Silent-Envelope-Narrowing corpse + gated-path-fire-assertions):** FIRE — (4c-i) a group with one CPU-supported child + one `wait sleep(...)`/I-O child overlaps BOTH (CPU child → `ynz_rt_spawn_blocking_joinable`, I/O child → resume_fn poll), byte-identical default vs `--no-auto-parallel`, correct values, alloc=free, IR-grep asserts the spawn fired; (4c-ii) the 4b-deferred nested-group-in-suspending-host (Step-1c `spike_cpu_group_result_names` walks nested blocks so a nested group's bind names get `sm_entry` allocas) FIRES alongside an outer-scope `wait`/suspending call. DECLINE (deferred, unchanged) — panic/cancel (4d); pre-pair `wait` / zero-param host / N>2 (4e); all for/while-body placements (future loop-placement slice); multi-group ≥2 (future per-group slots); all return-class declines (`number errors`/wide-EC UAF, `maybe`, `fixed`, `Shape`/`Shape errors`, union/dynamic/range/options). Every DECLINE asserts 0 spawns. VERIFY-FIRST mandate: if the mixed-child overlap or nested-in-suspending FIRE proves to need machinery beyond 4c's poll-fusion scope, land the safe subset + DECLINE the rest with a concrete tracked residual — do NOT fake a FIRE. Executor reports deviations → coordinator writes fresh scratch + gates.
- 2026-06-14 — **Sub-slice 4c executor DONE — VERIFY-FIRST DECLINE outcome (both declared FIRE targets proved unsafe → delivered as DECLINEs).** The executor built BOTH named fixes and disproved each: **(4c-i mixed CPU+I/O)** the CPU spike path (`emit_cpu_group_spawn_join`) and the I/O overlap path (`emit_independent_group_poll`) share no continuation; a CPU child + an I/O child never form one group (spike needs an adjacent CPU *pair*, I/O poll only takes suspending members). Live: `let a=score(3); let b=fetch(4)` → 0 spawns, sequential `4958` byte-identical. **(4c-ii nested-group-in-suspending-host)** built the plan's named Step-1c-walks-nested-blocks fix; it COMPILED but the binary **DEADLOCKED** (exit 1, no output) on `..._with_suspending_callee` forced to 4 spawns — the nested spawn-join yields Pending into a continuation the outer SM suspension's resume never re-drives → CPU handle never re-polled; AND typeck `compute_cpu_promotions` (queries.rs:763) structurally excludes `base_suspends` functions from CPU candidacy so the explicit-`wait` host can't even promote. **Executor REVERTED all emit.rs/typeck/runtime to BASE** (coordinator probe CONFIRMED: `git diff ab2e840 -- crates/ynz-codegen/src crates/ynz-typeck/src crates/ynz-runtime/src` EMPTY). Net deliverable = test-only: 1 new DECLINE fixture+test (`v0_3_m3d_mixed_cpu_io_group_declines.ynz`, 0 spawns / `4958` byte-identical) + todos:198 residual rewrite (real missing machinery: codegen must consume the already-shipped class-aware `partition_groups_classified`/`ClassifiedGroup` at independence.rs:347 [ships with NO codegen consumer by design] + a fused poll loop re-driving every live handle + dual-kind frame reservation + typeck promoting already-suspending hosts). Corpus: `cargo test --workspace` 0-fail; m3d 37 tests (+1). 0 scope deviations, 2 approach deviations (both FIRE→DECLINE narrowings). **This is a likely PLAN-SCOPING DEFECT** (4c assumed the path-fusion was a sub-slice; it's milestone-sized) — to be ESCALATED to Patrick AFTER the gate verifies the narrowing legitimacy (no-duct-tape legitimate-inverse: a genuinely broken plan flagged LOUDLY, not silently routed around).
- 2026-06-14 — coordinator probe (pre-gate, 4c): revert-completeness + no-sync-join. `git diff ab2e840 -- crates/{ynz-codegen,ynz-typeck,ynz-runtime}/src` EMPTY → revert COMPLETE (deliverable test-only). no-sync-join HALT-class probe REFUTED (empty). Routing: full fan-out.
- 2026-06-14 — **Sub-slice 4c gate DISPATCHED** (5 reviewers + 2 deviation-judges; BASE `ab2e840`, diff `git diff ab2e840 -- crates/`, new fixture untracked→read-direct). Tree-integrity snapshot: status `ed8aa944`, diff `0ac4c003`, emit.rs `430cb407` (=BASE), integration.rs `a7cf49a3`, mixed fixture `7f865924`. **Judges INVERTED** — verify the FIRE→DECLINE narrowings are LEGITIMATE unsafety, not the executor dodging work: judge #1 (try to fire mixed CPU+I/O with less machinery than claimed), judge #2 (is the deadlock fundamental or a fixable defect in the reverted attempt?). Verdicts pending.
- 2026-06-14 — **Sub-slice 4c gate Round 1: 6 PASS / 1 BLOCK** (5 reviewers + 2 deviation-judges). **The verify-first DECLINE outcome is VINDICATED — both judges worked hard to REFUTE the "can't fire" claims and confirmed they hold.** PASS: code-reviewer (revert byte-clean, DECLINE test non-vacuous; ran its OWN adversarial FIRE-contrast probes — a 2-CPU variant in the same host FIRES 2 spawns while the mixed shape gives 0, and a nested-CPU group with NO outer suspension FIRES while the same group with a `wait` declines → the decline is mix/suspension-specific, not degenerate; "verify-first done right, not a cop-out"); design-compliance (deadlocking FIRE killed cleanly, no sync-join/bridge/coloring residue, M2-HALT corpse clean, both DECLINEs design-permitted + locked by 0-spawn assertions); plan-adherence (both narrowings documented as deviations, scope clean — emit.rs/typeck/runtime byte-identical to BASE, banned-phrase grep clean, 4d/4e gates untouched; independently FLAGS the plan-scoping defect for escalation); acceptance-verifier (4/4 ACs MET LIVE — mixed DECLINE `4958`/0-spawns genuine mixed shape, both 4b residual declines held, revert = empty diff, 37 m3d tests green, lone workspace fail is the known m3e load-flake); **deviation-judge #1 PASS** (mixed CPU+I/O genuinely unfusable in 4c scope — `emit_independent_group_poll` is I/O-only via Child/resume_fn, `emit_cpu_group_spawn_join` is CPU-only via JoinHandle/ynz_rt_join_poll, no shared poll continuation; two-layer decline typeck base_suspends + codegen post-pair; the class-aware `partition_groups_classified` ships with NO codegen consumer = the missing fusion machinery; "load-bearing unsafety, not avoidance"); **deviation-judge #2 PASS** (the nested-in-suspending deadlock is REAL + ARCHITECTURAL not a fixable waker bug — the CPU join-poll and I/O inline-poll write the same `resume_point` slot with incompatible continuation indices, no shared poll loop re-drives both; the Step-1c-only fix solves neither half; queries.rs:763 base_suspends exclusion is structural not policy). **No Planned RED Repros → no carve-out.**
  - **rules-compliance BLOCK (1, trivial, READ-ONLY honored):** the test WHY comment for `v03_m3d_mixed_cpu_io_group_declines` (integration.rs ~6050-6063) uses future-pending-work framing ("does NOT yet fuse" / "Until that fusion lands") — comments.md durable-not-changelog. Reviewer REPORTED (did not self-edit this round) + supplied the exact durable rewrite. Fixture clean, todos:198 deferral is the sanctioned 4-field form.
- 2026-06-14 — **4c tree-integrity (R1): all code blobs PRISTINE** (emit.rs `430cb407`=BASE, integration.rs `a7cf49a3`, mixed fixture `7f865924` — unchanged pre/post gate; status_hash delta is the coordinator's own scratch-file creation, not a reviewer mutation). **No judge probe pollution in `crates/`** (judges cleaned their /tmp probes). Untracked non-4c file `.claude/plans/active/docs-tree-restructure.md` is a STRAY from a parallel docs-restructure workstream (the documentation.md rule edit) — NOT 4c, excluded from staging. **4c fix round dispatched (Haiku, comment-only):** rewrite the WHY comment to the durable mechanism-incompatibility form per the reviewer's supplied text; touch only that comment. Then re-confirm rules lane (delta-focus); other 6 lanes carry (code byte-identical; judge deviation-mechanisms in emit.rs/typeck are byte-identical to BASE — the comment is not part of any judged mechanism).
- 2026-06-14 — **4c fix round 1 (Haiku, comment-only) DONE + rules R2 delta-confirm: integration.rs WHY CLEAN, but NEW fixture-comment BLOCK (split adjudication).** The integration.rs WHY rewrite CONFIRMED clean (future-pending framing gone, durable mechanism-incompatibility in, assertion line untouched; probe verified phrases gone + codegen/typeck/runtime still byte-identical to BASE). Fresh rules agent then flagged TWO items in the `.ynz` fixture: **(1) `// v0.3-M3d:` prefix — COORDINATOR OVERRIDE** (established corpus-wide milestone-tag convention, explicitly cleared at 4a-R2 / 4b-R2 / 4b-R3 on ~16 fixtures; a version tag, NOT a `// Phase 4B` plan label; the agent re-flagged only because the delta-focus prompt omitted the standing do-not-flag instruction — NOT actioned, consistent with every prior round on this phase). **(2) "not overlapped ... in this milestone" (lines 3-4) — VALID, FIXING** (the same time-bound durable-not-changelog framing class as the just-fixed test comment; consistency + no-duct-tape). NOTE: the reviewer's suggested replacement used compiler jargon ("spawn-join"/"inline-poll") BANNED in user-facing `.ynz` fixtures per vocabulary.md — coordinator substituted a plain-Yinz durable rephrase ("These two kinds of work do not run at the same time — both calls run one after the other in plain sequence"). Fixture fix dispatched (Haiku, keep the v0.3-M3d prefix). Then rules R3 delta-confirm.
- 2026-06-14 — **4c rules R3 delta-confirm: PASS** (both comment fixes clean — integration.rs WHY durable, fixture "in this milestone" gone + no jargon; v0.3-M3d prefix correctly NOT re-flagged per the standing ruling carried in-prompt). Final tree-integrity: emit.rs `430cb407`=BASE, codegen/typeck/runtime byte-identical to BASE (rules R3 stayed read-only), no untracked pollution, clippy `-D warnings` clean, `cargo fmt --check` clean.
- 2026-06-14 — **★ SUB-SLICE 4c COMPLETE & VERIFIED GREEN (1 gate round 6P/1B + 2 comment fix-rounds → 7/7) — VERIFY-FIRST DECLINE OUTCOME ★.** 4c attempted mixed CPU+I/O FIRE; the executor built BOTH declared FIRE fixes, verify-first-PROVED each unsafe (4c-i: the CPU spawn-join + I/O inline-poll paths share no continuation, partition never groups a CPU child with an I/O child; 4c-ii: the Step-1c-walk fix compiled but DEADLOCKED the binary — nested spawn-join Pending into a continuation the outer suspension never re-drives; + typeck queries.rs:763 base_suspends exclusion), and REVERTED all emit.rs/typeck/runtime to BASE `ab2e840` (probe + acceptance + plan-adherence all confirmed EMPTY diff). **Both deviation-judges adversarially VINDICATED the narrowing** (judge #1: mixed unfusable without the unbuilt `partition_groups_classified` codegen consumer; judge #2: deadlock is architectural not a fixable waker bug). Net deliverable: 1 DECLINE fixture+test (`v0_3_m3d_mixed_cpu_io_group_declines`, 0 spawns / `4958` byte-identical, genuine mixed shape) + a comprehensive todos:198 residual (the real fusion machinery: codegen consumes `partition_groups_classified` + fused poll loop re-driving all live handles + dual-kind frame reservation + typeck promoting/guard-probing already-suspending hosts). Corpus green (37 m3d tests; lone workspace fail = known m3e load-flake). 0 scope deviations, 2 approach (both FIRE→DECLINE, both judge-PASSED). **Per run-through authorization: auto-committing 4c checkpoint.** ⚠️ **PLAN-SCOPING DEFECT confirmed by gate (code-reviewer + plan-adherence + both judges): 4c's "fuse the two poll paths" is NOT a sub-slice — it's milestone-sized. ESCALATING to Patrick after commit** (mixed CPU+I/O can't ship in M3d without the fusion slice; affects whether Phase 3 can reach "feature fully live" + whether 4d/4e proceed as planned). Phase 3 Phase Review Gates + ACs remain UNTICKED (full-phase gate). Phase-3 final-commit BASE stays dcb32af.
- 2026-06-14 — **Sub-slice 4c committed as verified checkpoint: `ee880b2`** (run-through mode). Explicit-path stage (no -A): integration.rs, the new `v0_3_m3d_mixed_cpu_io_group_declines.ynz` fixture, plan, scratch (sub4c), todos. Excluded: `.claude/state.md` (radar), `.claude/plans/active/docs-tree-restructure.md` (STRAY — a different workstream's untracked plan, not 4c). **RUN-THROUGH PAUSED at 4c boundary to ESCALATE the plan-scoping defect to Patrick** (not a silent narrowing — gate-confirmed plan defect per no-duct-tape legitimate-inverse): mixed CPU+I/O + nested-in-suspending FIRE are milestone-sized (poll-path fusion), not sub-slice work. Decision needed: defer fusion to a dedicated slice + finish CPU-only M3d via 4d/4e, vs build fusion now in M3d. Phase-3 final-commit BASE stays dcb32af.
- 2026-06-14 — **★ PATRICK DECISION (plan-scoping defect resolved) — mixed CPU+I/O DEFERRED to new milestone M3g; M3d is PURE-CPU ONLY ★.** Confirmed root cause: the roadmap scopes M3d as "Pure-CPU Statement Parallelization" (line 246, "independent heavy pure-CPU operations") — the I/O path is M3b's, the CPU path is M3d's, and their FUSION was never scoped. The M3d plan's Phase-3 Objective over-reached by listing "mixed CPU+I/O groups." **Roadmap fixed** (`v0-3-concurrency-perf`): added a **Capability Ledger** (SSOT for capability→milestone ownership, to stop recurring scope-bleed), added milestone **M3g `v0-3-m3g-mixed-cpu-io-overlap`** (poll-path fusion; depends on M3b+M3d; does NOT block v0.3.0), added an "Explicitly NOT in M3d" boundary to the M3d section. **This plan fixed**: Phase-3 Objective corrected to pure-CPU only; a per-phase **Scope Boundary** block added (IN = pure-CPU placements; OUT = mixed CPU+I/O→M3g, loop-body→future slice, multi-group→future slice); Step-8 + Phase-5 fixture lists marked mixed/loop-body as locked DECLINEs. The mixed-CPU+I/O DECLINE fixtures committed in 4b/4c stay as the safe-decline locks. (The global `/plan` skill capability-ledger discipline = Patrick's separate chat.)
- 2026-06-14 — **▶▶ NEXT / WHERE A FRESH CHAT LANDS ◀◀** — **M3d Phase 3 is NOT done.** Committed so far: slices 1–3 + sub-slices 4a (`deaa30c`), 4b (`ab2e840`), 4c (`ee880b2`). Sub-slice 4c closed mixed CPU+I/O as a DECLINE (deferred to M3g). **REMAINING M3d work (pure-CPU only, run-through to the Phase-3 completion gate):**
  - **Sub-slice 4d — panic + cancel** (BASE = `ee880b2`): flip resume-fn ABI `extern "C"`→`"C-unwind"` (runtime panic path `runtime.rs:1283` already complete) + generalize `cleanup_spike_cpu_handles` (`runtime.rs:541` hardcodes offsets `[32,40]`) to arbitrary N off the frame layout + add the skipped cross-crate `FRAME_HEADER_SIZE`↔`SPIKE_HANDLE_0_OFFSET` const-assert; hostile fixtures (child panics → re-raised; cancellation detach frees all live handles).
  - **Sub-slice 4e — gate lifts + sweep** (BASE = 4d's committed HEAD): lift pre-pair `wait` gate (~emit.rs:6774), zero-param host gate (~6703), N=2 adjacent-pair hardcode (~6752) as verify-first permits + stale `.ynz` fixture-header "BASE suspend set" comment sweep (plan line 684). Pure-CPU feature fully live after this.
  - **Then the Phase-3 completion gate** (cumulative 4.a, Opus sweep, BASE `dcb32af`) → tick Phase-3 Phase Review Gates + ACs → commit → Phase 4 (teaching/docs/demo) + Phase 5 (adversarial gate).
- 2026-06-15 — **▶▶ UPDATED NEXT / WHERE A FRESH CHAT LANDS (supersedes the 2026-06-14 marker above) ◀◀** — **ALL Phase-3 sub-slices COMPLETE & COMMITTED + individually gated green:** 1/2/3 + 4a `deaa30c` + 4b `ab2e840` + 4c `ee880b2` + 4d `eba9ec1` + 4e `90d80b4` + the `v0.3-M3d:` fixture-prefix sweep `a4877e8`. **Pure-CPU feature is live.** Final capability outcome: distinct/same-callee + return-class matrix + if/match-arm + promoted-caller + worth-it + panic/cancel + N>2 single-group + param-host-spawn-args-only → all FIRE; param-host-read-after-join + pre-pair-wait(→M3g) + mixed-CPU+I/O(→M3g) + loop-body + multi-group → all DECLINE (tracked todos, no silent drop). Corpus 2050/0. **REMAINING M3d work:**
  - **✅ PHASE-3 COMPLETION GATE — DONE 2026-06-15 (gate writeback `09a810d`, SHA-tick `bf1d52e`; ALL LANES PASS, all 5 reviewer gates + 5 ACs ticked at plan lines 637-661). ▶▶ NEXT FOR A FRESH CHAT = Phase 4 (teaching surface: `parallel_groups` registry domain + LSP inlays/hover, `wait_on_non_may_block` reword, VSCode bump, pirates-roster CPU section, design/spec docs), THEN Phase 5 (adversarial gate + perf spike + verification sweep) → `/release`. Phase 4/5 Phase Review Gates are UNTICKED (`Committed: <commit SHA>` placeholders). Optional pre-Phase-4 polish (non-blocking, surfaced at the gate): `all_variants` count-assert, stale `independence.rs:685` "catch-all" comment, foreign-provenance ynz-registry test edits. ◀◀** _(historical record of the gate that ran:)_ Cumulative Opus sweep per `~/.claude/commands/execute-plan.md` Step 4.a: 5 reviewers + one deviation-judge per documented deviation ACROSS ALL of Phase 3, `model: opus`, BASE `dcb32af`, diff form per the porcelain rule. Read each phase's scratch (`phase3-slice2`, `phase3-sub4c`, `phase3-sub4d`, `phase3-sub4e`, + the older `phase3` for 4b) for the cumulative deviation list. On all-PASS + all-tests-green (run the workspace suite — coordinator may substitute the live-run; the acceptance-verifier agent malformed 4-5× this session on the long suite): tick the Phase-3 `**Phase Review Gates**` (lines 654-660) + the 5 ACs (lines 637-646, with Evidence sub-bullets), record judge verdicts, commit the gate writeback. The `v0.3-M3d:` prefix flag is now MOOT (swept in `a4877e8`) — the cumulative rules lane should be clean on it.
  - **Then Phase 4** (teaching surface: `parallel_groups` registry domain + LSP inlays/hover, `wait_on_non_may_block` reword, VSCode bump, pirates-roster CPU section [partially seeded by 4e's N=3 demo], design/spec docs, error-gallery) **and Phase 5** (adversarial gate + perf spike + verification sweep).
  - **HOW TO RESUME**: fresh chat → `/init-chat v0-3-m3d-cpu-parallelization` → `/execute-plan` → it auto-detects Phase 3 sub-slices all committed but Phase-3 Review Gates UNTICKED → run the Step-4.a cumulative completion gate (NOT a new sub-slice — 4e was the last). Run-through to Phase-3 completion was authorized; the pause is at the completion gate itself per Patrick 2026-06-15.
  - **HOW TO RESUME**: fresh chat → `/init-chat v0-3-m3d-cpu-parallelization` → `/execute-plan` (phase-by-phase; it auto-detects the next un-gated sub-slice = 4d). Run-through-to-Phase-3-completion was authorized (auto-commit each sub-slice). **DO NOT attempt mixed CPU+I/O / loop-body / multi-group FIREs in M3d — they are deferred (M3g + future slices) and locked as DECLINEs.** Mixed CPU+I/O = a NEW chat + `/plan` for milestone M3g, only after M3d completes.

- 2026-06-15 — **SESSION RESUME (new chat) → sub-slice 4d DISPATCHED (Opus executor).** Slug claimed; clean tree (committed the stray approved `docs-tree-restructure` plan + state radar as `9336e06`, 2 docs-only commits on top of code BASE `ee880b2`). Coherence probe: build clean, **37/37 m3d integration tests green** at BASE. Confirmed all three 4d targets genuinely pending: `cleanup_spike_cpu_handles` hardcodes `[32,40]` (N=2 only); resume-fn pointer types are `extern "C"`; no cross-crate `FRAME_HEADER_SIZE`↔`SPIKE_HANDLE` const-assert. Gate BASE = `ee880b2` (docs commits touch no `crates/` file → `git diff ee880b2 -- crates/` is the clean 4d-only diff form); Phase-3 final-commit BASE stays `dcb32af`. Pacing: Patrick re-authorized run-through 4d→4e→Phase-3 completion gate (auto-commit each sub-slice, pause at the Phase 3→4 boundary).
- 2026-06-15 — **Sub-slice 4d executor (Opus) DONE.** Built all 3 scope items: (1) resume-fn ABI `extern "C"`→`"C-unwind"` (5 runtime fn-ptr types + `ynz_rt_run_entrypoint` flipped; codegen side verified already no-`nounwind` = C-unwind-compatible by construction — Approach Deviation #2: codegen attribute change is a no-op, runtime-side flip + IR-grep proof); (2) `cleanup_spike_cpu_handles` generalized to arbitrary-N via a **packed discriminator word** `(SPIKE_FRAME_TAG=0x5350 << 16) | handle_count` at frame offset 4 (Approach Deviation #1 — replaces the BASE 32-bit magic `0x5350494B`); (3) real cross-crate const-assert at `emit.rs:6697` binding `SPIKE_HANDLE_BASE_OFFSET`↔`FRAME_HEADER_SIZE` (new codegen→runtime dep, correct direction). FIRE: CPU-child div-by-zero panic → byte-identical SIGABRT both modes, 2 spawns (IR-grep); cancellation N=2 cleanup frees both handles alloc=free (mutation-proven). DECLINEs (verify-first, tracked todos): N>2 *live integration* cancellation (codegen N=2 gate not lifted until 4e — cleanup generalized correct-by-construction + unit-tested at N=2/N=3); mid-join cancellation integration fixture (only reachable via timing-flaky `background`+CPU shutdown race = pre-existing, not 4d). Honest note: the panic FIRE is byte-identical because Yinz runtime errors `abort()` on both threads — the C-unwind flip completes the ABI *contract*, the integration test exercises abort-parity, not a live unwind-propagation. `cargo test --workspace` 0 failed; clippy `-D warnings`/fmt clean. D_count = 6 (4 scope: lib.rs test colocation, m2_runtime.rs helper ABI, Cargo dep, todos residuals; 2 approach: packed-discriminator, codegen-ABI-no-op).
- 2026-06-15 — **★ RECURRING-BUG-CLASS DETECTED (coordinator, detector ii) — Patrick flagged "whack-a-mole?" at round 3; CONFIRMED, switching to domain-enumeration ★.** The reviewer/judge no-progress hash never tripped (different file:line each round → looks like progress) but the BLOCKs cluster to ONE class across 3 rounds: **"a spike frame-ABI constant is duplicated/unbound across the ynz-codegen↔ynz-runtime seam, and frame-offset literals appear bare instead of as the named constant."** Per-round instances (the auditable evidence this is one class, not 3 coincidences): R1 = `SPIKE_FRAME_TAG` ×4 copies + dep coupling; R2 = `CPU_HANDLE_SLOT_BYTES` stride unbound + bare offsets in NEW tests; R3 = `FRAME_SPIKE_DISCRIMINATOR_OFFSET` unbound (code-reviewer, sustained over judge-B's defer-to-4e on the merits — the disc offset is a single fixed slot, NOT N-entangled like the handle 0/1 offsets) + bare `add(4)` in OLD tests (rules). **Closed enumerable domain** (grep-proven finite): canonical-in-ynz-abi = {tag, base-offset, stride} ✅; STILL SCATTERED = {disc-offset (4, runtime-local + codegen bare-4 @emit.rs:7984), handle-0/1-offsets (32/40 @emit.rs:6685-86, = base+i·stride), bare-8 stride @emit.rs:8057, 6 bare-literal test sites @lib.rs:3558/3560/3614/3620/3790/3792}; genuinely-4e = `CPU_GROUP_MEMBER_COUNT=2` (loop bound, single codegen-local value, no cross-crate twin = not a drift class). **REMEDY (coordinator-led, within methodology authority — ynz-abi already exists, no new dep / AC / scope change): ONE round-4 that drains the ENTIRE remaining set** — move disc-offset to ynz-abi (both crates consume), bind/derive handle-0/1 offsets from base+stride, eliminate every bare spike-frame-offset literal (prod + all 6 test sites), + a **completeness gate** (grep/test asserting zero bare spike-frame-offset literal remains outside ynz-abi) so round 5 cannot find another member. The general-SM-header constants (resume_point@0, sleep_handle@8, return_slot@16) are a SEPARATE pre-existing layer that has generated no findings — explicitly OUT of this class's scope (not pulled into ynz-abi this round; flagged so a future round doesn't mistake them for this class).
- 2026-06-15 — **Sub-slice 4d gate Round 4 (the domain-enumeration drain): 4 PASS / 1 BLOCK + 1 malformed.** Tree-integrity HELD. **PASS:** code-reviewer ("the drain held; the bug class is structurally dead instead of waiting for round five" — re-grep found ZERO stray members, const-asserts load-bearing, bare-8-as-ctx-size classification confirmed, completeness gate is the right structural backstop); plan-adherence (all 4 drain deliverables MET, 4e gates byte-INTACT, general-header layer untouched = no over-reach, 3 deviations rationale-clean); **deviation-judge-B PASS — the decisive whole-domain-closure verdict** (adversarially swept for a missed member: hex-literal evasion, const-alias indirection, result-slot cross-seam [confirmed single-crate write-only], bare-8 [confirmed ctx-size] — NO remaining spike-class member exists; the class is closed + the gate is a real backstop). **judge-B surfaced a PRE-EXISTING same-class residual ONE LAYER OVER** (general SM-header `FRAME_SLEEP_HANDLE_OFFSET=8` runtime vs `FRAME_OFFSET_SLEEP_HANDLE=8` codegen, dual-defined + held only by a prose "MUST stay in sync" comment, no const-assert) — correctly NOT a 4d BLOCK (documented carve-out in the gate header, pre-existing, out of pure-CPU M3d scope), tracked as a 4-field deferral in todos:201 (apply the 4d ynz-abi+const-assert+gate pattern to the general header in a future frame-ABI-hardening slice). **rules-compliance BLOCK (1, trivial, NOT a class member):** the new completeness-gate test fn missing its per-function Big-O annotation (comments.md Hard Rule 7) — generic new-Tier-2-fn finding, not another frame-ABI member. **acceptance-verifier MALFORMED** (returned a mid-task non-verdict at 22min — same agent-truncation failure mode as a prior design-compliance instance; deferred to round-5 fresh run on the final tree).
- 2026-06-15 — **Sub-slice 4d Round 5 (final): rules PASS + acceptance via COORDINATOR LIVE-RUN.** Fix: one `///` Big-O line on the gate test fn (`spike_frame_abi_no_bare_offsets.rs:69`) — probe-confirmed landed; all other code blobs byte-identical to the R4 PASS tree (so code-reviewer/plan-adherence/judge-B/design-compliance carried R4 PASS). **rules-compliance PASS** (Big-O resolved + re-swept full diff: all new Tier-2 fns annotated, zero duct-tape/changelog/graveyard hits). **acceptance: the agent returned MALFORMED a 3rd consecutive time** (mid-task non-verdict at 9min — recurring long-suite agent-truncation). Per verification.md (applies to coordinator), coordinator performed the independent live verification DIRECTLY (mechanical tree verification, not code review; substantive AC grading already covered by the 4 PASSing lanes): `cargo test --workspace` → **2043 passed / 0 failed**; named 4d tests all green (completeness gate, both cancellation unit tests, 3 nounwind tests, panic FIRE byte-identical); `cargo clippy --workspace -- -D warnings` clean; `cargo fmt --check` clean. Logged as coordinator-substituted acceptance (3× agent malform documented).
- 2026-06-15 — **★ SUB-SLICE 4d COMPLETE & VERIFIED GREEN (5 gate rounds: R1 9P/2B → R2 6P/2B → R3 BLOCK → R4 drain 4P/1B → R5 PASS) ★.** 4d = panic+cancel: resume-fn ABI `extern "C"`→`"C-unwind"` (runtime fn-ptr types flipped; codegen no-op verified — resume fns already no-`nounwind`, guarded by the new IR-grep test); `cleanup_spike_cpu_handles` generalized to arbitrary-N via a packed discriminator word; real cross-crate const-asserts. **The 5 rounds were NOT whack-a-mole noise — each fixed a genuine silent-heap-corruption-class drift surface; the coordinator's error was point-fixing one frame-ABI constant per round instead of enumerating (Patrick flagged it at R3).** R4 corrected that: extracted ALL spike frame-ABI constants into the zero-dep `ynz-abi` crate (tag/base/stride/disc-offset single-source; handle-0/1 const-asserted), eliminated every bare spike-offset literal (prod + test), + a SOURCE-SCAN completeness gate that fails any future bare literal at `cargo test` before review (the structural round-stopper per testing.md exhaustive-coverage). judge-B confirmed no remaining member. Corpus 2043/0; clippy/fmt clean; tree-integrity held every round. 1 new tracked residual (general-SM-header same-class drift, todos:201) + the prior 4d residuals (N>2 live cancellation→4e, background+CPU shutdown race). **Per run-through authorization: auto-committing 4d checkpoint, then proceeding to sub-slice 4e** (the LAST sub-slice: lift pre-pair `wait`/zero-param/N=2 gates as verify-first permits + fixture-header sweep → then Phase-3 completion gate). Phase 3 Phase Review Gates + ACs remain UNTICKED (gate the FULL phase; 4e pending). Phase-3 final-commit BASE stays dcb32af.
- 2026-06-15 — **Sub-slice 4e executor (Opus) DONE — 1 FIRE + 2 honest verify-first DECLINEs (the 4d lesson applied: no forced FIREs).** BASE `eba9ec1`. **Lift 3 (N>2 single group) FIRES**: `[u64;2]` offset arrays → `Vec<u64>`, count gate lifted to N≥2, `build_cpu_group_slots` fed the real member count, handle/result offsets derive from `ynz_abi` base+stride (folded the 4d carry-forward — removed the now-N-insufficient `SPIKE_HANDLE_0/1_OFFSET`+`SPIKE_RESULT_0/1_OFFSET` constants), DRY-consolidated 3 duplicated extraction blocks into ONE shared `spike_cpu_group_member_indices`+`spike_cpu_group_member_count` (-188 lines, kills the slice-1/2 parity-drift surface). N=3 fixture: 3 spawns, distinct values, byte-identical, alloc=free. **Lift 1 (pre-pair `wait`) DECLINES**: broad typeck lift fires but trips the slice-1 cross-boundary frame-sizing corpse (`imported_suspending_after_pair_declines_consistently_across_boundaries` fails); narrow lift collides with promoted⊥base_suspends = M3g dual-kind-frame (todos:198). **Lift 2 (zero-param host) DECLINES**: param read-after-join silently drops a value — param slot 0 (byte 32) aliases CPU handle slot 0; needs param-slot reservation past the reserve = real machinery beyond a gate flip (NEW residual). Both DECLINEs locked with 0-spawn fixtures. typeck verify-first probe fully REVERTED (0 diff). Fixture-header sweep done (r + s fixtures). Demo extended (pirates-roster N=3 section). Corpus **2046/0**; clippy/fmt clean; no-sync-join REFUTED; completeness gate green (no extension needed). Deviations: 3 approach (2 DECLINE narrowings + DRY consolidation) + 2 scope (s-fixture sweep, typeck-revert-noop) — scratch `phase3-sub4e-deviations.md`.
- 2026-06-15 — **Sub-slice 4e gate Round 1: 6 PASS / 2 BLOCK + 2 concerns.** PASS: design-compliance (coordinator-substituted — agent malformed 2× this gate; verified no new join primitive/sync-join/bridge/coloring, N>2 reuses existing poll-based shims, no new runtime ABI, DECLINEs = sequential), plan-adherence (3 lifts attempted with declared outcomes, deviations clean, demo extended, typeck revert 0-diff; raised the same scope-completion flag the coordinator did), code-reviewer (N>2 byte-correct in IR: handles 32/40/48 results 56/72/88, discriminator packs count=3, distinct values, alloc=free; DRY consolidation genuine; 2 NON-BLOCKING concerns), **judge-1 (pre-pair-wait DECLINE, inverted) PASS** (tried narrow/intrinsic-only/non-entrypoint lifts — DECLINE is STRUCTURAL: pre-pair-wait host ∈ base_suspends → excluded queries.rs:763; collapsing promoted⊥base_suspends needs M3g dual-frame machinery), **judge-3 (DRY consolidation parity) PASS** (5 adversarial inputs — count gate/reserve/extraction/emission all derive from the ONE `spike_cpu_group_member_indices`, drift structurally impossible; minor over-conservative-DECLINE edge for no-bind-member-0, never false-ADMIT). **BLOCK: deviation-judge-2 (param-host DECLINE, inverted)** — the wholesale `!f.params.is_empty()` decline OVER-declines: a param used ONLY in spawn args (never read post-join) is SAFE to fire (the byte-32 collision is a dead store; spawn-arg load precedes the handle write); narrower gate (decline only if a param is read in a post-join stmt) is ~20 lines in 4e scope. **BLOCK: rules-compliance** — the param-host DECLINE comment (integration.rs:5639) claimed "tracked in todos" but no entry existed (false-tracker corpse; coordinator omission — tracked the general-header residual but missed this one). **Concerns (code-reviewer, folded into fix):** independence-check slice over-conservative invariant needs a naming comment; 3 fixture headers reference 4e-deleted symbols (`SPIKE_HANDLE_0/1`/`SPIKE_RESULT_0`).
- 2026-06-15 — **Sub-slice 4e fix round (Opus) DONE.** **judge-2 BLOCK — param-host gate NARROWED**: wholesale decline replaced by `spike_param_read_after_join` + recursive `stmt_tree_ident_reads` (descends if/match/while/for bodies) + `collect_ident_names` made pub in typeck. Param-spawn-args-only now FIRES (recovered safe parallelism, incl. an N=3 variant @ 3 spawns); param-read-post-join still DECLINES. New FIRE fixtures `v0_3_m3d_param_host_spawn_args_only.ynz` (2 spawns, 9907) + `..._n3_spawn_args_only.ynz` (3 spawns, 14862). **rules BLOCK — param-host residual now tracked** (todos `m3d-param-host-read-after-join`, 4-field; integration.rs comment claim now accurate, narrowed scope). **Concerns fixed**: independence-invariant comment added; 3 dead-symbol fixture comments swept (grep clean). **Coordinator acceptance live-run (agent malformed 3-4× this session → substituted): `cargo test --workspace` 2048/0** (the lone earlier failure = known m3e contention flake `v03_m3e_alias_local_name_collision`, confirmed passing solo per todos:176); 4e param-host (declines + both spawn-args FIREs) + N=3 all green; clippy/fmt clean; no-sync-join REFUTED; completeness gate green. **Scope outcome (resolves the earlier pending escalation): M3d Phase-3 gate-lift scope completes as — N>2 FIRES, param-host PARTIALLY FIRES (spawn-args-only) + read-after-join DECLINES (residual), pre-pair-wait DECLINES (→M3g). All deferrals tracked; no silent drop. No 4f needed.**
- 2026-06-15 — **Sub-slice 4e gate Round 2: code-reviewer + judge-2 BLOCK (SAME one-line bug, independent convergence) / rules PASS / coordinator-acceptance PASS / plan-adherence PASS / carries (judge-1, judge-3, design).** **The narrowing fix introduced ONE under-decline edge — caught by the safety-direction re-gate, NOT by the green corpus (2048/0 is blind to it; no existing test has the shape).** Bug: `stmt_tree_ident_reads`'s `Stmt::For` arm (emit.rs:6888) calls `out.remove(var)` on the SHARED cross-statement accumulator → a post-join `for` whose loop-var name shadows a param erases that param's read recorded by an EARLIER post-join stmt → `spike_param_read_after_join` returns false → gate FIRES an unsafe param-read-post-join → byte-32 handle/param-slot corruption. Both agents traced it (code-reviewer: `print(i); for (i in …){}`; judge-2: `let z=n+1; for (n in …){}; return …z`). **NOT a class re-seeding** — code-reviewer confirmed the walk's `match`es are compiler-forced exhaustive (all 11 Stmt + 22 Expr variants, no `_=>`); the SINGLE defect is the shared-accumulator `remove`. **FIX: scope the loop-var removal to a loop-LOCAL read set** (collect body reads into a fresh set, remove `var` from THAT, union into `out` — so an earlier stmt's param read can't be erased; semantically correct: a body read of the shadowing loop-var isn't a param read) + a RED fixture (param read in stmt N + later `for` binding the same name in N+1 → must DECLINE). Per Rule 11 FIX (not revert to the wholesale over-decline judge-2 R1 already flagged). Different file:line than R1 BLOCKs → no-progress clear.
- 2026-06-15 — **Sub-slice 4e fix round 2 (Opus) DONE + gate Round 3: ALL PASS (rules BLOCK overridden).** The For-arm under-decline fix: `Stmt::For` now collects body reads into a loop-LOCAL `body_reads` set, strips the loop-var + destructure-bound names (`destructure_pattern` alias/field, `map_destructure_pattern` key/value) from THAT local set, unions into the shared `out` — so an earlier post-join stmt's param read can't be erased. `out.remove` is GONE from emit.rs entirely (coordinator grep). While/Match confirmed bind no value-namespace name. RED fixture `v0_3_m3d_param_host_loopvar_shadow_declines.ynz` (earlier param read + later for shadowing it → DECLINE, 9910, 0 spawns) + FIRE companion `..._fires.ynz` (pure loop-var shadow, param not else-read → FIRE, 9908, 2 spawns alloc=free). Verify-first proved the pre-fix 9907-vs-9910 corruption + the byte-identical fix. **Round-3 verdicts:** code-reviewer PASS ("byte-32 corruption can't reopen"; single admission chokepoint, exhaustive matches, minimal-pair fixtures with spawn-count+alloc=free teeth); **deviation-judge-2 PASS** (safety — 8 adversarial inputs incl. nested-for/map-destructure-shadow/While/Match/If-let-shadow: the loop-local set makes erasing an earlier param read "physically impossible"; no under-decline remains); coordinator acceptance live-run (agent malformed every attempt → substituted) **`cargo test --workspace` 2050/0**, all 5 param-host tests green, clippy/fmt clean, no-sync-join REFUTED. **CARRIED: plan-adherence (R2 PASS), judge-1 (pre-pair-wait DECLINE, R1 PASS), judge-3 (DRY parity, R1 PASS), design-compliance (coord-substituted PASS)** — their surfaces untouched by the For-arm fix. **rules-compliance BLOCK OVERRIDDEN (coordinator):** re-flagged the `// v0.3-M3d:` fixture-prefix as a banned phase-label (its persistent `ynz-phase-label-pattern` memory) — overridden as the established corpus-wide milestone-version-tag convention (~16 fixtures; filename already carries `v0_3_m3d_`; cleared identically at 4a-R2/4b-R2/4b-R3/4c/4d), NOT a `// Phase 4B` plan-label. ⚠️ **META-ISSUE for Patrick: this `v0.3-M3d:`-prefix dispute has cost a rules-flag EVERY round — warrants a one-time resolution (update the rules-compliance agent memory to encode the standing ruling, OR decide it's truly banned and sweep all ~16 fixtures once).** Surfaced at the 4e-commit checkpoint.
- 2026-06-15 — **★ SUB-SLICE 4e COMPLETE & VERIFIED GREEN (3 gate rounds: R1 6P/2B → R2 narrowing-introduced-1-under-decline 2-agent-converged → R3 ALL PASS) ★.** 4e = the FINAL Phase-3 sub-slice (gate lifts + sweep). **Outcome — M3d Phase-3 gate-lift scope completes honestly:** N>2 single group FIRES (3 spawns, distinct values, alloc=free, DRY-consolidated extraction); **param-host PARTIALLY FIRES** (spawn-args-only, incl. N=3 — recovered by judge-2's R1 over-decline catch + the R2/R3 loop-local-safety fix); param-host-read-after-join DECLINES (residual todos `m3d-param-host-read-after-join`, needs param-slot reservation); pre-pair-wait DECLINES (→M3g, structural per judge-1); fixture-header jargon swept; pirates-roster demo extended (N=3). All deferrals tracked; NO silent scope drop. Corpus 2050/0; clippy/fmt clean; tree-integrity held. **Per run-through authorization: auto-committing 4e checkpoint, then the Phase-3 completion gate.** Phase 3 Phase Review Gates + ACs still UNTICKED until the completion gate. Phase-3 final-commit BASE stays dcb32af.
- 2026-06-15 — **★ PHASE-3 COMPLETION GATE IN FLIGHT (this session, new chat) ★.** All 8 sub-slices committed (1/2/3 + 4a `deaa30c` + 4b `ab2e840` + 4c `ee880b2` + 4d `eba9ec1` + 4e `90d80b4` + prefix-sweep `a4877e8`); HEAD `e00ddca`. Clean tree. **Cumulative gate BASE = `dcb32af`, diff scoped to `crates/ examples/pirates-roster/ Cargo.toml Cargo.lock cspell.json`** (74 files, +5780/−733; the raw `dcb32af..HEAD` range's ~24 `design/` doc files are UNRELATED interleaved docs commits — excluded, verified Phase-3 commits touch zero design/spec/registry). **Per Patrick (spend-constrained): 4 judgment-heavy reviewers run as real Opus agents (code-reviewer, rules-compliance, plan-adherence, design-compliance); acceptance + ALL ~18 deviation-judges done by the COORDINATOR inline** (acceptance via live-run per standing authorization — the agent malformed 4-5× this session; deviation-judging inline to save ~18 Opus spawns, logged honesty caveat = less independent than a fresh agent). Tree-integrity snapshot: empty-tree status/diff (clean), emit.rs blob `fe929d59`, queries.rs `048193a4`, independence.rs `93563cc5`, runtime.rs `7b0ebc38`, abi/lib.rs `87d4aa05`. **VERDICTS IN SO FAR:** rules-compliance **PASS** (no violations); design-compliance **PASS** (M2-HALT class dead — poll-and-yield join, lone `block_on` is pre-existing entrypoint driver, codegen throws not bridges, zero coloring, both divergences carry named costs); coordinator HALT-class no-sync-join probe **REFUTED (clean)**; **coordinator deviation-judging ALL ~18 PASS** (5 DECLINE-narrowings safe-by-construction [sequential=byte-identical, 0-spawn-locked, fresh-judge-vindicated at sub-slice]; corruption-surface FIREs each per-class byte-identical-vs-oracle fixture-proven; **param-host byte-32 walk verified by hand** — `stmt_tree_ident_reads` match is compiler-forced-exhaustive [no `_=>`], For-arm uses fresh loop-local `body_reads`+`out.extend` [shared-accumulator `out.remove` erasure GONE], conservative direction [imprecision→over-decline never false-admit], RED+FIRE fixtures lock both). **PENDING:** code-reviewer (Opus), plan-adherence (Opus), `cargo test --workspace` live-run. On all-PASS → tick Phase-3 Phase Review Gates (lines 654-660) + 5 ACs (lines 637-646) w/ Evidence + record judge verdicts → commit writeback → STOP at Phase 3→4 boundary (Patrick's authorized pause).
- 2026-06-15 — **Sub-slice 4e gate Round 2 DISPATCHED** (code-reviewer + rules + plan-adherence + judge-2-resafety; carry judge-1/judge-3/design + coordinator-acceptance-PASS). judge-2 FLIPPED to safety direction: adversarially try to make the NARROWED gate fire an unsafe param-read-post-join (nested control flow, nested exprs, shadowing, N>2) → BLOCK if any under-decline corrupts; PASS if the walk is conservatively complete. Round-2 snapshot: status `232044ba`, diff `2d1be888`, emit.rs `aa0b7e7a`, independence.rs `93563cc5`, integration.rs `ba27a763`. Verdicts pending — writeback on return.
- 2026-06-15 — **Sub-slice 4e gate DISPATCHED** (5 reviewers + 3 judges; BASE `eba9ec1`, diff `git diff eba9ec1 -- crates/ examples/`, new fixtures read-direct). Tree-integrity snapshot: status `7aa5380e`, diff `fd0dea52`, emit.rs `f4365128`. **Judges 1&2 INVERTED** (try to SAFELY fire the declined pre-pair-wait / param-host cases — PASS only if genuinely unfireable in 4e scope); judge 3 stress-tests the -188-line DRY consolidation vs the slice-1/2 parity corpse. **SCOPE OUTCOME pending coordinator escalation**: the plan's Phase-3 scope boundary (line 611) listed all 3 gate lifts (zero-param/N>2/wait-barrier) as IN-M3d; 2 verify-first-DECLINED → Phase 3 would complete with N>2 lifted + pre-pair-wait (→M3g) + param-host (→new pure-CPU follow-up slice) deferred. After the gate confirms the DECLINEs are genuine (not over-declines), surface to Patrick: fire the Phase-3 completion gate now (accept the 2 deferrals) vs grow a 4f for the param-host param-slot-reservation work. Verdicts pending — writeback on return.
- 2026-06-15 — **Sub-slice 4d committed as verified checkpoint: `eba9ec1`** (run-through mode). Explicit-path stage (no -A): the new `crates/ynz-abi/` crate, emit.rs, runtime.rs, lib.rs, m2_runtime.rs, integration.rs, the new `v0_3_m3d_cpu_child_panic.ynz` fixture + `spike_frame_abi_no_bare_offsets.rs` gate test, both Cargo manifests + Cargo.lock, plan, scratch (sub4d), todos. Excluded: `.claude/state.md` (not modified this session). **Sub-slice 4e (gate lifts + sweep) resumes from BASE `eba9ec1`.** Phase-3 final-commit BASE stays dcb32af. Phase-3 Gates + ACs still UNTICKED.
- 2026-06-15 — **Sub-slice 4d gate Round 2: 6 PASS / 2 BLOCK** (5 reviewers + 3 fresh judges A/B/C; 4 round-1 judges carried — m2_runtime+todos files untouched, codegen-ABI-no-op+lib.rs-colocation comment/mechanical-only). **Tree-integrity HELD** (status/diff/blobs byte-identical pre/post — 2nd clean read-only round; one design-compliance instance returned a malformed non-verdict and was re-spawned with an explicit verdict-line requirement → PASS). No Planned RED Repros → no carve-out. **PASS (6):** acceptance-verifier (LIVE 5/5 ACs — `cargo test --workspace` **2041/0**, 40/40 m3d suite, both NB1 tests confirmed REAL incl. the non-vacuous mutation probe, const-assert live; constant-relocation is a clean behavioral no-op); plan-adherence (3/3 scope items intact post-refactor, **4e gates byte-INTACT** [zero-param 6874 / N=2 7735 / pre-pair-wait 6982 all unhit], no 4c/M3g machinery, all R2 deviations documented + banned-phrase-clean); design-compliance (re-spawn — "a BLOCK becoming a PASS": tokio gone from compiler tree, kernel posture strictly better, ynz-abi zero-dep kernel-safe, no coloring/join-model regression, runtime.rs:1272 reword more accurate); **judge-A ynz-abi-extraction PASS** (cargo-tree confirms no tokio/simdutf/num_cpus/ynz-runtime under ynz-codegen; ynz-abi single-node zero-dep tree, no cycle; base-offset const-assert genuinely cross-crate [two independent defs → E0080 on drift, not x==x]; old 32-bit magic 0x5350494B fully purged, tag+count format applied atomically both sides — no value drift); **judge-C NB1-test PASS** (exhaustively traced: handles attribute-group indirection #1/#42, vacuity guard `resume_defines>0`, inline check scoped to tail-after-last-`)` so function-name `nounwind` can't false-trigger, non-vacuous test genuinely drives the reject path via catch_unwind — real safety net). **rules-compliance R1 findings ALL resolved** (4 SPIKE_FRAME_TAG copies → 1 ynz_abi import; both Big-O added).
  - **CONVERGENT BLOCK — stride const-assert gap (code-reviewer + deviation-judge-B, same finding, independently):** the ynz-abi extraction bound `SPIKE_HANDLE_BASE_OFFSET` (const-assert at emit.rs:6698) + re-exported the tag, but codegen's **`CPU_HANDLE_SLOT_BYTES` (emit.rs:468) handle STRIDE has NO compile-time binding** to `ynz_abi::SPIKE_HANDLE_SLOT_BYTES`. Runtime's cleanup now scans stride-driven (`SPIKE_HANDLE_BASE_OFFSET + i*SPIKE_HANDLE_SLOT_BYTES`); if codegen's stride drifts (e.g. CpuJoinHandle grows to 16B) while ynz-abi's stays 8, the build succeeds, codegen writes handles at 32/48/64 while runtime reads 32/40/48 → `Box::from_raw` on a result-slot word → silent heap corruption on cancellation of any 2-child spike frame. Exact two-definition-drift class the const-assert exists to kill — closed for base-offset, left open for stride. **FIX: add a sibling `const _: () = assert!(CPU_HANDLE_SLOT_BYTES == ynz_abi::SPIKE_HANDLE_SLOT_BYTES as u64, "frame-ABI drift: ...")` in emit.rs** (identical shape to the base-offset assert; CPU_HANDLE_SLOT_BYTES is same-file at 468, no cycle). NEW finding vs R1 (different file:line) → no-progress clear.
  - **rules-compliance: BLOCK (1, NEW)** — [lib.rs:3667/3668/3669/3673/3677 + 3723-3729] the two new cancellation tests use **bare frame-offset literals** (`32`/`40`/`48`/`4`) where named ynz_abi constants are importable in-crate (no-duct-tape #4 provenance debt). **FIX: `use ynz_abi::{SPIKE_HANDLE_BASE_OFFSET, SPIKE_HANDLE_SLOT_BYTES}`; 32→BASE, 40→BASE+SLOT, 48→BASE+2*SLOT; for `4` promote `FRAME_SPIKE_DISCRIMINATOR_OFFSET` (runtime.rs:67) to pub(crate)+import OR move to ynz-abi.** Coherent with the stride fix (both = use canonical constants for offsets/stride).
  - **Non-blocking folded into the R3 fix (Rule 11 cheapest-moment):** code-reviewer NB — the NB1 non-vacuous proof covers only the attribute-GROUP nounwind path (`#1 {nounwind}`), not the INLINE path (`define ... nounwind {`); add a second doctored case asserting the checker rejects inline-nounwind too (cheap, additive, closes the safety-net's one hole). **4e carry-forward (coordinator-noted, NOT R3):** code-reviewer NB — codegen's `SPIKE_HANDLE_0/1_OFFSET` (emit.rs:6685-6688, =32/40) are a THIRD hardcode of the handle layout; 4e reworks the N=2-hardcoded spawn path anyway, so fold "codegen consumes ynz_abi::SPIKE_HANDLE_SLOT_BYTES directly + derive the 0/1 offsets" into 4e rather than touching the 4e-adjacent spawn path now.
- 2026-06-15 — **Sub-slice 4d Round 1 fixes applied** (Opus executor) + **pre-respawn probes all LANDED**. Convergent fix: extracted constants-only **`crates/ynz-abi`** (zero deps) holding `SPIKE_FRAME_TAG`/`SPIKE_HANDLE_BASE_OFFSET`/`SPIKE_HANDLE_SLOT_BYTES`; both ynz-codegen + ynz-runtime now `use ynz_abi::`; **codegen→runtime dep REMOVED** → tokio gone from the compiler's tree (resolves the dep BLOCK). The 4 duplicated `SPIKE_FRAME_TAG` test copies collapsed to one `use ynz_abi::SPIKE_FRAME_TAG` (resolves rules DRY finding). Big-O added to both integration.rs helpers (rules findings 2+3). NB1: added `v03_m3d_resume_fns_are_not_nounwind` IR-test + `..._non_vacuous` mutation-proof (closes the implicit no-`nounwind` gated-path gap). NB2: runtime.rs:1272 comment reworded to accurate LLVM framing. FRAME_HEADER_SIZE kept codegen-local (const-assert binds `ynz_abi::SPIKE_HANDLE_BASE_OFFSET == state_machine::FRAME_HEADER_SIZE`; moving it = ~15-call-site cascade for zero ABI benefit). Live: `cargo test --workspace` **2041/0** (2039+2 NB1), clippy `-D warnings`/fmt clean, const-assert E0080-on-drift verified-then-reverted, `cargo tree -p ynz-codegen | grep tokio` empty. Coordinator pre-respawn probes (7) ALL LANDED: tokio-gone, dep-removed, ynz-abi zero-dep, 0 local SPIKE_FRAME_TAG copies, both Big-O, const-assert binds ynz_abi, no-nounwind test present. Round-2 deviation set: 1 scope (ynz-runtime→ynz-abi dep) + 2 approach (FRAME_HEADER_SIZE codegen-local; NB1 test in integration.rs not codegen crate). The round-1 codegen→runtime-dep deviation is RESOLVED (dep gone).
- 2026-06-15 — **Sub-slice 4d gate Round 1: 9 PASS / 2 BLOCK** (5 reviewers + 6 deviation-judges). **Tree-integrity HELD** — status/diff hashes + all 6 in-scope blobs byte-identical pre/post (first fully-clean read-only round on this phase; the explicit read-only/no-mutation-probe guards in every prompt worked). No Planned RED Repros → no carve-out; both BLOCKs route to fix loop. **PASS (9):** code-reviewer (Opus — "clean, honest work"; independently verified all 3 cruxes: tag-resistance exhaustive, const-assert genuinely E0080s on drift, handle-count encoding correct; 2 non-blocking concerns folded into fix round below); plan-adherence (3/3 scope items landed, **4e gates byte-INTACT** — pre-pair `wait`/zero-param/N=2 hardcode untouched; no 4c/M3g machinery reintroduced; banned-phrase grep clean on all 6 rationales); acceptance-verifier (LIVE 5/5 ACs MET — `cargo test --workspace` **2039/0**, panic fixture IR-grep 2 spawns + byte-identical abort-parity, both cancellation unit tests mutation-proven alloc=free, const-assert load-bearing; panic honesty-caveat graded MET: "not silent-wrong" delivered by abort-parity); design-compliance (join poll-based, `C-unwind` is panic-ABI not coloring, two `block_on` sites pre-existing+unchanged, kernel-mode/cancellation match locked docs); **deviation-judge packed-discriminator PASS** (32→16-bit tag narrowing safe — 4 adversarial angles failed: offset 4 exclusively spike-written, `ynz_alloc_zeroed` zeroes all other frames, `children.len()!=2` guard pins count to the 2-slot reservation, single-caller never on recursion-chain children); **judge codegen-ABI-no-op PASS** (exhaustive: 40+ `.ll` fixtures + 21 objects via `readelf` — every `ynz_sm_*_resume` lacks `nounwind` + has `.eh_frame`, both runtime call sites `extern "C-unwind"`, no abort shim; nit: comment at runtime.rs:1272 imprecise); **judge m2_runtime-helper PASS** (ABI flip minimal+complete, spike-local `"C"` island correctly untouched, assertions unweakened); **judge lib.rs-colocation PASS** (all inside `#[cfg(test)]`, no production smuggle, discriminator format matches decode byte-for-byte, tests mutation-non-vacuous); **judge todos-residuals PASS** (both DECLINEs plan-consistent — N>2 live test *structurally* gated on 4e codegen lift, background race empirically pre-existing at BASE ~5/20 both ways, 4-field deferrals).
  - **deviation-judge codegen→runtime dep: BLOCK** — the new `ynz-codegen → ynz-runtime` dep (added only to read 2 ABI constants for the const-assert) drags tokio's full multi-thread runtime + simdutf8/memchr/num_cpus into the **compiler's** transitive dep tree (confirmed via `cargo tree`: tokio v1.52.3 now under ynz-codegen). No cycle, but a `--kernel` trap + "why is tokio in the compiler?" landmine. **FIX (judge-prescribed): extract a constants-only `crates/ynz-abi` crate (zero deps) that BOTH ynz-codegen + ynz-runtime depend on**; const-assert still fires at compile time; codegen→runtime dep removed. code-reviewer's "dep structurally clean (no cycle, leaf)" does NOT override this — it's a narrower finding (tokio-in-compiler transitive cost) code-reviewer didn't weigh; per Rule 11 the evidence-backed BLOCK stands.
  - **rules-compliance: BLOCK (3)** — (1) [lib.rs:3553/3664/3721/3782] DRY: 4 local copies of `const SPIKE_FRAME_TAG = 0x5350` shadow the canonical `pub const` (coding-style 3+=extract) → **subsumed by the ynz-abi extraction** (copies become `use ynz_abi::SPIKE_FRAME_TAG`); (2) [integration.rs:5298] missing Big-O on `strip_runtime_error_path`; (3) [integration.rs:5317] missing Big-O on `m3d_assert_panic_fires_byte_identical`. The two verify-first DECLINEs confirmed properly tracked (4-field form).
  - **Non-blocking concerns folded into the fix round (Rule 11 + no-duct-tape cheapest-moment; phase precedent 4a-R3→R4):** (NB1) code-reviewer #2 + judge nit — the `C-unwind` chain's correctness depends on the IMPLICIT no-`nounwind` invariant on emitted resume fns, currently untested → add an IR-grep test asserting `ynz_sm_*_resume` are NOT `nounwind` (closes a real gated-path-fire-assertion gap); (NB2) judge nit — clarify the imprecise "extern C → abort at boundary" comment at runtime.rs:1272 to the accurate LLVM-level framing (no-`nounwind` + `.eh_frame` ⇒ unwind propagates; abort shim is a Rust-typed-call-site artifact). Coordinator-owned doc fixes (NB3 plan carry-forward: C-unwind wired but Yinz-source-unreachable until a non-abort error path exists; NB4 scratch note: cancellation covered by unit tests not `.ynz` integration fixture, integration gap tracked to 4e) handled by coordinator directly.
- 2026-06-15 — coordinator probes (pre-gate, 4d). **(1) no-synchronous-join HALT-class**: `git diff ee880b2 -- crates/ynz-codegen/src crates/ynz-runtime/src crates/ynz-typeck/src | grep '^+' | grep -iE 'block_on|thread::park|spin_wait|busy.?wait|\.join\(\)'` → **REFUTED (empty)**. Routing: proceed to full fan-out. **(2) cross-crate const-assert + dep direction**: `const _: () = assert!(...)` at `emit.rs:6697` importing `SPIKE_FRAME_TAG`/`SPIKE_HANDLE_BASE_OFFSET` from ynz-runtime; `crates/ynz-codegen/Cargo.toml:24 ynz-runtime = { workspace = true }` → **CONFIRMED present** (headline deliverable landed; first regex was multi-line-blind). Routing: proceed. **(3) discriminator-collision surface**: 4d narrows the spike tag 32-bit→16-bit (high half `0x5350`); codegen writes offset 4 ONLY at the spike spawn path (`emit.rs:7951-7962`), normal frames zero-allocated → no confirmed collision, but the tag-resistance reduction is a real surface → **camped code-reviewer + deviation-judge #1 (packed-discriminator) on it** (adversarially confirm no non-spike frame can present high-half `0x5350` at offset 4). Routing: proceed to full fan-out unchanged (clean probe never shrinks the gate).
