---
name: "v0-3-concurrency-perf"
plan-id: "2026-05-21-v0-3-concurrency-perf"
status: "active"
roadmap-id: null
session-id: ["4da497e1-c5bf-4d07-9ffd-a297ade26351", "phase4-deferral-executor-2026-07-03-m5"]
created_at: "2026-05-21"
updated_at: "2026-07-04"
metadata:
  type: "roadmap"
legacy:
  note: "Fields below are preserved verbatim from the pre-migration .claude/plans/ ledger-format frontmatter (2026-07-01 migration to .claude/planning/). session-id history was not tracked pre-migration."
  slug: v0-3-concurrency-perf
  type: roadmap
  owner: Patrick Rizzardi
  status: active
  created: 2026-05-21
  last_updated: 2026-07-03
  milestones:
    - v0-3-m1-runtime-and-background
    - v0-3-m2-wait-and-state-machines
    - v0-3-m3a-suspension-codegen        # SPLIT 2026-06-01 from old "v0-3-m3-auto-parallelization"
    - v0-3-m3b-auto-parallelization      # I/O-overlap auto-parallel (interleaved inline poll, zero new runtime)
    - v0-3-m3d-cpu-parallelization       # SPLIT 2026-06-05 from M3b: pure-CPU statement parallelization (new runtime ABI + SM-promotion + deadlock-safe join)
    - v0-3-m3e-cross-module-frame-serialization  # SPLIT 2026-06-05 from M3b P1: full cross-module FrameLayout serialization (lifts P1's loud-reject guards for re-export-transitive / shape-in-callee / errors×transitive)
    - v0-3-m3f-codegen-correctness-fixes  # shipped 2026-06-09; child declared the roadmap but was omitted from this list — added during ledger-format migration
    - v0-3-m3g-mixed-cpu-io-overlap       # SPLIT 2026-06-14 from M3d Phase 3: mixed CPU+I/O poll-path fusion. MANDATORY per Patrick 2026-06-19 (efficiency mission) — sequence after M3d P5, before the v0.3.0 tag.
    - v0-3-m3c-shadow-parity              # ANCHOR-BUG FIX 2026-06-19: had a roadmap §Milestone 3c but was missing from this list (couldn't be scheduled). Documented deferral → post-v0.3.0 hotfix (Patrick 2026-06-19); 4-field anchor in §Milestone 3c + capability-ledger.
    - v0-3-m4-channels-arc-release       # RENAMED + SPLIT 2026-07-02 from "v0-3-m4-channels-soa-release" — auto-SoA carved out to v0-3-m5-auto-soa (recon proved it's not codegen-only; see roadmap §Milestone 4 split note)
    - v0-3-m5-auto-soa                   # SPLIT 2026-07-02 from M4 — deferred post-v0.3.0; documented no-duct-tape deferral in roadmap §Milestone 5
---


# Roadmap: v0.3 — Auto-Parallelization + Auto-SoA

## Vision

By the end of v0.3, existing v0.1 Yinz code that uses `wait` / `background` actually runs concurrently instead of sequentially. Independent operations in a function body auto-parallelize without any user action. `array<Shape>` hot loops that access only 1-2 fields get SoA layout automatically — 10-40× faster on cache-heavy workloads — with zero syntax change.

From the user's perspective: code they already wrote just gets faster. No rewrite required. No new keywords to learn (the user already wrote `wait` and `background` in v0.1 — they just didn't run concurrently yet). No opt-in flags. The compiler does the hard work.

This is the "Rust-level performance, TypeScript-level readability" promise delivered at runtime, not just at compile-time type safety. After v0.3, a junior Yinz developer writing sequential-looking code gets multi-core parallelism and SIMD-ready data layouts for free.

---

## Why Now

v0.1 shipped the full language surface (tag `v0.1.0`, 830 tests, M1–M8). v0.2 shipped the dev-loop tooling (LSP, fmt, watch — tag `v0.2.0`). Per [`docs/reference/REF-mvp-scope.md`](../../../../docs/reference/REF-mvp-scope.md), v0.3 is the concurrency + performance slot — the compiler's dependency-graph analysis and auto-SoA optimization.

Three things are now in place that weren't possible in v0.1:

1. **Ownership system is working.** M4's `share`/`lend`/`give` + `noalias` LLVM attributes give the compiler static aliasing proofs — the essential ingredient for both safe auto-parallelization (no data races by construction) and auto-SoA (safe layout transformation because field aliasing is provably non-overlapping).

2. **`wait`/`background` have correct sequential semantics.** v0.1 implemented `wait` as a passthrough and `background` as a synchronous call (confirmed: `emit.rs` line 3089 runs the inner expression to completion). The syntax is locked, the type checker validates ownership at spawn boundaries — only the runtime concurrency is missing.

3. **Salsa-backed incremental compiler.** The salsa query graph gives us the dependency information we need. Module-level queries are already tracked. The v0.3 may-block analysis is a new salsa-tracked pass on top of the existing `check_query`/`signature_query` infrastructure.

The "why now vs later" check from [`docs/reference/REF-mvp-scope.md`](../../../../docs/reference/REF-mvp-scope.md): delaying past v0.3 means users write `background doThing(data)` thinking it runs concurrently, but it actually runs sequentially — a correctness illusion. Every version past v0.1 that doesn't fix this extends that illusion.

---

## Constraints

- **No new user-facing syntax.** v0.3 is a compiler-internal milestone. The language surface (keywords, type system, ownership model) doesn't change. `wait`, `background`, `array<T>`, `shape` all stay exactly as they are.
- **Existing programs must produce identical output.** The only change is PERFORMANCE (faster) and SCHEDULING (concurrent execution of independent operations). Semantics are preserved: `wait` still means "complete before continuing"; `background` still means "run independently"; ownership still prevents data races.
- **No GC.** Auto-parallelization uses ownership semantics to eliminate races at compile time. No reference counting introduced for sequential code; `Arc`-equivalent only added cross-thread where ownership requires it.
- **Tokio is an internal implementation detail.** The work-stealing scheduler is provided by Tokio, bundled inside `libynz_rt.a`. Users never see Tokio, never write Rust async, never annotate functions with `async`. The no-function-coloring design (per [`docs/internal/implementation/IMP-no-function-coloring.md`](../../../../docs/internal/implementation/IMP-no-function-coloring.md)) is preserved: every function is still just `function`.
- **State machines are compiled to LLVM IR by `ynz-codegen`, not by Rust async/await.** Yinz controls the state machine transformation; Tokio provides the scheduler that polls them. Clean separation — the scheduler can be swapped without touching codegen.
- **All v0.2's rules carry forward:** WHAT/WHAT-INSTEAD/WHY diagnostics, plan invariants per [`.claude/rules/plan-invariants.md`](../../../rules/plan-invariants.md), feature registry entries for any new muted-hint domains or lint rules.
- **No kernel-mode concurrency.** `--kernel` mode programs (per [`docs/internal/implementation/IMP-no-runtime-mode.md`](../../../../docs/internal/implementation/IMP-no-runtime-mode.md)) get a compile error if they use `wait` or `background` — the Tokio-backed scheduler doesn't run in kernel mode. Same rule as v0.1 (currently sequential-only anyway).
- **Full teaching surface ships in the same milestone as the feature — no exceptions.** Every milestone that adds a new compiler behavior MUST include ALL of the following in the same milestone (not a follow-up): (1) registry entry for any new muted-hint domain or lint rule in [`registry/features.toml`](../../../../registry/features.toml); (2) LSP inlayHint handler extension or new lint emission for the feature; (3) WHAT/WHAT-INSTEAD/WHY text for every new compile error or warning introduced; (4) updated hover docs in the registry for any keyword whose behavior changes (`wait` and `background` both change meaning in v0.3 — hover text must be updated before the milestone tag ships); (5) VSCode extension version bump + at least one screenshot showing the new IDE surface; (6) `examples/pirates-roster/entrypoint.ynz` extended with a section demonstrating the new feature with inline comments pointing to the IDE behavior. If any of these surfaces can't fit in the milestone, they require a named `[[deferred_tooling_feature]]` entry in the registry with an explicit trigger for when they ship — NOT a silent omission. This constraint exists because forgetting one surface means the feature is invisible in the editor until someone remembers to fix it, which historically doesn't happen until a user complains.

---

## Architectural Decisions Made

These are locked before any v0.3 execution plan starts. Each milestone's execution plan must conform.

- **M2/M3 boundary correction — may-block analysis ENGINE belongs in M2, not M3 (DECIDED 2026-05-31, Patrick)**: The original split (M2 = state machines; M3 = may-block analysis) was unbuildable: a CORRECT state-machine layer requires knowing which functions suspend, and without that analysis M2 was forced into a runtime `block_on` sync bridge as a stopgap. That bridge (a) crashed at runtime (the M2 Phase-5 MILESTONE-INTEGRITY HALT — "Cannot start a runtime from within a runtime") and (b) contradicted [`docs/internal/implementation/IMP-no-function-coloring.md`](../../../../docs/internal/implementation/IMP-no-function-coloring.md) ("No Function Coloring": whole-program transitive may-block analysis + auto-inserted `wait`, stackless state machines, NO bridge anywhere). Three rounds of adversarial plan-review + a P0 gate + five per-phase gates never caught it because every review checked the plan against itself, never against the design doc. **Correction:** the intra-compilation-unit transitive may-block fixpoint (may-block set = `{sleepAsync}` for M2) moves M3→M2 — it's load-bearing for M2 correctness; M3 EXTENDS it cross-module (M8 prereq) + adds auto-parallelization + I/O-auto-`wait`. — Rejected: keeping the bridge (ships a known runtime crash + violates the design + "back to JS/Rust thread-holding"). Rejected: requiring explicit `wait` / function coloring in M2 (reverses the locked no-coloring decision — [`docs/internal/implementation/IMP-no-function-coloring.md`](../../../../docs/internal/implementation/IMP-no-function-coloring.md) line 11). WHY this is the right answer: it IS the documented design; it ships zero slow paths; it builds the exact substrate M3 needs (vindicating the original HALT decision "do the right long-term thing — M3 builds on M2's substrate"). Enforcement going forward: [`.claude/rules/plan-invariants.md`](../../../rules/plan-invariants.md) "Design-Doc Alignment" + project CLAUDE.md "design docs are governing" + graveyard "Plan Contradicts a Governing Design Doc" — added 2026-05-31 so no plan again drifts from a design doc unreviewed.

- **M3 SPLIT into M3a + M3b (DECIDED 2026-06-01, Patrick) — and three stale M3 assumptions corrected**: The original single "M3 = Cross-Module + Auto-Parallelization" bundled ~10 distinct capabilities across two unrelated concerns (suspension-codegen completion vs. dependency-graph analysis), and the codegen half is a hard prerequisite for the analysis half — the SAME unbuildable-boundary shape that caused the M2 HALT. So M3 splits:
  - **M3a — Suspension Codegen Completion** (`v0-3-m3a-suspension-codegen`): finishes the state-machine substrate. Lifts the two *painful* M2 positional guards via frame-backed mutable locals (`LocalCrossesWait` + `WaitInsideLoop` — `wait` now works inside loops and a local can cross a suspension); does the decided `sleepAsync`→`sleep` / `sleepMs`→`sleepBlocking` rename; re-documents the two *kept* guards as permanent design decisions (see next bullet). Ships value on its own: existing `wait` code that previously hit a compile error now compiles and runs correctly. Plan FIRST.
  - **M3b — Cross-Module May-Block + Auto-Parallelization** (`v0-3-m3b-auto-parallelization`): the old M3's analysis half — cross-module `suspends` propagation, the dependency-graph auto-parallelize pass (= auto-`wait` + auto-`background` for independent groups), `background` auto-give/copy inference, CPU/IO routing refinement, the `wait_points` + `background_routing` IDE teaching surfaces, and wiring `--no-auto-parallel`. Built ON M3a's completed substrate. Planned after M3a wraps.
  - **Three stale assumptions in the old M3 scope, corrected by source verification 2026-06-01** (so M3b's plan doesn't re-derive them wrong): (1) **M8 cross-file typeck is NOT a blocker — it already shipped.** `check_query` resolves cross-file imports today (`queries.rs` `module_signatures_query` → `resolve_imports`; passing `m8_combo_modules_*` + `v0_3_m2_cant_infer_cross_module` tests). The `Item::ImportDecl(_) => {}` arm (`check.rs:342`) is an *intentional* no-op — resolution happens upstream. The ONLY cross-module gap left is propagating the `suspends` flag, and it rides the existing salsa `module_signatures_query`/`FunctionSig.suspends` across same-project files — **no compiled-package format needed** (the old roadmap text "propagating through compiled-package metadata" overestimated the work). (2) **`ynz_rt_spawn` (I/O pool) already exists** (`runtime.rs:525`, shipped M2) — not "new in M3." (3) **The sleep rename was decided but not done** — current code still uses `sleepAsync`/`sleepMs`; M3a P0 does it. — Rejected: keeping one mega-M3 (the over-packed size that let the M2 design-drift survive three review rounds). Rejected: planning auto-parallel first (inverts the codegen→analysis dependency). WHY this is documented HERE and loudly: the M2 HALT happened because the "we're splitting/fixing this" decision lived only in a chat + a buried Findings Log, so a fresh chat couldn't see it. This bullet is the tripwire.

- **Kept M2 positional guards = PERMANENT design decisions, NOT deferred-to-M3 limitations (DECIDED 2026-06-01, Patrick)**: Two of the four M2 `wait`-position guards STAY as compile errors forever; M3a does NOT lift them and must reword their diagnostics (which today wrongly say "ships in v0.3-M3"):
  - **`SubExprSuspendViolation`** (suspending call nested inside a larger expression, e.g. `wait a() + wait b()`) STAYS. WHY: it enforces Golden Rule 7 (one operation per line with a named variable); the step-by-step form (`let a = wait fa()` / `let b = wait fb()` / `combine(a, b)`) is the preferred style AND gets the same concurrency for free once M3b's statement-level auto-parallel ships. The roadmap's earlier "`wait`-in-expression semantics" Architectural Decision (support `wait a() + wait b()`) is SUPERSEDED by this — it was redundant-at-best and blessed a style the language discourages.
  - **`MutualSuspensionCycle`** (two-or-more *different* suspending functions that mutually call each other — `ping`↔`pong`) STAYS. WHY: rare to nonexistent in real code, always restructure-able to self-recursion, and lifting it needs fiddly mixed-layout per-frame size metadata for near-zero payoff. NOTE: *self*-recursion (one suspending fn calling itself — the `next()`/recursive-descent pattern) is ALREADY supported (M2 heap-boxed child frames); only the mutual case is guarded.
  - Both diagnostics get reworded in M3a P0 from "ships in v0.3-M3" to the design rationale above, and the kept constraints get a "permanent positional constraints" note in [`docs/internal/implementation/IMP-concurrency.md`](../../../../docs/internal/implementation/IMP-concurrency.md). The two LIFTED guards (`LocalCrossesWait` + `WaitInsideLoop`) are M3a's hard core.

- **State-machine frame allocation = COMPOSITION, not per-call heap allocation (DECIDED 2026-05-31, Patrick — "absolute fastest" requirement)**: A child state machine's live-across-suspension state composes INTO the parent's frame, computed at compile time (the same mechanism Rust uses to nest `async fn` futures). Heap allocation happens ONLY at (a) `background` spawn points — one allocation per spawned task tree — and (b) recursion points (break the otherwise-infinite frame size, à la Rust's `Box::pin` for recursive async). — Rejected: heap-allocating a frame per state-machine call (simpler but leaves allocations on the floor and misses [`docs/internal/implementation/IMP-no-function-coloring.md`](../../../../docs/internal/implementation/IMP-no-function-coloring.md)'s "low memory, fast spawn — like Rust's async" target; violates Golden Rule 8 zero-cost-abstractions). This is the allocation model M2's `lower_function_with_waits` must implement. Asterisks NOT taken in v0.3 (tracked, not lost): work-stealing scheduler stays (thread-per-core revisited only if a workload demands it); `io_uring`/`kqueue`/IOCP I/O backend is captured in [`docs/internal/scratchpad/SCRATCH-stdlib-filesystem.md`](../../../../docs/internal/scratchpad/SCRATCH-stdlib-filesystem.md) for the v0.5 file module. Bottom line locked: stackless state machines + compiler-inferred coloring + composed frames = the known-fastest task-concurrency model for analyzable code (Rust's performance ceiling, Go's ergonomics).

- **Tokio as embedded scheduler**: Tokio's multi-thread work-stealing runtime is bundled inside `libynz_rt.a`. Generated Yinz binaries call `ynz_rt_init()` at startup (the Yinz equivalent of `#[tokio::main]`) which boots the Tokio runtime. The Tokio runtime stays alive for the program's lifetime. — Rejected: writing a work-stealing scheduler from scratch. Rationale: Tokio is 5+ years of production hardening (epoll/kqueue/IOCP, work-stealing). The novel part of v0.3 is the COMPILER analysis, not the scheduler. Spending 4-8 phases building a scheduler from scratch is the wrong use of time.

- **Tokio preemption model vs. design doc compile-time checkpoints**: [`docs/internal/implementation/IMP-no-function-coloring.md`](../../../../docs/internal/implementation/IMP-no-function-coloring.md) "Scheduler Preemption Model" locks compile-time-assisted safe-point insertion at loop back-edges and function call sites. Tokio 1.x's budget system yields at `await` points only — it does NOT insert preemption checks at loop back-edges. This means: for the initial v0.3 implementation, Yinz MUST emit its own preemption check calls at loop back-edges and function call sites per the design doc, using a runtime helper `ynz_rt_check_preempt()` that calls into Tokio's cooperative yield mechanism. Tokio's budget system alone is insufficient for pure-CPU loops. The design-doc time quantum (~10ms) maps to a Tokio budget override at runtime init. — This is resolved in M1 P0 research spike; if the cost of check insertion is too high, the design doc's loop back-edge requirement gets relaxed with explicit rationale.

- **State machine transformation in `ynz-codegen`**: Functions containing `wait` are transformed into state machines in LLVM IR. The state machine struct holds the function's live variables across `wait` points. Each `wait` point desugars to a Tokio-compatible `waker.wake()` + `Poll::Pending` return from the state machine's `resume()` function. The transformation happens in `crates/ynz-codegen/src/emit.rs` — a new `lower_function_with_waits()` path parallel to the existing `lower_function()`. — Rationale: Yinz can't use Rust's `async`/`await` desugaring (Rust-specific compiler feature; Yinz compiles to C-ABI LLVM IR). But Yinz CAN emit the exact state machine IR that Rust's async desugars to, interfacing with Tokio's `Future` polling protocol via the `ynz_rt_spawn(fn_ptr, ctx)` bridge.

- **`ynz_rt_spawn` ABI bridge**: The runtime exposes `extern "C" fn ynz_rt_spawn(fn_ptr: *const fn(*mut c_void) -> i32, ctx: *mut c_void)` — a C-ABI function that wraps the Yinz state machine in a Tokio `Future` and spawns it onto the work-stealing scheduler. The `ctx` pointer carries the state machine's heap-allocated frame (owned by the task; dropped when the task completes). — Design detail: resolved in v0.3-M1 P0 research spike.

- **`background` semantics in M1 — blocking thread pool, not I/O scheduler**: In v0.3-M1, `background fn(args)` compiles to `ynz_rt_spawn_blocking(fn_ptr, args_copy)` — using Tokio's `spawn_blocking` (the blocking thread pool), NOT `spawn` (the I/O work-stealing scheduler). Why: M1 ships before state machine desugaring (M2). A background function that contains `wait` calls internally would run those `wait` calls synchronously (blocking behavior), which must NOT run on the I/O scheduler threads (it would starve I/O completions). `spawn_blocking` routes to a separate OS-thread pool designed for blocking work. After M2 ships state machines, background functions whose call graph is may-block get routed to the I/O scheduler; CPU-bound functions stay on spawn_blocking (per the CPU/I/O routing decision). The M1 value statement: "`background fn()` runs on a separate thread from main — the main thread continues immediately." True for ALL function types in M1; I/O-blocking functions don't block main, they block their blocking thread.

- **`background` ownership inference timing = M3**: [`docs/internal/implementation/IMP-concurrency.md`](../../../../docs/internal/implementation/IMP-concurrency.md) "Compiler inference for ownership" locks: value unused after background call → auto-`.give`; value used after → auto-`.copy`. This auto-inference requires the call-graph analysis that ships in M3 (to determine whether a value is used in a transitive path after the `background` call). Until M3 ships, users MUST write `.give` or `.copy` explicitly at every `background fn()` call site — the v0.1 explicit-annotation typeck check (`check.rs:1216`) stays in place through M1 and M2. M3 execution plan must specify "lift auto-give/copy inference for background call sites" as a named phase step.

- **`wait`-in-expression semantics — ⚠️ SUPERSEDED (twice), DO NOT IMPLEMENT**: This decision claimed `wait a() + wait b()` schedules both concurrently and that "explicit `wait` is a hint ('I need this result'), not an ordering constraint." **Both halves are dead.** (1) `wait` in a sub-expression is a compile error (`SubExprSuspendViolation`, a permanent positional constraint — see "Kept M2 positional guards" above). (2) Explicit user-written `wait` **IS an ordering barrier**, not a non-ordering hint — locked 2026-06-05 (Model A). The authoritative, un-confusing statement of what `wait` means lives in [`docs/internal/implementation/IMP-concurrency.md`](../../../../docs/internal/implementation/IMP-concurrency.md) → "Suspension vs. Ordering — What's Automatic and What `wait` Does (LOCKED 2026-06-05)": suspension is auto (never typed); independent ops (reads + different-resource writes) auto-parallelize; data-dep + same-resource ownership auto-order; `wait foo()` forces a causal order between otherwise-independent operations. This bullet is retained only as a tombstone so the dead framing isn't re-derived.

- **Auto-`wait` insertion scope (M3) = suspension correctness only, not ordering**: M3 auto-inserts `wait` at call sites where the callee is may-block AND the call site doesn't already have an explicit `wait`. This ensures the compiler-generated suspension point prevents blocking the I/O scheduler thread. This is DISTINCT from user-written `wait` for ORDERING: `wait chargePayment(order)` before `sendConfirmationEmail(order)` where the user needs sequential side-effect ordering. Auto-inserted `wait` does NOT guarantee ordering between independent may-block calls — those run concurrently. The compiler only inserts `wait` to ensure the function body can suspend correctly; ordering is the user's responsibility via data dependencies or explicit `wait`. — Consequence: `M3.auto-wait` and `M3.auto-parallelize` are the SAME feature: auto-`wait` + auto-`background` for independent groups. They ship together, not as separate passes.

- **Auto-SoA decision criteria** (from [`docs/internal/scratchpad/SCRATCH-future-auto-soa.md`](../../../../docs/internal/scratchpad/SCRATCH-future-auto-soa.md)): SoA layout applied when: (1) array length > SIZE_THRESHOLD entries at the analysis-time proof point — initial constant is 64; **M5** P0 must run a benchmark spike (workload: physics-update loop over `array<Player>` with `x`/`y` field access, measured on the CI machine) and either confirm 64 or update with a new constant + benchmark evidence, (2) hot loop accesses ≤ 2 fields, (3) no FFI export of the array, (4) the shape has NO `lend self` methods that access more than the hot-loop fields (if multiple fields are accessed via mutable methods, SoA splits what the method expects to be contiguous — suppress transform for that shape). ⚠️ **CORRECTED 2026-06-05→2026-07-02 (M4/M5 split note)**: "Codegen-only change" is now known WRONG — recon at M4 planning time found `array<Shape>` has no contiguous field storage; SoA needs a new array representation. Tier 3 lint suggestion surfaces the transform decision. Debugger integration: **DEFERRED OUTRIGHT (Patrick-signed four-field deferral, 2026-07-03)** via `[[deferred_tooling_feature]] dap-soa-unified-view` — supersedes the earlier "best-effort, defer if DAP work exceeds 3 phases" framing; zero DWARF/debug-info substrate exists in the compiler, so even a minimal DAP integration would be a bespoke non-DWARF hack. Revisit trigger: DWARF emission ships or a debugging-tooling milestone is scheduled. **Owning milestone: M5** (split from M4 2026-07-02 — see roadmap §Milestone 5). **Execution plan: `2026-07-03-v0-3-m5-auto-soa`** (planned 2026-07-03; folds in `v0-3-m3c-array-by-value` by-value element storage — ONE array representation owns both, SoA rides it as a layout variant).

- **Work-stealing scheduler thread count = CPU cores**: Tokio's default thread count is `num_cpus::get()`. No user-facing configuration in v0.3 (that's a potential v1.x lint-config surface if demand surfaces). CPU-bound tasks auto-route to `tokio::task::spawn_blocking` (Tokio's blocking thread pool) per the call-graph analysis.

- **Atomic ordering for channels and auto-Arc = acquire-release default**: Per [`docs/internal/implementation/IMP-no-function-coloring.md`](../../../../docs/internal/implementation/IMP-no-function-coloring.md) "Atomic Ordering Default" (locked pre-v0.2). Channel `.send()` / `.receive()` operations and auto-Arc reference-count operations use acquire-release memory ordering. Sequential-consistency is not the default. The user-facing opt-in API for the seq-cst global-order case is named TBD at M4 execution-plan time; it ships as a `[[deferred_language_feature]]` entry if the API surface is too broad for M4. — Consequence: code relying on seq-cst assumptions without the explicit opt-in WILL produce wrong results under acquire-release. The no-jargon user surface does not expose `memory_order_*` — the opt-in is via a named method (candidate: `.withGlobalOrdering()` per design doc, final name TBD at M4 P0).

- **`background` task panic behavior = best-effort discard**: Per [`docs/internal/implementation/IMP-concurrency.md`](../../../../docs/internal/implementation/IMP-concurrency.md) "Error Cancellation." When a background task panics, the panic does NOT propagate to the spawning scope — it's caught by the Tokio task wrapper and logged (or silently discarded if no log module is available). The spawning scope never sees the panic; it only sees the join barrier complete. This matches Tokio's `JoinHandle::await` behavior on panic (returns `Err(JoinError)`), but Yinz's fire-and-forget form does NOT expose a result — the panic information is discarded. The supervisor pattern ([`docs/internal/scratchpad/SCRATCH-future-supervisor.md`](../../../../docs/internal/scratchpad/SCRATCH-future-supervisor.md)) is deferred to a future version. — Consequence for v0.3: if a `background` task panics, the program continues. This is documented in the error gallery and WHAT/WHAT-INSTEAD/WHY for the `background` keyword hover in the LSP.

- **Cross-implementation consistency test harness**: All execution plans from M1 onward MUST include a fixture-based semantic equivalence test that runs every `examples/pirates-roster/` program and every `crates/ynz-codegen/tests/` fixture in BOTH (a) a forced-sequential mode (`ynz build --no-auto-parallel` flag, added in M1) and (b) default auto-parallel mode, and asserts identical stdout/stderr/exit-code. This flag ALSO serves as the regression guard if auto-parallelization introduces a scheduling bug. `--no-auto-parallel` must be supported from M1 onward as a mandatory test utility even if it only matters from M3 when actual auto-parallelization begins.

- **Reordering verification technique**: Property tests for "lend-sequenced operations are never reordered" use the `--no-auto-parallel` flag to establish the sequential baseline, then run under `--deterministic-schedule` mode (a separate Tokio runtime flag that uses a round-robin scheduler for reproducible task ordering). Concrete test: a program where operation A writes to a binding and operation B reads from it — B must always see A's write. If the ownership system allows both to compile, the `lend`/`share` semantic guarantees ordering; if both compile correctly the ownership system would have rejected the concurrent form already. The actual property test is: any program that compiles under M3+ produces identical output in `--no-auto-parallel` and default modes.

- **Feature Registry Entries mandate for all v0.3 execution plans**: Each v0.3 milestone execution plan MUST include `### Feature Registry Entries` per [`.claude/rules/plan-invariants.md`](../../../rules/plan-invariants.md). Known entries across the milestone: M1 may add `[[deferred_tooling_feature]]` entries for background-handle-form; M3 MUST activate the `wait_points` muted-hint domain (currently in [`registry/features.toml`](../../../../registry/features.toml) as protocol-only); M4 MUST activate the `channel_capacity` muted-hint domain + add Tier 3 lint rules `cross-thread-fields-not-padded` and `prefer-yielding-sleep` to the registry (M4 shipped exactly these two — the only `[[lint_rule]]` entries in [`registry/features.toml`](../../../../registry/features.toml) as of 2026-07-03); `array-using-soa-layout` is REASSIGNED to **M5** with the 2026-07-02 M4/M5 split (it rides M4's `[[lint_rule]]` mechanism; ships via plan `2026-07-03-v0-3-m5-auto-soa`, Phase 7).

- **Required Pre-Work before v0.3-M1 starts — DECIDED 2026-05-21**: Two pre-work items ship as dedicated PRs before M1 begins:
  1. **Parser infinite-loop bug**: `v0_2_m1_errors.ynz` and `m1_errors.ynz` cause the parser to hang on error recovery. **Decision: fix it** (dedicated PR before M1 P1). Workaround exclusion is duct tape per `no-duct-tape.md` Rule 10.
  2. **M8 cross-file typeck**: ~~must be APPROVED and EXECUTED before M3 starts~~ — **✅ RESOLVED / STALE (corrected 2026-06-01 by source verification).** Cross-file type-checking ALREADY SHIPPED: `check_query` resolves imports via `module_signatures_query` → `resolve_imports` (the `Item::ImportDecl(_) => {}` arm at `check.rs:342` is an *intentional* no-op — resolution happens upstream). Passing tests: `m8_combo_modules_*`, `v0_3_m2_cant_infer_cross_module`. The original rationale (LSP-only resolution) was true at some earlier point but is no longer accurate. The ONLY remaining cross-module gap is `suspends`-flag propagation, which is M3b's narrow task on the existing foundation — NOT a separate M8 prerequisite. See "M3 SPLIT" Architectural Decision, corrected assumption #1.

- **Auto-SoA debugger integration — DECIDED 2026-05-21, RETARGETED 2026-07-02, DEFERRED OUTRIGHT 2026-07-03**: DAP integration ("show unified Player view when inspecting SoA-laid-out array in lldb/gdb") was originally M4-REQUIRED, then retargeted to M5 with the 2026-07-02 split. **Final disposition (Patrick-signed four-field deferral, 2026-07-03, at M5 plan time):** deferred outright via `[[deferred_tooling_feature]] dap-soa-unified-view` — NOT in M5, not best-effort. M5 plan recon grep-verified that zero DWARF/debug-info substrate exists anywhere in the compiler; even one phase of DAP would be a bespoke non-DWARF hack (duct tape), or would hold a perf milestone hostage to building DWARF emission from scratch. The stale "escalate if >3 phases / do NOT pre-emptively defer" framing assumed a DWARF substrate to build on; there is none. Revisit trigger: DWARF emission ships or a debugging-tooling milestone is scheduled. Full four-field record: plan `2026-07-03-v0-3-m5-auto-soa` §Future Requirements #1.

- **Sleep intrinsic naming + blocking-vs-yielding teaching — DECIDED 2026-06-01, Patrick**: the two sleep intrinsics shipped in M1/M2 are mis-named with programmer jargon. Locked corrections:
  1. **RENAME (cheapest now — no external users; do at M3 kickoff or as a standalone PR):** `sleepAsync` → `sleep` (the yielding/cooperative sleep, the default, used as `wait sleep(ms)`); `sleepMs` → `sleepBlocking` (the thread-holding sleep, the labeled exception). WHY: "Async" is exactly the function-coloring jargon Yinz hides (Golden Rule 12 + `vocabulary.md` — Yinz uses `wait`, never `async`/`await`); `wait sleepAsync` says "suspend" twice; and `sleepMs`/`sleepAsync` mix naming bases (unit vs mechanism). The yielding form is the idiomatic default (`sleep`), the blocking form names its danger (`sleepBlocking`) per `stdlib-design.md` Rule 1. Scope: [`registry/features.toml`](../../../../registry/features.toml), `M2_MAY_BLOCK_INTRINSICS` in `emit.rs`, typeck dispatch, the `v0_3_m*` fixtures + insta snapshots, hover/CHANGELOG text.
  2. **NON-KERNEL TEACHING — `prefer-yielding-sleep` Tier 3 lint — SHIPS IN M4 (REQUIRED).** When `sleepBlocking(ms)` is used in a non-kernel program, a Tier 3 lint (yellow squiggle, suggestion, dismissable — NOT an error) nudges toward `wait sleep(ms)`. WHAT/WHAT-INSTEAD/WHY: "wait sleep frees the thread to run other tasks; sleepBlocking holds it idle. Use sleepBlocking only when you need to hold the thread (e.g. --kernel mode)." **Belongs in M4, not earlier, for TWO reasons:** (a) M4 builds the `[[lint_rule]]` registry mechanism anyway (`cross-thread-fields-not-padded`; M5's `array-using-soa-layout` rides the same mechanism later, per the 2026-07-02 split) — `prefer-yielding-sleep` rides the same machinery; building it as a one-off check before that infra exists is duct tape per `no-duct-tape.md`. (b) M4's `background` handle-form removes the ONE legit non-kernel blocking-sleep pattern — the `sleepMs(N)` main-thread keepalive (used today in `v0_3_m2_concurrent_waits_proof.ynz`, whose comment already says "M4 handle-form will eliminate the need for this"). So "almost 0 non-kernel uses" only becomes true AFTER M4; linting earlier would nag a still-legitimate pattern. — Must NOT be an error (rare legit uses + respect explicit intent per `auto-promotion.md`); must be a suggestion.
  3. **KERNEL TEACHING — `KernelModeRejectsWait` WHAT-INSTEAD amendment — ships whenever `--kernel` mode is wired to EMIT (post-v0.3 kernel milestone; the diagnostic is registry-reserved but wired in 0 code sites today).** When `wait sleep(ms)` (or any suspension point) is used in `--kernel` mode → compile error per `no-runtime-mode.md`. The existing `KernelModeRejectsWait` template tells you what you CAN'T do but not what to do instead — amend its `what_instead` to redirect: "Use `sleepBlocking(ms)` — it pauses without needing the scheduler." Golden Rule 11 WHAT-INSTEAD must be actionable.
  - Symmetry locked: **kernel mode pushes you toward `sleepBlocking` (error on `wait`); normal mode pushes you toward `wait sleep` (lint on `sleepBlocking`).** Design note: [`docs/internal/implementation/IMP-no-function-coloring.md`](../../../../docs/internal/implementation/IMP-no-function-coloring.md) "Sleep Intrinsics" + [`docs/internal/implementation/IMP-no-runtime-mode.md`](../../../../docs/internal/implementation/IMP-no-runtime-mode.md).

---

## Open Architectural Questions

These BLOCK the milestones listed next to them. They must be resolved (and the resolution locked into the execution plan) before coding the blocking milestone starts.

- **State machine ABI with Tokio's `Future`**: The exact layout of the generated LLVM IR state machine and how it interfaces with `tokio::task::spawn` / `Poll<Output>` must be locked before M2 coding begins. A research spike in M2 P0 prototypes the simplest possible `wait`-able function (one `wait` point, no branching) and validates end-to-end execution. Blocks: v0-3-m2-wait-and-state-machines.

- **`background` ownership validation at spawn boundary**: `background fn(args)` requires that `args` are either `.give` (ownership transfer) or `.copy` (trivially copyable). The typeck check exists in v0.1 (`check.rs:1214`). Does the v0.3 auto-give inference extend the check, or does the explicit check stay as-is? Blocks: v0-3-m1-runtime-and-background M1 P1.

---

## Risks

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| State machine LLVM IR is more complex than anticipated; M2 spans multiple sessions | High | Medium | M2 P0 is a dedicated spike with a clear accept/reject gate. If the simplest case takes >3 days, we revisit the approach before committing to full implementation. |
| Tokio ABI bridge has unsound behavior on task cancellation or stack overflow | Medium | High | M1 P0 prototype covers: spawn + join (verify main continues before fn completes), spawn + drop (cancel — panic discarded per best-effort design), spawn + panic (runtime continues; panic discarded). Each path must be verified before M1 ships. |
| May-block analysis over-approximates; every function is marked may-block | Medium | Medium | Ground truth: stdlib I/O intrinsics are the source of may-block. The upward propagation through user code is a fixed-point computation. Conservatively correct is acceptable; false positives add `wait` overhead but don't break correctness. Measurement: count `may_block = true` function ratio in `pirates-roster`; flag if >30% of user functions are over-marked. |
| Auto-parallelization silently reorders side-effect-ordered operations | Medium | High | Auto-parallelize only fires for operations with NO shared mutable state (ownership typeck is the safety gate). Property test technique: `--no-auto-parallel` produces identical stdout/stderr/exit-code to default-parallel mode for EVERY fixture in `examples/` and `crates/ynz-codegen/tests/`. Any divergence = bug. Ordering semantics are per the "auto-`wait` scope" decision: user must write explicit `wait` for ordered side effects; compiler only auto-parallels provably-independent operations. |
| `m8-typeck-cross-file-resolution` is a hard prerequisite for may-block analysis | **Decided** | High | **M8 required — ships before M3 P2.** Decision locked 2026-05-21 (see Required Pre-Work in Architectural Decisions). Schedule M8 immediately after v0.2.0 ships. |
| Auto-SoA silently breaks shapes with `lend self` methods | Low | Critical | Per Architectural Decisions: SoA suppressed for any shape with `lend self` methods that access more than the hot-loop fields. Detection: **M5** execution plan (SoA's owning milestone as of the 2026-07-02 split) must enumerate all shapes in `examples/` + test fixtures and verify SoA candidates are correctly filtered. `--no-auto-parallel` mode also disables SoA for the cross-implementation consistency test. |
| `background` + auto-Arc wraps a `lend`-borrowed value across thread boundary | Low | High | Ownership typeck already prevents `share` across `background` boundary (`check.rs:1216`). `lend` (exclusive mutable access) also cannot cross a thread boundary — the compiler must reject `background fn(lend val)` with "cannot lend across thread boundary; use `.give` or `.copy`." M1 typeck must cover this. |
| `wait` inside `for` loop auto-paralelizes incorrectly | Low | High | Loop iterations remain sequential (per [`docs/internal/implementation/IMP-concurrency.md`](../../../../docs/internal/implementation/IMP-concurrency.md)). `wait` inside a `for` body is N sequential waits. The dependency graph analysis MUST NOT auto-parallelize across loop iteration boundaries. M3 integration test: `for (item in items) { wait process(item) }` — assert single-threaded sequential execution. |
| Auto-SoA layout breaks serialization | Low | High | No FFI in v0.3 (FFI is v2+). Serialization codegen is compile-time-generated per [`.claude/rules/stdlib-design.md`](../../../rules/stdlib-design.md) Rule 6 and reads field layout from the compiler's representation. **REFRAMED 2026-07-03 (Patrick, at M5 plan time — was "assert a serialize/deserialize roundtrip"):** no serializer exists until v0.8, so a roundtrip test is untestable vacuity today. Instead, a forward-compat design obligation on **M5**: the SoA representation MUST expose layout metadata (the `LayoutDecision` artifact, exercised by the Tier 3 lint hover so it exists as tested code) sufficient for v0.8's compile-time serializer codegen to reconstruct unified values, plus a design note in IMP-collections. See plan `2026-07-03-v0-3-m5-auto-soa` (risk E10, Design-Doc Alignment Divergence 2). |
| Compile-time cost of analysis passes exceeds acceptable limits | Low | Medium | Analysis passes (may-block, dependency graph, SoA candidate) are profiled in each milestone's verification step. Target: <10% increase in `ynz build --release` wall-clock on `examples/pirates-roster/`. |
| Channel send to a closed channel / dropped receiver | Medium | Medium | M4 must specify behavior: `send()` to a dropped receiver returns a typed `errors` result — the user handles or propagates. NOT a silent drop. Design detail locked in M4 P0. |

---

## Milestones

### Milestone 1: v0.3-M1 — Runtime Bootstrap + Working `background` — multi-session
**Value delivered**: `background process(data)` runs on a separate thread — main continues immediately. Previously it ran synchronously and main waited for it to complete (`emit.rs:3089` stub). After M1: `background saveAnalytics(event)` after `return response` returns the response before analytics runs. `wait` calls inside the background function still block that blocking-pool thread (state machines are M2), but they don't block main. The program is correct; background I/O work may take longer than optimal until M2.
**Execution plan**: `v0-3-m1-runtime-and-background` (status: planned)
**Depends on**: nothing — first up. Pre-condition: v0.2.0 released; parser infinite-loop bug fixed (Required Pre-Work above).
**Rough scope**:
- Add `tokio` + `num_cpus` to `ynz-runtime` Cargo.toml (bundled in `libynz_rt.a`)
- `ynz_rt_init()` boots Tokio multi-thread runtime + blocking thread pool; called from generated `main`
- `ynz_rt_spawn_blocking(fn_ptr, ctx_ptr, ctx_size)` C-ABI bridge via `tokio::task::spawn_blocking` (M1 routes ALL background tasks to blocking pool; I/O routing upgrades in M3)
- `ynz_rt_check_preempt()` C-ABI helper emitted at loop back-edges + function call sites
- `ynz_rt_shutdown()` for clean teardown after `main` returns
- `ynz build --no-auto-parallel` flag: forces sequential execution mode (required for the cross-implementation consistency test harness from M1 onward)
- `ynz-codegen` emits `ynz_rt_spawn_blocking(...)` for `Expr::Background`; `ynz_rt_check_preempt()` at loop back-edges
- Large-copy warning: `background fn(largeStruct)` where `largeStruct` is used after spawn → compile warning "copying N bytes into background task — consider passing ownership" (per [`docs/internal/implementation/IMP-concurrency.md`](../../../../docs/internal/implementation/IMP-concurrency.md) IDE warning on large copies)
- Integration tests: main continues before background fn completes (timing-verified); `.give` and `.copy` accepted; `.share` rejected (`check.rs:1216`); `--no-auto-parallel` produces identical output to sequential baseline
- Demo: `examples/pirates-roster/entrypoint.ynz` extended with `background` section; `examples/primantis-orders/v0_3_m1_errors.ynz`
- **Teaching surface (REQUIRED in M1, not deferred)**:
  - Registry: update `wait` and `background` keyword hover docs in [`registry/features.toml`](../../../../registry/features.toml) — current text says they run sequentially, which is now wrong
  - New compile errors with WHAT/WHAT-INSTEAD/WHY: `background fn(lend val)` → "cannot lend across thread boundary; use `.give` or `.copy`"
  - Large-copy compiler warning: WHAT/WHAT-INSTEAD/WHY text locked before M1 ships
  - VSCode extension: bump version, add `background-concurrent.png` screenshot
- **Registry entries**: one `[[deferred_tooling_feature]]` entry for `background-handle-form`
**Ships via**: `/pr` per phase, `/release` for `v0.3.0-m1` tag

### Milestone 2: v0.3-M2 — `wait` Suspension + State-Machine Codegen + Intra-Unit May-Block Inference — multi-session
> **⚠️ BOUNDARY CORRECTED 2026-05-31 (see Architectural Decisions "M2/M3 boundary correction").** This milestone originally deferred ALL may-block analysis to M3 and used a runtime `block_on` bridge as the M2 stopgap. That bridge both crashed at runtime (the M2 Phase-5 HALT) AND contradicted [`docs/internal/implementation/IMP-no-function-coloring.md`](../../../../docs/internal/implementation/IMP-no-function-coloring.md)'s documented no-coloring model. The intra-compilation-unit may-block analysis is LOAD-BEARING for a correct state-machine layer and therefore moves M3→M2; M3 keeps cross-module propagation + auto-parallelization.
**Value delivered**: Functions that (transitively) reach a suspension point actually suspend and resume instead of blocking — and you do NOT have to write `wait` for that to happen. Per [`docs/internal/implementation/IMP-no-function-coloring.md`](../../../../docs/internal/implementation/IMP-no-function-coloring.md) ("No Function Coloring"), the compiler infers which functions are state machines from the call graph and shows the inferred `wait` as a muted hint; every caller of a suspender is itself a state machine and uses inline poll-and-yield — no function coloring, no thread-holding bridge. This makes existing `wait` code both correct AND fast (Rust-async-level efficiency).
**Execution plan**: `v0-3-m2-wait-and-state-machines` (status: planned)
**Depends on**: v0.3-M1 (Tokio runtime must be running before state machines can suspend into it)
**Rough scope**:
- M2 P0: research spike validated against the REAL compiler — compile real `.ynz` through `./target/debug/ynz`, NOT hand-written Rust/IR. (The original spike validated a hand-written model that diverged from the emitted codegen → false ACCEPT → the Phase-5 HALT. Never re-introduce that.)
- New `lower_function_with_waits()` codegen path in `emit.rs`; transforms any function the may-block analysis marks as suspending into an LLVM state machine struct + `resume(frame, waker)` function
- **Intra-compilation-unit transitive may-block analysis (NEW — moved from M3):** a call-graph fixpoint with the may-block set = `{sleepAsync}` (M2's only suspension source). A function is a state machine iff it transitively reaches a may-block call. This eliminates the runtime bridge entirely — because every caller of a suspender is itself a state machine, every suspending call is inline poll-and-yield; the only `block_on` is the legitimate `main→entrypoint` top-level driver.
- **Frame composition (zero-cost-abstraction allocation model):** a child state machine's live state composes INTO the parent's frame (computed at compile time), like Rust nests `async fn` futures. Heap-allocate only at `background` spawn points and at recursion points (break the infinite-size cycle, à la Rust's `Box::pin`). NOT a heap allocation per state-machine call — that would miss the design's "low memory, fast spawn" target.
- **Typed return slot:** value-returning state machines (`-> int` / `-> string` / `-> T errors` with `wait`) thread the return value through a properly-typed frame slot (holds i64 / pointer / the `{i64,i64}` errors ABI).
- **Can't-infer cases → clean compile error (per the design's "externals are on the user"), NOT a bridge:** cross-module calls before M8 lands; dynamic dispatch through a `dynamic Contract` vtable; FFI (none in M2 yet). The compiler emits a teaching error directing the user to make the boundary explicit; M3 lifts the cross-module case via M8 + cross-module propagation.
- `Expr::Wait(inner, _)` desugars to inline poll-and-yield: poll `inner`'s resume fn forwarding the same `waker_ctx` → ready: read its typed return slot + continue; pending: return `Poll::Pending` up to the driver.
- Integration tests (ALL validated through `./target/debug/ynz run` on real `.ynz`): one `wait`, sequential `waits`, `wait` inside `if`, NESTED state machines (SM-from-SM), `background` from inside a state machine, value-returning + errors-cascade through suspension, cancellation-no-leak.
- **Boundary note (what stays M3+):** CROSS-MODULE may-block propagation (needs M8 cross-file typeck), auto-parallelization of independent statement groups, and I/O auto-`wait` insertion (when stdlib I/O intrinsics ship and join the may-block set). M2 builds the analysis ENGINE over the `{sleepAsync}` set; later milestones feed it more may-block sources without changing the engine.
- **Teaching surface (REQUIRED in M2, not deferred)**:
  - New compile warning WHAT/WHAT-INSTEAD/WHY: "`wait` on a function that can never block — the `wait` has no effect" + suggestion to remove it
  - New compile errors for `wait` on bad expression types (non-call) if any surface
  - `wait_points` domain: protocol handler exists from v0.2-M5; M2 implementation spike must validate the handler fires correctly for explicit `wait` before M3 adds auto-`wait`
  - Demo: `pirates-roster` mock I/O section showing `wait` suspending; `v0_3_m2_errors.ynz`
  - VSCode extension: bump version, add `wait-suspension.png` screenshot showing the coroutine suspending behavior
**Ships via**: `/pr` per phase, `/release` for `v0.3.0-m2` tag

### Milestone 3a: v0.3-M3a — Suspension Codegen Completion — multi-session
> **⚠️ SPLIT 2026-06-01 from the old single "M3".** This is the codegen-completion half — the substrate M3b's analysis builds on. See the "M3 SPLIT into M3a + M3b" Architectural Decision above for the full rationale + the three corrected stale assumptions.
**Value delivered**: `wait` works in every position that previously compile-errored. A `let` local can be declared before a `wait` and used after it; a `wait` can appear inside a `while`/`for` loop body and inside a `match` arm. Existing `wait` code that hit the M2 `LocalCrossesWait` / `WaitInsideLoop` errors now compiles AND runs correctly (frame-backed mutable locals: flush-before-suspend + reload-at-resume; loop counter/iterator preserved across the pause). Also lands the decided `sleepAsync`→`sleep` / `sleepMs`→`sleepBlocking` rename and re-documents the two permanently-kept positional guards. Composed-frame allocation model preserved (one `ynz_alloc` per task tree; loops do NOT allocate per iteration).
**Execution plan**: `v0-3-m3a-suspension-codegen` (status: planned)
**Depends on**: v0.3-M2 (the state-machine substrate this completes). NO dependency on M8 — cross-file typeck already shipped (see corrected stale assumption #1 in the split decision).
**Rough scope**:
- P0 — Doc lockdown + sleep rename: `sleepAsync`→`sleep`, `sleepMs`→`sleepBlocking` across registry/intrinsics.rs/emit.rs/check.rs/may_block.rs/inlay_hint_passes.rs + ~15 fixtures + insta snapshots + hover/CHANGELOG. Reword the two KEPT guards' diagnostics (`SubExprSuspendViolation` check.rs:506-509, `MutualSuspensionCycle` queries.rs) from "ships in v0.3-M3" to the design rationale; add a "permanent positional constraints" note to [`docs/internal/implementation/IMP-concurrency.md`](../../../../docs/internal/implementation/IMP-concurrency.md). Audit registry `[[deferred_*]]` entries naming the four guards; reclassify (lifted-two → shipped; kept-two → permanent, not deferred).
- P1 — Frame-backed mutable locals (lift `LocalCrossesWait`, remove guard at check.rs:460-477): repurpose the existing `locals_crossing_wait` analysis (check.rs:4880) from error-driver to codegen-driver; allocate frame local slots (the `load_local_slot`/`store_local_slot` helpers + offset-32 locals section already exist — used only for params today); store-at-def + store-after-mutation + load-after-resume; grow composed frame size. Typed slots: int/bool (i64), pointer string/shape/array (ptr_to_int), number/float (bitcast).
- P2 — While-loop suspension (lift `WaitInsideLoop`, while case): loop induction state becomes a frame-backed local; resume_point re-enters the loop body mid-iteration. Highest-risk codegen phase.
- P3 — For-loop + match-arm suspension (lift `WaitInsideLoop`, remaining cases): for-over-array/map iterator state frame-backed; match-arm suspension (closer to the already-working `if` case). Remove guard at check.rs:434-445; retire `wait_in_loop_or_match_body`.
- P4 — Demo + error gallery + teaching + verification: extend `pirates-roster` with a real loop-with-`wait` + local-crossing-`wait` section; move the now-compiling cases OUT of the `primantis-orders` error gallery; update the two KEPT-guard fixtures + snapshots to the reworded diagnostics; `--no-auto-parallel` identical-output check (still a no-op until M3b but the harness must pass); jargon_audit (sleep rename REMOVES `Async` jargon); cross-impl consistency on all new fixtures.
- **Teaching surface (REQUIRED in M3a)**: reworded kept-guard diagnostics (WHAT/WHAT-INSTEAD/WHY, no false "ships in M3"); updated `sleep`/`sleepBlocking` hover docs in registry; demo + error-gallery extensions; VSCode version bump (no NEW IDE surface — no new muted-hint domain or lint in M3a; that's M3b — so the bump carries only the renamed-intrinsic hover text, no new screenshot required).
- **NOT in M3a** (→ M3b): cross-module `suspends` propagation, the dependency-graph auto-parallelize pass, auto-`wait` insertion, `background` give/copy inference, CPU/IO routing, `wait_points`/`background_routing` IDE hints firing.
**Ships via**: `/pr` per phase, `/release` for a `v0.3.0-m{next}` tag (m-number tracks ship order)

### Milestone 3c: v0.3-M3c — Full Variable Shadowing Parity (per-binding-slot identity) — single-session (1-2 sessions)
> **Added 2026-06-03 (Patrick) out of the M3a Phase 2 shadow saga.** M3a Phase 2 established that Yinz ALLOWS variable shadowing ([`docs/internal/implementation/IMP-linting.md`](../../../../docs/internal/implementation/IMP-linting.md) `shadowed-variables` lint = warn, not error) and made it work in NON-async functions (entry-block alloca + restore-all lexical scope). It does NOT yet work inside async (state-machine) functions: the frame-slot system keys crossing-local slots by NAME, so two same-named bindings around a suspension share one slot → the second write clobbers the first. M3a P2 round 6 tried to partially lift this (allow non-crossing shadows in SM functions) and hit the EXACT silent-wrong-answer the deferral doc predicted (`reload_params_from_frame` writes the resumed value into the shadow's alloca, leaving the real binding uninitialized → garbage read); round 7 reverted to the documented conservative clean-reject. M3c does the COMPLETE fix.
>
> **📌 DOCUMENTED DEFERRAL — Patrick-approved 2026-06-19 (post-v0.3.0 hotfix).** Per `no-duct-tape.md` §When To Document Instead Of Fix (4 fields): **WHAT** is deferred — full shadowing parity across `wait`; v0.3.0 ships the safe `ShadowsCrossingLocal` clean-reject instead. **WHY** — the clean-reject is a papercut (rename your variable), NOT a correctness or perf gap; the efficiency mission prioritizes M3g (mandatory perf) + M4 (release) first. **COST to fix later** — 1-2 sessions (per-binding-slot identity keying; M3a P2 proved the partial fix silently miscompiles, so the complete fix + the full shadow-matrix adversarial gate are required — see Rough scope). **TRIGGER** — schedule as a v0.3.x hotfix after v0.3.0 ships, or earlier on user demand for name-reuse around a suspension. **Anchor bug FIXED 2026-06-19**: `v0-3-m3c-shadow-parity` added to the front-matter `milestones:` list (was absent — couldn't be scheduled); the capability-ledger row now owns it. This is a tracked deferral with a named trigger, NOT an open-ended "someday."
**Value delivered**: A nested `let` may re-use a name already bound (param or local) anywhere — including across/around a `wait` inside an async function — and compiles correctly (Rust-level shadowing parity). Lifts the `ShadowsCrossingLocal` clean-reject; users no longer must rename inner bindings in async functions.
**Execution plan**: `v0-3-m3c-shadow-parity` (status: NOT YET PLANNED — run `/plan` when picked up; do not detail-plan now)
**Depends on**: v0.3-M3a (the suspension-codegen substrate + the non-async lexical-scope fix it shipped). Independent of M3b — can land before or after.
**Rough scope** (per `docs/internal/implementation/IMP-concurrency.md:301`, 1-2 sessions): assign each `let` declaration a unique binding ID (source span or monotonic counter); key crossing-local frame slots by binding ID rather than name; propagate ID-keyed resolution through the flush/reload + typeck layers so "same name, different slot" is distinguishable at every read/write site. Then remove the `ShadowsCrossingLocal` reject (params + locals) and amend [`docs/internal/implementation/IMP-concurrency.md`](../../../../docs/internal/implementation/IMP-concurrency.md) + retire the `shadow-crossing-local-support` registry deferral. **Adversarial gate is mandatory** — the M3a P2 history (6 fix-rounds, each a new shadow variant; round 6 a silent miscompile) proves this area is whack-a-mole-prone; the plan MUST include the full shadow matrix (param/local × crossing/non-crossing × if/while/match × wait-before/inside/after the shadow scope × multiple runs to catch garbage reads).
**Trigger to schedule** (`docs/internal/implementation/IMP-concurrency.md:303`): user demand for re-using a name around a suspension in a program that can't be restructured, OR proactively as concurrency polish. Cross-refs: `docs/internal/implementation/IMP-concurrency.md:241-305`, `registry/features.toml:1135` (`shadow-crossing-local-support`), [`.claude/todos.md`](../../../todos.md) "Soon" (per-binding-slot shadow support).
**Ships via**: `/pr`, `/release` for a `v0.3.0-m{next}` tag.

### Milestone 3b: v0.3-M3b — Cross-Module May-Block Propagation + Auto-Parallelization — **SHIPPED 2026-06-09 (v0.3.0-m5)**
> **⚠️ SPLIT 2026-06-01 from the old single "M3" (second half).** Built ON M3a's completed suspension substrate. The intra-compilation-unit may-block analysis ENGINE shipped in M2; M3b EXTENDS it across module boundaries and adds the dependency-graph parallelization pass. It does NOT build the engine from scratch. **Plan this AFTER M3a wraps** — do not detail-plan it now (requirements will sharpen based on what M3a's codegen surfaces).
**Value delivered**: Independent operations in a function body auto-parallelize without the user writing `background` or `wait`. The compiler's dependency graph analysis identifies statement groups with no shared mutable state and schedules them concurrently. May-block detection (built intra-unit in M2) now propagates ACROSS module boundaries, and the compiler inserts `wait` at I/O call sites automatically once stdlib I/O intrinsics exist and join the may-block set. The IDE's muted `wait` hint surface (per [`docs/reference/REF-ide-hints.md`](../../../../docs/reference/REF-ide-hints.md) `wait_points` domain) fires on real cross-module + I/O data.
**Execution plan**: `v0-3-m3b-auto-parallelization` (status: done — all phases shipped; cumulative opus gate 5/5; released as part of `v0.3.0-m5`)
**Depends on**: v0.3-M3a (the completed suspension substrate). M8 cross-file typeck is ALREADY DONE (corrected stale assumption #1) — not a blocker; M3b only needs to add cross-module `suspends`-flag propagation on the existing foundation.
**Rough scope**:
- Cross-module may-block propagation: EXTEND M2's intra-unit `may_block` fixpoint (salsa-tracked) to propagate across file/module boundaries via M8's compile-time cross-file typeck (M8 ships before M3 P2 — decided 2026-05-21). Also ADD the I/O stdlib intrinsics to the may-block set as they ship (M2's set was `{sleepAsync}` only). The fixpoint algorithm itself is M2's; M3 widens its inputs (cross-module edges + I/O intrinsics) and lifts M2's "cross-module call = clean error" stopgap.
- Auto-`wait` and auto-parallelize are the SAME pass: the dependency-graph analysis identifies independent statement groups; groups with may-block (suspending) callees overlap via **interleaved inline polling** (embed both sub-frames, interleave their inline polls on one thread — ZERO new runtime; the enclosing fn is already a state machine). **⚠️ CORRECTED 2026-06-05: the "groups with no may-block callees run on CPU pool" half is NOT in M3b.** Runtime verification showed pure-CPU overlap needs ACTUAL extra threads with a joinable handle, which the existing spawn ABI lacks (`ynz_rt_spawn`/`spawn_blocking` return void) — that is real new-runtime work and moved to its own milestone `v0-3-m3d-cpu-parallelization`. M3b's independence analysis is built class-agnostic so M3d reuses it (no double-build).
- IDE `wait_points` muted-hint domain: the `wait_points` registry domain (currently protocol-only) starts firing. Muted `wait` appears before I/O calls; hover tooltip: WHAT/WHAT-INSTEAD/WHY per Golden Rule 11.
- CPU-bound task routing: calls with no may-block transitive callees route to `tokio::task::spawn_blocking` (from M1) vs I/O scheduler (new in M3).
- `ynz_rt_spawn(fn_ptr, ctx)` (NON-blocking, I/O pool) added in M3 alongside existing `ynz_rt_spawn_blocking` — M3 is when the routing distinction matters.
- `background` ownership auto-give/copy inference: value unused after background call → auto-`.give`; value used after → auto-`.copy`. Required call-graph analysis ships here alongside may-block analysis.
- IDE routing hint: background tasks get muted `// routed to I/O pool` or `// routed to CPU pool` per auto-inference.
- `--no-auto-parallel` flag (added M1): verify that `--no-auto-parallel` and default-parallel produce identical stdout/stderr/exit-code on every `examples/pirates-roster/` fixture AND every `crates/ynz-codegen/tests/` fixture. This is the cross-implementation consistency gate.
- **Teaching surface (REQUIRED in M3, not deferred)**:
  - New `[[muted_hint_domain]]` registry entry: `background_routing` (Informational category: `// routed to I/O pool — calls db.fetch (may suspend)` / `// routed to CPU pool — no may-block calls in call graph`). Handler wired in LSP inlayHint.
  - `wait_points` domain fires for the first time with real auto-inserted `wait` data — validate hover tooltip text is correct WHAT/WHAT-INSTEAD/WHY (the domain was protocol-only until now)
  - New compile errors with WHAT/WHAT-INSTEAD/WHY: `wait` inside `for` body that the compiler cannot auto-parallelize → clear diagnostic explaining loop-iterations-sequential rule
  - `background` ownership auto-give/copy inference: when inference changes behavior, the IDE muted hint shows what was inferred (`.give` or `.copy`) at the call site using the existing `ownership_call_site` domain
  - Demo: `pirates-roster` auto-parallelization section with muted routing hints visible; `v0_3_m3_errors.ynz`
  - VSCode extension: bump version, add `auto-parallel.png` + `routing-hints.png` screenshots
**Ships via**: `/pr` per phase, `/release` for `v0.3.0-m3` tag

### Milestone 3d: v0.3-M3d — Pure-CPU Statement Parallelization — multi-session
> **⚠️ SPLIT 2026-06-05 from M3b.** The old M3b scope (and roadmap line 226) folded pure-CPU statement parallelization into M3b on the assumption it was a cheap extension of the I/O-overlap pass. Runtime verification during M3b planning (round-3 plan-review) proved otherwise: I/O-overlap works by INTERLEAVING suspensions on one thread (zero new runtime), but CPU work doesn't yield — overlapping it needs ACTUAL extra threads with a joinable handle, which the existing spawn ABI cannot provide (`ynz_rt_spawn`/`ynz_rt_spawn_blocking` both return void/fire-and-forget; a pure-CPU function isn't a state machine and can't await a join). That is genuinely-new runtime + codegen work sitting one wrong implementation choice from the `block_on` M2-HALT corpse — it deserves its own focused milestone, not a bolt-on to the already-HIGH-risk M3b auto-parallel phase.
**Value delivered**: Two (or more) independent heavy pure-CPU operations in a function body run on separate cores — real multicore speedup — with zero user syntax and byte-identical results to `--no-auto-parallel`. The "your code just got faster" win for compute-bound code, complementing M3b's I/O-overlap win.
**Execution plan**: `v0-3-m3d-cpu-parallelization` (status: NOT YET PLANNED — run `/plan` when picked up; do not detail-plan now)
**Depends on**: v0.3-M3b (reuses M3b's class-agnostic independence analysis — data-dep + transitive write-effect — built once there; M3d adds CPU candidacy + the runtime/codegen on top, NO rebuild). Independent of M3c/M4.
**Rough scope** (its `/plan` sharpens this):
- **New C-ABI runtime: a joinable `spawn_blocking` + a pollable join-handle** (today's spawn primitives return void). The enclosing function awaits the handle via the existing poll-yield protocol — never a synchronous block (sync join = the `block_on` corpse; deadlock-safe async join is mandatory).
- **State-machine promotion of non-suspending functions**: a function whose only concurrency is an independent CPU group must become a state machine so it can suspend at the join — new SM-lowering trigger (today only `sleep`-reaching functions are SMs), plus the composed-frame layout for a function with no other suspension points.
- **Conservative worth-it heuristic** (perf-only; correctness gate-guaranteed by `--no-auto-parallel` regardless): spawn a pure-CPU call onto the blocking pool ONLY when the callee transitively contains a loop/recursion (proxy for "does real work"); trivial/leaf/arithmetic runs inline, never threaded. The benchmark-calibrated cost threshold that refines this proxy is M3d's own perf-spike (same "ship a safe constant, calibrate with evidence" protocol as auto-SoA's SIZE_THRESHOLD; may share the M4 CI-machine run).
- **Blocking-pool exhaustion / deadlock safety**: N independent CPU groups (or recursion that spawns CPU groups) must not exhaust the blocking pool and deadlock on a synchronous join — adversarial gate mandatory.
- **Kernel-mode**: a CPU-only auto-parallel function under `--kernel` (no runtime/scheduler) — clean reject vs. inline-sequential fallback (decide at plan time).
- Demo (`pirates-roster` CPU multicore section) + error gallery + cross-impl consistency on all CPU fixtures.
**Explicitly NOT in M3d (owned elsewhere — per the Capability Ledger):** **mixed CPU+I/O overlap** (one CPU child + one I/O `wait` child in one group) is **M3g** — it is the FUSION of M3b's I/O inline-poll path and M3d's CPU join-poll path, not pure-CPU work, and the 4c verify-first pass (2026-06-14) proved fusing them is milestone-sized (deadlocks without a shared continuation). M3d locks the safe DECLINE (mixed groups → sequential, byte-identical) so M3g can't silently regress it. Also NOT in M3d: loop-body CPU groups (declined — future loop-placement work), multi-group ≥2 (declined — future per-group-slot work), unsupported return-classes (declined). M3d delivers pure-CPU statement parallelization and nothing else.
**Trigger to schedule**: after M3b ships, or whenever compute-bound multicore speedup is wanted. **Adversarial gate is mandatory** — the CPU join is M2-HALT-corpse-adjacent (one synchronous-join mistake reintroduces the crash); the plan MUST validate every join path through the real compiler + a blocking-pool-exhaustion stress fixture.
**Ships via**: `/pr` per phase, `/release` for a `v0.3.0-m{next}` tag.

### Milestone 3e: v0.3-M3e — Cross-Module FrameLayout Serialization — **SHIPPED 2026-06-07**
> **⚠️ SPLIT 2026-06-05 from M3b Phase 1 (Patrick decision).** M3b P1 lifted the cross-module-call stopgap and made the may-block analysis propagate `suspends` across module boundaries (solid). It also attempted the *codegen* to make cross-module suspending calls actually run. That codegen turned out to be the same magnitude as M3a's intra-module suspension codegen: the importer must reconstruct the imported callee's FULL `FrameLayout` (child sub-frame offsets, errors-capable `{i64,i64}` staging slot, shape-embed slot indices) to compose its own frame correctly. P1 shipped a SCALAR `composed_frame_size` shortcut that is lossy-by-design — it sizes the embed but corrupts the internal offsets, deterministically SIGILL-ing/aborting on: re-export/multi-level transitive chains (A→B→C where B re-exports a fn calling A's suspending export), shape crossing-locals inside a cross-module suspending callee, and errors-capable-transitive exports. The adversarial gate (J-A + code-reviewer, round 2) caught all three via live runs. Cramming an export-table ABI serialization change into a P1 fix loop was the wrong call; it deserves its own focused, adversarially-gated milestone — exactly how M3a's hard cases were deferred to M3c.
**Value delivered**: EVERY analyzable cross-module suspending call runs correctly — re-export chains, shape/number/float locals and returns across the boundary, errors×transitive — with no silent miscompile and no SIGILL. Lifts the P1 loud-reject guards.
**Execution plan**: `v0-3-m3e-cross-module-frame-serialization` (status: done — all 3 phases shipped)
**Shipped commit**: `cbd027e` (Phase 2 lift) on branch `feat/m3b-auto-parallelization`.
**What shipped**:
- Codegen-side cross-module `frame_layouts_query` (salsa-tracked, `ynz-codegen`) with LLVM-accurate `FrameLayout` per source file. Importer reads callee's real `total_size` + `n_locals` via the query.
- `compute_composed_frame_size` / `typeck_type_frame_slots` / `FunctionSig.composed_frame_size` deleted (the lossy parallel reimplementation, no-duct-tape #7).
- Recursive cross-module child resolution (Guard G2): re-export chains (A→B→C) compose correctly via `frame_layouts_query(callee_file)`.
- Universal-reject guard lifted. Dynamic-dispatch-through-vtable and FFI still reject via the existing may-block unresolvable-edge path (correct, not a band-aid).
- Full 19-fixture danger matrix green: every value type × {crossing-local, loop-var, return} × {direct, transitive, re-export, caller-own-frame} × {scalar, shape, number/decimal128, errors-capable}. Zero silent miscompiles; alloc=free on all fixtures.
- 5 adversarial-tester fixes landed (background-spawn frame sizing, aliased-import resume-symbol keying, kernel-reject, float-corruption-detection, diamond-import memoization).
**Ships via**: Merged to `main` (PR #70) and released as part of `v0.3.0-m5` (bundled with M3b) on 2026-06-09.

### Milestone 3f: v0.3-M3f — Pre-Existing Codegen Correctness Fixes — single-session (1-2 sessions)
> **Tracked 2026-06-09 (Patrick decision at the `v0.3.0-m5` release).** Two silent-wrong / crash codegen bugs were surfaced during M3b adversarial review and CONFIRMED pre-existing — identical behavior with and without `--no-auto-parallel`, so the M3b cross-impl oracle proves they predate auto-parallelization (base M3a / EC-return codegen, NOT M3b regressions). M3b shipped *around* them (the `pirates-roster` demo uses distinct callees to dodge bug 1). Logged on the roadmap so the deferral can't rot in `todos.md`.
**Value delivered**: two correctness holes closed — no silent wrong value from same-callee wide-`errors` returns; no compile crash from a crossing local used inside post-`wait` control flow.
**Execution plan**: `v0-3-m3f-codegen-correctness-fixes` (status: planned — plan before M4 cuts the `v0.3.0` final tag)
**Blocks**: the `v0.3.0` final release. These are Golden Rule 5 (compile-time safety) violations; `v0.3.0` must not ship with a known silent miscompile.
**Tracked bugs** (full repro + root cause + fix direction live in [`.claude/todos.md`](../../../todos.md)):
1. ~~**Same-callee wide-`errors` staging-slot value-aliasing** (HIGH — silent wrong value). `let p1 = fetchPrice("lower"); let p2 = fetchPrice("upper")` where `fetchPrice -> number errors` returns an arg-dependent decimal128 → BOTH bindings read the SECOND call's value. ROOT: a `-> T errors` return whose ok-value is too wide for the EC ok-word puts the value in the callee's 16-byte staging slot and the EC struct's ok-word POINTS into it; a second call to the same callee reuses the slot, clobbering the first binding before it is read. Same staging-slot-dangling class as the `ec-wrapper-collect-on-completion` deferral, but in the inline-poll / sequential path. FIX: copy-on-bind — copy the wide-`errors` ok-value into the binding's own stable storage before the next same-callee call can clobber the slot.~~ **SHIPPED v0.3-M3f (2026-06-09, commit `9e7ee78`)** — copy-on-bind in `bind_sm_return_value` + `lower_errors_capable_call_result` (`emit.rs:5059`, `emit.rs:12723`).
2. ~~**Crossing local used inside post-`wait` nested control flow → LLVM "does not dominate all uses" compile crash**. `let a = slowCall(); wait other(); if (cond) { print(a) }` crashes at compile time. ROOT: the crossing-local liveness scan (`crossing_local_names`) only registers top-level-after-`wait` uses; a use inside an `if`/`while`/`for`/`match` body reached after a suspension is missed, so `a` is never frame-backed. The M3b-P5 fix frame-backed only top-level parallel-group bindings; this is the same gap one layer deeper. FIX: recurse the read-after-`wait` liveness scan into nested CF bodies — the WRITE side (`collect_crossing_writes`) already recurses; mirror it for reads.~~ **SHIPPED v0.3-M3f (2026-06-09, commit `05262d3`) — REFRAME**: the LLVM crash symptom had already closed via earlier M3 work; what M3f actually fixed is the parallel-group bool sibling silent miscompile (`default` ≠ `--no-auto-parallel`) in the same root area (truncate i64→i1 before bool alloca store, `bind_sm_result_and_flush` `emit.rs:5702-5726`).
**Related (same crossing-local family — fold into bug-2's fix pass if cheap)**: aliased shape + `lend`-across-`wait` loses the write-back (`let b = a` shape-alias × frame-backed crossing-local `lend` mutation after a `wait` → the second mutation through the alias is dropped, prints `n:10` not `n:110`). Tracked in `todos.md`; same M3a frame-backed-crossing-local write-back discipline intersecting shallow-shape-alias semantics (m3c family).
**Ships via**: `/pr`, then folds into the `v0.3.0` release (M4) or a standalone `v0.3.0-m6` tag — Patrick's call at plan time.

### Milestone 3g: v0.3-M3g — Mixed CPU+I/O Overlap (poll-path fusion) — **SHIPPED 2026-07-02 (merged to main; `v0.3.0-m{next}` tag not yet cut)**
> **⚠️ SPLIT 2026-06-14 from M3d Phase 3 (Patrick decision).** The M3d execution plan's Phase 3 **Objective** listed "mixed CPU+I/O groups" as an M3d deliverable — but the roadmap scopes M3d as **pure-CPU only** (the CPU/I/O split was deliberate: I/O overlap = M3b's interleaved inline poll, pure-CPU overlap = M3d's joinable spawn+join; their FUSION was never scoped). M3d sub-slice 4c ran a full verify-first pass: the executor BUILT both attempted fusion fixes and PROVED each unsafe (the CPU join-poll and I/O inline-poll paths share no continuation; forcing a nested CPU group to fire alongside an outer suspension DEADLOCKS the binary — the spawned handle is never re-polled from the outer suspension's resume). Two adversarial deviation-judges vindicated the DECLINE as genuine milestone-sized unsafety, not avoidance. So this capability gets its own milestone instead of bloating M3d. **This split is itself the first catch of the new Capability Ledger discipline.**
**Value delivered**: a function body that mixes one heavy CPU call and one I/O (`wait`) call as independent operations runs them *concurrently* (CPU on a worker core, I/O suspended) instead of in sequence — the last gap in "all independent operations auto-parallelize," covering groups that mix the two work-classes. Until M3g, such mixed groups run sequentially (correct, just not overlapped).
**Execution plan**: `v0-3-m3g-mixed-cpu-io-overlap` (status: done — all phases shipped 2026-07-02; completion gate sealed, cross-phase scaffolding cleared)
**MANDATORY (Patrick 2026-06-19).** Originally framed as a non-blocker perf enhancement; the "most efficient language" mission reclassifies mixed-group sequential execution as a real perf gap that v0.3 must close. Sequenced after M3d Phase 5, before the v0.3.0 tag, per the mandate. M3d's safe DECLINE (mixed→sequential, byte-identical) was the correctness floor M3g had to not regress — the DECLINE fixtures flipped from DECLINE-asserting to FIRE-asserting as the shipped acceptance signal.
**Depends on**: v0.3-M3b (the I/O inline-poll path) AND v0.3-M3d (the pure-CPU join-poll path) — both must be complete; M3g fuses them.
**Rough scope** (its `/plan` sharpens this — full root-cause + machinery live in [`.claude/todos.md`](../../../todos.md) "mixed CPU+I/O overlap" residual, written by M3d sub-slice 4c):
- **Codegen poll-path fusion**: route codegen off typeck's class-aware `partition_groups_classified` / `ClassifiedGroup` (already shipped in typeck with NO codegen consumer by design), so ONE group's continuation drives BOTH a CPU join-poll (`ynz_rt_join_poll`) and an I/O inline-poll (`resume_fn`) — re-driving every live spawn handle on each resume of the shared continuation.
- **Dual-kind frame layout**: reserve BOTH a CPU handle/result-slot region AND the I/O child sub-frame in the same group's frame layout.
- **Typeck — promote already-suspending hosts**: let a function already in `base_suspends` (its own `wait` or a suspending callee) still drive `m3d_spike` for a CPU group in its body, AND guard-probe its CPU-join crossings (today `compute_cpu_promotions` / guard-probe skip `base_suspends` functions, so a nested CPU group inside a suspending host never fires).
- **Deadlock-safety gate (mandatory — M2-HALT-corpse-adjacent)**: the fused continuation must never deadlock (the 4c failure mode) — adversarial gate + the M3d DECLINE fixtures (`v03_m3d_mixed_cpu_io_group_declines`, `..._nested_group_with_outer_wait`, `..._nested_group_with_suspending_callee`) FLIP from DECLINE-asserting to FIRE-asserting as the acceptance signal.
- Demo (`pirates-roster` mixed CPU+I/O section) + error gallery + cross-impl consistency.
**Trigger to schedule**: ~~immediately after M3d Phase 5 completes~~ — done, M3g shipped.
**Ships via**: merged to `main`; `/release` for the `v0.3.0-m{next}` tag not yet cut (next scheduling decision: fold into the `v0.3.0` final release with M4, or cut a standalone `v0.3.0-m8` first — Patrick's call at M4 plan time, same pattern as M3f).

### Milestone 4: v0.3-M4 — Channels + Auto-Arc + v0.3.0 Release — multi-session
> **⚠️ SPLIT 2026-07-02 (Patrick decision, at `/plan` recon time) — Auto-SoA carved OUT to M5.** Recon for this milestone read the actual array runtime and found the roadmap's "codegen-only change" framing for auto-SoA was wrong: `array<Shape>` is pointers-to-separately-heap-allocated shapes behind a uniform 8-byte-slot `YnzArray` C-ABI (`ynz_array_get/set/push`) — there is no contiguous field storage to reinterpret into SoA. Building it means a SECOND, parallel array representation living alongside the existing one: exactly the twin-computation-drift class [`.claude/rules/authoritative-derivation.md`](../../../rules/authoritative-derivation.md) exists to prevent, and the same silent-miscompile shape that already cost M3a/M3d/M3e/M3g whack-a-mole rounds. Compounding: zero benchmark harness exists anywhere in the repo (no `criterion`, no `[[bench]]`), so the SIZE_THRESHOLD calibration the roadmap mandates has nothing to run on; and there's a real unnamed interaction between SoA field-splitting and this milestone's own cross-thread false-sharing padding (both are layout transforms on the same shape and can contradict each other). Channels/background-handle-form/auto-Arc, by contrast, build on REAL substrate — M3d already shipped the joinable-handle ABI (`CpuJoinHandle`, poll-based, zero `block_on`). Splitting lets the release-blocking work ship on solid ground instead of being hostage to the highest-uncertainty item. Same discipline as the M3a/M3b/M3d/M3e/M3g splits — recon proved the boundary was wrong, so the roadmap moves, not the plan.
**Value delivered**: `channel<T>()` for bounded task communication (the `background` handle-form). Cross-thread shared state gets auto-`Arc` wrapping. Cuts the `v0.3.0` release tag — first version where Yinz code actually runs concurrently. Also folds in the still-uncut `v0.3.0-m{next}` release housekeeping for M3f/M3g (both merged to `main`, neither tagged — Patrick decision 2026-07-02: fold into this final `v0.3.0` tag rather than cut standalone `-mN` tags for them).
**Execution plan**: `v0-3-m4-channels-arc-release` (status: DONE — all 7 phases (0-6) executed and sealed; SHIPPED as the `v0.3.0` release, tagged + published 2026-07-03. Plan: [`.claude/planning/done/2026-07-02-v0-3-m4-channels-arc-release/plan.md`](../../done/2026-07-02-v0-3-m4-channels-arc-release/plan.md). Note: auto-Arc's codegen EMISSION and the false-sharing padding transform's CURRENT throughput benefit are deferred to v0.4+ per FRAGO 008/009 in that plan's `audit.md` — see the Capability Ledger rows below.)
**Depends on**: v0.3-M3b (I/O overlap — channels reuse the suspension/poll model) AND v0.3-M3d (the joinable-handle ABI — background-handle-form builds on `CpuJoinHandle`/`ynz_rt_join_poll`). Both shipped.
**Rough scope**:
- `channel<T>()` type: bounded (capacity default 64, configurable); `ynz_channel_send` / `ynz_channel_recv` C-ABI bridge via Tokio's `mpsc` channel internals. `channel.send()` on a bounded-full channel is a NEW SUSPENSION SOURCE (Tokio's `send().await` backpressure) — it must join the may-block set and be state-machine-hosted like any other suspending call, not just modeled as an `errors` return. Confirm at plan time whether a dropped-receiver `send()` maps cleanly onto Tokio's `SendError` or needs Yinz-level `errors` wrapping.
- Channel muted-hint domain: `channel_capacity` registry domain fires inline capacity hint `⟨64⟩` per [`docs/internal/implementation/IMP-no-function-coloring.md`](../../../../docs/internal/implementation/IMP-no-function-coloring.md) "Channel/Queue Primitives"
- Background handle-form: `let h = background fn()` → `h` is a task handle with `.send()` / `.receive()`. NOTE (recon 2026-07-02): this is NOT a thin wrapper over the existing join machinery — `ynz_rt_join_poll` is one-shot (consumes the result, drops the box on Ready), so repeated `.receive()` and `.send()` INTO a running task need real channel wiring into the spawned frame, not a `CpuJoinHandle` reuse. A handle whose task never receives is a deadlock class (M2-HALT-corpse-adjacent) — adversarial gate mandatory, same DECLINE/hostile-fixture pattern as M3d/M3g.
- Auto-Arc: when a value is shared across a `background` spawn boundary and ownership rules require reference counting (shared immutable state accessed from multiple tasks), codegen emits `Arc::new(value)` + `Arc::clone()`. IDE muted hint shows the auto-Arc (cautionary red-tinted styling per [`docs/internal/implementation/IMP-no-function-coloring.md`](../../../../docs/internal/implementation/IMP-no-function-coloring.md) — confirm at plan time whether cautionary-red rendering is an existing LSP code path or net-new). The auto-Arc boundary must be exact against the existing `share`/`lend`-across-`background`-boundary reject (`check.rs:1216`, unverified by recon — confirm at plan time per Design-Doc-Alignment): wrong-way is either a silent data race (should-reject gets Arc'd) or a false compile error (should-Arc gets rejected).
- False sharing auto-padding: shapes with fields accessed from different `background` tasks get 64-byte cache-line alignment + inter-field padding. Codegen-only; Tier 3 lint `cross-thread-fields-not-padded` fires if padding can't be applied automatically. NOTE: this is a layout transform alongside the project's existing shape-field auto-reorder (`auto-promotion.md`) — no interaction conflict expected since SoA (the OTHER layout transform that could contradict padding) is now in M5, not this milestone.
- **`prefer-yielding-sleep` Tier 3 lint** (per "Sleep intrinsic naming + teaching" architectural decision): fires on `sleepBlocking(ms)` in non-kernel programs, suggesting `wait sleep(ms)` (suggestion, dismissable — NOT an error). This is the M4 home for the non-kernel half of the blocking-vs-yielding teaching; M4 is also where the handle-form removes the last legit non-kernel blocking-sleep use (the `sleepMs` keepalive), so the lint stops nagging a valid pattern. NOTE: depends on the `sleepAsync`→`sleep` / `sleepMs`→`sleepBlocking` rename being done first (already done in M3a) — no blocker.
- **`[[lint_rule]]` registry mechanism**: recon confirmed this entry-kind does NOT exist in `registry/features.toml` today — no schema, no parser, no LSP wiring. M4 builds it from scratch to carry `cross-thread-fields-not-padded` + `prefer-yielding-sleep`. M5 (auto-SoA) reuses this mechanism rather than rebuilding it for `array-using-soa-layout` — confirmed shared infra, not duplicated.
- Cargo.toml bump 0.2.x → 0.3.0, CHANGELOG generated from merged PRs since the last tag (must correctly fold M3f + M3g + M4's own work — M3f/M3g shipped un-tagged, per the split note above), `/release` for `v0.3.0` tag (no `-mN` suffix)
- Demo: `pirates-roster` showcase with channels, background handle-form, auto-Arc hint annotations; `v0_3_m4_errors.ynz`
- **Teaching surface (REQUIRED in M4, not deferred)**:
  - New `[[muted_hint_domain]]` registry entries: `channel_capacity` (Addition category: `⟨64⟩` inline hint) AND `auto_arc` (Informational category: cautionary red-tinted `// Arc<T> — shared across tasks, ref-counted` hint)
  - New `[[lint_rule]]` registry entries: `cross-thread-fields-not-padded` + **`prefer-yielding-sleep`** — each with WHAT/WHAT-INSTEAD/WHY hover text
  - Channel error messages: `send()` to closed channel, channel-full backpressure suspension — WHAT/WHAT-INSTEAD/WHY for each
  - VSCode extension: version bump, updated `wait`/`background` hover docs (updated text from M1 carries forward), screenshots for channels + auto-Arc hint
  - `pirates-roster` must demonstrate all M4 surfaces: channel communication, background handle-form, auto-Arc hint, false-sharing padding
**Ships via**: `/pr` per phase, `/release` for `v0.3.0` tag (final, folds M3f + M3g)

### Milestone 5: v0.3-M5 — Auto-SoA (Struct-of-Arrays layout) — multi-session
> **⚠️ SPLIT 2026-07-02 (Patrick decision) from the old M4.** See M4's split note above for the full recon evidence. **Documented deferral per `no-duct-tape.md` §When To Document Instead Of Fix (4 fields):** **WHAT** is deferred — auto-SoA layout for `array<Shape>` hot loops, ships as its own milestone AFTER `v0.3.0`. **WHY** — SoA needs a brand-new array representation (not a codegen tweak on the existing `YnzArray` uniform-8-byte-slot runtime) plus a benchmark harness that doesn't exist anywhere in the repo yet; both are unbudgeted, higher-risk work than the channels/Arc capabilities that DO have real substrate (M3d's joinable-handle ABI), and bundling them would hold the whole `v0.3.0` release hostage to the highest-uncertainty item. **COST to fix later** — full milestone (multi-session): new `soa_candidate_query` analysis pass, a parallel SoA-aware array runtime path (or a from-scratch design that unifies AoS/SoA behind one accessor — TBD at `/plan` time), the SIZE_THRESHOLD benchmark harness (no `criterion`/`[[bench]]` exists in the repo today — build-from-scratch), the SoA-suppression filter (shapes with `lend self` methods touching >hot-loop fields, FFI export, serialization roundtrip), the SoA-vs-cross-thread-padding interaction (two layout transforms on one shape — M4 ships padding without this conflict since SoA isn't present yet; M5 must resolve it when SoA arrives), and best-effort DAP debugger integration (`players[0]` shows unified `Player` view under SoA layout) *[superseded 2026-07-03: DAP deferred OUTRIGHT via `[[deferred_tooling_feature]] dap-soa-unified-view` — see the Architectural-Decisions DAP bullet + the scope bullet below]*. **TRIGGER** — schedule as the next roadmap milestone immediately after `v0.3.0` ships (M4), or earlier if a real workload demonstrates the cache-locality win is needed before then.
**Value delivered**: `array<Shape>` hot loops that access ≤2 fields get SoA layout automatically (10-40× cache improvement on large arrays) — the last capability from the original v0.3 vision, delivered without holding the concurrency-correctness release hostage to it. **Also delivers (FOLDED IN, Patrick 2026-07-03):** by-value inline element storage for `array<Shape>` (owned element bytes in the heap buffer — permanently fixes the array-element stack-dangling miscompile class and lifts the M3a `ArrayShapeRuntimeFieldWithWait` interim guard) + the symmetric `map<K,Shape>` by-value fix; SoA rides the new contiguous storage as a layout variant of the ONE array representation.
**Execution plan**: `v0-3-m5-auto-soa` (status: **planned + active** — plan-id `2026-07-03-v0-3-m5-auto-soa`, authored 2026-07-03)
**Depends on**: v0.3-M4 (the `[[lint_rule]]` registry mechanism M4 builds; SoA's `array-using-soa-layout` lint reuses it rather than rebuilding). Independent of M4's channels/Arc/padding capabilities otherwise.
**Scope** (sharpened + made authoritative by plan `2026-07-03-v0-3-m5-auto-soa`, 2026-07-03 — the plan wins on any divergence; full recon evidence lives there and in the split notes):
- **Recon-first — ✅ DONE 2026-07-03**: the M5 plan's recon re-derived the real scope against the actual `YnzArray`/`emit.rs` representation (file:line-verified terrain in the plan's ¶1).
- **FOLDED IN (Patrick 2026-07-03):** the previously-unscheduled `v0-3-m3c-array-by-value` work ([`SCRATCH-future-array-by-value-element-storage.md`](../../../../docs/internal/scratchpad/SCRATCH-future-array-by-value-element-storage.md), Patrick-approved 2026-06-04, never planned) + the `map<K,Shape>` symmetric fix. ONE elem_size-aware by-value representation owns both AoS storage and SoA (a layout variant of it) — a second/parallel array representation must never exist (plan risk E1, B1 elimination). By-value ships first (plan Phases 2–3), SoA rides it (Phases 4–5); lifts `ArrayShapeRuntimeFieldWithWait` + its registry deferral.
- New analysis pass `soa_candidate_query(db, source) -> Vec<SoaCandidate>`: `array<Shape>` binding whose primary loop accesses ≤2 fields, per the existing decision criteria (array length > SIZE_THRESHOLD, no FFI export, no `lend self` methods touching more than the hot-loop fields).
- SoA-aware array representation: **RESOLVED 2026-07-03 (the fold)** — a unified redesign: the elem_size-aware by-value `YnzArray` is the ONE representation; SoA is a segmented-layout variant of it (same header, same ownership, same drop path). No parallel accessor path exists, by design (plan risks E1/E3).
- Benchmark harness (criterion or equivalent) to calibrate SIZE_THRESHOLD with real evidence on the CI machine, per the roadmap's existing mandate — this harness may also retroactively validate M3d's cost-threshold constant if it was shipped unvalidated.
- SoA-vs-cross-thread-padding interaction: **RESOLVED 2026-07-03 (plan recorded decision D11) — padding wins; a cross-thread-padded shape's arrays are never SoA'd**, enforced through ONE authoritative layout-decision source both transforms read, proven by a byte-layout-asserting both-candidate fixture (plan risk E3).
- Serialization forward-compat (REFRAMED 2026-07-03, Patrick — see the Risks-table row): no serializer exists until v0.8, so no roundtrip test now; instead the representation exposes `LayoutDecision` layout metadata sufficient for v0.8's compile-time serializer codegen, + a design note in IMP-collections (plan Phase 7, risk E10).
- Tier 3 lint `array-using-soa-layout` on the `[[lint_rule]]` registry mechanism M4 built.
- Debugger integration: **DEFERRED OUTRIGHT** (Patrick-signed four-field deferral, 2026-07-03) via `[[deferred_tooling_feature]] dap-soa-unified-view` — zero DWARF substrate exists; revisit when DWARF emission ships or a debugging-tooling milestone is scheduled (plan §Future Requirements #1).
- Demo + error gallery extensions; cross-impl consistency (`--no-auto-parallel` must also disable SoA — recon flagged this flag has never gated a layout transform before, confirm it actually does).
**Trigger to schedule**: ✅ triggered — `/plan` ran 2026-07-03 (`2026-07-03-v0-3-m5-auto-soa`). Execution sequencing (originally gated on the v0.3.0 tag) is governed by that plan's ¶3.4 + its audit.md (FRAGO 002–004 recorded waiver).
**Ships via**: `/pr` per phase, `/release` for a `v0.3.x` patch-line tag (Patrick's call, locked at plan time 2026-07-03).

### Unscoped Capability: Authoritative-Derivation Write-Time Guard (mechanical hook) — needs a milestone
> **⚠️ CAPABILITY DISCOVERY 2026-07-03 — source: the AAR + rule-author disposal from plan [`2026-07-02-v0-3-m4-channels-arc-release`](../../done/2026-07-02-v0-3-m4-channels-arc-release/plan.md)'s completion (v0.3-M4, shipped as `v0.3.0`).** The twin-computation-drift disease ([`.claude/rules/authoritative-derivation.md`](../../../rules/authoritative-derivation.md) — a compiler pass re-deriving an already-authoritative answer in a second place instead of threading the one real source) recurred FOUR times WITHIN that single plan's execution alone: a `SuspendSet` type-alias twin at Phase 0, a latent `check.rs` twin caught at Phase 2, a `DEFAULT_CHANNEL_CAPACITY` twin that was a real BLOCKER at Phase 5, and a `lock_or_recover` mutex-helper twin caught by the plan's own cumulative cross-phase completion gate — despite the sibling rule being in-context and cited at every phase's recon. rule-author independently re-ran the global `corpse-recurrence-escalation.md` two-question diagnostic against all four instances and confirmed the escalation floor is genuinely crossed — this is a **lever failure**, not a rule-wording problem. The escalation verdict text is already recorded in [`.claude/graveyard.md`](../../../graveyard.md)'s existing "Parallel Per-Type Dispatch / Flat-Scan Re-Derivation in Suspension Codegen" corpse entry; what does NOT exist yet is the actual fix that escalation calls for ("a genuinely recurring corpse graduates to a mechanical, phase-boundary or write-time check, sitting in front of the reactive stages that already exist"). That fix is a `hook-author` artifact requiring its own design + build session per this repo's artifact-lifecycle discipline (a hook is a different artifact type with its own author — never authored ad-hoc mid-AAR), so it lands here as an unscoped capability, NOT in the already-closed-and-released M4 plan. **Documented deferral per `no-duct-tape.md` §The escape hatch (4 fields):** **WHAT** is deferred — a write-time or phase-boundary MECHANICAL check that catches a second independent derivation of an already-authoritative computed value/classifier/helper (a new `pub const`/helper/classifier duplicating one that already exists) BEFORE it is committed — NOT another reactive review pass (those already exist and are exactly what kept missing this 4 times in one plan); it sits in FRONT of the existing reactive `graveyard-auditor` stage rather than replacing it. **WHY** — `corpse-recurrence-escalation.md`'s own diagnostic test was independently re-run against 4 real instances from `2026-07-02-v0-3-m4-channels-arc-release`'s execution (`SuspendSet` twin P0; latent `check.rs` twin P2; `DEFAULT_CHANNEL_CAPACITY` twin P5 — a real BLOCKER; `lock_or_recover` twin at the cumulative completion gate) and confirmed BOTH conditions of a lever failure: the sibling rule (`authoritative-derivation.md`) was genuinely in-context and cited at every phase's recon, AND every instance was caught only downstream by a gate/reviewer, never by the acting executor's own pre-flight. A 4th rewording of the rule would repeat an already-failed intervention — the same class of "cheap-now, rebuild-right-later" trap `no-duct-tape.md` itself warns against. **COST to fix later** — a real design + build session for `hook-author`: a live-hooks-spec fetch, a dedup gate, and a stdin-JSON smoke-test per this repo's own hook-authoring discipline — NOT a same-session bolt-on. **TRIGGER** — the next milestone-planning session that touches compiler-pass or derived-constant work, OR immediately if Patrick wants to prioritize it standalone.

---

## Capability Ledger (SSOT for capability → milestone ownership)

> **Added 2026-06-14 (Patrick) to stop recurring scope-bleed.** Every named capability in this initiative maps to EXACTLY ONE milestone here. This table is the source of truth for "which milestone owns what." An execution-plan phase that delivers a capability the ledger assigns to a DIFFERENT milestone (or doesn't list at all) is scope-bleed — caught at plan-review by diffing the phase's named capabilities against this table, not at execution time. The recurring failure this prevents: a phase **Objective** silently bundles a capability the roadmap put elsewhere (M3d's plan listed "mixed CPU+I/O" — a capability the roadmap split into M3b's I/O path + M3d's CPU path, never the fusion). New capabilities discovered mid-execution get a row here (status `unscoped → needs a milestone`) before they're worked, never folded into the active milestone.

| Capability | Owning milestone | Status | Notes |
|---|---|---|---|
| Runtime bootstrap + fire-and-forget `background` | M1 | shipped | |
| `wait` suspension + state-machine codegen + intra-unit may-block inference | M2 | shipped | |
| Suspension codegen — hard cases (heap-boxed child frames, recursion) | M3a | shipped | |
| Full variable-shadowing parity (per-binding-slot identity) | M3c | planned | |
| Cross-module may-block propagation | M3b | shipped | |
| **I/O statement overlap** — suspending-callee groups overlap via interleaved inline poll, ZERO new threads | M3b | shipped | one thread, interleaved suspensions |
| Cross-module `FrameLayout` serialization | M3e | shipped | |
| **Pure-CPU statement parallelization** — joinable `spawn_blocking` + async join + SM-promotion; independent heavy CPU calls run on separate cores | M3d | **active** | pure-CPU groups ONLY; M3d declines mixed-CPU+I/O / loop-body / multi-group / unsupported return-classes safely to sequential |
| Pre-existing codegen correctness fixes (same-callee wide-`errors`, crossing-local-in-post-`wait` CF) | M3f | planned | blocks the `v0.3.0` final tag |
| **Mixed CPU+I/O overlap** — fuse the CPU join-poll path + the I/O inline-poll path so ONE group can carry a CPU child AND an I/O (`wait`) child concurrently | **M3g** | **shipped** | shipped 2026-07-02, merged to `main`; `v0.3.0-m{next}` tag not yet cut |
| Channels + background handle-form + auto-Arc + false-sharing padding + `v0.3.0` release (folds M3g tag; M3f already shipped at `v0.3.0-m6`) | M4 | **shipped** | shipped as `v0.3.0` (tagged + published 2026-07-03); renamed `v0-3-m4-channels-arc-release`; SoA split out 2026-07-02. NOT fully live today: auto-Arc codegen EMISSION (boundary analysis + hint + runtime substrate shipped; emission deferred v0.4+, FRAGO 008) and false-sharing padding's CURRENT throughput benefit (transform + lint live but forward-looking no-op until auto-Arc emission creates truly-shared instances, FRAGO 009) — both in the M4 plan's `audit.md` |
| Auto-SoA (Struct-of-Arrays layout for `array<Shape>` hot loops) | **M5** | **planned** (plan `2026-07-03-v0-3-m5-auto-soa`) | split out of M4 2026-07-02 — not codegen-only (recon: needs a new array representation + a benchmark harness, neither exists); ships post-`v0.3.0`. Planned 2026-07-03: array-by-value element storage + `map<K,Shape>` fix FOLDED IN (row below) — ONE elem_size-aware representation owns AoS-by-value + SoA-as-layout-variant (plan risk E1) |
| Array by-value element storage + `map<K,Shape>` symmetric fix — elements owned by-value inline in the heap buffer; fixes the array-element stack-dangling miscompile class; lifts the M3a `ArrayShapeRuntimeFieldWithWait` interim guard | **M5** | planned | previously UNSCHEDULED (scratch doc `v0-3-m3c-array-by-value`, Patrick-approved 2026-06-04, never planned) — FOLDED INTO M5 by Patrick 2026-07-03 so one representation redesign owns both by-value storage and SoA (plan `2026-07-03-v0-3-m5-auto-soa`, Phases 2–3; risk E1) |
| **Authoritative-derivation write-time guard** — mechanical hook (write-time or phase-boundary) detecting a NEW re-derivation of an already-authoritative computed answer (a second `pub const`/helper/classifier duplicating one that already exists) BEFORE it lands; sits in FRONT of the existing reactive `graveyard-auditor` stage, not replacing it | **unscoped** — needs a future milestone/session (NOT M4, which is closed + released) | **unscoped → needs a milestone** | Capability discovery 2026-07-03 from M4's AAR + rule-author disposal: twin-computation-drift recurred 4× within the M4 plan's execution alone despite `authoritative-derivation.md` in-context at every phase's recon; `corpse-recurrence-escalation.md` lever-failure floor confirmed. `hook-author` artifact — own design session, never ad-hoc. Full 4-field anchor in roadmap §Unscoped Capability above. |

---

## Out of Scope

These were considered for v0.3 and explicitly deferred:

- **Custom user `Iterable<T>` implementations** — deferred to v1.0 per [`docs/reference/REF-mvp-scope.md`](../../../../docs/reference/REF-mvp-scope.md). No change from plan.
- **Background handle-form before M4** — `let h = background fn()` is M4 (channels); M1-M3 only has fire-and-forget.
- **Cross-loop parallelization** — loop iterations remain sequential. Per [`docs/internal/implementation/IMP-concurrency.md`](../../../../docs/internal/implementation/IMP-concurrency.md) "Loop Iterations — Sequential by Default."
- **Deadlock detection at runtime** — complex; deferred to later. [`docs/internal/implementation/IMP-no-function-coloring.md`](../../../../docs/internal/implementation/IMP-no-function-coloring.md) flags this as an Open Question for the v0.2 implementation milestone; it remains open for v0.3.
- **Debugger SoA view** — DEFERRED OUTRIGHT 2026-07-03 (Patrick-signed four-field deferral) via `[[deferred_tooling_feature]] dap-soa-unified-view`; was best-effort-in-M4, retargeted to M5 with the 2026-07-02 split, then deferred outright at M5 plan time (zero DWARF substrate exists). Revisit when DWARF emission ships or a debugging-tooling milestone is scheduled — see plan `2026-07-03-v0-3-m5-auto-soa` §Future Requirements #1.
- **Sized integer variants, FFI, GPU dispatch** — v2+ per [`docs/reference/REF-mvp-scope.md`](../../../../docs/reference/REF-mvp-scope.md). No change.
- **`ynz lint` / v0.4 linting tier** — v0.4 is next after v0.3. The Tier 3 lint rules introduced in v0.3 (SoA, false-sharing, mutable-when-const) are part of the normal compile output per [`docs/internal/implementation/IMP-linting.md`](../../../../docs/internal/implementation/IMP-linting.md) (compiler IS the linter).
- **v0.3.x patch releases** — possible for critical bugs; not pre-planned. Same policy as v0.2.x.
- **Batch utilities for parallel loop processing** — [`docs/internal/implementation/IMP-concurrency.md`](../../../../docs/internal/implementation/IMP-concurrency.md) notes "developers will use batch utilities from the standard library" for parallel loop processing. No such batch utilities ship in v0.3 — that's a stdlib module (likely bundled with a future iteration of v0.5+ file or a concurrency stdlib). Users who need parallel loop processing in v0.3 write explicit `background` calls with a manual join barrier. Documented in Out of Scope so the gap is visible.

---

## Open Questions for Patrick

*(All three original open questions resolved 2026-05-21 — see Required Pre-Work and Auto-SoA debugger decision in Architectural Decisions above.)*

- No remaining open questions at roadmap level. Execution plan open questions will surface in each milestone's `/plan` session.

## Capability Ledger

(merged from the pre-migration companion `capability-ledger.md` file — 2026-07-01)

Every capability the initiative will deliver maps to an owning milestone (or an explicit NEEDS-PLANNED row). Done children record what they delivered in Notes (the done-child ownership record standing in for retroactive per-phase Scope Boundary blocks). The one active child (`v0-3-m3d-cpu-parallelization`) carries its in-phase Scope Boundary blocks in the plan file itself.

| Capability | Owning milestone | Status | Notes |
|---|---|---|---|
| Runtime bootstrap + working `background` — background work runs off-main on a blocking-pool thread; main continues immediately | v0-3-m1-runtime-and-background | done | Delivered (child done). `wait` inside background still blocks its pool thread until M2. |
| `wait` suspension + state-machine codegen + intra-unit may-block inference — reaching a suspension point suspends/resumes (no thread-holding); inferred state machines, no function coloring | v0-3-m2-wait-and-state-machines | done | Delivered (child done; shipped as v0.3.0-m3 with M10). |
| Suspension codegen completion — `wait` in every position (let-before/used-after, loop bodies, match arms); frame-backed mutable locals; `sleepAsync`→`sleep` rename | v0-3-m3a-suspension-codegen | done | Delivered (child done). Composed-frame allocation preserved (no per-iteration alloc). |
| Cross-module may-block propagation + I/O-overlap auto-parallelization — independent ops auto-parallelize without `background`/`wait`; may-block propagates across modules; auto-`wait` at I/O sites | v0-3-m3b-auto-parallelization | done | Delivered (child done; shipped as v0.3.0-m5). IDE muted-`wait` hint fires on real cross-module + I/O data. |
| Pure-CPU statement parallelization — independent heavy pure-CPU ops run on separate cores, zero user syntax, byte-identical to `--no-auto-parallel` | v0-3-m3d-cpu-parallelization | active | In progress. PURE-CPU scope only (mixed CPU+I/O is M3g). Scope Boundary blocks live in the child plan. |
| Cross-module FrameLayout serialization — every analyzable cross-module suspending call runs correctly (re-export chains, shape/number/float locals+returns, errors×transitive); lifts P1 loud-reject guards | v0-3-m3e-cross-module-frame-serialization | done | Delivered (child done; shipped 2026-06-07; all 3 phases). |
| Pre-existing codegen correctness fixes — same-callee wide-`errors` return no longer silent-wrong; crossing local in post-`wait` control flow no longer crash-compiles | v0-3-m3f-codegen-correctness-fixes | done | Delivered (child done; shipped 2026-06-09). Added to roadmap `milestones:` during ledger-format migration (child declared the roadmap but the list omitted it). |
| Mixed CPU+I/O overlap (poll-path fusion) — a group mixing one heavy CPU call and one I/O `wait` call runs them concurrently instead of sequentially | v0-3-m3g-mixed-cpu-io-overlap | done | Delivered (child done; shipped 2026-07-02, merged to `main`). Fused continuation drives both a CPU join-poll and an I/O inline-poll off one shared resume; DECLINE fixtures flipped to FIRE-asserting as the acceptance signal. Release tag (`v0.3.0-m{next}`) not yet cut — M4 plan time decides standalone tag vs. folding into the `v0.3.0` final release. |
| Channels + background handle-form + auto-Arc + false-sharing padding + v0.3.0 release — `channel<T>()`, task handle `.send()`/`.receive()`, cross-thread auto-`Arc`, cross-thread padding, `[[lint_rule]]` registry mechanism, v0.3.0 release tag (folds M3g's uncut tag; M3f already shipped at `v0.3.0-m6`) | v0-3-m4-channels-arc-release | **DONE** | Shipped as the `v0.3.0` release (tagged + published 2026-07-03; all 7 phases sealed). Renamed + descoped 2026-07-02 (was `v0-3-m4-channels-soa-release`) — auto-SoA split to M5 after recon proved it needs a new array representation + benchmark harness, neither of which exist. Built on M3d's real joinable-handle ABI. **Deferral pointer (don't read "done" as fully-live)**: auto-Arc's codegen EMISSION is deferred to v0.4+ (FRAGO 008 — boundary exactness, teaching hint, and Arc runtime substrate shipped; emission awaits registry entry `auto-arc-codegen-emission`), and the false-sharing padding transform is real + tested but a forward-looking no-op for CURRENT throughput (FRAGO 009 — every v0.3 crossing is an exclusive-ownership handoff that can't false-share; benefit arrives with auto-Arc emission). Both recorded in the M4 plan's `audit.md`. |
| Auto-SoA — `array<Shape>` hot-loop Struct-of-Arrays layout, 10-40× cache improvement | v0-3-m5-auto-soa | planned (plan-id `2026-07-03-v0-3-m5-auto-soa`, 2026-07-03) | **Documented deferral, Patrick-approved 2026-07-02** (full 4-field anchor in roadmap §Milestone 5). WHAT: auto-SoA layout, deferred out of M4. WHY: needs a new array representation (not codegen-only — `array<Shape>` is pointers-to-heap-shapes behind a uniform-slot runtime with no contiguous field storage) + a benchmark harness that doesn't exist in the repo (no criterion/`[[bench]]` anywhere); both unbudgeted and higher-risk than M4's channels/Arc work. COST to fix later: full milestone (new analysis pass, SoA-aware array representation, benchmark harness, suppression filter, SoA-vs-padding interaction). TRIGGER: fired — **PLANNED 2026-07-03** as `2026-07-03-v0-3-m5-auto-soa`, with array-by-value element storage + the `map<K,Shape>` fix FOLDED IN (see dedicated row) so ONE representation owns both; DAP deferred outright via `[[deferred_tooling_feature]] dap-soa-unified-view` (Patrick-signed 2026-07-03). |
| Array by-value element storage + `map<K,Shape>` symmetric fix — `array<Shape>`/`map<K,Shape>` elements stored by value inline in the heap buffer (elem_size-aware ABI); permanently fixes the stack-dangling miscompile the M3a guard masks; lifts `ArrayShapeRuntimeFieldWithWait` | v0-3-m5-auto-soa | planned | Previously UNSCHEDULED (`v0-3-m3c-array-by-value` scratch doc, Patrick-approved 2026-06-04, never planned). FOLDED INTO M5 by Patrick 2026-07-03 (plan `2026-07-03-v0-3-m5-auto-soa`, Phases 2–3): one elem_size-aware representation owns both by-value AoS and SoA, eliminating the twin-substrate drift class (plan risk E1). |
| Authoritative-derivation write-time guard — a write-time/phase-boundary MECHANICAL hook that catches a second independent derivation of an already-authoritative computed value/classifier/helper (new `pub const`/helper/classifier duplicating an existing one) before it is committed; front-of-pipeline complement to the reactive `graveyard-auditor`, not a replacement | unscoped — needs a future milestone/session (explicitly NOT `v0-3-m4-channels-arc-release`, which is closed + released) | unscoped → needs a milestone | **Capability discovery 2026-07-03** (source: AAR + rule-author disposal at plan `2026-07-02-v0-3-m4-channels-arc-release`'s completion). WHAT: mechanical write-time/phase-boundary twin-derivation check — NOT another reactive review pass. WHY: `corpse-recurrence-escalation.md`'s diagnostic re-run against 4 real M4 instances (`SuspendSet` twin P0, latent `check.rs` twin P2, `DEFAULT_CHANNEL_CAPACITY` twin P5 — real BLOCKER, `lock_or_recover` twin at the cumulative gate) confirmed a lever failure: rule in-context + cited every phase, every catch downstream-only; a 4th rule rewording would repeat a failed intervention. COST: real `hook-author` design + build session (live-hooks-spec fetch, dedup gate, stdin-JSON smoke-test). TRIGGER: next milestone-planning session touching compiler-pass or derived-constant work, or immediately on Patrick's call. Full 4-field anchor in roadmap §Unscoped Capability. Escalation verdict already recorded in `.claude/graveyard.md`'s existing corpse entry (don't re-record). |
| Full variable shadowing parity (per-binding-slot identity) — nested `let` may re-use a bound name across/around a `wait`; lifts `ShadowsCrossingLocal` reject | v0-3-m3c-shadow-parity | NEEDS-PLANNED (DEFERRED → post-v0.3.0 hotfix) | **Documented deferral, Patrick-approved 2026-06-19** (full 4-field anchor in roadmap §Milestone 3c). WHAT: shadowing parity across `wait`. WHY deferred: v0.3.0 ships the safe `ShadowsCrossingLocal` clean-reject (rename-the-variable compile error) — a papercut, NOT a correctness or perf gap; M3g (mandatory perf) + M4 (release) take priority. COST to fix later: 1-2 sessions (per-binding-slot identity keying; M3a P2 proved the partial fix silent-miscompiles → needs the complete fix + full shadow-matrix adversarial gate). TRIGGER: v0.3.x hotfix after v0.3.0 ships, or earlier on user demand. **Anchor bug FIXED 2026-06-19**: `v0-3-m3c-shadow-parity` added to roadmap `milestones:` list — now a valid schedulable anchor. |
