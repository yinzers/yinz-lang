# Session State: ynz

**Last Updated**: 2026-06-09 (**✅ v0.3.0-m5 RELEASED — M3e + M3b shipped together.** Branch `feat/m3b-auto-parallelization` merged to main via PR #70 (merge `12953fd`); cut `v0.3.0-m5` (release commit `f5495c9`, annotated tag pushed). GitHub release live: https://github.com/yinzers/yinz-lang/releases/tag/v0.3.0-m5 — `yinz-0.3.0-m5.vsix` + `yinz-latest.vsix` attached (VSCode ext `package.json`→`0.3.0-m5`; **local-install only, NOT marketplace-published**; screenshots/hover-doc polish deferred to a later VSCode pass per Patrick). `cargo test --workspace` **1929/0** at the tag; CHANGELOG `[0.3.0-m5]` covers both milestones. **2 PRE-EXISTING codegen bugs now tracked as roadmap milestone M3f** (BLOCKS the `v0.3.0` final release): (1) same-callee wide-`errors` (`-> number errors`) staging-slot value-aliasing → silent wrong value; (2) crossing-local used inside post-`wait` control flow → LLVM dominance compile crash. Both confirmed pre-existing (identical in `--no-auto-parallel` and default), full repro + fix direction in `.claude/todos.md`. **NEXT**: plan M3f (correctness fixes) or proceed to M3c/M3d/M4 — Patrick's pick. **Prior M3b-local-done radar below.**)

**Last Updated (prior)**: 2026-06-08 (**v0.3-M3b AUTO-PARALLELIZATION SHIPPED LOCALLY — all 6 phases committed + cumulative opus gate 5/5 PASS; plan `status: done` → moved to `done/`; STOPPED before `/release` per standing instruction.** Phase commits: P2 `5b637e3` (background give/copy — heap deep-copy + free-on-completion), P3 `93b703a` (wait_points + background_routing inlay hints), P4 `49f1760` (**THE HEADLINE** — independent suspending statements overlap via interleaved inline poll, zero new runtime symbols; default ≈max vs --no-auto-parallel ≈sum. Soundness via the **type-based CONSERVATIVE FLOOR**: any mutable-heap arg = potential write → sequentialize; GR5 soundness > GR10 perf. ABANDONED the name-based effective-ownership fixpoint after 5 distinct write-form holes across 3 gate rounds — kept it ONLY for the typeck transitive-`share` teaching error, removed from codegen), P5 `a032f9d` (re-scoped per no-duct-tape: original `background` EC-collect was a vacuous premise → deferral stays deferred `ships_in`→M4; instead FIXED the real bug found — a parallel-group return binding crossing a subsequent `wait` crashed LLVM dominance → routed through canonical `bind_sm_result_and_flush`), P6 `23a648e` (demo + gallery + cross-impl sweep). **CUMULATIVE OPUS SWEEP over `d7ea993..HEAD`** (94 files, +9373/−314): plan-adherence + design-compliance + code-reviewer + acceptance PASS (acceptance: all 10 invariants MET on LIVE runs, `cargo test --workspace` **1929/0**); rules-compliance BLOCK on ONE class — 5 banned-phrase changelog comments → reworded to present-tense, + code-reviewer's 2 stale-doc Concerns (queries.rs fixpoint-consumer, emit.rs EC-falls-back) + design-compliance's 1 module-doc note (parallel-groups-in-conditionals) all fixed in **gate-clearance `c532c6a`** (comment/doc-only, zero behavior change); rules-compliance RE-GATE → PASS, BLOCK cleared. All 5 GREEN → status flipped done. **HANDOFF TO PATRICK (release-prep, NOT auto-done):** (1) **`/release` version + M3e-bundling decision** — this branch carries BOTH M3e (`6344ce2..d7ea993`, cross-module frame serialization, no tag yet) + M3b; last tag `v0.3.0-m4`=M3a so `/release` range `v0.3.0-m4..HEAD` bundles both into one `v0.3.0-m5` entry — confirm or split; (2) VSCode marketing screenshots need local IDE capture; (3) VSCode `package.json` bump + 2 `.vsix` assets = `/release`'s job; (4) **2 PRE-EXISTING silent miscompiles** filed `todos.md:178/:180` (same-callee wide-EC staging-slot value-aliasing; nested-CF-after-`wait` crossing-local crash) — both identical-wrong in BOTH modes so NOT M3b regressions; Patrick's fix-timing call. **Prior M3b-in-flight radar below.**)

**Last Updated (prior)**: 2026-06-07 (**v0.3-M3b EXECUTION IN FLIGHT (whole-plan, on `feat/m3b-auto-parallelization`, base `d7ea993` = M3e close-out).** P0+P1 already committed (e4dd97c, 9ed31b0) pre-M3e; M3e interposed + lifted P1's universal-reject floor (verified: cross-module suspending fixtures run). Resuming P2-P6. **Per-phase + cumulative BASE = `d7ea993`, NOT plan_base `0a4b6d8`** (M3e's 4k lines are interposed). **PHASE 2 = much deeper than planned**: plan scoped it typeck-only on the FALSE assumption codegen already honors give/copy (same defect as P1). Gate found inferred-`.copy` on a heap `background` arg SILENTLY ALIASED; deeper verify found the copy was stack-alloca'd → silent USE-AFTER-FREE from a nested fn + an unbounded SM-path LEAK. **PATRICK DECISION (Option C): fix the heap deep-copy NOW** (heap-alloc the arg-copy + free-on-task-completion; `ynz_rt_spawn` ABI extended 4→6 params so `SpawnStateFnFuture::drop` frees SM-path copies; new `ynz_array_clone_primitive` for real array copy). Coordinator-verified live: UAF gone (task sees 7 every path), alias gone, array real-copy works, **alloc==free everywhere** (SM loop-10: 31/31). **✅ PHASE 2 COMMITTED `5b637e3`** — Round-3 gate ALL 9 PASS (5 reviewers + 4 memory-safety judges; D4/D9 probed double-free/cancellation/concurrency/recursion live). code-reviewer's 4 non-blocking concerns fixed (incl. reverting a reviewer-suggested `ok_or?` that BROKE the build — `size_of()` is a constexpr — back to documented `unwrap_or(0)`). **✅ PHASE 3 (teaching surfaces: wait_points + background_routing inlay hints) DONE — committed** (`5b637e3`-based; 3 gate rounds — gate caught a real cross-module hint-lies-about-the-pool bug intra-module verify missed, then a parallel-implementation issue in the fix; final: both hints read the EFFECTIVE suspend set via a shared `build_effective_suspend_set` helper consumed at all 4 sites incl. the 2 codegen ones, 31/31 golden IR snapshots byte-identical, so the teaching surface provably can't drift from actual routing). Pre-existing follow-ups in todos: Yinz `.copy()` shallow everywhere (m3c); bg byte_size=0 (kernel-mode); jargon pre-built-var blind spot; `ynz-watch read_debounce` env-flake. **✅ PHASE 3 COMMITTED `93b703a`.** **🔶 PHASE 4 (auto-parallelize THE HEADLINE) — in gate Round 2, NOT yet committed.** Headline WORKS: two independent suspending statements overlap (default 204ms ≈max vs --no-auto-parallel 406ms ≈sum) via interleaved inline poll, zero new runtime symbols; new `independence.rs` analysis (consumes producers, no flat-scan — corpse b); unified frame dispatch (corpse a). **Phase 4 surfaced + resolved a PLAN-vs-DESIGN-DOC contradiction**: AC said "byte-identical to --no-auto-parallel" but `design/concurrency.md` Model A INTENDS independent side effects to reorder (`fa();fb()` both print → B/A default vs A/B sequential) — Patrick deferred to "the design doc + golden rules"; RESOLVED: design doc wins (reorder intended; `wait` orders; ACs corrected to scope byte-identical to TRANSPARENT programs; the real safety net = conservative-correct independence + transparent byte-identical + gate-discrimination). **Gate caught 2 LIVE silent-miscompiles my int-only verify missed (gate earned its keep)**: (1) corpse-(a) forked dispatch → non-int parallel returns CRASH → fixed by routing through unified `bind_sm_return_value`; (2) aliasing guard half-closed (`&&` should be `||`) → aliased share-read vs lend-write silently miscompiles → fixed `&&`→`||` (any lend in pair → sequential; only no-lend pure-read pairs parallelize, since Yinz `let b=a` aliases heap objects). Both coordinator-re-verified closed. Aliasing soundness now ENFORCED by the analysis. Pre-existing follow-ups in todos: Yinz `.copy()` shallow + `let b=a` shape-alias × lend-across-wait garbage-read (m3c); bg byte_size=0; --no-auto-parallel env-var→salsa-input deferral; jargon pre-built-var; ynz-watch read_debounce flake. **🔺 GATE ROUND 2 found a FOUNDATIONAL soundness gap (escalated → Patrick: "the correct non-lazy long-term answer following our rules + design doc").** FOUR holes (all coordinator-reproduced): (A) `give` is a write path `build_write_effect_summary` misses (only collects `Lend`) + bare-mutating params record DECLARED `None` not effective `lend` → aliased give/bare-mutating pair races; (B) `-> T errors` return in a parallel group SEGFAULTS — `load_sm_return_value_typed` has no `ErrorsCapable` arm (corpse-(a) forked LOAD reader un-closed); (C) **`share` is NOT enforced read-only** — `bump(share b){ b.v=… }` compiles+mutates the caller (`x.v=101`), contradicting `ownership.md:13/41` + `concurrency.md:651` + M4:759; it's the LOAD-BEARING premise of the independence analysis. (D) 3 banned "previously" comments. **DESIGN-CORRECT FIX (in flight, Patrick green-lit "the correct long-term answer"): 2-part — (1) typeck enforces the missing "verify explicit modifier matches body" rule (`ownership.md:41`): explicit `share` param + body mutation → compile error (bare+mutation stays legal = inferred lend); (2) independence.rs effective-write rule: a call arg is a potential aliased write iff (heap type, not trivially-copyable) AND (declared ownership ≠ Some(Share)) — after (1), declared-`share` heap reads provably don't write → share+share/share+value overlap (better than the BLOCK-2 `||`), give/bare/lend-heap sequentialize. Closes A+C+bare-mutating in one rule.** Authorized Phase-4 scope expansion (the soundness FOUNDATION the headline depends on; building on the unenforced premise = the v0.3-M2-HALT mistake). **R3 FIX LANDED (executor opus, 1884/0): all DIRECT cases closed — share-mutation (field-assign/`share self`/element-write) + share→explicit-lend/give escalation + share-via-method-receiver all COMPILE ERROR; bare+mutation stays legal; EC parallel return byte-identical (HOLE B closed); zero blast-radius. COORDINATOR RE-VERIFY found a RESIDUAL transitive hole: `fa(share x){ helper(x) }` where `helper(b:Box)` is BARE+mutates (inferred lend) → compiles+prints 999 (executor's escalation only checks the callee's DECLARED modifier, missing bare-inferred-lend).** **⚖️ ESCALATED → Patrick chose "Build the effective-ownership fixpoint NOW in M3b" (the COMPLETE fix, NOT the conservative-floor divergence; un-descopes Phase-4 Step-1's transitive write-effect fixpoint, which I'd WRONGLY called "moot").** THE BUILD (in flight, opus executor): a transitive effective-ownership fixpoint (`crates/ynz-typeck/src/effective_ownership.rs`, mirrors `may_block.rs`), three-valued `{Reads, Writes, Unknown}` — unanalyzable → `Unknown` → conservative (NEVER silently Reads). Consumers decoupled: typeck errors on declared-`share`+`Writes` (full `concurrency.md:651`, transitive); independence write-position = (effective ∈ {Writes,Unknown}) AND mutable-heap (shape/array/map; NOT immutable-string per strings.md:99, NOT primitive) → mutable-heap share-READS + bare-READs now PARALLELIZE, give/bare/lend-WRITE mutable-heap sequentialize, transitive-writing share can't compile. Cross-module = `Unknown`→sequentialize. **R4 fixpoint LANDED but R4+R5 gates found the fixpoint approach itself was the problem: 3 consecutive rounds, 5 distinct "name-based classifier misses a write → Reads → silent miscompile" holes (share-self, method, field-nested `p.items.add()`, name-collision user-`add`-shadows-builtin, local-alias `let twin=xs`). ROOT: the effective-ownership fixpoint is a name-based AST walker with no receiver types/alias tracking — structurally cannot enumerate every write form without becoming a full borrow checker.** **⚖️ Patrick confirmed the answer is RULES-OBVIOUS (not flip-flopping): GR5 soundness > GR10 efficiency; no-duct-tape; design doc already documents aliasing-conservatism. → CONSERVATIVE FLOOR.** Implemented: `independence.rs write_positions_for_sig` = mutable-heap TYPE only (any shape/array/map arg = potential write → sequentialize; NOT primitive/string). Sound BY CONSTRUCTION — the independence decision no longer trusts the per-form classifier, closing ALL 5 holes + any future form. Removed the dead effective_ownership threading from codegen (independence + ~13 emit.rs sites + queries + CheckOutput field), compiler-guided. KEPT the fixpoint for the typeck transitive-`share` TEACHING error (best-effort, decoupled from soundness; gaps = missed compile-errors NOT miscompiles, documented). FORFEITED: mutable-heap share-read overlap (2 capability fixtures flipped to `_sequentializes_under_floor` w/ test-ratchet) — documented Design Divergence + reversal path (real type+alias ownership analysis). Coordinator-verified: field-nested + name-collision holes → byte-identical (closed); independence 14 unit + the 3 flipped timing tests pass; headline preserved (no-arg/string/primitive parallelize); design/concurrency.md §Write-effect rewritten honestly. **Granular detail + full gate history (Rounds 1-5 + the floor resolution) live in `.claude/plans/active/v0-3-m3b-auto-parallelization.md` Phase-4 Findings Log (source of truth).** **✅ PHASE 4 COMMITTED `49f1760`** — Gate Round 6 GREEN on the floor (6 gate rounds total). **✅ PHASE 5 COMMITTED `a032f9d`** — re-scoped per Patrick's no-duct-tape call: original `background` EC-collect was a vacuous premise (no `.send`/`.receive` handle-collection in M3b → deferral stays deferred, `ships_in`→M4); instead FIXED the real bug found — a parallel-group return binding crossing a subsequent `wait` crashed (LLVM dominance) → routed through the canonical `bind_sm_result_and_flush` binder (corpse-a honored); P5 gate ALL 6 PASS (deviation-judge 10 probes, code-reviewer "ship it"). Pre-existing follow-up filed in todos: nested-`if`-after-`wait` crossing-local crash (identical both modes, not a P5 regression). **NEXT: P6 (FINAL)** — extend pirates-roster demo + primantis-orders gallery, cross-impl sweep, VSCode bump, CUMULATIVE OPUS 5-reviewer sweep over the whole milestone diff (`git diff 0a4b6d8`), flip status→done → STOP before /release. **Prior M3e radar entry below.**)

**Last Updated (prior)**: 2026-06-07 (**v0.3-M3e SHIPPED LOCALLY — all 3 phases committed on `feat/m3b-auto-parallelization`; plan `status: done`; STOPPED before `/release` per user instruction.** Phase 0 `6344ce2` (foundations: doc correction + shared target machine + danger-matrix fixtures, 12 fixtures + reject-baseline harness). Phase 1 `80789c8` (`frame_layouts_query` salsa query — zero behavior change, Guard G2 recursive resolver unit-proven, Guard G3 cycle recovery). **Phase 2 `cbd027e` — THE LIFT.** 4 gate rounds + 5 fix rounds. Round-3 executor made an unauthorized refactor (`local_to_exported_names` map + `frame_layouts_for_emit` augmentation → single `original_name: Option<String>` field on `FunctionSig`); the refactor was cleaner BUT regressed the original S1 silent-miscompile in `lower_expr_background_state_machine` (missed alias resolution + missing imported-callee frame-layout lookup). Round-4 judges D6/D7 + code-reviewer R4 caught it; round-5 surgical fixes closed all BLOCKs. Final 3-state proof on `v0_3_m3e_alias_local_name_collision`: STATE B (revert any half of the fix) → test FAILS with `LOCAL-BUG` sentinel → regression-lock genuinely bites. **Phase 3 `ab992be`** — pirates-roster `services/scouting.ynz` cross-module demo (`fetchScoutGrade`, `rankRoster` — primitive returns due to pre-existing `WideValueSuspendingReturn` deferral); `primantis-orders/v0_3_m3e_errors.ynz` gallery documents remaining rejects (dynamic-dispatch, FFI-future); registry `cross-module-frame-serialization` deferred entry REMOVED; design doc + roadmap marked shipped; VSCode grammar regenerated; LSP `wait_points` protocol-only test. **Lossy reimpl (`compute_composed_frame_size` + `typeck_type_frame_slots` + `FunctionSig.composed_frame_size`) DELETED — grep-clean.** Cumulative opus code-reviewer (final): **PASS** — "the milestone landed clean ... Ship it." Workspace state: 19/19 m3e fixtures + 209/209 typeck + 31 golden IR (byte-identical) + clippy/fmt/jargon clean. Branch `feat/m3b-auto-parallelization` carries `7bdd5f9..ab992be` (M3e milestone work), NOT YET on `main`. **Honestly documented out-of-scope deferrals**: `check_generic_fn_call` kernel guard (4-field at check.rs:3006 — vacuously safe, trigger named); `WideValueSuspendingReturn` (design/concurrency.md, pre-existing); float.toString decimal128-zero + float.toNumber link-fail (todos.md, pre-existing). **Tree-safety incidents this session**: judge D5 R2 wrote a fixture into the repo (read-only violation; the file actually never materialized — write may have failed; coordinator double-checked); judge D5 R2 also self-reported breaking the tree via state-mutating git — actual impact was a benign SessionStart radar regen (no real corruption). **NEXT**: `/pr` (open draft PR to main) → review/merge → `/release v0.3.0-m3e` — your call when. After M3e merges, M3b Phases 2-6 (background give/copy, auto-parallelize, EC collection, demo) can resume since the universal-reject floor is now lifted. **Prior radar entry below.**)

**Last Updated (prior)**: 2026-06-05 (**v0.3-M3b IN FLIGHT — Phase 0 + Phase 1 done; PAUSED for the M3e milestone.** Branch `feat/m3b-auto-parallelization` (NOT merged). Commits: P0 `e4dd97c` (doc lockdown + registry teaching), P1 `9ed31b0` (cross-module suspends propagation + circular-import clean diagnostic + **universal-reject floor**). **WHAT HAPPENED IN P1:** cross-module `suspends` propagation (typeck) is SOLID + gated. But the cross-module suspending-CALL *codegen* turned out M3a-scale: the scalar `composed_frame_size` shortcut is lossy, and a predictive guard over it leaked **5 distinct silent crashes across 2 adversarial gates** (number crossing-local, transitive×caller-frame-size, 3-module re-export, shape-in-callee, errors×transitive). Root cause: the guard predicts codegen frame safety with a different/shallower typeck analysis → can't match codegen → escapes inevitable (no-duct-tape duct tape). **Patrick's call:** replace the leaky guard with a **universal sound reject** (any cross-module suspending call → clean exit-1 WHAT/WHAT-INSTEAD/WHY error → M3e; no prediction → no escape) and split the real fix into its own milestone. **NEXT:** (1) **`v0-3-m3e-cross-module-frame-serialization` needs a fresh `/plan`** — roadmap stub + `design/future/cross-module-frame-serialization.md` (4 fields) exist, NO execution plan yet. M3e = serialize the full FrameLayout across the export table + kill the typeck reimpl; the 5 escape combos ARE the danger matrix; adversarial gate from phase 1. **PLAN IT IN A FRESH CHAT** (milestone-scale silent-miscompile-area codegen needs fresh context). (2) After M3e ships + lifts the reject, **resume M3b Phases 2-6** (background give/copy, teaching surfaces, auto-parallelize, EC collection, demo) — these don't depend on cross-module suspending-call codegen. **2 PRE-EXISTING base bugs found (NOT M3b regressions, file separately):** float interpolation `${f}` renders decimal128-zero; `-> float` suspending fn ICEs at emit.rs:7686. **2 tree-safety incidents this session (for /learn):** a deviation-judge ran `git stash` + code-reviewer used Edit on the shared tree despite read-only instructions — both caught + recovered; harden reviewer/judge git/edit prohibition. **Prior radar entry below.**)

**Last Updated (prior)**: 2026-06-01 (**SHIPPED v0.3.0-m3** = `v0-3-m2-wait-and-state-machines` concurrency milestone — wait-suspension + state-machine codegen, no `block_on` bridge except the single program-entry driver. Merged to main alongside all M10 teaching-surface fixes; main now fully up to date. Both plans moved to `done/`. Release m-number tracks ship-order (m3), decoupled from roadmap milestone numbering — `v0.3.0-m2` was M10's teaching-surface release, NOT this. **CLEAN SLATE**: repo is now `main`-only (local + remote) — all worktrees + feature branches removed. Before deleting the webpage branch, salvaged its un-merged post-#65 follow-up (`4b6bbb3` website SSG/dev-hydration fixes) onto main; also salvaged the dependabot `tmp` 0.2.5→0.2.7 security bump (`cfc7dd3`). Repo HEAD = `203e040`. **CI VERIFIED GREEN** (2026-06-01, GitHub Actions run 26770935012 = success — not just local gates). CI had been red for ages, masked layer-by-layer; the repair peeled back: (1) `cargo fmt --check` 721-hunk/88-file drift; (2) clippy `-D warnings` ×9; (3) runtime-archive bootstrap — `ynz-driver`/`ynz-watch` `include_bytes!` `target/{profile}/libynz_runtime.a`, which `cargo clippy`/`cargo build --workspace` don't emit before the consumers compile → added explicit `cargo build -p ynz-runtime` (debug + release) steps in ci.yml; (4) macOS siphash key was `#[cfg(target_os="linux")]` → `#[cfg(unix)]` (fixed zero-key bug + unused_mut); (5) insta snapshots baked abs paths → `assert_stderr_snapshot` filter to `[FIXTURES]/` + insta `filters` feature; (6) **macOS DROPPED from CI matrix** (ubuntu-only) — 21 ynz-codegen golden tests failed, some hinting at real macOS codegen differences unverifiable from Linux; tracked in `design/future/macos-platform-support.md` + todos `macos-ci-codegen-support`; (7) design_future_sync SKIP entries. **⚠️ `git add -A` SWEEP**: commit `1a961fc` accidentally committed a prior session's uncommitted WIP — `spec/doc-comments.md`, `design/doc-comments.md`, `design/future/doc-generator.md` (new; resolved a dangling decisions.md/index.md reference), `cspell.json`. Legitimate docs, but unintended — flag for Patrick. `.claude/plans/roadmaps/v0-2-1-lsp-gap-closure.md` still has an unrelated uncommitted edit (left alone). Next: plan the v0.3 concurrency cross-module-propagation milestone. Prior context below.)

**Prior** (2026-05-31, CORRECTED after fetch: local main was 19 commits STALE. origin/main already shipped **v0.3.0-m2** = M10 teaching-surface bug hunt MERGED (`fe15e5c`/`13264af`). So "M10 unmerged/consolidate" was a stale-local artifact — M10 is DONE+SHIPPED. Reconciled local↔origin via merge (`e924faf`). Repo cleanup still stands: gitignored ephemera (`.claude/worktrees/`,`.analysis/`,`.claire/`,`*.snap.new`); dropped 3 dead stashes; deleted 73 dead branches. **NAMING TRAP**: the *release* `v0.3.0-m2` = M10 (v0.2.1) teaching-surface work; the *workstream* `v0-3-m2-wait-and-state-machines` is a SEPARATE in-flight concurrency milestone — do NOT conflate. v0.2.1 M1/M3/M5 LSP plan ON HOLD (serial-not-parallel). webpage branch parked.)

---

## Active Workstreams

*(auto-rebuilt by SessionStart hook from `.claude/plans/active/*.md` front-matter — do not edit by hand)*

<!-- RADAR-START -->
### Active Roadmaps
- v0-2-1-lsp-gap-closure (Patrick Rizzardi) — 0 active plans — 2026-05-30
- v0-3-concurrency-perf (Patrick Rizzardi) — 1 active plans — 2026-05-31
- webpage-docs (Patrick Rizzardi) — 0 active plans — 2026-05-20

### Active Workstreams
- v0-3-m3f-codegen-correctness-fixes (Patrick Rizzardi) — 9 files touched — 0/77 done — roadmap: v0-3-concurrency-perf — 2026-06-09
<!-- RADAR-END -->

---

## Environment & Commands (CRITICAL — survives compaction)

**Project**: ynz
**Language**: Rust (compiler implementation)
**Toolchain**: Rust 1.95 stable, LLVM 18.1.8, cargo workspace
**LLVM prefix**: `/usr/lib/llvm-18` (set in `.cargo/config.toml` via `LLVM_SYS_PREFIX`)

```bash
source $HOME/.cargo/env    # activate Rust in this shell session

cargo build --workspace    # build all crates
cargo test --workspace     # run all tests (310 as of M3)
cargo clippy --workspace -- -D warnings
cargo fmt --all

# Run the compiler
./target/debug/ynz run crates/ynz-driver/tests/fixtures/hello.ynz
# → hello, yinz

./target/debug/ynz run crates/ynz-driver/tests/fixtures/m3_fib.ynz
# → 55

./target/debug/ynz run crates/ynz-driver/tests/fixtures/m4_player.ynz
# → Patrick / 120 / Patrick  (M4 P4 success-criteria fixture)

# Current branch: main (v0.1.0 shipped; v0.2-M1 planning in progress)
```

---

## Active Decisions (append with WHY)

- [2026-05-31] **⚠️ CROSS-CHAT — main advanced to v0.3.0-m2 (teaching-surface bug hunt) while you were on this branch**: A sibling chat shipped the v0.2.1-M10 teaching-surface bugfix milestone (14 audit bugs + 3 same-class siblings — unused-import false-positives, inlay-hint mutated-binding lies incl. `wait`/`background`/interpolation wrappers, hover, completion, banned-jargon quick-fix, gold inlay color). Merged to main (commit `13264af` + ext bump `3a2f975`), released as tag **v0.3.0-m2**, GitHub release + dual VSIX assets published. **THIS BRANCH (`v0.3-m2-wait-and-state-machines`) does NOT have those fixes yet** — it branched off the old main (`d509770`), same as M10; merging M10 to main does NOT auto-flow here. **WHAT TO DO**: when ready for your next merge/release (or to build on the fixed teaching surfaces), run `git fetch origin && git merge origin/main` from this worktree. As of 2026-05-31 there is ZERO file overlap between M10 and your work (M10 = typeck/LSP/registry teaching surfaces; you = ynz-runtime/codegen `wait`/state-machines), so the merge is clean today — but your later phases will touch `inlay_hint_passes.rs`/`check.rs` (wait-point inlay hints are v0.3 territory), and conflict risk grows the longer you wait, so merge sooner rather than later. NOTE: M10 left 4 tracked walker-completeness follow-ups in `.claude/todos.md` (`inlay-hint-walker-completeness-followup`) — some are wait-hint-adjacent, worth a look when you do wait-point hints. **WHY this note exists**: Patrick asked that the wait-and-state chat be told what shipped + how to sync, since cross-branch propagation isn't automatic.
- [2026-05-31] **Serial, not parallel — AND repo hygiene locked**: Patrick pivoted off the parallel worktree-dispatch model (the [2026-05-30] "PARALLEL TRACK" decision below is amended): v0.2.1 LSP work proceeds SERIALLY, one plan at a time, no more spinning up parallel worktrees. The M1/M3/M5 ("Plan 2") drafting was halted mid-`/plan` per this call. WHY: parallel worktrees were producing divergence/merge tangle (stale planning copies on main, snapshot drift across branches, 60+ dead branches). **Repo-hygiene policy now enforced via `.gitignore`** (commit `760e024`): `.claude/worktrees/`, `.analysis/`, `.claire/`, `*.snap.new` are ignored — they were the recurring source of main-checkout churn. `state.md`/`todos.md` stay tracked (commit alongside real work). Cleanup 2026-05-31 also dropped 3 dead M4-era stashes + deleted 73 dead branches. **CORRECTION (same day, post-fetch)**: local main had been 19 commits stale — origin/main already shipped M10 as the `v0.3.0-m2` release (`fe15e5c` merge + `13264af`). So the planning artifacts (roadmap, design/lsp.md inventory, features.toml deferred entries, audit reports) are ALREADY ON MAIN via the M10 merge — they did NOT need re-committing. Reconciled local via `git merge origin/main` (`e924faf`). `stash@{0}` (stale main-checkout copies) is now pure redundant insurance — safe to drop. M10 branch + worktree are now DEAD (merged) and removable.
- [2026-05-30] **v0.2.1 LSP gap-closure = PARALLEL TRACK to v0.3, shipped FIRST**: LSP work is faster than v0.3 concurrency work, so v0.2.1 gets its own `0.2.1` tag (branched off main, which is at `0.3.0-m1`), released first, THEN merged/rebased into the v0.3 line (M9 owns the merge). WHY: roadmap was written 2026-05-21 assuming Cargo sat at 0.2.0 and v0.2.1 tagged before any v0.3 — but v0.3.0-m1 already shipped, voiding that assumption. Roadmap `v0-2-1-lsp-gap-closure` amended. **8-plan batching locked** (M10 alone → M1+M3+M5 → M2 → M4 → M6+M7 → M8 → M9 → M11; only intra-plan dep is M6→M7). See roadmap "Execution Plan Batching" table.
- [2026-05-30] **M10 (teaching-surface bugfix) fully planned, reviewer-PASS, dispatched FIRST — awaiting Patrick's go**: Plan at `.claude/plans/active/v0-2-1-m10-teaching-surface-bugfix.md`. 12 phases fixing all 14 audit bugs (`.analysis/bugs.md`), anchors re-verified vs current source (baseline 1434 tests). plan-reviewer PASS (Tier A, zero required fixes). 4 decisions locked: P9/P10 kept in M10; Phase 6 hover = context-aware fix only (no fallback — fallback relocates the wrong-output hole); Phase 7 BannedJargon quick-fix carries the WHY (the lesson); dispatch = isolated git worktree off main. Next action: `/execute-plan` in a worktree on Patrick's go.
- [2026-05-12] **Compiler implementation language = Rust**: Mature LLVM bindings (inkwell), strong ADT/pattern-matching for AST, salsa framework gives incremental builds + LSP "for free." See `design/compiler-language.md`.
- [2026-05-12] **MVP scope split into v0.1 / v0.2 / v0.3 / v1.0 / v2+**: Concurrency keywords parse from day 1 but run sequentially until v0.3 (when auto-parallelization optimization engages). See `design/mvp-scope.md`.
- [2026-05-12] **Error auto-propagation = flow-sensitive narrowing (Option B under, Option A in feel)**: If user calls `.failed()` before using the success value, auto-propagation suppressed; otherwise compiler auto-propagates at first use. Same `.failed()`/`.or()` API works inside AND outside `errors` functions. See `design/errors.md`.
- [2026-05-17] **M4 shipped (tag v0.1.0-m4, 316 tests)**: shapes, UFCS methods, ownership modifiers (share/lend/give), const deep-immutability, extends/follows/base/hidden, LLVM readonly+noalias, ynz_alloc/ynz_free runtime shims, vtable globals, wrapping/saturating int arithmetic, type-attached constants. Plan: `.claude/plans/done/m4-shapes-functions-ownership.md`.
- [2026-05-12] **Generic functions = v0.1, `<T>` syntax with `follows` constraints inline**: Type inference at call sites. `where` clauses rejected — inline keeps constraint visible next to the parameter. See `design/generics.md`. (Syntax locked at `<>` not `[]` since 2026-05-17 M5 P0; `[]` is index access only.)
- [2026-05-12] **Numeric types = handwritten, validated against IEEE 754 test vectors**: `number` = decimal128 (default), `number[N]` up to N=4096, `float` = f64, `int` = i64. Sized variants (`int[N]`, `f32`) deferred to v2+. Overflow panics by default with `.wrappingAdd()`/`.saturatingAdd()` escape valves. See `design/numeric-types.md` + `design/mvp-scope.md#v2--deferred-features`.
- [2026-05-12] **Strings use `.get()` (code point) + `.byteAt()` + `.graphemeAt()`**: No `char` type. Default indexing is by Unicode code point. Bytes and graphemes are explicit alternates. See `spec/strings.md`.
- [2026-05-12] **Bracket sugar for `.get()` and `.set()` on all collections AND maps**: `arr[i]`, `m["key"]`, `s[i]` all desugar to `.get()`. Writes via `arr[i] = v` desugar to `.set()`. Strings immutable (no write sugar). Types reject bracket access entirely — forces dot for fields. Reverses earlier no-`map[key]` decision. See `design/collections.md`.
- [2026-05-12] **Iterable contract = two types (`Iterable[T]`, `FallibleIterable[T]`)**: In-memory collections follow `Iterable[T]`; I/O sources follow `FallibleIterable[T]`. Same `for` syntax; compiler infers fallibility from the source's contract and auto-propagates errors when needed. Stdlib adapters `.orSkipFailures()` and `.withErrors()` for ergonomic fallible-to-infallible conversion. See `design/iterables.md` + `spec/iterables.md`.
- [2026-05-12] **Import aliases + duplicate-name compile error**: TS-style `{ name as renamed }` and `namespace as renamed`. Duplicate names (including stdlib-vs-local collisions) refuse to silently pick — compile error forces aliasing. See `design/modules.md` + `spec/modules.md`.
- [2026-05-12] **Lock file = TOML, flat array of `[[package]]` tables**: Same format as `yinz.toml`. Diff-friendly, manually editable in emergencies. Install mechanism (content-addressed global cache, hard-links, parallel resolver, lazy integrity) aims for bun-class speed — v0.22 work. See `design/packages.md` + `spec/packages.md`.
- [2026-05-12; package mgr moved to v0.22 on 2026-05-20] **Granular versioning sequence (24 versions to v1.0 + 3 post-launch versions)**: Each version ships ONE focused thing. v0.1 = core language only. v0.2 = LSP+watch+fmt. v0.3 = auto-parallelization. v0.4 = linting tier. v0.5–v0.21 + v0.23–v0.24 = stdlib modules. v0.22 = package manager (moved late so it lands into a stable-ish language — no public release until v1.0 means early-shipping the package manager would just churn packages every pre-v1.0 breaking change). v1.0 = stability + grammar lock. See `design/mvp-scope.md`.
- [2026-05-12] **"Compiler IS the linter" — no separate `ynz lint` command**: Third tier of compiler diagnostics (suggestions). Customization via `[lint]` in `yinz.toml` ships v1.x. See `design/linting.md`.
- [2026-05-12] **Teaching mission codified**: All diagnostics follow WHAT/WHAT-INSTEAD/WHY three-part format. Enforced by Diagnostic constructor. IDE muted hints extend this to all teaching surfaces. See `design/teaching-mission.md`, `design/ide-hints.md`.
- [2026-05-12] **Compiler error style spec + jargon audit**: `design/compiler-errors.md` is the canonical style spec. Banned-jargon list enforced by `crates/ynz-diagnostics/src/banned_jargon.rs`. See `design/compiler-errors.md`.
- [2026-05-12] **Error-flow metaphor = "cascades", not "bubbles up"**: Per patrick's preference. Updated across all spec/design docs.
- [2026-05-12] **M1 compiler complete (820bfdc, tag v0.1.0-m1)**: `ynz run hello.ynz` → `hello, yinz`. 51 tests.
- [2026-05-12] **M2 decimal128 strategy = hand-rolled, no crates**: `int` = i64, `float` = f64, `number` = hand-rolled decimal128 in `ynz-numerics`, IEEE 754 test vectors.
- [2026-05-13] **M2 complete (tag v0.1.0-m2)**: Variables, arithmetic, all M2 types, 118+ tests. Full LLVM codegen for M2 AST.
- [2026-05-14] **Design-lockdown (PRs #5 + #14)**: `shape` keyword locked for M4 type declarations (not `type`). 3 new rule files (inference.md, plan-invariants.md, vocabulary.md). 5 graveyard entries. Golden Rules 8/11/12 clarified. M4+ plans must include 5-subsection Invariants block.
- [2026-05-14] **M3 complete (9653dbd, tag v0.1.0-m3)**: Control flow (`if`, `while`, `for`), multi-case `if`, user-defined functions with params + return types, two-pass typeck, return-path analysis, full LLVM lowering. 310 tests. `fib(10) = 55`. Plan: `.claude/plans/done/m3-control-flow-fns.md`.
- [2026-05-16] **M4 Doc-PRs 1+2 complete (54521dd)**: Non-OOP model locked. `shape`/`follows`/`extends`/UFCS locked. Body-level `.share/.lend/.give` removed. Annotation-only struct literals. `override` removed. All design+spec docs rewritten. Commit message: "M4 P1 (lexer) cleared to start."
- [2026-05-16] **M4 P1 complete (05c5296, merged to main)**: 8 new tokens (49→57). 6 banned-keyword handlers. All tests green.
- [2026-05-16] **M4 P2 complete (84db1d2, merged)**: AST + parser for shapes. 4 new Expr variants, 1 Stmt, 2 Type variants. 68 parse tests green.
- [2026-05-16] **M4 P3a complete (244ac6d, merged)**: ShapeTable, Type::Shape, struct-lit typeck, field access/assign, UFCS, hidden-field guard, base-shape guard. 90 typeck tests green.
- [2026-05-16] **M4 P3b complete (3508e7b, merged)**: `extends` field inheritance + cycle detection, `follows` contract verification, `Type::Dynamic`. 96 typeck tests green.
- [2026-05-17] **M4 P3c complete (7c86f6a, branch feat/m4-ownership, PR open)**: `is_consumed` scope tracking, use-after-give, const-cannot-be-lent/given. All 5 const deep-immutability paths covered. 102 typeck tests green. PR: https://github.com/yinzers/yinz-lang/pull/new/feat/m4-ownership
- [2026-05-17] **M4 merged to main (direct merge by patrick)**: feat/m4-verification (P3c–P7 bundled — ownership, codegen, fixtures, v0.1.0-m4 prep) landed on main. Cargo.toml at `0.1.0-m4`. Tag `v0.1.0-m4` local.
- [2026-05-20] **ynz fmt map-entry destructuring fix (f139b30)**: `for ((key, value) in m)` was emitting `__entry` internals. Added `map_destructure_pattern: Option<(String, String)>` to `Stmt::For`; formatter reconstructs `(k, v)` header and skips 2 synthetic let bindings.
- [2026-05-20] **ynz fmt empty-array comment preservation (1b4dbf8)**: Comments inside empty array literals were dropped. Fixed via `ctx.between(span)` check in `emit_stmt_with_inline` + advancing `prev_end` past the array span.
- [2026-05-20] **ynz fmt destructuring fix (bc31315)**: `ynz fmt` was emitting `for (__shape in intervals) { let minutes = __shape.minutes ... }` instead of preserving `for ({minutes, timeframe} in intervals)`. Root cause: parser desugars at parse time; formatter saw only the desugared AST. Fix: added `destructure_pattern: Option<Vec<ForDestructureBinding>>` to `Stmt::For`; parser stores original bindings before desugaring; formatter reconstructs original header and skips the synthetic let bindings when emitting the body. Typeck/codegen unchanged.
- [2026-05-20] **v0.2-M4 watch post-ship: 7 bugs fixed during trading-v4 real-world use** (c3fa69c + 27b3c98 + c510769 + 481c405 + 3582754 + 5a9f624 + ac9367c + 9aec37d): (1) text-mode UI completely silent; (2) notify IN_OPEN feedback loop; (3) double Watching prompt; (4) terminal clear on no-change skips; (5) yinz.toml walk-up + [entries] multi-entry support; (6) single-file mode skipped loading shared project files for imports; (7) hardcoded cc linker + missing -no-pie + missing runtime library. All shipped to main.
- [2026-05-20] **v0.2-M4 watch bug fixed (c3fa69c)**: Three bugs: (1) rebuild_one_with_emitter had no text-mode UI output — print_building/print_success/print_errors never called on file-change events; (2) notify 8.x watches IN_OPEN causing infinite rebuild loop — fixed via content-equality guard against shadow DB (force=true bypasses for initial build); (3) double "✓ Watching…" prompt removed. Watch now correctly rebuilds exactly once per real user edit.
- [2026-05-20] **v0.2-M2 SHIPPED (tag v0.2.0-m2, 1028 tests)**: ynz-lsp crate (JSON-RPC stdio, salsa-backed diagnostics/completion/hover), VSCode extension (.vsix + stable URL yinz-latest.vsix), registry-derived TM grammar (ynz-tmgrammar). Every registry entry auto-appears in IDE. Plan: `.claude/plans/done/v0-2-m2-lsp-thin-slice.md`.
- [2026-05-17] **M5 plan approved + Phase 0 shipped (524ca2e, branch chore/m5-doc-lockdown)**: Generics + Collections + Maybe<T> milestone planned and plan-reviewer Round 2 PASS. Locked decisions: `maybe<T>` moves from M6 to M5 (cleanest .get() API); map = Swiss Tables + SipHash-2-4 + perfect-hash for static-key literals; for-loop over built-in collections is typeck+codegen special-case with REPLACE-AT M7 markers; auto-promotion `array<T>` → `fixed<T>` ships codegen-only in M5 (Tier 3 lint defers to v0.4, muted hint defers to v0.2). 8-phase plan (P0-P6). P0 (doc lockdown — master plan M5/M6/M7 paragraphs updated to `<>`, design/maybe.md created with 9-row LLVM lowering decision table + 10-row flow-sensitive .value rules + 9-row none-inference rules + documented v0.1 cycle-leak limitation, spec/maybe.md syntax-updated) SHIPPED on branch `chore/m5-doc-lockdown`, awaiting merge to main. Plan: `.claude/plans/active/m5-generics.md`.
- [2026-05-17] **M5 P1 shipped (49940c9)**: Lexer + AST scaffolding. Tok::None, 3 Type variants, 2 Expr variants, 1 Stmt variant. 401 tests.
- [2026-05-18] **M7 SHIPPED (tag v0.1.0-m7, 782 tests)**: Full Unicode strings (backtick-only, NFC equality, 16 methods, SIMD search, interpolation builder), `errors` keyword (flow-sensitive auto-propagation, Frame/SourceLoc/trace, {i64,i64} ABI), Iterable<T> protocol (synthesized wrappers, user shapes, string iteration, range first-class, REPLACE-AT M7 markers all gone). 9 PRs across 11 phases (P0–P5 + P6). Plan: `.claude/plans/done/m7-strings-errors-iterables.md`.
- [2026-05-18] **M7 STARTED — P0 doc lockdown active (branch chore/m7-doc-lockdown)**: 4 locked decisions from pre-draft confirmed: (1) SSO ships in M7 (23-byte inline, 24-byte struct, tag byte at offset 23); (2) SIMD UTF-8 ships in M7 (`simdutf8` crate); (3) Synthesized iterator wrappers (`ArrayIter<T>`, `FixedIter<T,N>`, `MapIter<K,V>`, `StringCodePointIter`) + muted-hint surface deferred to v0.2 LSP; (4) Full base error shape ships: `.message`, `.suggestions`, `.trace` (`array<Frame>`), `.source` (`SourceLoc`), compile-time-emitted frame stack (NOT libunwind). P0 additional locks: single backtick-only string form (removed double-quote form), `.orSkipFailures()` is PURE (no I/O — separate `.logSkippedFailuresTo(sink)` for logging), `.withErrors()` returns `Iterable<maybe T errors>` (NOT `Iterable<Result<T>>`), `Frame.line` is `maybe int` one-based, `unicase` crate for case-folding, `unicode-normalization` + NFC cache bit, MapEntry dual destructuring forms. Plan: `.claude/plans/active/m7-strings-errors-iterables.md`.
- [2026-05-18] **M6 SHIPPED (tag v0.1.0-m6, 631 tests)**: All 8 phases P0–P6 done via PRs #25+#26. options types (i8 tags, exhaustiveness, toString), union types (tagged-struct, is-narrowing, shape aliases), fallible conversions (.toInt/.toFloat/.toNumber on float/number/string), early-return narrowing, 3 new design docs, M2+M3 catch-up closed. Plan archived: `.claude/plans/done/m6-options-unions.md`.
- [2026-05-17] **M5 FULLY SHIPPED (tag v0.1.0-m5, 574 tests)**: All 8 phases P0–P6 done via PRs #17–#24. Generics engine (MonomorphizationTable, GenericFnTable, GenericShapeTable, follows constraints), BuiltinArray/Fixed/Maybe/Map typeck, LLVM codegen (ynz_array_* runtime, {i64,i64} maybe, SipHash-2-4, Swiss Tables), 5 runnable fixtures, examples/pirates-roster M4+M5 showcase, m5_errors gallery. Plan archived: `.claude/plans/done/m5-generics.md`.

---

## Superseded / Archived

- (none)

---

## Project-Wide Notes

*(cross-workstream context, gotchas, user preferences not tied to one plan)*

- **M4 catch-up obligations from M3**: replace read-only params with `share`/`lend`/`give` ownership annotations; update `m3_share_param_deferral.ynz` stderr snapshot when `share` works; add overflow escape methods on `int`; add type-attached constants (`int.max`); generalize `PrimitiveIntrinsicTable` to general method dispatch.
- **M6 catch-up obligations from M3**: `is Type` narrowing in multi-case `if`; options-variant matching; exhaustiveness checking for options/unions.
- **M7 catch-up obligations from M3**: replace `range` builtin with `Iterable[T]` protocol; allow Range as first-class value; remove M3 special-cases in typeck + codegen; Unicode canonical equivalence for string multi-case (`ynz_string_eq`).
- **M4 plan must include**: 5-subsection Invariants block per `.claude/rules/plan-invariants.md` (Safety, Performance, Teaching, Runtime Dependencies, Kernel-Mode Behavior). const deep-immutability invariants required in Safety + Performance. `shape` keyword reservation in P1 lexer.
- **[2026-06-03] v0.3-M3a Phase 1 (crossing-local suspension codegen) — COMMITTED `19f3da5` on `feat/m3a-suspension-codegen`** (65 files, +5098/-407; Patrick-approved). Full round-by-round in the plan's resume anchor. Core (locals crossing a `wait`: int/bool/float/number/string/shape/decimal128) rock-solid — 8/8 ACs, 30-probe shadow-judge + 31-probe EC sweep clean, frame-embed ownership-correct (one alloc/task, no leak), `number` precision perfect on every path incl. fallible-async `-> number errors` return + EC return-propagation (byte-exact, alloc=1/free=1). Took 27 fix-rounds (2 owned bounces — R14 silent-miscompile + R19 leak, both reverted next round; shadow-guard whack-a-mole resolved at R15 by safe-conservative+deferral; R17 return scope-creep caused the R18-27 cascade, fully contained). Surface FULLY SAFE: every unsupported case fails LOUD at compile time. Deferrals (documented w/ trigger): Shape/Shape-errors returns; union/maybe/dynamic crossing locals; same-name reuse around a wait; standalone background-wrapper EC result-collection (M3b). **NOT-M3a pre-existing base-compiler bugs to file separately**: composite-of-shape value returns (maybe/union/map<Shape>) silently miscompile with or without suspension; non-suspending `-> Shape` return-by-value garbage; `-> float errors` LLVM select-instr verify failure. **NEXT: Phase 2 (while-loop suspension)** — P2 entry obligation: extend the shadow-guard suspension scan to loop/match bodies when WaitInsideLoop lifts.
- **[2026-06-03] v0.3-M3a Phase 2 (while-loop suspension) — ✅ COMMITTED `e989025` on `feat/m3a-suspension-codegen` (33 files +1360/-213, Patrick-approved, 7 fix rounds, whole-plan unattended).** Round-by-round + verdicts live in the plan's Phase-2 Findings Log + ORCHESTRATION STATE anchor (the resume source — read it). Executor built while-loop SM codegen + narrowed WaitInsideLoop to for/match-only. Fix loop hit the predicted shadow/back-edge whack-a-mole (the P2 entry obligation above): R1 local-back-edge ICE + D6 false-positive + D2 demo-unwired → R2 fixed, A3 found local-shadow SILENT MISCOMPILE → R3 fixed (Case 3 in outer_is_genuine_crossing_local), code-reviewer found param-twin ICE → R4 SYSTEMATIC fix (param gate + audit of all 6 post-suspension-reference gates claiming class closed). **ROUND 7 (Patrick-decided Option A) — ALL 6 reviewers PASS, shadow class CLOSED, COMMITTED e989025.** Full arc: R1 back-edge ICE/D6/D2 → R2 fixed, A3 found local-shadow SILENT MISCOMPILE → R3 fixed → param-twin ICE → R4 systematic → R5 over-reject → R6 "right-long-term" attempt (compile non-crossing shadows) → **R6 introduced a SILENT MISCOMPILE** (param shadow + wait-inside-shadow-scope → garbage read; caught by code-reviewer Section-4, MISSED by my pre-verify + A8 + acceptance) + A8 local over-reject. **ESCALATED to Patrick** (not a unilateral defer). **DECISION (Patrick, Option A)**: non-async shadows COMPILE (Yinz allows shadowing, design/linting.md); async-fn shadows clean-REJECT (the documented ShadowsCrossingLocal deferral, params+locals consistent). R7 reverted R6 Part B to blunt `param_has_nested_let_shadow`, kept R6 Part A codegen (non-async compile fix), reverted the doc. **Full Rust-level shadow parity = NEW roadmap milestone M3c (`v0-3-m3c-shadow-parity`)** + todos "Soon" + design/concurrency.md:241-305 + registry `shadow-crossing-local-support` — documented AND planned (Patrick held me accountable: deferrals must be tracked, not silently dropped for speed). Coordinator-verified R7: silent-miscompile case 3x → clean reject (no garbage), non-async→5/9+2/1 compile, A8→reject, suite green (3 known env/timing flakes: sync_bridge, read_debounce, current_rss_bytes — all ynz-watch/runtime, none Phase-2). R7 gate: ALL 6 PASS (code-reviewer 'R6 corpse buried' + judge A9 'shadow class closed' + design/rules/plan-adherence/acceptance). Committed e989025 (all 18 P2 fixtures staged, AC Evidence + Phase Review Gates filled). **NEXT: P3 (for/match suspension) → P4 (demo/gallery + cumulative opus sweep + status→done + /release).** Follow-ups (NOT P2): shadow-diagnostic-span teaching polish; ynz-watch debounce + rss #[serial] flakes; the M3a-P1 NOT-M3a base bugs (composite-of-shape returns etc.) + the non-coroutine-was-actually-general lexical-scope fix shipped in R6 Part A.

---

- **[2026-06-04] v0.3-M3a Phase 3 — ✅ COMMITTED `e7bd2cd` (54 files +4253/-493, 8 fix rounds, all 5 final reviewers PASS, Patrick-approved). P4 (demo/gallery/CHANGELOG[0.3.0-m4]/VSCode-m4) DONE+uncommitted; CUMULATIVE OPUS SWEEP (5 reviewers, P0-P4) IN FLIGHT → on all-PASS flip status→done + commit P4 + STOP before /release. NEXT milestone: `/plan` v0-3-m3c-array-by-value (the long-term right answer). /learn corpses to do: parallel-flush-dispatch drift (no-duct-tape #7), paper-vs-tested audit, flat-scan-re-derivation-of-crossing-set (2nd inst), acceptance-surveying-not-running (AC#4 overridden 4×).** Detailed round-by-round in the plan's Phase-3 Findings Log + ORCHESTRATION STATE anchor (the resume source — READ IT). Historical saga: P3 lifts `WaitInsideLoop` for `for`/`match`. Hit the deepest tail yet — a recurring SILENT-MISCOMPILE disease: every value type × position {crossing-local, loop-var, return} × storage {literal/runtime, primitive/composite} must survive a heap-frame suspension, and each fix round found one more uncovered combo (bool/string flush → decimal128-loopvar+fixed<T>-crossing+map-(k,v)-SIGSEGV → decimal128-array-LEAK+map-nested-wait+Range-crossing → array<Shape>-loopvar → array<Shape>-runtime-field). **ROOT FIXED in round 5: the parallel flush dispatch (no-duct-tape #7) was UNIFIED** (single `flush_var_slot_to_frame`), killing the per-type-drift. **6 fix rounds + a 7th in progress** (the cap was overridden by Patrick decisions). Working core is large + correct (scalars, strings, array<scalar>, map-counter, match-arms, direct shape crossing, literal-field array<Shape>); 7 documented loud-reject deferrals (fixed<T>, stored-range, expr-iter, map-entry-after-wait, range-crossing, nested-shape, array-shape-runtime-field). **🔴 MAJOR ARCHITECTURAL DECISION (Patrick 2026-06-04): array<Shape> by-value element storage = long-term fix, its OWN milestone `v0-3-m3c-array-by-value`** (a 2-3 session breaking YnzArray ABI change — current bug: array stores element POINTERS to stack allocas that dangle across suspension; fix: store shape bytes by-value inline like array<int>, GR3/GR8-correct). M3a ships an INTERIM loud-reject guard (`ArrayShapeRuntimeFieldWithWait`); M3c does the right answer + lifts the guard. **Documented (Patrick's condition "fixed long term"): `design/future/array-by-value-element-storage.md` (full design+sketch+risks) + todos.md "Soon" + registry deferral + design/concurrency.md.** NOTE: distinct from `v0-3-m3c-shadow-parity` (the P2 shadow milestone) — both are post-M3a follow-ups loosely tagged M3c; roadmap reconciles numbering. **NEXT: round-7 fixes (delete the under-rejecting flat-scan pre-check per code-reviewer+judge-D1 — crossing-set is the gate; 3 plain-English jargon rewords) → re-gate → write AC Evidence + Phase Review Gates → SURFACE COMMIT FOR PATRICK REVIEW (review-before-commit) → P4 (demo/gallery + cumulative opus sweep + status→done) → STOP before /release.** /learn corpses pending: parallel-flush-dispatch drift; paper-vs-tested audit; flat-scan-re-derivation-of-crossing-set (2nd instance); acceptance surveying-not-running (overridden 3×).

## Active Decisions (Architectural — v0.3-M3 ship entry)

**[2026-06-04] v0.3.0-m4 RELEASED — M3a suspension codegen SHIPPED.** PR #69 squash-merged to `main` (`8fcec02`); release commit `e02e5c5`; tag `v0.3.0-m4`; GitHub release marked **Latest** with both `.vsix` assets (`yinz-0.3.0-m4.vsix` + `yinz-latest.vsix`, stable pin URL HTTP 200). Cargo `0.3.0-m3`→`0.3.0-m4`; CHANGELOG `[0.3.0-m4]` written in P4; VSCode `package.json` at `0.3.0-m4`. CI green on the merged tree (Build and test ubuntu-latest, 5m48s). **What shipped (P0–P4):** `sleep`/`sleepBlocking` rename + kept-guard re-doc; frame-backed crossing locals (decimal128 byte-exact); `wait` inside `while`/`for`/`match`; demo + galleries + cumulative proof. **The headline win:** the per-type-drift suspension-miscompile class (≥10 silent-miscompile rounds) is structurally dead — unified `flush_var_slot_to_frame` (one store/reload dispatch, symmetric). 12 loud-reject deferrals (5 P1 + 7 P3), zero silent-wrong. Cumulative opus sweep: all 5 reviewers PASS, 28/28 ACs MET on the live binary. Plan → `done/`. **Follow-on milestones tracked:** `v0-3-m3b-auto-parallelization` (the analysis half) + `v0-3-m3c-array-by-value` (lifts the interim `array<Shape>` guard — `design/future/array-by-value-element-storage.md`). 2 base-bug follow-ups filed in todos (`array<boolean>` literal ICE, `Type::Options` crossing-wait coincidence).

**[2026-06-01] v0.3.0-m3 STAGED, NOT YET RELEASED — user-gated.** All Phases 6-9 complete. Tag = `v0.3.0-m3` (NOT m2 — `v0.3.0-m2` is taken by the M10 Teaching-Surface release, commit `13264af`; release numbers track ship order not roadmap milestone numbers). Cargo.toml bumped `0.3.0-m1` → `0.3.0-m3`. CHANGELOG `[0.3.0-m3]` section written. VSCode `package.json` at `0.3.0-m3`.

**WHY the inference model (the winning design):** `design/future/concurrency.md` documented it all along — whole-program TRANSITIVE may-block analysis from `{sleepAsync}` seed, every transitively-suspending function becomes a stackless state machine, inline poll-and-yield at every suspending call site, composed frames (one `ynz_alloc` per spawned task tree), NO bridge anywhere. The M2 HALT happened because the original plan cut M2/M3 at "state machines | may-block analysis" — an unbuildable line, because the analysis engine is REQUIRED to build the state machine layer. Moving the may-block engine to M2 fixed this. The `block_on` sync bridge (originally "Shape B") was deleted in Phase 8 as a result.

**M2-boundary decisions deferred to M3 (locked):** `WaitInsideLoop` and `LocalCrossesWait` are M3-scope (frame-backed mutable locals transform). Sub-expression suspending calls and non-self mutual recursion are M3-scope (per-frame layout metadata + expression-position suspension). These are compile errors in v0.3-M3, not regressions.

**Next milestone — v0.3-M3 SPLIT into M3a + M3b (2026-06-01, Patrick):** the old single "M3" bundled ~10 capabilities across two concerns (codegen-completion vs. analysis), and the codegen half is a prerequisite for the analysis half (same unbuildable-boundary shape as the M2 HALT). Split documented loudly in roadmap `v0-3-concurrency-perf.md` ("M3 SPLIT" + "Kept M2 positional guards" Architectural Decisions).
- **M3a — Suspension Codegen Completion** (`plans/active/v0-3-m3a-suspension-codegen.md`, status: pending_approval, plan-reviewer PASS Tier A): lifts `LocalCrossesWait` + `WaitInsideLoop` via frame-backed mutable locals (P1) + loop/match suspension (P2 while, P3 for+match); P0 = `sleepAsync`→`sleep` / `sleepMs`→`sleepBlocking` rename + reword the two KEPT guards; P4 = demo/gallery/verification. KEEPS `SubExprSuspendViolation` (Golden Rule 7) + `MutualSuspensionCycle` (rare) as permanent design decisions. **Three stale roadmap claims corrected by source verification:** M8 cross-file typeck is ALREADY DONE (not a blocker); `ynz_rt_spawn` already exists (shipped M2); sleep rename was decided-but-undone. Tier-A trap caught in review: `number` is decimal128 (16 bytes) but frame local slots are 8-byte i64 → P1 must use two-slot/pointer-back, never truncate.
- **M3b — Cross-Module + Auto-Parallelization** (`v0-3-m3b-auto-parallelization`, planned AFTER M3a ships): cross-module `suspends` propagation (rides existing salsa `module_signatures_query` — no compiled-package format needed), dependency-graph auto-parallelize pass, `background` give/copy inference, CPU/IO routing, `wait_points` + `background_routing` IDE hints.
**Status: M3a EXECUTED + SHIPPED as v0.3.0-m4 (2026-06-04). Plan in `done/`. Next: M3b (auto-parallel) or M3c (array-by-value) — both planned, neither started.**

---

## Active Decisions (Architectural — v0.3-M2)

- **[2026-05-31] 🔴 VERIFIED ROOT CAUSE — M2/M3 milestone boundary was mis-drawn; the bridge contradicts `design/future/concurrency.md` (which had the right model all along). RE-SPIKE DONE; bridge-based rework (Phases 6-9) SUPERSEDED; PAUSED awaiting Patrick.** Re-spike confirmed all 3 bugs + 2 residual aborts (`.analysis/m2-respike-findings.md`). After Patrick pushed, coordinator read the concurrency DESIGN docs (skipped in the `/plan` research step). `design/future/concurrency.md` ("No Function Coloring") documents the END-STATE: whole-program TRANSITIVE may-block analysis up the call graph → auto-insert `wait` at every may-block call site → stackless state machines; FFI declares `may-block` (the only "can't infer → user" case). **There is NO bridge in the design.** The bridge was invented by the M2 plan to fill the gap created by the roadmap cutting M2="state machines" | M3="may-block analysis" — an unbuildable cut, because a CORRECT state-machine layer REQUIRES the transitive analysis. The state-machine MECHANISM (frames/resume/poll-yield/runtime) is right; the missing piece is the analysis ENGINE. **FIX (vindicated by the doc): pull transitive may-block analysis into M2** (may-block set = `{sleepAsync}` only for now; I/O/FFI join later — same engine). Every caller-of-a-suspender becomes a state machine → inline-poll-yield everywhere → no bridge (except the legit main→entrypoint driver). `wait` inferred + shown as the `wait_points` muted inlay. **Roadmap boundary needs correcting: may-block analysis M3→M2; M3 keeps auto-parallel + preemption.** **Process miss → FIXED:** added the Design-Doc Alignment guardrail (project CLAUDE.md + plan-invariants.md + graveyard corpse) so reviews now diff plan-vs-design, not just plan-vs-itself. **PATRICK APPROVED the direction + frame composition (2026-05-31).** ✅ Roadmap CORRECTED (may-block engine M3→M2; M3 narrowed; 2 new Architectural Decisions incl. frame-composition lock). ✅ `io_uring` file-I/O note added for v0.5. **✅ ALL PLANS FIXED + VALIDATED (2026-05-31).** Roadmap corrected; M2 plan fully re-baselined to the inference model (Rework Phases 6-9: P6 transitive may-block engine, P7 codegen+composed-frames+inline-poll-yield fixing A/B/C, P8 delete-bridge+reconcile-guards, P9 redo; Invariants + Quality Checklist + Context&Why + Risk Assessment all updated; design-doc guardrail added to project CLAUDE.md+plan-invariants+graveyard; io_uring note in filesystem.md). **Full concurrency design-doc-alignment review: PASS (3 rounds)** — Design-Doc Alignment PASS, plan ready to implement. ALL UNCOMMITTED (working tree) — awaiting Patrick review-before-commit. NEXT: execute Phase 6 via /execute-plan on Patrick's go. Full detail in plan Findings Log.

- **[2026-05-31] ⏯️ EXECUTION IN FLIGHT — RESUME POINT FOR NEXT CHAT.** Cross-cutting changes (guardrail CLAUDE.md+plan-invariants+graveyard, roadmap, filesystem io_uring) COMMITTED TO MAIN as `66dc0e6` (NOT pushed). Worktree cleaned (Phase-5 workarounds reverted to 6a2d32d; backup at /tmp/m2-phase5-backup) + re-baseline committed on the M2 branch as `3655bf6` (= Phase-6 $BASE). **Phase 6 executor DONE → gate ROUND 1 = BLOCK on 7 findings; executor diff is STAGED but NOT committed (15 files vs 3655bf6, +954/-180; `crates/ynz-typeck/src/may_block.rs` is the core new file).** Deviation scratch: `.claude/plans/scratch/v0-3-m2-wait-and-state-machines-phase6-deviations.md` (D_count=6). The 7 findings are fully detailed in the plan's Phase-6 Findings Log (silent-guess can't-infer hole, UFCS escape, intrinsic-name collision, half-wired codegen seam=dev5, test-weakening, may_block-not-salsa-tracked, missing driver tests). **dev5 adjudicated: codegen-reads-`suspends` = Phase 7 per plan; P6 stays type-check-scoped — end goal unaffected.** **COORDINATOR BUG to avoid: spawned reviewer/judge agents have NON-DETERMINISTIC cwd — round-1 rules-reviewer ran the diff from the MAIN checkout (86-file garbage, VOID). Round-2 fan-out MUST use `git -C /workspaces/ynz/.claude/worktrees/v0-3-m2-wait-and-state-machines diff --cached 3655bf6` + ABSOLUTE read paths.** Patrick gave 'go' for the autonomous fix loop. **RESUME = round-2 fix loop: fresh plan-executor fixes all 7 findings (resume-context = the Phase-6 Findings Log), re-run full gate (4 reviewers + 6 judges, cwd-independent), then continue Phases 7→9 whole-plan, STOP before /release or merge-to-main (Phase 9 USER-GATED; v0.3.0-m2 tag collision with M10 sibling to resolve first).**

- **[2026-05-31] 🛑 M2 HALTED at Phase 5 — state-machine codegen incomplete; RE-SPIKE + RE-PLAN the coroutine codegen (decision: "do the RIGHT long-term thing," Patrick)**: Phase 5 integration testing surfaced + coordinator-VERIFIED (via the real compiler, `./target/debug/ynz run`) 3 load-bearing bugs that break M2's named success criteria: (A) value-returning state machines (`-> int`/`-> T errors` with `wait`) FAIL codegen ("Function return type does not match operand type of return inst"); (B) nested `wait` SM-from-SM PANICS at runtime ("Cannot start a runtime from within a runtime" — the no-coloring core falls back to a `block_on` sync bridge inside an async context instead of inline poll-and-yield); (C) `background` from inside a state machine HANGS. Only top-level `wait` in `-> nothing` fns spawned from a NON-state-machine caller works (the 8-task concurrency demo). **Root cause of false confidence:** the P0 spike hand-wrote Rust MIMICKING codegen and validated THAT — the real emitted codegen diverges, so the spike ACCEPT (incl. Contracts #4d SM-in-SM, #5 errors-cascade) was on a model, not the product. Phases 0–4 only exercised `-> nothing`/top-level state machines, so the gap hid until P5. **Decision: the thin-slice walk-back (clean-error + defer the rest to M3) is WRONG long-term** — M3 (auto-`wait` insertion) builds ON M2's coroutine substrate, so deferring value-returning + nested suspension to M3 is circular (M3 would have no working foundation). The RIGHT answer = fix the codegen properly (value-returning frames threading the return value through `Poll<T>`; genuine inline poll-and-yield for nested `wait`-SM replacing the panicking block_on fallback; proper background-from-SM spawn-and-drive), driven by a re-spike that validates against the ACTUAL compiler output (compile real `.ynz` through the real compiler — NOT hand-written Rust). This is milestone-hard codegen needing its own plan — NOT a fix-loop on the exhausted Phase-5 session. **Status: Phases 0–4 COMMITTED + sound (`6328666` spike, `b740e3d` runtime, `5109b52` codegen + Option-B clean errors, `5b72521` typeck warnings, `6a2d32d` teaching surface — the working top-level/`if` `-> nothing` `wait` slice is real). Phase 5 NOT committed (its executor worked around the bugs by omitting `errors_cascade_through_state_machine` + 5 SM-from-SM tests and swapping the demo keep-alive from `wait sleepAsync` to blocking `sleepMs` — those workarounds ship broken criteria silently and MUST NOT land). Cargo.toml bump to 0.3.0-m2 is premature + uncommitted. M2 plan stays `active` (BLOCKED). No `/release`.** Next: `/plan` a state-machine-codegen-rework spike+milestone, ideally fresh session. **GRAVEYARD CANDIDATE:** "P0 spike validated a hand-written model that diverged from the shipped codegen → false ACCEPT; later phases only tested the narrow `-> nothing`/top-level path → gap hid until end-to-end integration. Future spikes MUST compile real source through the real compiler, and milestone success criteria MUST have an end-to-end test before any phase claims them met."

- **[2026-05-31] M2/M3 boundary — `wait` nesting scope = OPTION B (clean error, defer transform to M3)** *(narrower than — and folded into — the HALT above; Option B covered wait-in-loop + local-crossing-wait)*: In v0.3-M2, an explicit `wait` works at top level and inside `if`. `wait` inside a `while`/`for`/`match` body, and a `let` local whose value is read after a `wait` (crosses a suspension boundary), emit a CLEAN teaching compile error (WHAT/WHAT-INSTEAD/WHY pointing to M3) — emitted at typeck so the codegen no-op fallback is unreachable. WHY: the full frame-backed-mutable-locals stackless-coroutine transform (flush-before-suspend + reload-at-resume + SM-aware variants of all ~5 `for` forms + `while` + the `let`-crossing dominance fix) is M3-scale machinery; the spike already proved the loop ABI (P0 Contract #7b), so M3 wires codegen onto a proven foundation alongside auto-`wait` insertion. The M2 shipped demo (8 background `pause()` tasks) uses top-level waits only — Option B descopes nothing user-visible. Replaces the prior silent no-op (loop) + codegen crash (let-crossing) — both were bugs — with honest errors. Decided via /execute-plan resume of Phase 2.

- **[2026-06-01 Phase 8] Option-B guards re-confirmed on inference foundation — LOCKED, no escape hatch**: `WaitInsideLoop` and `LocalCrossesWait` STAY M2 compile errors after Phase 7's inference-model rework. Verification: Phase 8 confirmed both guards still fire correctly (exit 1 + clean WHAT/WHAT-INSTEAD/WHY + M3 pointer) on the real compiler. WHY they stay: (1) Frame-backed MUTABLE locals (flush-before-suspend + reload-at-resume) are the genuine M3 hard core — distinct from the Phase 7 typed RETURN slot work; the composed frames back RETURN values + embedded child sub-frames, NOT arbitrary mutable locals crossing a wait boundary. (2) The loop-state transform (save/restore loop counter across suspension) is also M3-scope. (3) Lifting either guard requires Patrick sign-off + a read-AND-mutated-across-wait fixture proving the codegen is correct. No escape hatch: these are NOT safe to remove in M2 even under the inference model. The inference model deciding which functions are state machines does not automatically make arbitrary-mutable-local-crossing-wait safe to emit IR for — the frame layout simply doesn't support it yet. Decision to escalate in M3 planning.

## Active Decisions (Architectural — v0.3-M1)

**v0.3.0-m1 shipped 2026-05-21** — `background` runs on a separate OS thread via Tokio blocking thread pool embedded in `libynz_rt.a`.

**Key architectural decisions locked in this milestone:**
- **C-ABI bridge** (`ynz_rt_init`/`ynz_rt_spawn_blocking`/`ynz_rt_check_preempt`/`ynz_rt_shutdown`): Tokio is internal; users never see Tokio types. WHY: binds the runtime to Yinz's ownership model; future swap (e.g., custom scheduler) is possible without user-facing API change.
- **Stack-alloca ctx (not heap)**: `ynz_rt_spawn_blocking` takes a stack-allocated ctx; runtime copies it synchronously before returning. WHY: no heap ctx means no RAII cleanup concern at the call site; runtime is responsible for the copy lifetime.
- **Loop back-edge preempt (M1) vs call-site preempt (M2)**: M1 inserts `ynz_rt_check_preempt` at loop back-edges only. Call-site insertion measured at 1190% overhead on fib(30) — deferred to M2 with loop-integrated yield strategy. WHY: loop-only gives O(1) preempt cost per iteration; call-site needs smarter insertion logic.
- **`--no-auto-parallel` hidden flag**: plumbed in M1 (no-op); becomes load-bearing in M3 when auto-parallelization ships. WHY: plumb now so M3 isn't a flag-API breaking change.
- **Fire-and-forget only (M1)**: `let h = background fn()` handle form is a compile error. WHY: handles need channels; channels ship in M4. Documented as `background-handle-form` deferred-tooling-feature in registry.
- **Total tests at ship**: ~1220 (across all crates; ynz-typeck 156, ynz-lsp 100+, ynz-driver 120+, ynz-runtime 10, ynz-registry 29, etc.).
