target/
*.ll
*.o
*.bc
Cargo.lock.bak

# AI-workflow ephemera — never belong in the index on the main checkout.
# (gitignore has no inline comments; each pattern stands alone.)
# git worktree checkouts live here — separate working trees, not files to track
.claude/worktrees/
# analyzer-agent scratch reports (regenerable; durable copies ride with their milestone branch)
.analysis/
# stray junk dir from a worktree-scaffolding misfire
.claire/
# un-accepted insta snapshots — review/accept in-branch, never commit the .new
*.snap.new

# Compiled binaries emitted by `ynz build`/`ynz run` during integration tests.
# These land adjacent to the source .ynz file when no output path is specified.
crates/ynz-driver/tests/fixtures/hello
crates/ynz-driver/tests/fixtures/m3_fib
crates/ynz-driver/tests/fixtures/m_duplicate_entrypoint

# VSCode extension build artifacts
tooling/vscode-ynz/node_modules/
tooling/vscode-ynz/out/
tooling/vscode-ynz/*.vsix
# Build outputs for example projects — `ynz build examples/<name>/` writes here.
# Matches `bin` directly inside any example project root.
examples/*/bin

# Website build artifacts (belt-and-suspenders; website/.gitignore is primary)
website/node_modules/
website/.nuxt/
website/.output/
