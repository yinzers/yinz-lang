FROM oven/bun:1.2.21-alpine
WORKDIR /app
# Mirrors docker-compose.yml command so IDE/editor-level runners use the same image.
# In normal dev, use docker compose instead of building this directly.
CMD ["sh", "-c", "bun install && bun run dev --host 0.0.0.0"]
