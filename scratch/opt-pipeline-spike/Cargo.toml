[package]
name = "opt-pipeline-spike"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
# Same pin + feature as the workspace root Cargo.toml — the spike must prove the
# surface at the EXACT version the compiler builds against.
inkwell = { version = "0.9.0", features = ["llvm18-1-prefer-dynamic"] }

# Standalone: keep this throwaway spike out of the ynz workspace's crate tree.
[workspace]
